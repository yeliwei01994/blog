// This file is generated by scripts/generate-diary-content.ts. Do not edit.
import type { DiaryArticle } from '../features/diary/diary-types';

export const diaryArticles: DiaryArticle[] = [
  {
    "id": "2026-08-28-prompt-context-harness",
    "title": "8/25–8/28 周报：官网优化、视觉分析平台与 AI Agent 工程理解",
    "description": "总结本周坤元智算官网页面优化、个人视觉分析平台链路升级，以及从提示词工程到循环工程和节点工程的学习理解。",
    "publishedAt": "2026-08-28",
    "updatedAt": "2026-08-28",
    "draft": false,
    "html": "<p>本周主要围绕三个方向展开：完成坤元智算官网生态合作页面与首页的优化，继续梳理个人视觉分析平台的系统链路，同时学习提示词工程、上下文工程、驾驭工程、循环工程和节点工程。前两部分让我看到“一个系统如何落地”，最后一部分则帮助我理解“如何让 AI Agent 稳定地完成复杂任务”。</p>\n<p>提示词工程解决的是“怎么把任务说清楚”；上下文工程解决的是“模型应该看到哪些信息”；驾驭工程解决的是“怎样让 Agent 在真实环境中稳定运行”；循环工程解决的是“怎样让系统根据反馈持续推进”；节点工程解决的是“如何把循环中的不同动作拆分、连接和协作”。它们共同构成了从一次对话到完整智能体系统的能力链路。</p>\n<h2 id=\"一本周主要工作\">一、本周主要工作</h2>\n<h3 id=\"1-坤元智算官网生态合作页面与首页优化\">1. 坤元智算官网：生态合作页面与首页优化</h3>\n<p>本周主要完成坤元智算官网生态合作页面、首页导航、首页算力资源卡片、快速开始助手和解决方案详情页加载体验的优化。这部分工作不是单纯修改几个页面样式，而是同时处理了信息结构、品牌视觉、路由关系、交互入口、资源加载和回归验证。</p>\n<h4 id=\"页面建设\">页面建设</h4>\n<p>新增 <code>/partner</code> 生态合作页面，页面定位是向个人用户、开发者、创作者、AI 应用团队、模型厂商及科研公益团队介绍合作方式，并引导潜在伙伴提交合作方向。页面结构参考了国内算力平台的生态合作页面，但重新组织了适合坤元智算的内容和品牌表达。</p>\n<p>页面由以下几个部分组成：</p>\n<ol>\n<li><strong>Hero 首屏</strong>：突出“连接算力与产业，共建智算新生态”的主题，通过主标题、说明文字和行动按钮快速说明页面用途。</li>\n<li><strong>合作模式</strong>：由原来的横向信息表格改成六张信息卡片，桌面端使用三列网格，移动端切换为单列，降低连续阅读压力。</li>\n<li><strong>伙伴权益</strong>：恢复 01、02、03、04 编号卡片布局，分别表达算力能力、技术扶持、联合推广和生态共建等合作价值，并使用大号背景编号增加层次。</li>\n<li><strong>合作流程</strong>：整理为“提交合作方向 → 完成技术与业务沟通 → 开启项目协同与长期合作”三个步骤，让用户知道提交信息后会经历什么。</li>\n<li><strong>底部 CTA</strong>：改成独立的蓝色渐变圆角块，与上下内容保持留白，避免使用一整条满宽色块造成页面结束过于生硬。</li>\n</ol>\n<p>这次还明确移除了“生态伙伴案例”和“查看更多合作场景”入口。这样做不是减少内容，而是暂时去掉缺少真实业务资料、容易形成空壳的模块，先保证页面信息真实、结构完整，后续再根据实际合作案例补充。</p>\n<h4 id=\"hero-视觉和品牌色彩\">Hero 视觉和品牌色彩</h4>\n<p>Hero 使用蓝色数字地平线背景，设计重点包括：</p>\n<ul>\n<li>蓝色作为主基调，用深蓝、蔚蓝和青色高光表达科技感，并与官网现有品牌体系保持一致。</li>\n<li>中心区域保持低细节，为居中的中文标题和按钮留出可读空间。</li>\n<li>两侧和底部保留网络节点、数据流和数字结构，让背景承担氛围表达，而不是抢夺信息层级。</li>\n<li>图片本身不生成 Logo、文字、水印或 UI 面板，避免图片文字与页面真实文案重复。</li>\n<li>桌面端优先保证宽屏效果，同时考虑移动端裁切后标题不会被复杂纹理干扰。</li>\n</ul>\n<p>之前页面中偏紫色的元素已经统一调整为坤元智算的蓝色体系。这样页面视觉更加稳定，也避免单独的合作页面与官网其他页面产生明显割裂。</p>\n<h4 id=\"导航与交互调整\">导航与交互调整</h4>\n<p>首页和合作页面的导航同步进行了调整：</p>\n<ul>\n<li>移除“软件产品”主导航入口，但保留软件产品页面和路由，避免已有链接失效。</li>\n<li>在原导航位置接入“生态合作”，桌面端和移动端保持入口关系一致。</li>\n<li>合作页面首屏使用透明导航叠加在 Hero 背景上，滚动后切换成与首页一致的白色背景导航。</li>\n<li>恢复“联系我们”作为顶部导航入口，不再把“快速开始”放在主导航中。</li>\n</ul>\n<p>“快速开始”改成全站右下角悬浮的“坤元小助手”。浮动入口只保留聊天符号，不显示额外文字和 Logo；点击后展开助手面板，面板内保留欢迎语、聊天记录和四个快捷问题：</p>\n<ul>\n<li>我想了解算力服务</li>\n<li>帮我分析一个 AI 产品需求</li>\n<li>我需要搭建行业解决方案</li>\n<li>帮我生成一个坤元智算的企业宣传图</li>\n</ul>\n<p>当前没有增加输入框和发送按钮，用户通过预设问题触发演示流程。对话内容在浮层内部滚动，不会把页面整体撑长；宣传图快捷项会在回复中展示企业宣传图。这个方案更适合当前以展示和引导为主的官网阶段，后续如果接入真实客服或 AI 服务，再扩展自由输入能力。</p>\n<h4 id=\"首页算力资源卡片\">首页算力资源卡片</h4>\n<p>首页算力资源区域的问题主要不是价格数据错误，而是信息层级不够明显：普通资源卡片之间区分度较低，边框和分隔线颜色接近，小字号参数对比度偏低，不同计费方式放在一起时不容易横向比较，CTA 卡片与普通资源卡片的角色差异也不够突出。</p>\n<p>本次没有修改价格数据和核心交互逻辑，主要调整视觉表达：</p>\n<ul>\n<li>普通卡片增加渐变背景和更清晰的边框；</li>\n<li>增加 hover 上浮和阴影效果，强化卡片可交互的反馈；</li>\n<li>GPU 标签保持右对齐，避免与价格和名称争夺视觉焦点；</li>\n<li>提升价格、参数说明和底部分隔线的可读性；</li>\n<li>CTA 卡使用更明显的蓝色渐变、发光边框和阴影；</li>\n<li>CTA 按钮改为更清晰的实心主按钮，并增强 hover 状态。</li>\n</ul>\n<p>这部分的目标不是增加装饰，而是让用户更容易区分普通资源、价格信息和主要行动入口。</p>\n<h4 id=\"解决方案详情页的加载体验\">解决方案详情页的加载体验</h4>\n<p>针对首页“自研 IB 网络解决方案”和“超节点集群建设方案”点击后短暂闪现、体感像卡顿的问题，使用浏览器 Network 面板分析首次加载过程。排查后发现：</p>\n<ul>\n<li>详情页采用 React 路由级懒加载，用户点击后才请求对应页面代码包。</li>\n<li>两个详情页页面代码包约 5 KB，相关拓扑 SVG 约 5 KB，响应速度较快，不是主要瓶颈。</li>\n<li>导航 Logo 原文件约 33 KB，首次请求下载耗时约 1.95 秒，是当时最明显的资源传输瓶颈。</li>\n<li>Logo 请求使用生产静态资源路径并返回 200，问题主要在资源体积和懒加载时机，而不是接口代理错误。</li>\n</ul>\n<p>对应的处理分为三部分：</p>\n<ol>\n<li>使用项目现有图片压缩脚本压缩 Logo 及相关 PNG/JPG，将导航 Logo 从约 33 KB 压缩到约 4.8 KB，原始资源备份到 <code>assets-original</code>，方便恢复和对比。</li>\n<li>新增统一的解决方案页面预加载模块，在鼠标移入、键盘聚焦和触摸开始时提前加载目标页面代码。用户真正点击时，页面 chunk 通常已经准备完成。</li>\n<li>移除会让等待感更明显的旋转加载图标，只保留不可见的固定高度占位，避免加载期间页面布局跳动。</li>\n</ol>\n<p>同时在 <code>index.html</code> 中增加 favicon 配置，直接复用现有 Logo 标志图，避免重复维护一份品牌图形资源。</p>\n<h4 id=\"主要修改文件与验证\">主要修改文件与验证</h4>\n<p>本次页面工作的文件职责比较清晰：</p>\n<pre><code class=\"language-text\">PartnerPage.tsx                 页面内容、Hero、卡片、流程和 CTA\nNav.tsx                         顶部导航、透明导航和滚动样式\nrouter.tsx / routePaths.ts      页面路由和路径配置\nsiteLinks.ts                    站点链接配置\nindex.css                       Hero 标题、说明和按钮动画\nQuickStartAssistant.tsx         悬浮助手和快捷提问交互\nSiteLayout.tsx                  将助手挂载到全站布局\nsolutionPagePrefetch.ts         解决方案详情页预加载\npartner-page.mjs                页面内容、路由和结构校验\n</code></pre>\n<p>已经验证生态合作页面路由、核心文案、助手相关结构、预加载逻辑和生产构建。后续还需要根据真实浏览器截图继续微调移动端 Hero 的字号、留白、背景裁切和导航展开状态，并确认合作卡片内容与实际业务政策保持一致。</p>\n<h4 id=\"视觉设计经验\">视觉设计经验</h4>\n<p>学习了一下常见的设计比例</p>\n<ul>\n<li>黄金比例：<code>1:1.618</code>，适合强调自然、优雅和动态。</li>\n<li>白银比例：<code>1:√2</code>，适合纸张和结构化系统。</li>\n<li><code>1.25</code>（小三度）适合温和、紧凑的字号与间距层级。</li>\n<li><code>1.333</code>（大三度）层次清晰，适合一般网页排版。</li>\n<li><code>1.5</code>（纯四度）适合拉开标题与正文、卡片与留白的差异。</li>\n<li><code>1.618</code>适合更有张力的标题和版式层级；<code>2</code>适合形成强烈的视觉对比。</li>\n</ul>\n<p>实际落地时，UI 界面优先使用 <code>4px、8px、12px、16px、24px、32px</code> 的间距倍数；海报更常用 <code>2:3、3:4、4:5</code>；网页横幅可使用 <code>16:9</code> 或 <code>3:1</code>，需要电影感时使用 <code>21:9</code>。正文为 <code>16px</code> 时，标题可以按 <code>1.25、1.333、1.5</code> 倍递进；分栏则可以尝试 <code>1:1、1:2、2:3、3:5</code>，不必执着于黄金分割。</p>\n<h3 id=\"2-个人视觉分析平台从功能开发转向完整闭环\">2. 个人视觉分析平台：从功能开发转向完整闭环</h3>\n<p>个人视觉分析平台这部分工作，重点不是单独增加一个识别接口，而是把视频从上传到事件审核、证据查看和回放的链路串起来。系统目标可以概括为：上传一段视频后，后台能够异步分析，识别结果能够转化为业务事件，用户能够解释、审核、追踪和回放这些事件。</p>\n<h4 id=\"系统架构与职责边界\">系统架构与职责边界</h4>\n<p>整体架构可以分为前端、网关、API、消息队列、持久化存储、媒体文件和分析 Worker：</p>\n<pre><code class=\"language-text\">浏览器 / React\n       │ HTTP、JSON、multipart\n       ▼\nNginx：静态文件、API 反向代理、媒体访问\n       │\n       ├── Rust API：鉴权、上传、任务和查询\n       ├── MySQL：任务、事件、规则和审核事实\n       ├── Redis Stream：vision:jobs 异步任务消息\n       └── media：原始视频、抽帧证据和回放文件\n                         │\n                         ▼\n                Worker：FFmpeg → YOLO → 规则引擎\n</code></pre>\n<p>各组件的边界需要保持清晰：Nginx 负责统一入口、React 静态文件、API 代理和媒体访问；Rust API 负责鉴权、文件接收、任务创建、状态查询和审核接口；Redis 只负责异步任务通知和短期队列状态；MySQL 负责长期保存任务、事件、规则和审核历史；视频及证据图片放在共享媒体目录，生产环境可以替换成对象存储；Worker 承担耗时的抽帧、模型推理和事件生成；Docker 则统一服务网络、运行时环境、数据卷和启动依赖。</p>\n<p>这里最重要的数据原则是：<strong>MySQL 保存事实，Redis 负责调度，媒体卷保存文件，Worker 执行计算，Nginx 提供访问入口。</strong> Redis 不能作为最终结果的唯一存储，MySQL 也不应该承担大文件存储和高频队列消费。</p>\n<h4 id=\"一次视频任务的完整流程\">一次视频任务的完整流程</h4>\n<p>一次上传任务大致经过以下步骤：</p>\n<ol>\n<li>React 通过 <code>POST /api/v1/videos/upload</code> 发送视频文件。</li>\n<li>API 清理上传文件名，避免路径穿越，并将视频写入媒体目录。</li>\n<li>API 读取视频时长，在 MySQL 创建 <code>video_jobs</code> 任务记录。</li>\n<li>API 向 Redis Stream <code>vision:jobs</code> 写入包含 <code>job_id</code> 的消息，然后尽快返回任务信息。</li>\n<li>Worker 从 Stream 消费任务，根据 <code>job_id</code> 加载任务、规则和视频路径。</li>\n<li>Worker 使用 FFmpeg 抽取 JPEG 帧，将帧发送给 YOLO 服务进行识别。</li>\n<li>规则引擎根据目标类别、置信度、区域和持续时间生成业务事件。</li>\n<li>Worker 将事件、检测对象和证据写入数据库，并更新任务进度和最终状态。</li>\n<li>React 轮询 <code>GET /api/v1/jobs/{id}</code>，任务完成后刷新事件列表。</li>\n</ol>\n<p>这种拆分把“API 快速接收任务”和“后台长时间计算”分开。即使视频分析需要几十秒，上传接口也不需要一直占用 HTTP 请求；多个视频同时上传时，也可以通过 Redis 排队并增加 Worker 数量来扩展处理能力。</p>\n<h4 id=\"任务worker-和媒体管理\">任务、Worker 和媒体管理</h4>\n<p>任务状态不应只用一个布尔值表示，而需要有明确的生命周期，例如：</p>\n<pre><code class=\"language-text\">pending → processing → completed\n                    └→ failed\n                    └→ cancelled\n</code></pre>\n<p>任务记录至少需要保存任务名称、视频文件名、时长、状态、进度、源文件路径、创建时间、更新时间和软删除时间。任务支持重命名、删除和软删除时，还要区分数据库记录、原始视频和检测回放，不能因为删除一条任务误删其他任务的媒体文件。</p>\n<p>Worker 在 <code>WORKER_MODE=1</code> 时消费 Redis Stream。它需要把 YOLO 返回的检测类别、置信度、边界框、时间戳和模型版本转换成平台内部结构。边界框还要同时兼容像素坐标和 <code>0～1</code> 的归一化坐标，否则会出现识别框位置偏移。视频时长、抽帧进度和当前批次也要写回任务状态，便于前端展示进度和排查失败位置。</p>\n<h4 id=\"规则引擎区域和事件证据\">规则引擎、区域和事件证据</h4>\n<p>模型输出本身不是最终业务结果。平台还需要通过规则把检测结果转成用户能够理解的事件，目前重点梳理了人员停留、人员进入区域和最大人员数量等规则：</p>\n<table>\n<thead>\n<tr>\n<th>规则</th>\n<th>关注内容</th>\n<th>关键条件</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>人员停留</td>\n<td>人员在画面中持续出现</td>\n<td>类别、最低置信度、最短持续时间</td>\n</tr>\n<tr>\n<td>人员进入区域</td>\n<td>人员是否进入指定区域</td>\n<td>区域形状、目标中心点、进入时间</td>\n</tr>\n<tr>\n<td>最大人员数量</td>\n<td>同时出现的人数是否超限</td>\n<td>人数上限、统计窗口、置信度</td>\n</tr>\n</tbody></table>\n<p>规则中的最低置信度、最短持续时间、人数上限、启用状态和版本应该保存到 <code>event_rules</code>，而不是全部写死在 Worker 中。Worker 开始任务前从 MySQL 刷新规则，确保用户保存的新配置能够应用到新任务，而不是继续使用旧的内存配置。</p>\n<p>区域编辑支持鼠标拖动绘制矩形和多边形，也支持空白画布和视频证据作为背景。之前“重置区域后点击应用区域没有效果”的问题，根本上涉及前端状态、保存动作和 Worker 配置是否一致；修复后，区域保存和应用能够按照新的配置执行。区域外目标仍触发事件的问题也进行了修复，鼠标绘制过程增加节流，避免频繁更新导致卡顿或黑屏。</p>\n<p>事件证据不再使用占位图片，而是保存真实抽帧结果，例如：</p>\n<pre><code class=\"language-text\">media/evidence/&lt;event-id&gt;/\n</code></pre>\n<p>每个事件关联时间点、证据帧、检测框和目标信息。前端可以显示“人员 11 次检测 · 平均置信度 34%”这类中文摘要，并把内部事件类型转换为更容易理解的文字：</p>\n<pre><code class=\"language-text\">person_stay        → 人员停留\nperson_enter_zone  → 人员进入区域\nperson_count_limit → 最大人员数量\n</code></pre>\n<h4 id=\"事件审核查询和回放\">事件审核、查询和回放</h4>\n<p>事件结果需要进入业务审核，而不是停留在模型输出页面。审核状态可以从待复核进入确认或忽略，再进入已处置和已关闭；需要再次处理时也可以进入处理中。审核操作保存审核人、备注、处置结果、旧状态、新状态和审核时间，详情页通过时间线展示完整的变化过程。</p>\n<p>审核按钮还需要处理处理中状态、成功和失败提示，以及重复提交保护。事件列表增加组合查询、分页、排序、CSV 导出和 HTML 报告接口，使事件能够被筛选、确认、追踪和交付。</p>\n<p>回放时需要把事件的 <code>start_time_ms</code>、<code>end_time_ms</code> 和视频真实时长统一起来。前端不能只根据事件数量或当前页面状态猜测时间轴；如果 FFprobe 获取时长异常，需要从视频容器格式、编码信息和读取方式一起排查。只有事件时间、证据帧和原始视频保持一致，用户才能从事件列表跳转到准确的回放位置。</p>\n<h4 id=\"性能和可靠性优化\">性能和可靠性优化</h4>\n<p>视频分析是一个长任务，前端和 Worker 都需要控制资源使用：</p>\n<ul>\n<li><strong>轮询退避</strong>：任务状态轮询从固定每秒一次调整为 <code>1 秒 → 2 秒 → 3 秒 → 4 秒 → 5 秒</code>，状态发生变化时恢复较短间隔，长时间不变时降低请求频率。</li>\n<li><strong>限制事件查询数量</strong>：事件列表接口增加 <code>limit</code> 参数，默认最多返回 50 条，避免数据增长后一次返回过多内容。</li>\n<li><strong>流式上传</strong>：视频按数据块写入临时文件，上传完成并校验后再切换成最终文件；失败时清理临时文件，避免留下不完整视频。</li>\n<li><strong>降低检测频率</strong>：默认检测间隔由 100ms 调整为 200ms，并支持 <code>DETECTION_INTERVAL_MS</code> 配置，范围为 100ms 至 5000ms。间隔太大可能漏掉短事件，需要结合真实视频验证。</li>\n<li><strong>YOLO 批量推理</strong>：新增 <code>POST /v1/infer/batch</code>，一次发送多张帧图片及其 <code>frame_id</code>、<code>timestamp_ms</code>，默认批量大小为 4，减少逐帧 HTTP 请求和模型调用开销。</li>\n<li><strong>并发上限</strong>：Worker 默认最多同时执行 2 个批次，<code>YOLO_CONCURRENCY</code> 限制在 1 至 4，批量大小限制在 1 至 16，避免配置过大造成显存或内存压力。</li>\n</ul>\n<h4 id=\"视频平台性能实测与进一步优化\">视频平台性能实测与进一步优化</h4>\n<p>在完成批量推理和并发处理后，又使用同一类视频任务进行了多组对比测试。每组任务处理 44 帧，产生 121 个检测结果和 3 个事件，所有任务均成功完成且没有错误。</p>\n<table>\n<thead>\n<tr>\n<th>配置</th>\n<th align=\"right\">Batch</th>\n<th align=\"right\">并发数</th>\n<th align=\"right\">总耗时</th>\n<th align=\"right\">相对基线</th>\n<th align=\"right\">加速比</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>基线</td>\n<td align=\"right\">1</td>\n<td align=\"right\">1</td>\n<td align=\"right\">43.702 秒</td>\n<td align=\"right\">100%</td>\n<td align=\"right\">1.00x</td>\n</tr>\n<tr>\n<td>批量并发</td>\n<td align=\"right\">4</td>\n<td align=\"right\">2</td>\n<td align=\"right\">25.023 秒</td>\n<td align=\"right\">57.3%</td>\n<td align=\"right\">1.75x</td>\n</tr>\n<tr>\n<td>大 Batch 并发</td>\n<td align=\"right\">8</td>\n<td align=\"right\">2</td>\n<td align=\"right\">24.645 秒</td>\n<td align=\"right\">56.4%</td>\n<td align=\"right\">1.77x</td>\n</tr>\n<tr>\n<td>高并发</td>\n<td align=\"right\">8</td>\n<td align=\"right\">4</td>\n<td align=\"right\">25.740 秒</td>\n<td align=\"right\">58.9%</td>\n<td align=\"right\">1.70x</td>\n</tr>\n</tbody></table>\n<p>从整体结果看，<code>8/2</code> 是当前测试中的最佳配置：总耗时比 <code>1/1</code> 减少约 19.057 秒，下降 43.6%。<code>4/2</code> 已经获得了大部分收益，<code>8/2</code> 只进一步减少约 378ms；当并发数从 2 提高到 4 后，耗时反而增加约 1.095 秒，说明并发并不是越高越好。</p>\n<p>各阶段耗时对比进一步说明了性能变化：</p>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th align=\"right\"><code>1/1</code></th>\n<th align=\"right\"><code>4/2</code></th>\n<th align=\"right\"><code>8/2</code></th>\n<th align=\"right\"><code>8/4</code></th>\n<th>结论</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>视频探测</td>\n<td align=\"right\">94ms</td>\n<td align=\"right\">75ms</td>\n<td align=\"right\">88ms</td>\n<td align=\"right\">80ms</td>\n<td>影响很小</td>\n</tr>\n<tr>\n<td>抽帧</td>\n<td align=\"right\">468ms</td>\n<td align=\"right\">272ms</td>\n<td align=\"right\">274ms</td>\n<td align=\"right\">263ms</td>\n<td>不是主要瓶颈</td>\n</tr>\n<tr>\n<td>YOLO 推理</td>\n<td align=\"right\">20.250s</td>\n<td align=\"right\">2.121s</td>\n<td align=\"right\">2.088s</td>\n<td align=\"right\">2.174s</td>\n<td>批量处理收益明显</td>\n</tr>\n<tr>\n<td>规则分析</td>\n<td align=\"right\">3.081s</td>\n<td align=\"right\">3.108s</td>\n<td align=\"right\">3.004s</td>\n<td align=\"right\">3.042s</td>\n<td>基本稳定</td>\n</tr>\n<tr>\n<td>证据保存</td>\n<td align=\"right\">3.048s</td>\n<td align=\"right\">3.067s</td>\n<td align=\"right\">2.972s</td>\n<td align=\"right\">3.012s</td>\n<td>基本稳定</td>\n</tr>\n<tr>\n<td>标注视频编码</td>\n<td align=\"right\">19.748s</td>\n<td align=\"right\">19.396s</td>\n<td align=\"right\">19.134s</td>\n<td align=\"right\">20.116s</td>\n<td>后续优化重点</td>\n</tr>\n</tbody></table>\n<p>最明显的变化发生在 YOLO 阶段：逐帧调用时需要处理 44 个批次，推理耗时约 20.250 秒；使用 <code>batch=4、concurrency=2</code> 后，批次数减少为 11，YOLO 耗时降到 2.121 秒；使用 <code>batch=8、concurrency=2</code> 后进一步降到 2.088 秒。此时 YOLO 只占总耗时约 8.5%，已经不再是系统的主要瓶颈。并发提高到 4 后，CPU 或线程竞争开始出现，还影响了视频编码阶段。</p>\n<h4 id=\"标注视频编码的瓶颈定位\">标注视频编码的瓶颈定位</h4>\n<p>批量推理优化后，总耗时仍然在 24 秒左右，说明瓶颈已经从模型推理转移到了 <code>annotated_video_encode</code>。这个阶段并不只是“调用一次 FFmpeg”，实际包含四个步骤：</p>\n<pre><code class=\"language-text\">重新读取原始 JPG\n  → 在检测帧上绘制边界框、类别和时间\n  → 将检测帧复制成完整的视频帧数量\n  → 调用 FFmpeg 编码 MP4\n</code></pre>\n<p>首先调整 FFmpeg 参数：</p>\n<pre><code class=\"language-text\">preset=veryfast\nCRF=28\n</code></pre>\n<p>这一步牺牲少量画质，换取更快的编码速度和更小的文件体积，使标注视频编码阶段从约 19.134 秒降到 17.617 秒，总耗时降到 23.784 秒。但继续拆分阶段后发现，真正耗时的并不是 FFmpeg 本身，而是逐帧绘制标注：</p>\n<pre><code class=\"language-text\">annotate_frames：17.156 秒\nffmpeg_encode：0.990 秒\nduplicate_frames：0.113 秒\n</code></pre>\n<p>因此将优化重点转向标注帧并行处理，通过 <code>ANNOTATION_CONCURRENCY=4</code> 同时处理多张检测帧。对比结果如下：</p>\n<table>\n<thead>\n<tr>\n<th>指标</th>\n<th align=\"right\">并行前</th>\n<th align=\"right\">并行后</th>\n<th align=\"right\">变化</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>总耗时</td>\n<td align=\"right\">24.015 秒</td>\n<td align=\"right\">11.083 秒</td>\n<td align=\"right\">减少 53.8%</td>\n</tr>\n<tr>\n<td><code>annotate_frames</code></td>\n<td align=\"right\">17.156 秒</td>\n<td align=\"right\">4.403 秒</td>\n<td align=\"right\">减少 74.3%</td>\n</tr>\n<tr>\n<td><code>annotated_video_encode</code></td>\n<td align=\"right\">18.308 秒</td>\n<td align=\"right\">5.624 秒</td>\n<td align=\"right\">减少 69.3%</td>\n</tr>\n<tr>\n<td><code>duplicate_frames</code></td>\n<td align=\"right\">113ms</td>\n<td align=\"right\">97ms</td>\n<td align=\"right\">基本不变</td>\n</tr>\n<tr>\n<td><code>ffmpeg_encode</code></td>\n<td align=\"right\">990ms</td>\n<td align=\"right\">1.070 秒</td>\n<td align=\"right\">基本不变</td>\n</tr>\n<tr>\n<td>YOLO 推理</td>\n<td align=\"right\">2.333 秒</td>\n<td align=\"right\">2.101 秒</td>\n<td align=\"right\">基本不变</td>\n</tr>\n<tr>\n<td>检测数</td>\n<td align=\"right\">121</td>\n<td align=\"right\">121</td>\n<td align=\"right\">结果一致</td>\n</tr>\n<tr>\n<td>事件数</td>\n<td align=\"right\">3</td>\n<td align=\"right\">3</td>\n<td align=\"right\">结果一致</td>\n</tr>\n</tbody></table>\n<p>这组结果说明，视频平台的优化顺序应该是先用性能数据定位瓶颈，再决定增加 Batch、模型并发还是帧处理并发。批量推理解决了逐帧调用模型的开销；标注帧并行解决了 CPU 图像处理的开销；而 FFmpeg 编码本身已经只占较小比例，继续单独调编码参数的收益有限。</p>\n<h4 id=\"性能优化后的阶段性结论\">性能优化后的阶段性结论</h4>\n<p>目前视频分析平台已经形成了比较明确的优化路径：</p>\n<pre><code class=\"language-text\">逐帧 YOLO 推理\n  → Batch + Worker 并发\n  → YOLO 不再是主要瓶颈\n  → 拆分标注视频生成阶段\n  → 标注帧并行处理\n  → 总耗时从约 44 秒降到约 11 秒\n</code></pre>\n<p>后续还需要继续观察不同视频分辨率、帧数、编码格式和 GPU/CPU 环境下的结果，避免把当前单个样本的最佳参数直接当成所有场景的固定配置。同时需要持续记录每个阶段耗时、检测数量、事件数量和错误数，确保性能提升没有以检测结果不一致或事件遗漏为代价。</p>\n<h2 id=\"二先理解五种工程分别在解决什么\">二、先理解五种工程分别在解决什么</h2>\n<p>可以先用一个简单的类比来理解：如果把 AI Agent 比作一匹能力很强但不完全可预测的马，那么：</p>\n<pre><code class=\"language-text\">提示词工程：告诉它应该往哪里走\n上下文工程：给它地图、路况和任务资料\n驾驭工程：修建道路、设置护栏、安排补给，并检查它是否真的到达目的地\n</code></pre>\n<p>这几个概念的关注范围逐步扩大：</p>\n<table>\n<thead>\n<tr>\n<th>工程层次</th>\n<th>核心问题</th>\n<th>主要优化对象</th>\n<th>典型交互方式</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>提示词工程</td>\n<td>怎么把话说清楚？</td>\n<td>指令、角色、示例、格式和限制</td>\n<td>一问一答</td>\n</tr>\n<tr>\n<td>上下文工程</td>\n<td>模型需要知道什么？</td>\n<td>历史、文档、记忆、检索结果和工具输出</td>\n<td>信息注入后生成</td>\n</tr>\n<tr>\n<td>驾驭工程</td>\n<td>Agent 如何可靠完成任务？</td>\n<td>工作流、权限、状态、测试、反馈和监控</td>\n<td>多步骤持续执行</td>\n</tr>\n<tr>\n<td>循环工程</td>\n<td>如何让任务持续推进？</td>\n<td>触发器、观察、调整、停止条件和记忆</td>\n<td>反馈闭环</td>\n</tr>\n<tr>\n<td>节点工程</td>\n<td>任务应该如何拆分？</td>\n<td>节点输入、输出、分支和连接关系</td>\n<td>工作流编排</td>\n</tr>\n</tbody></table>\n<p>这几个层次之间的逻辑关系是逐步扩展的：首先，提示词工程把任务、角色、要求和输出标准说清楚；然后，上下文工程为模型准备完成任务所需的资料、历史和工具结果；驾驭工程为 Agent 接入工具、状态和验证机制；循环工程让它根据观察结果不断调整，直到完成、暂停或交给人处理；节点工程则把循环中的动作拆分成可组合的单元，并明确它们之间的连接、分支和协作关系。</p>\n<p>因此，提示词工程是交互起点，上下文工程是信息基础，驾驭工程是运行保障，循环工程是持续改进机制，节点工程是把复杂动作组织起来的流程结构。前面的层次解决“模型是否理解、是否拥有足够信息”，后面的层次继续解决“任务如何执行、结果如何验证、系统如何持续推进以及多个动作如何协作”。</p>\n<h2 id=\"三提示词工程把任务说明白\">三、提示词工程：把任务说明白</h2>\n<h3 id=\"1-提示词工程是什么\">1. 提示词工程是什么</h3>\n<p>提示词工程是设计、优化和测试输入指令的过程，目标是让 AI 更准确地理解任务，并按照预期的方式输出结果。</p>\n<p>它并不是寻找某一句“神奇咒语”，而是把原本模糊的需求变成一份清晰的任务说明书。提示词越能明确任务目标、背景、边界和验收标准，模型越容易给出可用结果。</p>\n<p>例如，下面的提示词比较模糊：</p>\n<pre><code class=\"language-text\">帮我写个方案。\n</code></pre>\n<p>改进后可以写成：</p>\n<pre><code class=\"language-text\">你是一名市场营销顾问。\n\n请为一款面向大学生的记账 App 制定推广方案，包含：\n1. 目标用户\n2. 用户痛点\n3. 推广渠道\n4. 活动创意\n5. 预算建议\n\n要求：\n- 使用 Markdown 标题和表格输出；\n- 每个部分给出可执行的建议；\n- 不超过 1000 字；\n- 不要使用空泛的宣传口号。\n</code></pre>\n<p>后一个版本并没有使用复杂术语，但它交代了角色、任务、内容范围、输出格式和限制条件，因此更容易得到稳定结果。</p>\n<h3 id=\"2-四要素框架\">2. 四要素框架</h3>\n<p>写提示词时，可以先检查下面四个要素：</p>\n<table>\n<thead>\n<tr>\n<th>要素</th>\n<th>需要思考的问题</th>\n<th>示例</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>角色</td>\n<td>AI 应该以什么身份回答？</td>\n<td>资深产品经理、英语教师</td>\n</tr>\n<tr>\n<td>指令</td>\n<td>AI 具体要做什么？</td>\n<td>总结、分析、翻译、生成</td>\n</tr>\n<tr>\n<td>背景</td>\n<td>AI 需要知道哪些前提？</td>\n<td>读者、用途、已有资料</td>\n</tr>\n<tr>\n<td>限制</td>\n<td>有什么边界和标准？</td>\n<td>字数、格式、禁用内容、判断条件</td>\n</tr>\n</tbody></table>\n<p>角色设定可以帮助模型采用合适的知识角度和表达方式，但角色不是越夸张越好。比起“你是世界上最厉害的专家”，更有效的是说明专业领域、行为方式和判断原则：</p>\n<pre><code class=\"language-text\">你是一名有 8 年经验的产品经理，擅长 B2B SaaS 产品。\n回答时先给结论，再说明依据；涉及方案时同时考虑成本、风险和实施难度。\n</code></pre>\n<h3 id=\"3-用示例和模板控制输出\">3. 用示例和模板控制输出</h3>\n<p>当用户希望 AI 按固定格式写作、分类或提取数据时，给出示例通常比抽象描述更有效。这种方法通常称为少样本学习（Few-shot Learning）。</p>\n<p>好的示例需要满足三个条件：</p>\n<ul>\n<li>覆盖任务中常见的情况；</li>\n<li>输入和输出格式保持一致；</li>\n<li>示例本身就是理想结果，而不是随意拼凑的内容。</li>\n</ul>\n<p>如果格式非常重要，可以直接提供模板：</p>\n<pre><code class=\"language-text\">请使用以下模板输出：\n\n## 产品分析\n\n### 核心优势\n- [优势 1]\n- [优势 2]\n\n### 主要风险\n- [风险 1]\n- [风险 2]\n\n### 综合建议\n[一句话结论]\n</code></pre>\n<h3>4. 让模型给证据，而不是只给结论</h3>\n<p>对于分析、判断和事实性任务，应该要求模型先提取依据，再做判断。例如：</p>\n<pre><code class=\"language-text\">请先列出支持结论的关键事实，再给出最终判断。\n如果提供的资料不足，请明确说明无法判断，不要自行补充。\n</code></pre>\n<p>这里的重点不是强迫模型展示全部内部思考，而是让输出包含可检查的证据和判断依据。这样更容易发现模型是不是漏看资料，或者先有结论、再为结论寻找理由。</p>\n<h3 id=\"核心优势\">5. 提示词工程的边界</h3>\n<p>提示词工程能够改善模型的行为，但不能解决所有问题：</p>\n<ul>\n<li>再好的指令也无法替代缺失的业务资料；</li>\n<li>不能仅靠提示词保证事实永远正确；</li>\n<li>复杂任务如果只用一条超长提示词，仍然可能遗漏步骤；</li>\n<li>需要实时数据时，必须接入检索、数据库或工具；</li>\n<li>在医疗、法律和财务场景中，必须进行专业核实。</li>\n</ul>\n<p>因此，提示词工程更像是 AI 应用的语言接口，而不是完整的可靠性方案。</p>\n<h2>四、上下文工程：管理模型的工作记忆</h2>\n<h3 id=\"综合建议\">1. 上下文不只是提示词</h3>\n<p>上下文是模型生成回答前能够看到的全部信息，通常包括：</p>\n<pre><code class=\"language-text\">系统指令\n用户输入\n历史对话\n项目文档\n当前代码\n检索结果\n用户偏好\n工具返回值\n任务状态\n</code></pre>\n<p>提示词只是上下文的一部分。提示词工程主要关注“这条指令怎么写”，上下文工程则关注“现在应该把哪些信息放进模型的工作区”。</p>\n<p>例如，让 Agent 修改一个大型项目时，真正有价值的上下文可能包括：项目目录结构、相关模块代码、接口约定、数据库结构、错误日志、测试命令和之前已经做过的修改。只给一句“帮我修复这个项目”，即使指令没有语法问题，模型也缺少完成任务所需要的材料。</p>\n<h3 id=\"4-让模型给证据而不是只给结论\">2. 上下文是有限资源</h3>\n<p>模型的上下文窗口有容量限制，而且信息越多不一定越好。无关资料、重复历史和过时文档会挤占真正重要的信息，导致模型注意力下降、前后矛盾或遗漏关键条件。</p>\n<p>因此，上下文工程的目标不是“把所有资料都塞进去”，而是：</p>\n<pre><code class=\"language-text\">在有限的上下文窗口中，选择最相关、最新、可信且足够完成任务的信息。\n</code></pre>\n<p>常见操作包括：</p>\n<ul>\n<li>召回：从文档库、数据库或代码库中找到相关内容；</li>\n<li>过滤：删除与当前任务无关或重复的信息；</li>\n<li>压缩：把长历史、日志和文档提炼成摘要；</li>\n<li>排序：把最重要的任务、约束和证据放在更容易被关注的位置；</li>\n<li>组装：按照模型更容易理解的顺序构造完整输入；</li>\n<li>外部存储：把不适合长期放入上下文的信息保存到数据库或文件中，按需读取。</li>\n</ul>\n<h3 id=\"5-提示词工程的边界\">3. 上下文工程的常见组成</h3>\n<p>一个较完整的上下文系统通常包含：</p>\n<table>\n<thead>\n<tr>\n<th>组件</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>系统指令</td>\n<td>定义身份、原则和行为边界</td>\n</tr>\n<tr>\n<td>短期记忆</td>\n<td>保存当前对话和当前任务状态</td>\n</tr>\n<tr>\n<td>长期记忆</td>\n<td>保存用户偏好、历史决策和稳定知识</td>\n</tr>\n<tr>\n<td>检索系统</td>\n<td>从文档、数据库或 API 获取相关资料</td>\n</tr>\n<tr>\n<td>工具编排</td>\n<td>管理工具调用及其结果如何回到上下文</td>\n</tr>\n<tr>\n<td>查询增强</td>\n<td>将模糊问题转换为更适合检索的请求</td>\n</tr>\n<tr>\n<td>输出结构</td>\n<td>让结果可以被后续步骤继续使用</td>\n</tr>\n</tbody></table>\n<h3>4. 资料和指令要分离</h3>\n<p>当模型需要分析外部文本、用户输入或网页内容时，可以使用语义化标签区分指令与数据：</p>\n<pre><code class=\"language-text\">请只根据 &lt;reference&gt; 中的内容回答 &lt;question&gt;。\n如果参考资料不足，请说明无法判断。\n\n&lt;reference&gt;\n这里是需要参考的文档内容。\n&lt;/reference&gt;\n\n&lt;question&gt;\n这里是具体问题。\n&lt;/question&gt;\n</code></pre>\n<p>这种组织方式可以减少混淆，也有助于防范外部文本中的提示词注入。标签名称最好能表达内容性质，例如使用 <code>&lt;contract&gt;</code>、<code>&lt;code&gt;</code>、<code>&lt;resume&gt;</code>，而不是笼统的 <code>&lt;text1&gt;</code>、<code>&lt;text2&gt;</code>。</p>\n<h2>五、驾驭工程：让 Agent 稳定完成工作</h2>\n<h3 id=\"2-上下文是有限资源\">1. 驾驭工程是什么</h3>\n<p>Harness Engineering（驾驭工程）关注的是围绕 AI Agent 建立一整套约束、反馈、编排和控制机制。它不直接训练模型，而是设计模型运行的环境，让 Agent 能够在真实任务中保持方向、接受检查并从失败中恢复。</p>\n<p>它的核心思想可以概括为：</p>\n<blockquote>\n<p>人类掌舵，智能体执行。</p>\n</blockquote>\n<p>当 Agent 从“一次回答”变成“连续完成几十个步骤”时，问题就不再只是提示词是否写得好，而是：</p>\n<ul>\n<li>它是否知道任务做到哪一步？</li>\n<li>它是否可以访问不该访问的资源？</li>\n<li>它是否有明确的完成标准？</li>\n<li>它是否真的运行了测试？</li>\n<li>失败后是否能够回到正确步骤？</li>\n<li>长时间运行后是否会积累混乱和技术债务？</li>\n</ul>\n<h3 id=\"3-上下文工程的常见组成\">2. Agent 的典型失败模式</h3>\n<p>长任务中，Agent 常见的失败方式包括：</p>\n<ol>\n<li><strong>试图一步完成所有事情</strong>：上下文很快膨胀，任务中途产生半成品，下一轮又无法理解之前做了什么。</li>\n<li><strong>过早宣布任务完成</strong>：看到局部结果正常，就忽略了还没有完成的功能和边界情况。</li>\n<li><strong>写完代码就认为功能完成</strong>：单元测试或单个接口请求通过，并不代表端到端流程真的可用。</li>\n<li><strong>复制代码库中的坏模式</strong>：Agent 很擅长模仿，如果项目中存在混乱的结构，它也会快速复制并放大问题。</li>\n<li><strong>进入无效循环</strong>：反复尝试相同的修复方法，却没有记录失败原因，也没有改变策略。</li>\n</ol>\n<p>这些问题说明，单纯增加提示词长度并不能解决 Agent 的长期可靠性问题，需要从运行环境和流程上增加护栏。</p>\n<h3 id=\"4-资料和指令要分离\">3. 四类核心护栏</h3>\n<h4>上下文护栏：让 Agent 知道该看什么</h4>\n<p>项目说明文件、任务文档和按需检索机制可以帮助 Agent 快速理解工作范围。文档不应该变成一份越来越长、无人维护的规则清单，而应该保留稳定入口，并能链接到更具体的说明。</p>\n<p>一份好的项目指南通常应该说明：</p>\n<ul>\n<li>项目如何启动和测试；</li>\n<li>目录和模块分别负责什么；</li>\n<li>哪些规则必须遵守；</li>\n<li>哪些文件可以修改；</li>\n<li>完成任务前必须执行哪些验证。</li>\n</ul>\n<p>更重要的是，文档应该随着 Agent 的失败案例持续更新。每一次失败都可以反过来推动项目环境变得更清晰。</p>\n<h4>架构护栏：让错误难以进入系统</h4>\n<p>架构规则不能只写在文档里，还可以通过类型系统、Linter、依赖检查和 CI 自动执行。例如规定模块只能按照下面的方向依赖：</p>\n<pre><code class=\"language-text\">Types → Config → Repo → Service → Runtime → UI\n</code></pre>\n<p>如果下层模块反向依赖上层模块，CI 可以直接阻止合并。这里的错误信息也应该告诉 Agent 为什么违反规则、正确方式是什么，而不是只显示一个难以理解的错误编号。</p>\n<h4>反馈护栏：让 Agent 知道自己做错了</h4>\n<p>测试、Lint、类型检查、浏览器验证和代码审查都可以组成反馈回路：</p>\n<pre><code class=\"language-text\">执行任务 → 运行验证 → 收集错误 → 返回 Agent → 修复 → 再次验证\n</code></pre>\n<p>反馈必须足够具体。如果只告诉 Agent“失败了”，它很可能继续猜测；如果把失败命令、错误位置、预期结果和实际结果一并提供，它才更容易进行针对性修复。</p>\n<h4>熵管理护栏：持续清理系统混乱</h4>\n<p>随着代码、文档和配置不断变化，系统会产生技术债务、过时文档、重复实现和架构偏差。驾驭工程不应该等问题积累到很严重时再集中处理，而应当像垃圾回收一样持续进行小规模清理。</p>\n<p>可以定期扫描：</p>\n<ul>\n<li>文档与代码是否一致；</li>\n<li>是否存在重复或废弃模块；</li>\n<li>是否违反架构依赖规则；</li>\n<li>测试是否覆盖关键路径；</li>\n<li>任务记录和实际状态是否一致。</li>\n</ul>\n<h3>4. 驾驭工程与传统 Agent 框架的关系</h3>\n<p>传统 Agent 框架主要帮助开发者构建智能体，例如定义消息路由、工具调用和任务生命周期。驾驭工程关注的则是智能体运行起来以后如何保持可靠：如何保存状态、如何重放任务、如何限制成本、如何监控过程、如何恢复错误以及如何在没有人工盯守的情况下完成验证。</p>\n<p>因此，驾驭工程不是 SDK 或框架的替代品，而是位于模型和 Agent 框架之上的运行控制层。</p>\n<h2>六、循环工程：让系统持续推进目标</h2>\n<h3>1. 什么是循环工程</h3>\n<p>循环工程（Loop Engineering）关注的是如何设计、运行和改进一个反馈闭环，让 Agent 不只是执行一次，而是能够持续规划、行动、观察结果、修复问题，直到达到明确的完成条件。</p>\n<p>它与提示词工程的区别在于：提示词工程优化一条指令的质量，循环工程优化“什么时候提示、接下来做什么、结果是否合格、什么时候停止”的完整过程。</p>\n<p>一个基本循环可以分成五个阶段：</p>\n<ol>\n<li><strong>意图</strong>：明确目标、范围和成功标准；</li>\n<li><strong>上下文</strong>：收集当前任务所需的代码、文档、历史和工具结果；</li>\n<li><strong>行动</strong>：修改文件、调用工具、运行命令或生成方案；</li>\n<li><strong>观察</strong>：读取测试结果、编译错误、运行日志或人工 Review；</li>\n<li><strong>调整</strong>：根据观察更新计划，继续下一轮，或者在满足条件时停止。</li>\n</ol>\n<p>循环的关键不在“重复调用模型”，而在于每一轮都能获得新的有效反馈。测试失败、类型错误和 Review 意见都应该转化为下一轮的上下文，而不是被简单地丢弃。</p>\n<h3>2. 内循环与外循环</h3>\n<p>Agent 自己完成“读文件、修改代码、运行测试、处理错误”的过程，可以看作内循环。循环工程设计的是更高一层的外循环：发现任务、分派 Agent、保存状态、检查结果，并决定是否开启下一轮。</p>\n<pre><code class=\"language-text\">内循环：感知 → 推理 → 行动 → 观察 → 再行动\n外循环：发现任务 → 分派执行 → 验证结果 → 记录状态 → 开启下一轮\n</code></pre>\n<p>这也是“从手动提示 Agent”走向“设计一个系统来提示 Agent”的变化。人不再需要亲自推动每一步，但仍然需要设计目标、约束、验证方式和停止条件。</p>\n<h3 id=\"4-驾驭工程与传统-agent-框架的关系\">3. 循环工程的关键组成</h3>\n<p>一个可以长期运行的 Loop 通常需要：</p>\n<ul>\n<li>自动触发器：决定什么时候启动任务；</li>\n<li>节点和工作流：规定每轮要执行哪些动作；</li>\n<li>上下文刷新：在关键观察后重新收集相关信息；</li>\n<li>持久记忆：保存任务状态、已尝试方案和未解决问题；</li>\n<li>验证器：检查输出是否满足完成条件；</li>\n<li>权限控制：限制工具和外部操作的范围；</li>\n<li>失败恢复：设置重试、回退、暂停和人工介入机制。</li>\n</ul>\n<p>例如，修复一个 Bug 的循环不应该只是：</p>\n<pre><code class=\"language-text\">让 Agent 修复 Bug → Agent 说已经修复\n</code></pre>\n<p>更可靠的设计是：</p>\n<pre><code class=\"language-text\">读取失败测试 → 分析原因 → 修改代码 → 运行测试\n    ↑                                  ↓\n    └──── 测试失败：记录错误并进入下一轮 ────┘\n                         ↓\n                 测试通过并完成人工确认\n</code></pre>\n<h3>4. 循环工程的风险</h3>\n<p>循环会放大系统原本的优点和缺点。目标不清晰时，Agent 可能反复空转；验证信号不可靠时，Agent 可能“通过了测试但没有解决真实问题”；上下文没有刷新时，它会基于过期假设继续工作；权限过大时，错误可能被自动扩大。</p>\n<p>因此，循环工程必须遵守几个原则：</p>\n<ul>\n<li>从范围很小、结果可验证的任务开始；</li>\n<li>明确写出停止条件，而不是只写“把问题修好”；</li>\n<li>优先采用小范围、可回退的修改；</li>\n<li>自动验证和人工审查同时保留；</li>\n<li>对循环次数、成本、超时和权限设置上限；</li>\n<li>把失败案例沉淀为新的规则、节点或上下文资料。</li>\n</ul>\n<h2>七、节点工程：把任务拆成可组合的单元</h2>\n<h3 id=\"2-内循环与外循环\">1. 什么是节点工程</h3>\n<p>理解循环之后，再看节点会更自然：循环负责让系统持续推进，节点则是循环中可以被调用、验证和组合的具体工作单元。节点工程关注的是，一个复杂任务应该拆成哪些步骤，以及这些步骤如何通过状态、条件和输出连接起来。</p>\n<p>在工作流或 Agent 系统中，一个节点通常代表一次明确的动作，例如接收输入、检索资料、调用模型、执行工具、校验结果或请求人工确认。节点工程的核心不是把流程画得复杂，而是让每个节点都有清晰的输入、处理逻辑、输出和失败处理方式。</p>\n<h3 id=\"3-循环工程的关键组成\">2. 节点应该如何设计</h3>\n<p>一个可靠的节点至少应该明确下面几件事：</p>\n<table>\n<thead>\n<tr>\n<th>设计内容</th>\n<th>需要回答的问题</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>输入</td>\n<td>节点需要哪些数据？数据来自用户、上一个节点还是外部工具？</td>\n</tr>\n<tr>\n<td>处理</td>\n<td>节点具体完成什么动作？使用模型、规则还是普通代码？</td>\n</tr>\n<tr>\n<td>输出</td>\n<td>后续节点需要什么结构和字段？</td>\n</tr>\n<tr>\n<td>失败</td>\n<td>输入无效、调用失败或结果不合格时怎么办？</td>\n</tr>\n<tr>\n<td>验证</td>\n<td>如何判断节点已经完成并且结果可信？</td>\n</tr>\n<tr>\n<td>权限</td>\n<td>节点是否可以写文件、调用接口或执行高风险操作？</td>\n</tr>\n</tbody></table>\n<p>例如，一个资料问答流程可以拆成：</p>\n<pre><code class=\"language-text\">用户问题\n  → 问题改写节点\n  → 知识检索节点\n  → 证据筛选节点\n  → 回答生成节点\n  → 引用与格式校验节点\n  → 最终回复\n</code></pre>\n<p>这里的每个节点都有相对单一的职责。检索失败时可以重试或提示资料不足，格式校验失败时可以重新生成，而不需要让整个流程从头开始。</p>\n<h3 id=\"4-循环工程的风险\">3. 节点工程与 Agent 的关系</h3>\n<p>并不是所有节点都必须由大模型完成。确定性强的任务，例如字段校验、权限判断、数值计算和状态更新，通常更适合使用普通代码或规则；需要理解自然语言、处理模糊意图和进行开放式判断的部分，才适合交给模型或 Agent。</p>\n<p>因此，节点设计需要在“智能”和“确定性”之间做取舍：</p>\n<ul>\n<li>固定规则优先使用代码或工作流节点；</li>\n<li>需要理解和规划时使用模型节点或 Agent 节点；</li>\n<li>有副作用的动作前增加校验和人工确认节点；</li>\n<li>每个节点尽量只做一件能够被验证的事情。</li>\n</ul>\n<p>知乎相关文章中也强调，企业级 Agent 通常不是单个模型调用，而是由模型、知识库、工具、工作流、权限和日志共同组成；其中工作流负责流程节点、条件分支、变量处理和人工确认，Agent 负责理解任务和动态调用能力。</p>\n<h2 id=\"七节点工程把任务拆成可组合的单元\">八、五种工程如何连成一条链路</h2>\n<p>可以把一个可靠的 Agent 任务分成下面几层：</p>\n<pre><code class=\"language-text\">用户目标\n    ↓\n提示词工程：明确任务、角色、格式和边界\n    ↓\n上下文工程：准备文档、历史、工具结果和任务状态\n    ↓\n驾驭工程：编排能力、限制权限、保存状态、执行验证\n    ↓\n循环工程：根据观察结果继续行动，直到满足停止条件\n    ↓\n节点工程：把动作拆成节点，连接顺序、分支和协作关系\n    ↓\n最终交付：结果可验证、过程可追踪、失败可恢复\n</code></pre>\n<p>在实际使用中，可以按照故障表现定位问题：</p>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>更可能的问题层次</th>\n<th>改进方向</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>回答模糊、格式错误</td>\n<td>提示词工程</td>\n<td>收紧指令，提供模板和示例</td>\n</tr>\n<tr>\n<td>不知道项目背景、遗漏关键资料</td>\n<td>上下文工程</td>\n<td>调整检索、过滤和信息组织</td>\n</tr>\n<tr>\n<td>多轮任务逐渐跑偏</td>\n<td>上下文或状态管理</td>\n<td>压缩历史，保存外部状态，重新组装上下文</td>\n</tr>\n<tr>\n<td>没测试就宣布完成</td>\n<td>驾驭工程</td>\n<td>增加完成标准和自动验证门槛</td>\n</tr>\n<tr>\n<td>反复失败或陷入循环</td>\n<td>驾驭工程</td>\n<td>增加重试上限、错误分类和恢复策略</td>\n</tr>\n<tr>\n<td>越改越乱、文档逐渐失效</td>\n<td>熵管理</td>\n<td>定期扫描、重构和更新文档</td>\n</tr>\n</tbody></table>\n<h2>九、一个可复用的 Agent 任务模板</h2>\n<p>下面的模板把五种工程的思路放到了一起：</p>\n<pre><code class=\"language-text\">你是一名【角色】。\n\n任务目标：\n完成【具体任务】，最终交付【可验收的结果】。\n\n可用上下文：\n&lt;project_context&gt;\n【项目背景、相关文件、已有约定】\n&lt;/project_context&gt;\n\n工作规则：\n1. 开始修改前，先检查相关文件和现有实现。\n2. 将任务拆成可独立验证的步骤。\n3. 只修改任务范围内的文件。\n4. 不确定时说明缺少的信息，不要猜测。\n5. 每完成一个阶段，运行对应的测试或检查。\n6. 所有验证通过前，不要宣布任务完成。\n\n输出要求：\n1. 先给出执行计划。\n2. 完成后列出修改内容。\n3. 给出运行过的验证命令和结果。\n4. 说明仍然存在的风险或未解决问题。\n</code></pre>\n<p>这段模板仍然不是万能的，但它已经从“请 AI 帮我做事”推进到“让 AI 在一个有边界、有反馈、有验收标准的流程里做事”。</p>\n<h2>十、学习顺序和实践建议</h2>\n<p>如果刚开始接触这三个概念，不建议一开始就研究复杂的 Agent 框架。更适合按照下面的顺序练习：</p>\n<h3 id=\"3-节点工程与-agent-的关系\">第一阶段：先学会写清楚任务</h3>\n<p>选择一个每天都会做的事情，例如总结文章、写邮件、整理会议记录或修改代码。为同一个任务写三版提示词，逐步补充受众、用途、格式和限制，比较输出差异。</p>\n<h3>第二阶段：学会管理输入资料</h3>\n<p>把一份长文档或一个小项目交给 AI，观察它需要哪些资料才能完成任务。练习把资料分成“必须知道”“可能有用”和“当前无关”三类，再比较全部输入和筛选输入的效果。</p>\n<h3>第三阶段：把任务拆成步骤</h3>\n<p>将复杂任务拆成提取、分析、执行和验证几个阶段。前一步的结果作为下一步的输入，每一步都保留清晰的中间产物，而不是让 Agent 在一个超长对话里完成全部工作。</p>\n<h3>第四阶段：建立验证和反馈</h3>\n<p>为任务定义“什么算完成”。对于写作任务，可以检查结构、字数和事实来源；对于代码任务，可以检查类型、Lint、单元测试、端到端流程和浏览器行为。验证结果应该成为 Agent 下一轮行动的依据。</p>\n<h3 id=\"第一阶段先学会写清楚任务\">第五阶段：形成自己的工程资产</h3>\n<p>长期使用后，可以保存：</p>\n<ul>\n<li>常用提示词模板；</li>\n<li>项目上下文入口文件；</li>\n<li>任务拆解模板；</li>\n<li>自动化验证脚本；</li>\n<li>常见失败案例；</li>\n<li>提示词和工作流的版本记录。</li>\n</ul>\n<p>这样积累的就不只是几段好用的提示词，而是一套可重复使用的 AI 工作系统。</p>\n<h2>十一、最后的理解</h2>\n<p>提示词工程、上下文工程、节点工程、驾驭工程和循环工程确实是连在一起的，但它们解决的问题不在同一个层面：</p>\n<pre><code class=\"language-text\">提示词工程：让模型听懂\n上下文工程：让模型知道\n驾驭工程：让 Agent 做成\n</code></pre>\n<p>提示词工程是起点，因为任务必须先被表达清楚；上下文工程决定模型是否拥有完成任务所需的信息；节点工程把复杂任务拆成可组合、可验证的执行单元；驾驭工程为这些单元提供权限、状态和质量护栏；循环工程则让系统能够根据反馈持续推进，直到达到明确的完成条件。</p>\n<p>真正成熟的 AI 应用，不是依赖一条越来越长的提示词，而是把这五层结合起来：用清晰指令定义方向，用精准上下文提供依据，用节点组织任务，用驾驭工程设置护栏，再用循环和反馈不断修正过程。</p>\n<p>从这个角度看，AI 时代的工程师价值也正在发生变化。过去更多是直接编写每一行代码，未来则会越来越多地设计任务、上下文、工具、约束和反馈系统，构建一个能够帮助人稳定完成工作的“智能生产环境”。</p>\n<h2>资料来源</h2>\n<ul>\n<li><a href=\"https://www.runoob.com/ai-agent/prompt-engineering.html\">菜鸟教程：提示词工程</a></li>\n<li><a href=\"https://www.runoob.com/ai-agent/harness-engineering.html\">菜鸟教程：Harness Engineering（驾驭工程）</a></li>\n<li><a href=\"https://www.runoob.com/ai-agent/loop-engineering.html\">菜鸟教程：Loop Engineering（循环工程）</a></li>\n<li><a href=\"https://zhuanlan.zhihu.com/p/2062944981837427275\">知乎：节点、工作流与 AI Agent 相关内容</a></li>\n<li><a href=\"https://www.bilibili.com/video/BV1dpQTB3EXg/\">Bilibili：相关视频</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "一本周主要工作",
        "text": "一、本周主要工作"
      },
      {
        "depth": 3,
        "slug": "1-坤元智算官网生态合作页面与首页优化",
        "text": "1. 坤元智算官网：生态合作页面与首页优化"
      },
      {
        "depth": 4,
        "slug": "页面建设",
        "text": "页面建设"
      },
      {
        "depth": 4,
        "slug": "hero-视觉和品牌色彩",
        "text": "Hero 视觉和品牌色彩"
      },
      {
        "depth": 4,
        "slug": "导航与交互调整",
        "text": "导航与交互调整"
      },
      {
        "depth": 4,
        "slug": "首页算力资源卡片",
        "text": "首页算力资源卡片"
      },
      {
        "depth": 4,
        "slug": "解决方案详情页的加载体验",
        "text": "解决方案详情页的加载体验"
      },
      {
        "depth": 4,
        "slug": "主要修改文件与验证",
        "text": "主要修改文件与验证"
      },
      {
        "depth": 4,
        "slug": "视觉设计经验",
        "text": "视觉设计经验"
      },
      {
        "depth": 3,
        "slug": "2-个人视觉分析平台从功能开发转向完整闭环",
        "text": "2. 个人视觉分析平台：从功能开发转向完整闭环"
      },
      {
        "depth": 4,
        "slug": "系统架构与职责边界",
        "text": "系统架构与职责边界"
      },
      {
        "depth": 4,
        "slug": "一次视频任务的完整流程",
        "text": "一次视频任务的完整流程"
      },
      {
        "depth": 4,
        "slug": "任务worker-和媒体管理",
        "text": "任务、Worker 和媒体管理"
      },
      {
        "depth": 4,
        "slug": "规则引擎区域和事件证据",
        "text": "规则引擎、区域和事件证据"
      },
      {
        "depth": 4,
        "slug": "事件审核查询和回放",
        "text": "事件审核、查询和回放"
      },
      {
        "depth": 4,
        "slug": "性能和可靠性优化",
        "text": "性能和可靠性优化"
      },
      {
        "depth": 4,
        "slug": "视频平台性能实测与进一步优化",
        "text": "视频平台性能实测与进一步优化"
      },
      {
        "depth": 4,
        "slug": "标注视频编码的瓶颈定位",
        "text": "标注视频编码的瓶颈定位"
      },
      {
        "depth": 4,
        "slug": "性能优化后的阶段性结论",
        "text": "性能优化后的阶段性结论"
      },
      {
        "depth": 2,
        "slug": "二先理解五种工程分别在解决什么",
        "text": "二、先理解五种工程分别在解决什么"
      },
      {
        "depth": 2,
        "slug": "三提示词工程把任务说明白",
        "text": "三、提示词工程：把任务说明白"
      },
      {
        "depth": 3,
        "slug": "1-提示词工程是什么",
        "text": "1. 提示词工程是什么"
      },
      {
        "depth": 3,
        "slug": "2-四要素框架",
        "text": "2. 四要素框架"
      },
      {
        "depth": 3,
        "slug": "3-用示例和模板控制输出",
        "text": "3. 用示例和模板控制输出"
      },
      {
        "depth": 2,
        "slug": "产品分析",
        "text": "产品分析"
      },
      {
        "depth": 3,
        "slug": "核心优势",
        "text": "核心优势"
      },
      {
        "depth": 3,
        "slug": "主要风险",
        "text": "主要风险"
      },
      {
        "depth": 3,
        "slug": "综合建议",
        "text": "综合建议"
      },
      {
        "depth": 3,
        "slug": "4-让模型给证据而不是只给结论",
        "text": "4. 让模型给证据，而不是只给结论"
      },
      {
        "depth": 3,
        "slug": "5-提示词工程的边界",
        "text": "5. 提示词工程的边界"
      },
      {
        "depth": 2,
        "slug": "四上下文工程管理模型的工作记忆",
        "text": "四、上下文工程：管理模型的工作记忆"
      },
      {
        "depth": 3,
        "slug": "1-上下文不只是提示词",
        "text": "1. 上下文不只是提示词"
      },
      {
        "depth": 3,
        "slug": "2-上下文是有限资源",
        "text": "2. 上下文是有限资源"
      },
      {
        "depth": 3,
        "slug": "3-上下文工程的常见组成",
        "text": "3. 上下文工程的常见组成"
      },
      {
        "depth": 3,
        "slug": "4-资料和指令要分离",
        "text": "4. 资料和指令要分离"
      },
      {
        "depth": 2,
        "slug": "五驾驭工程让-agent-稳定完成工作",
        "text": "五、驾驭工程：让 Agent 稳定完成工作"
      },
      {
        "depth": 3,
        "slug": "1-驾驭工程是什么",
        "text": "1. 驾驭工程是什么"
      },
      {
        "depth": 3,
        "slug": "2-agent-的典型失败模式",
        "text": "2. Agent 的典型失败模式"
      },
      {
        "depth": 3,
        "slug": "3-四类核心护栏",
        "text": "3. 四类核心护栏"
      },
      {
        "depth": 4,
        "slug": "上下文护栏让-agent-知道该看什么",
        "text": "上下文护栏：让 Agent 知道该看什么"
      },
      {
        "depth": 4,
        "slug": "架构护栏让错误难以进入系统",
        "text": "架构护栏：让错误难以进入系统"
      },
      {
        "depth": 4,
        "slug": "反馈护栏让-agent-知道自己做错了",
        "text": "反馈护栏：让 Agent 知道自己做错了"
      },
      {
        "depth": 4,
        "slug": "熵管理护栏持续清理系统混乱",
        "text": "熵管理护栏：持续清理系统混乱"
      },
      {
        "depth": 3,
        "slug": "4-驾驭工程与传统-agent-框架的关系",
        "text": "4. 驾驭工程与传统 Agent 框架的关系"
      },
      {
        "depth": 2,
        "slug": "六循环工程让系统持续推进目标",
        "text": "六、循环工程：让系统持续推进目标"
      },
      {
        "depth": 3,
        "slug": "1-什么是循环工程",
        "text": "1. 什么是循环工程"
      },
      {
        "depth": 3,
        "slug": "2-内循环与外循环",
        "text": "2. 内循环与外循环"
      },
      {
        "depth": 3,
        "slug": "3-循环工程的关键组成",
        "text": "3. 循环工程的关键组成"
      },
      {
        "depth": 3,
        "slug": "4-循环工程的风险",
        "text": "4. 循环工程的风险"
      },
      {
        "depth": 2,
        "slug": "七节点工程把任务拆成可组合的单元",
        "text": "七、节点工程：把任务拆成可组合的单元"
      },
      {
        "depth": 3,
        "slug": "1-什么是节点工程",
        "text": "1. 什么是节点工程"
      },
      {
        "depth": 3,
        "slug": "2-节点应该如何设计",
        "text": "2. 节点应该如何设计"
      },
      {
        "depth": 3,
        "slug": "3-节点工程与-agent-的关系",
        "text": "3. 节点工程与 Agent 的关系"
      },
      {
        "depth": 2,
        "slug": "八五种工程如何连成一条链路",
        "text": "八、五种工程如何连成一条链路"
      },
      {
        "depth": 2,
        "slug": "九一个可复用的-agent-任务模板",
        "text": "九、一个可复用的 Agent 任务模板"
      },
      {
        "depth": 2,
        "slug": "十学习顺序和实践建议",
        "text": "十、学习顺序和实践建议"
      },
      {
        "depth": 3,
        "slug": "第一阶段先学会写清楚任务",
        "text": "第一阶段：先学会写清楚任务"
      },
      {
        "depth": 3,
        "slug": "第二阶段学会管理输入资料",
        "text": "第二阶段：学会管理输入资料"
      },
      {
        "depth": 3,
        "slug": "第三阶段把任务拆成步骤",
        "text": "第三阶段：把任务拆成步骤"
      },
      {
        "depth": 3,
        "slug": "第四阶段建立验证和反馈",
        "text": "第四阶段：建立验证和反馈"
      },
      {
        "depth": 3,
        "slug": "第五阶段形成自己的工程资产",
        "text": "第五阶段：形成自己的工程资产"
      },
      {
        "depth": 2,
        "slug": "十一最后的理解",
        "text": "十一、最后的理解"
      },
      {
        "depth": 2,
        "slug": "资料来源",
        "text": "资料来源"
      }
    ]
  },
  {
    "id": "2026-08-28",
    "title": "8/25–8/28 周报：官网优化、视觉分析平台与 AI Agent 工程理解",
    "description": "总结本周坤元智算官网页面优化、个人视觉分析平台链路升级，以及从提示词工程到循环工程和节点工程的学习理解。",
    "publishedAt": "2026-08-28",
    "updatedAt": "2026-08-28",
    "draft": false,
    "html": "<h2 id=\"这周主要做了什么\">这周主要做了什么</h2>\n<p>这周主要围绕三个方向展开：一是完成坤元智算官网生态合作页面和首页细节优化；二是继续完善个人视觉分析平台，从视频上传、异步处理、YOLO 推理扩展到事件审核和检测回放；三是结合 AI 编程实践，进一步理解提示词工程、上下文工程、驾驭工程、循环工程和节点工程之间的关系。</p>\n<h2 id=\"一坤元智算官网主要工作\">一、坤元智算官网主要工作</h2>\n<h3 id=\"1-完成生态合作页面\">1. 完成生态合作页面</h3>\n<p>本周对官网新增了 <code>/partner</code> 生态合作页面，页面定位是展示坤元智算面向个人用户、开发者、创作者、AI 应用、模型厂商以及科研公益团队的合作方式、伙伴权益和合作流程。</p>\n<p>页面结构主要包括：</p>\n<ul>\n<li>Hero 首屏：突出“连接算力与产业，共建智算新生态”的主题；</li>\n<li>合作模式：使用 6 张信息卡片，以三列网格展示不同合作对象、适用对象、合作方式和核心激励；</li>\n<li>伙伴权益：使用两列卡片展示算力能力、技术扶持、联合推广和生态共建；</li>\n<li>合作流程：用三个步骤说明提交方向、沟通确认和项目协同；</li>\n<li>底部 CTA：使用独立的蓝色渐变圆角块，和上下内容保持留白。</li>\n</ul>\n<p>这次页面调整没有继续使用连续的横向表格，而是改成更容易分组阅读的卡片结构。这样用户可以先根据合作对象找到对应卡片，再阅读合作方式和权益，信息层级比原来的表格式排版更加清楚。</p>\n<p>同时移除了生态伙伴案例和“查看更多合作场景”入口，避免页面内容继续扩张。合作模式和伙伴权益也没有增加额外的数字标签，让页面重点回到合作内容本身。</p>\n<h3 id=\"2-hero-背景和视觉体系\">2. Hero 背景和视觉体系</h3>\n<p>Hero 当前使用中等亮度的蓝色数字地平线背景。设计时主要考虑了三个问题：中文标题是否清楚、背景是否有科技感、移动端裁切后是否仍然完整。</p>\n<p>最终保留的背景图具有以下特点：</p>\n<ul>\n<li>使用蓝色科技感，不以紫色或粉色作为主视觉；</li>\n<li>中心区域保持低细节，为标题和说明文字留出空间；</li>\n<li>两侧和底部保留网络节点、数据流和数字结构；</li>\n<li>不在背景图中加入 Logo、文字、水印或 UI 面板；</li>\n<li>桌面端能够铺满首屏，同时兼顾移动端裁切。</li>\n</ul>\n<p>页面整体继续使用浅蓝、白色、深蓝和青色高光组成的品牌体系，并将之前偏紫色的元素统一调整为坤元智算的蓝色方向。</p>\n<h3 id=\"3-顶部导航和首页入口\">3. 顶部导航和首页入口</h3>\n<p>顶部导航进行了重新整理：</p>\n<ul>\n<li>移除“软件产品”主导航入口，但保留相关页面和路由；</li>\n<li>在对应位置加入“生态合作”，链接到 <code>/partner</code>；</li>\n<li>恢复“联系我们”入口，不将“快速开始”放到主导航中；</li>\n<li>保持桌面端和移动端的导航内容一致；</li>\n<li>生态合作页面首屏使用透明导航，滚动后切换为白色背景。</li>\n</ul>\n<p>这里需要区分“导航入口”和“页面能力”：移除主导航只是改变用户进入页面的方式，并没有删除软件产品页面，也没有破坏原有路由。</p>\n<p>“快速开始”则调整为全站右下角的悬浮助手。入口只保留聊天符号，点击后展开“坤元小助手”面板。面板保留欢迎语、聊天记录和四个快捷问题，内容在浮层内部滚动，不会撑长页面。</p>\n<p>四个快捷问题包括：</p>\n<ol>\n<li>我想了解算力服务；</li>\n<li>帮我分析一个 AI 产品需求；</li>\n<li>我需要搭建行业解决方案；</li>\n<li>帮我生成一个坤元智算的企业宣传图。</li>\n</ol>\n<p>当前助手主要用于演示交互，暂时不显示输入框和发送按钮，用户通过快捷提问触发预设流程。</p>\n<h3 id=\"4-首页视觉细节和解决方案加载体验\">4. 首页视觉细节和解决方案加载体验</h3>\n<p>首页算力资源区域主要调整了视觉层级，没有修改价格数据和核心业务逻辑：</p>\n<ul>\n<li>普通资源卡片增加更清晰的渐变背景和边框；</li>\n<li>增加 hover 上浮和阴影，区分卡片层级；</li>\n<li>提升价格、参数说明和分隔线的可读性；</li>\n<li>CTA 卡片使用更明显的蓝色渐变、发光边框和实心主按钮；</li>\n<li>保持 GPU 标签右对齐，方便用户横向比较。</li>\n</ul>\n<p>导航滚动时出现白线的问题，最终确认是实体底部边框和半透明背景叠加后产生了过强的边缘对比。处理方式是移除 <code>border-b</code>，保留 <code>backdrop-blur-md</code>，使用更明确的半透明背景和柔和阴影，让导航和页面内容之间衔接更加自然。</p>\n<p>针对“自研 IB 网络解决方案”和“超节点集群建设方案”详情页点击后短暂闪现的问题，使用浏览器 Network 面板分析了首次加载过程。页面代码本身只有约 5 KB，拓扑 SVG 资源也很小，明显的瓶颈是导航 Logo 首次加载耗时较长。</p>\n<p>这部分做了两项优化：</p>\n<ul>\n<li>使用项目现有图片压缩脚本，将导航 Logo 从约 33 KB 压缩到约 4.8 KB，原始资源按规则保留备份；</li>\n<li>保留路由懒加载，同时新增统一的页面预加载逻辑，在卡片鼠标移入、键盘聚焦或触摸开始时提前加载目标页面代码。</li>\n</ul>\n<p>加载状态没有继续使用旋转图标，因为它会让用户更明显地感知到等待。当前只保留固定高度的占位，避免页面布局跳动，同时让预加载逻辑在后台完成。</p>\n<h3 id=\"5-全站图标和验证\">5. 全站图标和验证</h3>\n<p>站点浏览器图标复用现有的纯 Logo 资源，在 <code>index.html</code> 中补充 favicon 配置，没有新增重复的图标文件。</p>\n<p>本周官网相关修改已经完成页面路由、页面内容、结构检查、TypeScript、ESLint 和生产构建验证。生态合作页面和解决方案页面预加载的回归检查也已通过。</p>\n<h3 id=\"6-视觉设计比例间距和版式\">6. 视觉设计：比例、间距和版式</h3>\n<p>在整理生态合作页面和首页视觉层级时，也补充学习了比例系统在网页设计中的作用。不同的比例适合不同的表达重点：黄金比例更强调自然、优雅和动态；白银比例更接近纸张和结构化系统；三分法以及 4:5、16:9 等比例，则更适合现代网页、海报和传播媒介。</p>\n<p>常见比例包括：</p>\n<ul>\n<li>黄金比例：<code>1:1.618</code>，层次更有张力，适合视觉重点和动态构图；</li>\n<li>白银比例：<code>1:√2</code>，适合纸张、文档和规则化版式；</li>\n<li><code>1.25</code>：小三度，适合温和、紧凑的字号和间距层级；</li>\n<li><code>1.333</code>：大三度，层次清晰，适合一般网页排版；</li>\n<li><code>1.5</code>：纯四度，适合区分标题与正文、卡片与留白；</li>\n<li><code>2</code>：倍增比例，适合制造强烈的视觉对比。</li>\n</ul>\n<p>实际设计中不需要执着于某一个比例，而是要结合页面内容和屏幕媒介选择合适的系统：</p>\n<ul>\n<li>UI 界面优先使用 <code>4px</code>、<code>8px</code>、<code>12px</code>、<code>16px</code>、<code>24px</code>、<code>32px</code> 这类倍数间距；</li>\n<li>海报常用 <code>2:3</code>、<code>3:4</code>、<code>4:5</code>，比黄金比例更容易落地；</li>\n<li>网页横幅可以使用 <code>16:9</code> 或 <code>3:1</code>，需要电影感时使用 <code>21:9</code>；</li>\n<li>正文为 <code>16px</code> 时，标题可以按 <code>1.25</code>、<code>1.333</code> 或 <code>1.5</code> 倍递进；</li>\n<li>版式分栏可以尝试 <code>1:1</code>、<code>1:2</code>、<code>2:3</code>、<code>3:5</code>，不必强行使用黄金分割。</li>\n</ul>\n<p>这部分学习对本次页面修改的启发是：视觉设计不是把页面装饰得更复杂，而是用稳定的比例、间距和层级帮助用户理解内容。合作模式使用卡片、Hero 保留中心留白、CTA 单独拉开层级，本质上都是在解决信息比例和阅读节奏问题。</p>\n<h2 id=\"二个人视觉分析平台\">二、个人视觉分析平台</h2>\n<h3 id=\"1-从视频上传到事件审核的完整链路\">1. 从视频上传到事件审核的完整链路</h3>\n<p>个人视觉分析平台本周的重点，是把原本“上传视频并调用模型”的功能，继续扩展为可以追踪和审核的视频事件分析闭环：</p>\n<pre><code class=\"language-text\">视频上传 → Redis 任务队列 → Worker 抽帧 → YOLO 推理\n    → 规则判断 → 事件与证据入库 → 事件审核 → 视频回放\n</code></pre>\n<p>各组件的职责进一步明确：</p>\n<table>\n<thead>\n<tr>\n<th>组件</th>\n<th>主要职责</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>React 前端</td>\n<td>上传视频、展示任务状态、查看证据和审核事件</td>\n</tr>\n<tr>\n<td>Rust API</td>\n<td>接收请求、保存任务、提供查询和审核接口</td>\n</tr>\n<tr>\n<td>Redis Stream</td>\n<td>传递异步任务消息，避免视频分析阻塞 HTTP 请求</td>\n</tr>\n<tr>\n<td>Worker</td>\n<td>抽帧、调用 YOLO、执行规则并写回结果</td>\n</tr>\n<tr>\n<td>MySQL</td>\n<td>保存任务、事件、规则、状态和审核历史</td>\n</tr>\n<tr>\n<td>FFmpeg</td>\n<td>抽取视频帧并编码检测回放</td>\n</tr>\n<tr>\n<td>YOLO 服务</td>\n<td>识别目标类别、置信度和边界框</td>\n</tr>\n</tbody></table>\n<p>最重要的数据原则是：MySQL 保存业务事实，Redis 负责任务调度，媒体目录保存视频和图片，Worker 负责计算，API 对外提供访问入口。</p>\n<h3 id=\"2-视频任务worker-和事件证据\">2. 视频任务、Worker 和事件证据</h3>\n<p>平台现在支持视频上传、任务创建、状态更新和本地媒体保存。Worker 在 <code>WORKER_MODE=1</code> 时从 Redis Stream <code>vision:jobs</code> 消费任务，使用 FFmpeg 抽取 JPEG 帧，再将帧发送给 YOLO 服务。</p>\n<p>任务状态、处理进度和源文件路径会持久化到 MySQL。上传文件名会先进行安全清理，避免路径穿越；任务删除时区分数据库记录、原始视频和检测回放，避免误删其他任务的媒体文件。</p>\n<p>事件详情已经从占位图片改为真实的抽帧证据，证据帧保存到：</p>\n<pre><code class=\"language-text\">media/evidence/&lt;event-id&gt;/\n</code></pre>\n<p>每张证据帧包含时间点、检测框和目标信息，前端可以在事件详情中查看对应画面。页面同时增加中文检测摘要，并将内部事件类型转换成更容易理解的文字，例如：</p>\n<pre><code class=\"language-text\">person_stay        → 人员停留\nperson_enter_zone  → 人员进入区域\nperson_count_limit → 最大人员数量\n</code></pre>\n<h3 id=\"3-规则配置和事件审核\">3. 规则配置和事件审核</h3>\n<p>规则方面增加了人员停留、人员进入区域和最大人员数量规则，支持最低置信度、最短持续时间、人数上限和启用状态。区域编辑支持矩形和多边形绘制，也支持使用空白画布或视频证据作为绘制背景。</p>\n<p>之前出现过重置区域后点击“应用区域”没有效果的问题，排查后完成了规则保存和区域应用的修复。Worker 开始任务前会从 MySQL 刷新规则，确保新任务使用最新配置，而不是继续使用旧的内存规则。</p>\n<p>区域外目标仍然触发事件的问题也已经修复。鼠标绘制区域时增加节流，减少频繁更新导致的卡顿或黑屏。</p>\n<p>事件状态现在支持：</p>\n<pre><code class=\"language-text\">待复核 → 确认 / 忽略 → 已处置 → 已关闭\n             └──────→ 处理中\n</code></pre>\n<p>审核操作会保存审核人、备注、处置结果、旧状态、新状态和审核时间。详情页增加审核时间线，审核按钮增加处理中状态、成功和失败反馈以及重复提交保护。</p>\n<p>事件列表还支持组合查询、分页、排序、CSV 导出和 HTML 报告。这样事件不再只是模型输出，而是成为可以筛选、确认、追踪和导出的业务记录。</p>\n<h3 id=\"4-性能优化和批量推理\">4. 性能优化和批量推理</h3>\n<p>前端任务状态轮询从固定的每秒一次，改成逐步退避：</p>\n<pre><code class=\"language-text\">1 秒 → 2 秒 → 3 秒 → 4 秒 → 5 秒\n</code></pre>\n<p>任务状态发生变化时重新使用较短间隔，长时间没有变化时降低请求频率。事件列表接口也增加了 <code>limit</code> 参数，默认最多返回 50 条，避免数据量增加后一次加载过多内容。</p>\n<p>视频上传改为流式写入临时文件，上传完成并校验后再切换为最终文件。这样可以降低大视频上传时的内存峰值，失败时也能清理不完整的临时文件。</p>\n<p>检测间隔从 100ms 调整为 200ms，并支持通过 <code>DETECTION_INTERVAL_MS</code> 配置。这个调整可以降低非实时分析场景下的计算压力，但间隔不能无限增大，否则可能漏掉持续时间很短的事件。</p>\n<p>YOLO 新增批量推理接口：</p>\n<pre><code class=\"language-http\">POST /v1/infer/batch\n</code></pre>\n<p>Worker 默认每批处理 4 帧，同时将批次并发限制为 2。批量结果通过 <code>frame_id</code> 关联原始帧，通过 <code>timestamp_ms</code> 恢复视频时间轴，不能简单依赖返回数组顺序。批量大小和并发数都设置了范围限制，避免配置过大造成内存或显存压力。</p>\n<h3 id=\"5-yolo-检测回放和时间轴修复\">5. YOLO 检测回放和时间轴修复</h3>\n<p>视频任务增加检测回放地址、状态和错误信息。Worker 完成推理后生成带检测框、类别名称、置信度和时间戳的 MP4 回放。</p>\n<p>详情页现在可以在“原始视频”和“YOLO 检测回放”之间切换，默认播放原始视频，避免用户把带框回放误认为原视频。切换视频源时重新创建或正确更新 <code>&lt;video&gt;</code> 元素，解决了切换后仍复用旧视频源的问题。</p>\n<p>之前出现过两个问题：回放帧率偏低、帧数不足，以及原本 9 秒的视频生成后变成 24 秒。排查后确认，检测帧数量不能直接代表原视频时长。</p>\n<p>现在的处理方式是：</p>\n<ol>\n<li>用 FFprobe 读取原视频真实时长、FPS 和总帧数；</li>\n<li>YOLO 继续按照可控间隔抽帧，控制推理成本；</li>\n<li>按原视频的总帧数补齐或保持最近一次检测画面；</li>\n<li>按原视频 FPS 进行编码；</li>\n<li>回放时长优先使用原视频时长。</li>\n</ol>\n<p>这次修复把“推理采样频率”和“最终视频时间轴”分开处理。批次大小和并发数只影响推理速度，不应该改变回放的时长和帧数。</p>\n<h2 id=\"三从提示词工程到循环工程的学习\">三、从提示词工程到循环工程的学习</h2>\n<p>本周最后一个学习重点，是把提示词工程、上下文工程、驾驭工程、循环工程和节点工程放到同一条逻辑链中理解。这五个概念不是互相孤立的技巧，而是 AI 从一次回答走向复杂任务执行时逐步扩大的工程范围。</p>\n<h3 id=\"1-提示词工程让模型听懂\">1. 提示词工程：让模型听懂</h3>\n<p>提示词工程解决的是如何把任务说清楚。它通过角色、指令、背景、格式和限制条件，减少模型对任务的猜测。</p>\n<p>比如“帮我写个方案”就比较模糊，而“你是一名市场营销顾问，请为一款面向大学生的记账 App 制定推广方案，列出目标用户、推广渠道和预算建议，并用 Markdown 表格输出”就明确得多。</p>\n<p>提示词工程的重点不是写出越来越长的指令，而是让任务目标、输出标准和禁止事项没有歧义。需要固定格式时，可以使用模板和少量高质量示例；涉及事实判断时，要让模型提供依据，并允许它在资料不足时说“不知道”。</p>\n<h3 id=\"2-上下文工程让模型知道\">2. 上下文工程：让模型知道</h3>\n<p>提示词只是模型输入的一部分。模型实际看到的上下文还包括历史对话、项目文档、当前代码、检索结果、工具返回值、用户偏好和任务状态。</p>\n<p>提示词写得越长，模型并不会因此无限知道更多内容。上下文窗口是有限的，多轮对话和长文档如果不经过管理，就可能丢失关键信息，产生上下文腐化。</p>\n<p>上下文工程要做的不是把所有资料都塞进去，而是完成召回、过滤、压缩和组装：找到当前任务相关的资料，删除噪声和重复内容，压缩过长历史，再按照模型容易理解的顺序组织输入。对于 AI 编程来说，项目结构、接口约定、错误日志、测试命令和修改记录，往往比一条更长的提示词更加重要。</p>\n<h3 id=\"3-驾驭工程让-agent-稳定运行\">3. 驾驭工程：让 Agent 稳定运行</h3>\n<p>当 AI 从一次回答变成连续执行几十个步骤时，就需要驾驭工程。它关注工具、权限、状态、测试、监控、错误恢复和持续维护。</p>\n<p>驾驭工程可以提供几类护栏：上下文护栏让 Agent 知道应该看哪些文档；架构护栏用类型系统、Linter 和 CI 阻止错误结构进入项目；反馈护栏把测试、日志和 Review 结果返回给 Agent；熵管理则持续清理过时文档、重复代码和技术债务。</p>\n<p>它解决的是 Agent 长期工作时的可靠性问题。人类不一定要亲自完成每个机械步骤，但仍然需要掌握目标、边界、权限和最终验收标准。</p>\n<h3 id=\"4-循环工程让-agent-持续推进\">4. 循环工程：让 Agent 持续推进</h3>\n<p>循环工程关注的是反馈闭环。一个基本的 Agent Loop 通常包含意图、上下文、行动、观察和调整五个阶段。</p>\n<p>例如修复 Bug 时，Agent 先读取失败测试，分析原因，修改代码，然后重新运行测试。如果测试失败，错误信息就应该成为下一轮的新上下文；如果测试通过，还要根据风险进行人工确认。</p>\n<p>循环工程和提示词工程的区别在于：提示词工程关注“一次怎么问”，循环工程关注“什么时候问、下一步做什么、结果是否合格以及何时停止”。循环还可以分成内循环和外循环：内循环负责读文件、修改代码和运行测试，外循环负责发现任务、分派执行、保存状态和开启下一轮。</p>\n<p>要让循环可靠运行，必须明确成功条件、停止条件、重试上限、超时和权限边界。目标不清楚时，Agent 会空转；验证不可靠时，它会误判完成；权限过大时，错误还可能被自动扩大。</p>\n<h3 id=\"5-节点工程把复杂任务组织起来\">5. 节点工程：把复杂任务组织起来</h3>\n<p>理解循环之后，再理解节点工程会更自然。循环解决的是“系统如何持续推进”，节点工程解决的是“每一轮具体由哪些动作组成，以及这些动作如何连接”。</p>\n<p>一个节点可以代表输入清洗、知识检索、模型调用、工具执行、结果校验或人工确认。每个节点都应该有明确的输入、处理逻辑、输出、失败处理、验证方式和权限范围。</p>\n<p>并不是所有节点都需要大模型。权限判断、字段校验、数值计算和状态更新更适合使用代码或规则；自然语言理解、开放式判断和复杂规划才适合交给模型或 Agent。当节点增加后，还需要考虑顺序、条件分支、并行执行、回退路径和多个 Agent 的协作。</p>\n<h3 id=\"6-五种工程的整体关系\">6. 五种工程的整体关系</h3>\n<p>这五种工程不是互相替代，而是逐层扩展：</p>\n<pre><code class=\"language-text\">提示词工程：让模型听懂任务\n上下文工程：让模型获得正确资料\n驾驭工程：让 Agent 在边界内运行\n循环工程：让 Agent 根据反馈持续推进\n节点工程：让复杂动作能够拆分、连接和协作\n</code></pre>\n<p>一条完整的 Agent 工作链路，可以理解为：先用提示词定义目标和规则，再用上下文提供资料；然后通过驾驭工程接入工具、权限和验证机制；接着用循环工程让 Agent 反复行动、观察和调整；最后用节点工程把不同动作组织成清晰的流程结构。</p>\n<h2 id=\"四本周的总结\">四、本周的总结</h2>\n<p>本周的官网工作主要解决了页面结构、导航关系、视觉层级和加载体验问题；视觉分析平台则继续完善了从视频上传到事件审核的完整链路，并通过批量推理、流式上传、轮询退避和回放时间轴修复降低系统压力、提高结果可靠性。</p>\n<p>这些工作表面上分属于前端页面、后端服务和模型调用，但背后有一个共同点：不能只关注某个局部功能能不能运行，还要关注它在完整链路中的职责、输入、输出、状态和验证方式。</p>\n<p>这也和今天对五种工程的理解联系起来了。提示词决定任务是否表达清楚，上下文决定模型是否拥有正确资料，驾驭工程决定 Agent 是否受到合理约束，循环工程决定它能否根据反馈继续推进，节点工程则决定复杂流程是否容易拆分、复用和维护。</p>\n<p>以后使用 AI 协助开发时，重点不应该只是“让 AI 多写一些代码”，而应该是设计一个能够理解目标、获取信息、调用工具、接受反馈并最终交付结果的工作系统。</p>\n<h2 id=\"资料来源\">资料来源</h2>\n<ul>\n<li><a href=\"https://www.runoob.com/ai-agent/prompt-engineering.html\">菜鸟教程：提示词工程</a></li>\n<li><a href=\"https://www.runoob.com/ai-agent/harness-engineering.html\">菜鸟教程：Harness Engineering（驾驭工程）</a></li>\n<li><a href=\"https://www.runoob.com/ai-agent/loop-engineering.html\">菜鸟教程：Loop Engineering（循环工程）</a></li>\n<li><a href=\"https://zhuanlan.zhihu.com/p/2062944981837427275\">知乎：节点、工作流与 AI Agent 相关内容</a></li>\n<li><a href=\"https://www.bilibili.com/video/BV1dpQTB3EXg/\">Bilibili：相关视频</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "这周主要做了什么",
        "text": "这周主要做了什么"
      },
      {
        "depth": 2,
        "slug": "一坤元智算官网主要工作",
        "text": "一、坤元智算官网主要工作"
      },
      {
        "depth": 3,
        "slug": "1-完成生态合作页面",
        "text": "1. 完成生态合作页面"
      },
      {
        "depth": 3,
        "slug": "2-hero-背景和视觉体系",
        "text": "2. Hero 背景和视觉体系"
      },
      {
        "depth": 3,
        "slug": "3-顶部导航和首页入口",
        "text": "3. 顶部导航和首页入口"
      },
      {
        "depth": 3,
        "slug": "4-首页视觉细节和解决方案加载体验",
        "text": "4. 首页视觉细节和解决方案加载体验"
      },
      {
        "depth": 3,
        "slug": "5-全站图标和验证",
        "text": "5. 全站图标和验证"
      },
      {
        "depth": 3,
        "slug": "6-视觉设计比例间距和版式",
        "text": "6. 视觉设计：比例、间距和版式"
      },
      {
        "depth": 2,
        "slug": "二个人视觉分析平台",
        "text": "二、个人视觉分析平台"
      },
      {
        "depth": 3,
        "slug": "1-从视频上传到事件审核的完整链路",
        "text": "1. 从视频上传到事件审核的完整链路"
      },
      {
        "depth": 3,
        "slug": "2-视频任务worker-和事件证据",
        "text": "2. 视频任务、Worker 和事件证据"
      },
      {
        "depth": 3,
        "slug": "3-规则配置和事件审核",
        "text": "3. 规则配置和事件审核"
      },
      {
        "depth": 3,
        "slug": "4-性能优化和批量推理",
        "text": "4. 性能优化和批量推理"
      },
      {
        "depth": 3,
        "slug": "5-yolo-检测回放和时间轴修复",
        "text": "5. YOLO 检测回放和时间轴修复"
      },
      {
        "depth": 2,
        "slug": "三从提示词工程到循环工程的学习",
        "text": "三、从提示词工程到循环工程的学习"
      },
      {
        "depth": 3,
        "slug": "1-提示词工程让模型听懂",
        "text": "1. 提示词工程：让模型听懂"
      },
      {
        "depth": 3,
        "slug": "2-上下文工程让模型知道",
        "text": "2. 上下文工程：让模型知道"
      },
      {
        "depth": 3,
        "slug": "3-驾驭工程让-agent-稳定运行",
        "text": "3. 驾驭工程：让 Agent 稳定运行"
      },
      {
        "depth": 3,
        "slug": "4-循环工程让-agent-持续推进",
        "text": "4. 循环工程：让 Agent 持续推进"
      },
      {
        "depth": 3,
        "slug": "5-节点工程把复杂任务组织起来",
        "text": "5. 节点工程：把复杂任务组织起来"
      },
      {
        "depth": 3,
        "slug": "6-五种工程的整体关系",
        "text": "6. 五种工程的整体关系"
      },
      {
        "depth": 2,
        "slug": "四本周的总结",
        "text": "四、本周的总结"
      },
      {
        "depth": 2,
        "slug": "资料来源",
        "text": "资料来源"
      }
    ]
  },
  {
    "id": "2026-08-26-two-day-summary",
    "title": "8/25–8/26 修改、新增与学习总结",
    "description": "总结 8 月 25 日至 26 日在坤元智算门户网站和个人视觉分析平台中的页面修改、视频分析链路升级、性能优化、问题排查与验证结果。",
    "publishedAt": "2026-08-26",
    "draft": false,
    "html": "<h3 id=\"这两天主要做了什么\">这两天主要做了什么</h3>\n<p>这两天同时推进了两个项目，但工作的内容不一样：坤元门户网站主要集中在页面结构、导航关系和视觉细节；个人视觉分析平台主要集中在视频任务链路、YOLO 检测回放、规则判断、事件审核和性能优化。</p>\n<p>坤元门户网站的工作偏前端页面完善，重点是把生态合作页面和首页细节调整得更清楚、更稳定。视觉分析平台的工作则更偏完整链路验证，从视频上传、Redis 任务队列、Worker 抽帧、YOLO 推理，到事件入库、证据展示和视频回放，逐步把原本分散的功能串起来。</p>\n<h2 id=\"一坤元智算门户网站\">一、坤元智算门户网站</h2>\n<h3 id=\"1-生态合作页面\">1. 生态合作页面</h3>\n<p>这两天完成了生态合作页面及其路由，并参考了 SiliconFlow 合作伙伴页面的信息组织方式。参考的重点是页面结构和视觉层级，而不是直接复制页面内容。</p>\n<p>页面主要完成了以下调整：\nHero：</p>\n<ul>\n<li>使用原创生成的算力生态背景图</li>\n<li>当前版本保持“左侧标题 + 右侧视觉”布局</li>\n<li>额外生成了一版适合标题居中的背景图，但暂未启用</li>\n</ul>\n<p>合作模式：</p>\n<ul>\n<li>从原来的横向信息表格改为 6 张卡片</li>\n<li>桌面端三列排列，移动端单列</li>\n<li>每张卡片包含：<ul>\n<li>适用对象</li>\n<li>合作方式</li>\n<li>核心激励</li>\n</ul>\n</li>\n<li>合作类型参考了硅基流动、腾讯云、华为云等国内页面</li>\n</ul>\n<p>伙伴权益：</p>\n<ul>\n<li>恢复 01 / 02 / 03 / 04 编号卡片布局</li>\n<li>增加大号背景编号和 hover 层次</li>\n<li>内容围绕算力、技术、市场推广和生态共建展开</li>\n</ul>\n<p>合作流程：</p>\n<ul>\n<li>新增三步流程：<ol>\n<li>提交合作方向</li>\n<li>完成技术与业务沟通</li>\n<li>开启项目协同与长期合作</li>\n</ol>\n</li>\n</ul>\n<p>已移除：</p>\n<ul>\n<li>生态伙伴案例</li>\n<li>合作伙伴 Logo 墙</li>\n<li>更多合作场景入口</li>\n</ul>\n<p>色彩：</p>\n<ul>\n<li>已将之前偏紫色的元素全部改为坤元智算的蓝色体系</li>\n</ul>\n<p>其他：</p>\n<ul>\n<li>首页导航中的“软件产品”入口已移除，但软件产品路由仍然保留</li>\n<li>TypeScript、ESLint、结构检查和生产构建均已通过</li>\n</ul>\n<p>相关文件主要集中在：</p>\n<pre><code class=\"language-text\">app/src/features/partner/\napp/src/app/router.tsx\napp/src/app/routePaths.ts\napp/src/app/layouts/Nav.tsx\nscripts/verify/partner-page.mjs\n</code></pre>\n<p>其中页面组件负责内容和视觉结构，路由文件负责页面能否被访问，导航组件负责入口展示，验证脚本则把页面要求固定下来。这样修改页面时，不只是看浏览器效果，也能检查页面是否仍然满足约定。</p>\n<h3 id=\"2-首页导航调整\">2. 首页导航调整</h3>\n<p>首页导航中移除了“软件产品”入口，但软件产品页面和路由仍然保留，因此这次调整只是改变用户从首页进入页面的方式，并没有删除原有页面能力。</p>\n<p>同时保留了“生态合作”入口，并通过页面验证、TypeScript 和 ESLint 检查确认路由关系没有被破坏。</p>\n<h3 id=\"3-导航滚动白线问题\">3. 导航滚动白线问题</h3>\n<p>首页导航滚动到不同颜色区域时，底部出现了一条比较明显的白线，影响导航和下方内容之间的衔接。</p>\n<p>排查后确认，问题主要不是布局高度，而是滚动态导航的实体底部边框和半透明背景叠加后产生了过强的边缘对比。最后采用的修改方式是：</p>\n<pre><code class=\"language-text\">移除 border-b\n保留 backdrop-blur-md\n使用更明确的 bg-white/30\n保留柔和阴影\n</code></pre>\n<h3 id=\"4-首页算力资源卡片\">4. 首页算力资源卡片</h3>\n<p>首页算力资源区域原本存在几个问题：普通资源卡片之间的层级不够明显，边框和分隔线颜色接近，小字号文字对比度偏低，不同计费方式放在一起时不容易横向比较，CTA 卡片和普通资源卡片的角色差异也不够突出。</p>\n<p>这次主要调整了视觉表达，没有修改价格数据和核心交互逻辑：</p>\n<ul>\n<li>普通卡片增加渐变背景和更清晰的边框；</li>\n<li>增加 hover 上浮和阴影效果；</li>\n<li>GPU 标签保持右对齐；</li>\n<li>提升价格、参数说明和底部分隔线的可读性；</li>\n<li>CTA 卡使用更明显的蓝色渐变、发光边框和阴影；</li>\n<li>CTA 按钮改为更清晰的实心主按钮，并增强 hover 反馈。</li>\n</ul>\n<p>这部分的重点不是增加更多装饰，而是让用户更容易区分普通资源、价格信息和主要行动入口。视觉层级清楚以后，页面不需要依赖大量文字解释，用户也能理解不同卡片的作用。</p>\n<h2 id=\"二个人视觉分析平台\">二、个人视觉分析平台</h2>\n<h3 id=\"1-从视频上传到事件审核的完整链路\">1. 从视频上传到事件审核的完整链路</h3>\n<p>这两天对视觉分析平台的主要升级，是把系统从“上传视频并调用模型”推进到一条可以验证的视频事件分析闭环，加上修复一些bug和测试功能：</p>\n<pre><code class=\"language-text\">上传视频\n    ↓\nRedis vision:jobs\n    ↓\nWorker 抽帧\n    ↓\nYOLO 推理\n    ↓\n规则引擎\n    ↓\n事件和证据入库\n    ↓\n事件流与详情审核\n    ↓\n原始视频或 YOLO 检测回放\n</code></pre>\n<p>MySQL 负责保存任务、事件、规则和审核历史；Redis 负责异步任务消息；视频和图片目前保存在本地 <code>media</code> 目录。这样的划分让视频文件、长期业务数据和临时任务消息各自承担不同职责。</p>\n<h3 id=\"2-视频任务与-worker\">2. 视频任务与 Worker</h3>\n<p>平台现在支持视频上传、任务创建、状态更新和本地媒体保存。Worker 在 <code>WORKER_MODE=1</code> 时从 Redis Stream <code>vision:jobs</code> 消费任务，并使用 FFmpeg 对本地视频抽帧，再将视频帧交给 YOLO 服务处理。</p>\n<p>任务状态、处理进度和源文件路径会持久化到 MySQL。上传文件名会先进行安全清理，避免文件名中包含路径穿越内容。任务也支持重命名、删除和软删除，删除时需要区分数据库记录、原始视频和检测回放，不能因为删除一条任务而误删其他任务的媒体文件。</p>\n<p>Worker 调用 YOLO 服务的单帧接口时，需要把检测类别、置信度、边界框、时间戳和模型版本转换成平台内部使用的数据结构。边界框还要兼容像素坐标和 0～1 的归一化坐标，否则会出现检测框位置偏移的问题。</p>\n<h3 id=\"3-事件证据和规则配置\">3. 事件证据和规则配置</h3>\n<p>事件详情已经从占位图片改为真实抽帧证据。证据帧保存到：</p>\n<pre><code class=\"language-text\">media/evidence/&lt;event-id&gt;/\n</code></pre>\n<p>证据帧包含时间点、检测框和目标信息，前端可以在事件详情中查看对应画面。页面还增加了中文检测摘要，例如“人员 11 次检测 · 平均置信度 34%”，并将内部事件类型转换成更容易理解的文字：</p>\n<pre><code class=\"language-text\">person_stay        → 人员停留\nperson_enter_zone  → 人员进入区域\nperson_count_limit → 最大人员数量\n</code></pre>\n<p>规则方面增加了人员停留、人员进入区域和最大人员数量规则，支持最低置信度、最短持续时间、人数上限和启用状态。区域编辑支持鼠标拖动绘制矩形和多边形，也支持空白画布和视频证据作为绘制背景。</p>\n<p>之前出现过一个问题：重置区域后点击“应用区域”没有效果。修复后，规则保存和区域应用能够按照新的配置执行。Worker 开始任务前还会从 MySQL 刷新规则，保证保存后的规则能够应用到新任务，而不是继续使用旧的内存配置。</p>\n<p>另外，区域外目标仍触发事件的问题也已经修复。鼠标移动绘制增加了节流，避免拖动区域时前端频繁更新导致卡顿或黑屏。</p>\n<h3 id=\"4-事件审核和查询\">4. 事件审核和查询</h3>\n<p>事件状态扩展为：</p>\n<pre><code class=\"language-text\">待复核 → 确认 / 忽略 → 已处置 → 已关闭\n             └──────→ 处理中\n</code></pre>\n<p>审核操作支持审核人、备注和处置结果，并保存旧状态、新状态、审核时间和审核历史。详情页可以展示审核时间线，审核按钮增加处理中状态、成功提示、失败提示和重复提交保护。</p>\n<p>事件查询还增加了组合查询、分页、排序、CSV 导出和 HTML 报告接口。这样事件不仅能被检测出来，也能够被筛选、确认、追踪和导出，逐渐从单纯的模型结果变成可操作的业务记录。</p>\n<h2 id=\"三视觉平台的性能优化\">三、视觉平台的性能优化</h2>\n<h3 id=\"1-减少前端状态轮询\">1. 减少前端状态轮询</h3>\n<p>浏览器中大量重复出现的 <code>GET</code> 请求主要来自任务状态轮询，而不是重复上传。原来的轮询固定每 1 秒请求一次，视频处理时间越长，请求数量越多。</p>\n<p>这次改成退避轮询：</p>\n<pre><code class=\"language-text\">1 秒 → 2 秒 → 3 秒 → 4 秒 → 5 秒\n</code></pre>\n<p>任务状态发生变化时重新使用较短间隔；任务长时间没有变化时逐步降低请求频率。这样可以减少 API 和浏览器端的压力，同时不影响任务最终状态的获取。</p>\n<p>事件列表接口增加 <code>limit</code> 参数，默认最多返回 50 条，前端显式请求有限数量的数据，避免事件数量增长后一次返回过多内容。</p>\n<h3 id=\"2-上传过程改为流式写入\">2. 上传过程改为流式写入</h3>\n<p>视频上传不再一次性读取到内存，而是按数据块写入临时文件，上传完成后再进行最终文件切换：</p>\n<pre><code class=\"language-text\">接收上传数据\n    ↓\n写入临时文件\n    ↓\n上传完成并校验\n    ↓\n切换为最终文件\n</code></pre>\n<p>这样可以降低大视频上传时的内存峰值。上传失败时也可以清理临时文件，不会留下一个看起来完整、实际内容不完整的视频文件。</p>\n<h3 id=\"3-降低默认检测频率\">3. 降低默认检测频率</h3>\n<p>视频检测间隔从 100ms 调整为 200ms，并支持通过环境变量配置：</p>\n<pre><code class=\"language-text\">DETECTION_INTERVAL_MS=200\n</code></pre>\n<p>有效范围为 100ms 至 5000ms。对于非实时分析场景，降低抽帧和推理频率可以减少 CPU、GPU、磁盘和 YOLO 服务压力。这个参数不能盲目调大，因为间隔过大可能会漏掉持续时间很短的事件，后续需要结合真实视频验证准确率。</p>\n<h3 id=\"4-yolo-批量推理\">4. YOLO 批量推理</h3>\n<p>新增批量推理接口：</p>\n<pre><code class=\"language-http\">POST /v1/infer/batch\n</code></pre>\n<p>一次请求上传多张帧图片，并携带每帧的 <code>frame_id</code> 和 <code>timestamp_ms</code>。当前默认每批处理 4 帧：</p>\n<pre><code class=\"language-text\">YOLO_BATCH_SIZE=4\n</code></pre>\n<p>服务端将多张图片作为一个 batch 传给 Ultralytics YOLO，避免每一帧都单独建立 HTTP 请求和执行一次完整调用。</p>\n<p>Worker 使用有上限的并发处理，默认最多同时执行 2 个批次：</p>\n<pre><code class=\"language-text\">YOLO_CONCURRENCY=2\n</code></pre>\n<p>并发参数限制在 1 至 4 之间，批量大小限制在 1 至 16 之间，避免配置过大导致内存或显存压力失控。</p>\n<p>批量响应不能简单依赖数组顺序，而是通过 <code>frame_id</code> 关联原始帧。即使不同批次返回顺序不同，也不会把检测结果写入错误的帧。Worker 收集并发结果后，会恢复处理顺序，同时保留每帧的时间戳、检测框和原始帧路径。</p>\n<p>一个批量请求可以抽象成下面的形式：</p>\n<pre><code class=\"language-json\">{\n  &quot;frames&quot;: [\n    {\n      &quot;frame_id&quot;: &quot;frame-001&quot;,\n      &quot;timestamp_ms&quot;: 0,\n      &quot;image&quot;: &quot;...&quot;\n    },\n    {\n      &quot;frame_id&quot;: &quot;frame-002&quot;,\n      &quot;timestamp_ms&quot;: 200,\n      &quot;image&quot;: &quot;...&quot;\n    }\n  ]\n}\n</code></pre>\n<p>其中 <code>timestamp_ms</code> 用来恢复视频时间轴，<code>frame_id</code> 用来保证结果关联正确，图片内容则交给 YOLO 服务完成批量推理。</p>\n<h2 id=\"四yolo-检测回放\">四、YOLO 检测回放</h2>\n<h3 id=\"1-检测回放\">1. 检测回放</h3>\n<p>视频任务增加检测回放地址、状态和错误信息字段。Worker 完成推理后生成带检测框的 MP4 回放，回放画面包含检测框、类别名称、置信度和时间戳。</p>\n<p>详情页增加“原始视频”和“YOLO 检测回放”两个选项，默认播放原始视频，避免用户把带框回放误认为原视频。切换视频源后会重新创建或正确更新 <code>&lt;video&gt;</code> 元素，修复了切换后仍然复用旧视频源的问题。</p>\n<p>删除任务时只清理对应的检测回放，不删除原始视频，避免不同用途的文件被混在一起处理。</p>\n<h3 id=\"2-回放低帧率和时长错误\">2. 回放低帧率和时长错误</h3>\n<p>之前出现过两个明显问题：回放帧率偏低、帧数不足，以及原本 9 秒的视频生成后变成 24 秒。排查后确认，检测结果的数量不能代表原视频的时长，也不能直接用检测帧数量计算回放时长。</p>\n<p>现在的处理逻辑是：</p>\n<ol>\n<li>使用 FFprobe 读取原视频真实时长、FPS 和总帧数；</li>\n<li>YOLO 仍按可控抽帧间隔推理，避免推理成本过高；</li>\n<li>根据原视频总帧数复制或保持最近一次检测画面；</li>\n<li>按原视频 FPS 编码；</li>\n<li>回放时长优先使用原视频时长，不再使用检测结果条数计算。</li>\n</ol>\n<p>批次大小和并发数只影响推理速度，不应改变视频时长或回放帧数。这次修复的关键是把“推理采样频率”和“最终视频时间轴”分开处理。</p>\n<h2 id=\"五claude-code-课程学习收获\">五、Claude Code 课程学习收获</h2>\n<p>这两天还补充学习了 DeepLearning.AI 与 Anthropic 的《Claude Code: A Highly Agentic Coding Assistant》课程。课程的重点不是让 AI 代替开发者写更多代码，而是说明如何把 AI 编程助手放进正常的软件工程流程中：先理解项目，再明确上下文，随后规划、实现、测试、调试、重构，最后通过 Git 和自动化工具保持修改可追踪。</p>\n<h3 id=\"1-先理解代码库再开始修改\">1. 先理解代码库，再开始修改</h3>\n<p>课程反复强调，面对陌生项目时不要直接让 Agent 修改代码，而应该先了解项目结构、启动方式、依赖关系、数据流和现有约定。稳定的项目知识可以写入 <code>CLAUDE.md</code> 一类的项目说明文件，具体任务再通过文件名、目录和问题描述补充上下文。</p>\n<p>这个方法也适用于这两天的实际工作。修改坤元门户网站时，需要先定位导航组件、路由配置和页面组件，不能只看到页面上的文字就直接修改。排查视觉分析平台的问题时，也需要沿着 API、Redis、Worker、YOLO 和视频编码的调用链逐段确认，不能只根据浏览器现象猜原因。</p>\n<h3 id=\"2-先规划再小步修改\">2. 先规划，再小步修改</h3>\n<p>课程建议复杂任务先拆分目标、相关文件、实现步骤和验证方式，再开始编码。修改过程中尽量保持范围明确，一次解决一个问题，避免同时改动过多模块后无法判断是哪一处引入了问题。</p>\n<p>这两天的两个项目也采用了类似方式。门户网站先分别处理生态合作页面、导航入口、滚动白线和算力卡片；视觉分析平台则把任务队列、规则区域、事件审核、回放和批量推理分开处理。这样每次修改都有明确的目标，出现问题时也更容易回到对应的代码路径。</p>\n<h3 id=\"3-用测试和运行结果反馈修改\">3. 用测试和运行结果反馈修改</h3>\n<p>课程中比较有价值的一点，是把测试、日志、浏览器截图和实际运行结果作为 Agent 的反馈，而不是只把错误信息交给 Agent 让它凭感觉修复。推荐的过程是：先复现问题，再定位调用链，必要时增加测试，修改后重新运行验证。</p>\n<p>这也是本次回放问题能够解决的原因。9 秒视频生成成 24 秒时，不能只修改播放器显示，而是要检查 FFprobe 读取的原视频时长、抽帧数量、编码 FPS 和回放时长计算方式，最后确认检测结果数量不能直接代表视频时长。批量推理也需要通过 <code>frame_id</code> 和 <code>timestamp_ms</code> 验证结果没有写错帧，而不能只确认接口返回成功。</p>\n<h3 id=\"4-项目记忆和自动化检查\">4. 项目记忆和自动化检查</h3>\n<p>课程还介绍了项目级说明文件、Git worktree、GitHub、Hooks、MCP 和自动化检查。它们解决的是不同问题：项目说明文件保存稳定知识，Worktree 隔离并行任务，Git 记录变更，Hooks 和 CI 自动执行检查，MCP 让开发助手可以使用设计稿、浏览器或其他外部工具。</p>\n<p>课程中的具体命令和工具配置可能会随版本变化，不能直接当作当前操作手册使用；但“清晰上下文、先规划、小步修改、自动验证、保留可追踪记录”这套方法仍然适用。</p>\n<h2 id=\"六总结\">六、总结</h2>\n<p>这两天在两个项目上完成的事情不同，但都不只是表面修改。坤元门户网站重点解决了页面入口、合作伙伴展示和首页视觉层级问题，并通过类型检查、ESLint、构建和页面验证确认修改结果。视觉分析平台则进一步打通了视频任务、Redis 队列、Worker 抽帧、YOLO 推理、规则判断、事件审核和检测回放，同时对轮询、上传、抽帧和批量推理进行了性能优化。</p>\n<p>另外，通过 Claude Code 课程的学习，我对“如何使用 AI 协助开发”也有了更明确的认识。AI 更适合参与调查、规划、实现和验证，但前提是开发者提供清晰的项目上下文，并对修改结果负责。实际工作中仍然需要自己确认需求、判断根因、选择修改范围，并通过测试和运行结果确认是否解决了问题。</p>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "这两天主要做了什么",
        "text": "这两天主要做了什么"
      },
      {
        "depth": 2,
        "slug": "一坤元智算门户网站",
        "text": "一、坤元智算门户网站"
      },
      {
        "depth": 3,
        "slug": "1-生态合作页面",
        "text": "1. 生态合作页面"
      },
      {
        "depth": 3,
        "slug": "2-首页导航调整",
        "text": "2. 首页导航调整"
      },
      {
        "depth": 3,
        "slug": "3-导航滚动白线问题",
        "text": "3. 导航滚动白线问题"
      },
      {
        "depth": 3,
        "slug": "4-首页算力资源卡片",
        "text": "4. 首页算力资源卡片"
      },
      {
        "depth": 2,
        "slug": "二个人视觉分析平台",
        "text": "二、个人视觉分析平台"
      },
      {
        "depth": 3,
        "slug": "1-从视频上传到事件审核的完整链路",
        "text": "1. 从视频上传到事件审核的完整链路"
      },
      {
        "depth": 3,
        "slug": "2-视频任务与-worker",
        "text": "2. 视频任务与 Worker"
      },
      {
        "depth": 3,
        "slug": "3-事件证据和规则配置",
        "text": "3. 事件证据和规则配置"
      },
      {
        "depth": 3,
        "slug": "4-事件审核和查询",
        "text": "4. 事件审核和查询"
      },
      {
        "depth": 2,
        "slug": "三视觉平台的性能优化",
        "text": "三、视觉平台的性能优化"
      },
      {
        "depth": 3,
        "slug": "1-减少前端状态轮询",
        "text": "1. 减少前端状态轮询"
      },
      {
        "depth": 3,
        "slug": "2-上传过程改为流式写入",
        "text": "2. 上传过程改为流式写入"
      },
      {
        "depth": 3,
        "slug": "3-降低默认检测频率",
        "text": "3. 降低默认检测频率"
      },
      {
        "depth": 3,
        "slug": "4-yolo-批量推理",
        "text": "4. YOLO 批量推理"
      },
      {
        "depth": 2,
        "slug": "四yolo-检测回放",
        "text": "四、YOLO 检测回放"
      },
      {
        "depth": 3,
        "slug": "1-检测回放",
        "text": "1. 检测回放"
      },
      {
        "depth": 3,
        "slug": "2-回放低帧率和时长错误",
        "text": "2. 回放低帧率和时长错误"
      },
      {
        "depth": 2,
        "slug": "五claude-code-课程学习收获",
        "text": "五、Claude Code 课程学习收获"
      },
      {
        "depth": 3,
        "slug": "1-先理解代码库再开始修改",
        "text": "1. 先理解代码库，再开始修改"
      },
      {
        "depth": 3,
        "slug": "2-先规划再小步修改",
        "text": "2. 先规划，再小步修改"
      },
      {
        "depth": 3,
        "slug": "3-用测试和运行结果反馈修改",
        "text": "3. 用测试和运行结果反馈修改"
      },
      {
        "depth": 3,
        "slug": "4-项目记忆和自动化检查",
        "text": "4. 项目记忆和自动化检查"
      },
      {
        "depth": 2,
        "slug": "六总结",
        "text": "六、总结"
      }
    ]
  },
  {
    "id": "2026-08-25",
    "title": "Vision Analysis Agent 系统架构学习整理",
    "description": "整理视觉事件检索平台的系统架构、数据模型、异步处理链路和部署理解。",
    "publishedAt": "2026-08-25",
    "draft": false,
    "html": "<h1>Vision Analysis Agent 系统架构学习整理</h1>\n<h2 id=\"一本次学习的核心结论\">一、本次学习的核心结论</h2>\n<p>这个系统不是“上传接口直接分析视频”，而是将一次视频处理拆成三个阶段：<strong>API 快速接收任务、Redis 排队、Worker 后台分析</strong>。这样可以把耗时的 <code>ffmpeg</code> 抽帧、YOLO 推理和事件生成从 HTTP 请求中分离出来。</p>\n<p>四项核心技术的职责边界如下：</p>\n<table>\n<thead>\n<tr>\n<th>技术</th>\n<th>在本系统中的核心职责</th>\n<th>不能替代的部分</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Nginx</td>\n<td>统一入口、前端静态文件、API 反向代理、媒体文件访问</td>\n<td>不负责视频分析和业务数据保存</td>\n</tr>\n<tr>\n<td>MySQL</td>\n<td>持久化任务、事件、规则及状态，支持查询、索引和事务</td>\n<td>不适合承担大文件存储和高频队列消费</td>\n</tr>\n<tr>\n<td>Redis</td>\n<td>使用 Stream 传递异步任务，承载短期队列状态</td>\n<td>不能作为任务最终结果的唯一存储</td>\n</tr>\n<tr>\n<td>Docker</td>\n<td>封装运行环境、服务网络、数据卷和启动依赖</td>\n<td>不等于自动完成监控、备份和高可用</td>\n</tr>\n</tbody></table>\n<p>最重要的数据原则是：<strong>MySQL 保存事实，Redis 负责调度，媒体卷保存视频，Worker 执行计算，Nginx 负责对外提供访问入口。</strong></p>\n<h2 id=\"二整体架构与一次请求的完整链路\">二、整体架构与一次请求的完整链路</h2>\n<pre><code class=\"language-text\">浏览器 / React\n       │  HTTP / JSON / multipart\n       ▼\nNginx（前端容器内）\n       ├── /              → 返回 React 静态文件\n       ├── /api/          → Rust API:8080\n       └── /media/        → 读取共享媒体目录\n                              │\n                              ├── MySQL 8.4：任务、事件、规则\n                              └── Redis 7 Stream：vision:jobs\n                                             │\n                                             ▼\n                                     Rust Worker\n                                             │\n                              ffmpeg 抽帧 → YOLO 推理\n                                             │\n                                             ▼\n                                  规则引擎生成事件\n                                             │\n                                             ▼\n                                        MySQL 写回\n</code></pre>\n<p>一次视频上传的主要步骤如下：</p>\n<ol>\n<li>React 通过 <code>POST /api/v1/videos/upload</code> 发送视频文件。</li>\n<li>API 对文件名进行清理，将视频写入 <code>/app/media</code>，并读取视频时长。</li>\n<li>API 创建一条 <code>video_jobs</code> 任务记录，初始状态为 <code>pending</code> 或 <code>processing</code>。</li>\n<li>API 向 Redis Stream <code>vision:jobs</code> 写入包含 <code>job_id</code> 的消息，并立即返回任务信息。</li>\n<li>Worker 从 Stream 读取任务，根据 <code>job_id</code> 从 MySQL 或内存状态加载任务。</li>\n<li>Worker 调用 <code>ffmpeg</code> 抽取 JPEG 帧，再调用 <code>YOLO_URL/v1/infer/frame</code> 识别目标。</li>\n<li>规则引擎根据目标类别、置信度和持续时间生成业务事件。</li>\n<li>Worker 将事件写入 <code>events</code>，同时把任务状态更新为 <code>completed</code> 或 <code>failed</code>。</li>\n<li>React 轮询 <code>GET /api/v1/jobs/{id}</code>，完成后刷新事件列表。</li>\n</ol>\n<h2 id=\"三nginx系统的统一入口\">三、Nginx：系统的统一入口</h2>\n<h3 id=\"1-nginx-在本系统中的作用\">1. Nginx 在本系统中的作用</h3>\n<p>Nginx 位于浏览器和 Rust API 之间，主要解决四个问题：</p>\n<ul>\n<li><strong>静态资源服务</strong>：直接返回 React 构建产物，不让 Rust API 处理 JS、CSS 和图片。</li>\n<li><strong>反向代理</strong>：将 <code>/api/</code> 请求转发给内部服务 <code>api:8080</code>。</li>\n<li><strong>统一访问地址</strong>：浏览器只需要访问同一个域名和端口，不需要知道后端容器地址。</li>\n<li><strong>媒体文件访问</strong>：从共享目录返回视频或证据文件，并设置访问缓存策略。</li>\n</ul>\n<p>当前 Nginx 并不是单独的 Compose 服务，而是通过 <code>frontend/Dockerfile</code> 进入前端运行镜像：构建阶段生成 <code>dist</code>，运行阶段使用 <code>nginx:1.27-alpine</code> 提供页面和代理。</p>\n<h3 id=\"2-nginx-请求示例\">2. Nginx 请求示例</h3>\n<pre><code class=\"language-text\">浏览器请求：POST /api/v1/videos/upload\n        ↓\nNginx 匹配 location /api/\n        ↓\nDocker 内部转发：http://api:8080/api/v1/videos/upload\n        ↓\nRust API 保存文件并返回 job_id\n</code></pre>\n<p>Nginx 的价值在于稳定接口边界：API 将来可以从单实例扩展为多个实例，前端仍然请求 <code>/api/...</code>。如果需要扩容，可进一步配置 <code>upstream api_servers</code>，但视频 Worker 的扩容应通过增加 Worker 容器数量完成，而不是让 Nginx 直接参与任务调度。</p>\n<h2 id=\"四mysql系统的事实来源和持久化中心\">四、MySQL：系统的事实来源和持久化中心</h2>\n<h3 id=\"1-mysql-保存什么\">1. MySQL 保存什么</h3>\n<p>MySQL 保存系统中需要长期查询、审核和追踪的事实：</p>\n<ul>\n<li>视频处理任务的名称、时长、状态、进度和来源地址；</li>\n<li>由检测和规则引擎生成的事件及证据；</li>\n<li>事件规则、目标类别、阈值和规则版本；</li>\n<li>任务与事件之间的关联关系；</li>\n<li>删除时间、创建时间和更新时间等审计所需信息。</li>\n</ul>\n<p>视频文件本身不放在 MySQL 中。当前实现将视频写入共享卷 <code>media_data</code>，在 <code>video_jobs.source_uri</code> 中保存路径。生产环境可以把文件替换为对象存储 URI，但数据库仍只保存元数据和地址。</p>\n<h3 id=\"2-核心表结构\">2. 核心表结构</h3>\n<h4 id=\"videojobs视频任务表\"><code>video_jobs</code>：视频任务表</h4>\n<table>\n<thead>\n<tr>\n<th>字段</th>\n<th>类型</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td><code>CHAR(36)</code></td>\n<td>UUID 主键，标识一次处理任务</td>\n</tr>\n<tr>\n<td><code>filename</code></td>\n<td><code>VARCHAR(255)</code></td>\n<td>原始或清理后的视频文件名</td>\n</tr>\n<tr>\n<td><code>duration_ms</code></td>\n<td><code>BIGINT UNSIGNED</code></td>\n<td>视频时长，单位毫秒</td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td><code>VARCHAR(32)</code></td>\n<td><code>pending</code>、<code>processing</code>、<code>completed</code>、<code>failed</code>、<code>cancelled</code></td>\n</tr>\n<tr>\n<td><code>progress</code></td>\n<td><code>TINYINT UNSIGNED</code></td>\n<td>处理进度，通常为 0–100</td>\n</tr>\n<tr>\n<td><code>source_uri</code></td>\n<td><code>TEXT</code></td>\n<td>视频文件路径或对象存储地址</td>\n</tr>\n<tr>\n<td><code>deleted_at</code></td>\n<td><code>DATETIME</code></td>\n<td>软删除时间，空值表示有效</td>\n</tr>\n<tr>\n<td><code>created_at</code> / <code>updated_at</code></td>\n<td><code>TIMESTAMP</code></td>\n<td>创建和更新时间</td>\n</tr>\n</tbody></table>\n<h4 id=\"events视觉事件表\"><code>events</code>：视觉事件表</h4>\n<table>\n<thead>\n<tr>\n<th>字段</th>\n<th>类型</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>id</code></td>\n<td><code>CHAR(36)</code></td>\n<td>事件 UUID</td>\n</tr>\n<tr>\n<td><code>job_id</code></td>\n<td><code>CHAR(36)</code></td>\n<td>外键，关联 <code>video_jobs.id</code></td>\n</tr>\n<tr>\n<td><code>event_type</code></td>\n<td><code>VARCHAR(128)</code></td>\n<td>业务事件，例如 <code>person_stay</code></td>\n</tr>\n<tr>\n<td><code>start_time_ms</code> / <code>end_time_ms</code></td>\n<td><code>BIGINT UNSIGNED</code></td>\n<td>事件在视频中的时间范围</td>\n</tr>\n<tr>\n<td><code>severity</code></td>\n<td><code>VARCHAR(32)</code></td>\n<td>风险等级，如 <code>high</code>、<code>medium</code></td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td><code>VARCHAR(32)</code></td>\n<td><code>unreviewed</code>、<code>confirmed</code>、<code>ignored</code></td>\n</tr>\n<tr>\n<td><code>confidence</code></td>\n<td><code>FLOAT</code></td>\n<td>事件置信度</td>\n</tr>\n<tr>\n<td><code>objects_json</code></td>\n<td><code>JSON</code></td>\n<td>目标类别、边界框、跟踪 ID 等检测结果</td>\n</tr>\n<tr>\n<td><code>evidence_json</code></td>\n<td><code>JSON</code></td>\n<td>帧地址、时间点或证据片段信息</td>\n</tr>\n<tr>\n<td><code>analysis_json</code></td>\n<td><code>JSON</code></td>\n<td>规则或大模型生成的摘要、建议</td>\n</tr>\n<tr>\n<td><code>rule_version</code></td>\n<td><code>VARCHAR(64)</code></td>\n<td>生成事件时使用的规则版本</td>\n</tr>\n<tr>\n<td><code>prompt_version</code></td>\n<td><code>VARCHAR(64)</code></td>\n<td>大模型提示词版本，可为空</td>\n</tr>\n<tr>\n<td><code>detector_version</code></td>\n<td><code>VARCHAR(64)</code></td>\n<td>检测器版本，如 <code>yolov8n</code></td>\n</tr>\n</tbody></table>\n<h4 id=\"eventrules事件规则配置表\"><code>event_rules</code>：事件规则配置表</h4>\n<table>\n<thead>\n<tr>\n<th>字段</th>\n<th>类型</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>event_type</code></td>\n<td><code>VARCHAR(128)</code></td>\n<td>主键，事件类型</td>\n</tr>\n<tr>\n<td><code>class_name</code></td>\n<td><code>VARCHAR(128)</code></td>\n<td>关注的目标类别，如 <code>person</code></td>\n</tr>\n<tr>\n<td><code>min_confidence</code></td>\n<td><code>FLOAT</code></td>\n<td>最低置信度阈值</td>\n</tr>\n<tr>\n<td><code>min_duration_ms</code></td>\n<td><code>BIGINT UNSIGNED</code></td>\n<td>最低持续时间</td>\n</tr>\n<tr>\n<td><code>version</code></td>\n<td><code>VARCHAR(64)</code></td>\n<td>规则版本</td>\n</tr>\n<tr>\n<td><code>updated_at</code></td>\n<td><code>TIMESTAMP</code></td>\n<td>最近修改时间</td>\n</tr>\n</tbody></table>\n<h3 id=\"3-表关系和索引\">3. 表关系和索引</h3>\n<pre><code class=\"language-text\">video_jobs 1 ─────────── N events\n                         │\nevent_rules 按 event_type 提供生成规则\n</code></pre>\n<p>当前迁移文件中定义了以下关键索引：</p>\n<pre><code class=\"language-sql\">INDEX idx_video_jobs_status (status),\nINDEX idx_video_jobs_created_at (created_at),\nINDEX idx_events_type_time (event_type, start_time_ms),\nINDEX idx_events_status (status)\n</code></pre>\n<p>例如查询最近的待复核事件：</p>\n<pre><code class=\"language-sql\">SELECT id, job_id, event_type, start_time_ms, severity, confidence\nFROM events\nWHERE status = &#39;unreviewed&#39;\nORDER BY created_at DESC\nLIMIT 50;\n</code></pre>\n<p><code>status</code> 索引帮助缩小扫描范围；如果未来经常按“事件类型 + 时间范围”检索，则 <code>idx_events_type_time</code> 可以直接服务这类查询。索引并非越多越好，因为插入和更新时也要维护索引。</p>\n<h2 id=\"五redis异步任务队列与短期状态\">五、Redis：异步任务队列与短期状态</h2>\n<h3 id=\"1-为什么需要-redis\">1. 为什么需要 Redis</h3>\n<p>Redis 在这个项目中用于实现“异步视频分析”。\n视频分析包含抽帧、YOLO 识别、规则判断和事件生成，通常比较耗时。如果 API 在接收视频后同步完成这些操作，前端请求会一直等待，多个视频同时上传时 API 也容易被占满。\n使用 Redis 后，API 只需快速完成三件事：</p>\n<ol>\n<li>保存视频文件；</li>\n<li>在 MySQL 创建视频任务；</li>\n<li>向 Redis 写入一条待处理任务。\n随后 API 立即返回，Worker 在后台从 Redis 获取任务并执行实际分析。</li>\n</ol>\n<pre><code>前端上传视频\n    ↓\nAPI 保存文件、创建 MySQL 任务\n    ↓\nRedis 写入待处理任务\n    ↓\nAPI 立即返回\n    ↓\nWorker 后台异步处理视频\n</code></pre>\n<p>Redis 的价值包括：</p>\n<ul>\n<li>上传接口不会被视频分析阻塞；</li>\n<li>多个视频任务可以排队处理；</li>\n<li>API 与视频分析逻辑解耦；</li>\n<li>可以扩展多个 Worker 并行消费任务；</li>\n<li>Worker 临时不可用时，任务仍保留在队列中等待处理。</li>\n</ul>\n<h3 id=\"2-redis-中应该保存什么\">2. Redis 中应该保存什么</h3>\n<p>Redis 不应保存视频文件，也不应保存最终事件结果。\n在当前项目中，Redis 只保存“任务通知”，即告诉 Worker：哪个视频任务需要处理。\n当前使用的 Redis Stream 名称为：vision:jobs\n消息示例：</p>\n<pre><code>{\n  &quot;job_id&quot;: &quot;7d112288-3eb7-4d2d-84e4-d07eed51e34c&quot;,\n  &quot;attempt&quot;: 0\n}\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>字段</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>job_id</code></td>\n<td>对应 MySQL <code>video_jobs.id</code>，Worker 用它查询视频路径和任务信息</td>\n</tr>\n<tr>\n<td><code>attempt</code></td>\n<td>当前处理尝试次数，用于后续失败重试机制</td>\n</tr>\n</tbody></table>\n<h2 id=\"六docker把服务网络和数据组织起来\">六、Docker：把服务、网络和数据组织起来</h2>\n<h3 id=\"1-服务组成\">1. 服务组成</h3>\n<p>当前 Compose 服务关系如下：</p>\n<table>\n<thead>\n<tr>\n<th>服务</th>\n<th>作用</th>\n<th>对外端口</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>frontend</code></td>\n<td>React 构建产物 + Nginx</td>\n<td><code>8088:80</code></td>\n</tr>\n<tr>\n<td><code>api</code></td>\n<td>Rust HTTP API</td>\n<td><code>8080:8080</code></td>\n</tr>\n<tr>\n<td><code>worker</code></td>\n<td>Rust 后台任务进程</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>yolo</code></td>\n<td>YOLOv8n 推理服务</td>\n<td><code>9000:9000</code></td>\n</tr>\n<tr>\n<td><code>mysql</code></td>\n<td>MySQL 数据库</td>\n<td>默认不直接暴露</td>\n</tr>\n<tr>\n<td><code>redis</code></td>\n<td>Redis Stream 队列</td>\n<td>默认不直接暴露</td>\n</tr>\n</tbody></table>\n<p><code>api</code> 和 <code>worker</code> 使用同一个 Rust 镜像，通过 <code>WORKER_MODE=1</code> 区分启动行为；API 监听 <code>8080</code>，Worker 进入 Redis 消费循环。</p>\n<h3 id=\"2-compose-配置解析\">2. Compose 配置解析</h3>\n<pre><code class=\"language-yaml\">api:\n  environment:\n    DATABASE_URL: mysql://vision:password@mysql:3306/vision_events\n    REDIS_URL: redis://redis:6379\n    YOLO_URL: http://yolo:9000\n  volumes:\n    - media_data:/app/media\n  depends_on:\n    mysql:\n      condition: service_healthy\n    redis:\n      condition: service_healthy\n</code></pre>\n<p>这里有三个重点：</p>\n<ol>\n<li>容器内使用服务名 <code>mysql</code>、<code>redis</code>、<code>yolo</code>，不能写宿主机 <code>localhost</code>。</li>\n<li><code>depends_on</code> 只控制启动顺序和健康条件，不代表业务接口一定成功，仍需检查日志和健康接口。</li>\n<li><code>media_data</code> 同时挂载给 API、Worker 和前端，保证上传文件、分析进程和媒体访问使用同一份数据。</li>\n</ol>\n<p>MySQL 使用 <code>mysql_data:/var/lib/mysql</code>，这是持久化的关键。如果只使用容器可写层，删除或重建容器后数据库数据会丢失。YOLO 模型则使用 <code>yolo_models</code> 缓存，避免每次重建容器都重新下载权重。</p>\n<h3 id=\"3-启动与验证\">3. 启动与验证</h3>\n<pre><code class=\"language-powershell\">cd D:\\vision-analysis-agent\ndocker compose config\ndocker compose up -d\ndocker compose ps\ndocker compose logs -f api worker\n</code></pre>\n<p>建议按以下顺序排查：</p>\n<ol>\n<li><code>docker compose ps</code> 确认容器是否运行。</li>\n<li>检查 MySQL 和 Redis 的 healthcheck。</li>\n<li>访问 <code>http://localhost:8088</code>，确认 Nginx 和前端可用。</li>\n<li>访问 <code>http://localhost:8080/health</code>，确认 API 可用。</li>\n<li>上传视频，确认 MySQL 出现任务记录、Redis 出现 Stream 消息。</li>\n<li>查看 Worker 日志，确认抽帧、YOLO 调用和事件写入完成。</li>\n<li>在前端确认任务状态、事件详情和审核状态一致。</li>\n</ol>\n<h2 id=\"七异常处理与运维重点\">七、异常处理与运维重点</h2>\n<h3 id=\"1-常见故障定位\">1. 常见故障定位</h3>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>优先检查</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>页面打不开</td>\n<td><code>frontend</code> 容器、Nginx 配置、8088 端口</td>\n</tr>\n<tr>\n<td>API 404/502</td>\n<td><code>/api/</code> 代理路径、<code>api:8080</code> 服务名和 API 日志</td>\n</tr>\n<tr>\n<td>视频上传失败</td>\n<td>Nginx 512 MB 限制、Axum 500 MB 限制、磁盘和 <code>media_data</code></td>\n</tr>\n<tr>\n<td>任务一直处理中</td>\n<td>Redis Stream、Worker 日志、共享媒体卷</td>\n</tr>\n<tr>\n<td>任务失败</td>\n<td><code>ffmpeg</code>、<code>YOLO_URL</code>、模型服务健康状态</td>\n</tr>\n<tr>\n<td>事件查询为空</td>\n<td>MySQL 连接、迁移是否执行、Worker 是否写入 <code>events</code></td>\n</tr>\n<tr>\n<td>重启后任务丢失</td>\n<td>是否读取 MySQL、Redis 是否配置持久化、任务是否具备重试</td>\n</tr>\n</tbody></table>\n<h3 id=\"2-安全和生产注意事项\">2. 安全和生产注意事项</h3>\n<ul>\n<li>Compose 中的开发密码只能用于本地环境，生产环境必须使用环境变量或密钥管理系统。</li>\n<li>MySQL 和 Redis 不应直接暴露公网，优先只加入内部 Docker 网络。</li>\n<li>上传文件名必须清理，不能直接使用用户输入拼接路径。</li>\n<li>生产环境应限制视频大小、视频格式、并发任务数和单任务运行时长。</li>\n<li>MySQL 应有定期备份；媒体文件应使用可靠对象存储或备份策略。</li>\n<li>Redis 生产版需要明确持久化、重试、死信和监控方案。</li>\n<li>需要记录 <code>job_id</code>、请求 ID、Worker 日志和模型版本，方便从一次上传追踪到最终事件。</li>\n</ul>\n<h2 id=\"八总结\">八、总结</h2>\n<p>Vision Analysis Agent 的核心架构可以概括为：<strong>Nginx 负责入口，Rust API 负责接入，Redis 负责排队，Worker 负责计算，MySQL 负责落库，Docker 负责把这些服务稳定地组织起来。</strong></p>\n<p>其中最重要的设计不是某一条命令，而是职责分离：API 不被视频分析阻塞，Redis 不承担最终数据，MySQL 不存放大文件，Worker 不直接面对浏览器，Nginx 不承载业务逻辑。理解这条边界，就能更准确地进行开发、部署、故障排查和后续扩容。</p>\n<h2 id=\"参考资料\">参考资料</h2>\n<ul>\n<li><a href=\"https://docs.docker.com/manuals/\">Docker Documentation</a></li>\n<li><a href=\"https://nginx.org/en/docs/\">Nginx Documentation</a></li>\n<li><a href=\"https://redis.io/docs/latest/\">Redis Documentation</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/\">MySQL 8.4 Reference Manual</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "一本次学习的核心结论",
        "text": "一、本次学习的核心结论"
      },
      {
        "depth": 2,
        "slug": "二整体架构与一次请求的完整链路",
        "text": "二、整体架构与一次请求的完整链路"
      },
      {
        "depth": 2,
        "slug": "三nginx系统的统一入口",
        "text": "三、Nginx：系统的统一入口"
      },
      {
        "depth": 3,
        "slug": "1-nginx-在本系统中的作用",
        "text": "1. Nginx 在本系统中的作用"
      },
      {
        "depth": 3,
        "slug": "2-nginx-请求示例",
        "text": "2. Nginx 请求示例"
      },
      {
        "depth": 2,
        "slug": "四mysql系统的事实来源和持久化中心",
        "text": "四、MySQL：系统的事实来源和持久化中心"
      },
      {
        "depth": 3,
        "slug": "1-mysql-保存什么",
        "text": "1. MySQL 保存什么"
      },
      {
        "depth": 3,
        "slug": "2-核心表结构",
        "text": "2. 核心表结构"
      },
      {
        "depth": 4,
        "slug": "videojobs视频任务表",
        "text": "videojobs：视频任务表"
      },
      {
        "depth": 4,
        "slug": "events视觉事件表",
        "text": "events：视觉事件表"
      },
      {
        "depth": 4,
        "slug": "eventrules事件规则配置表",
        "text": "eventrules：事件规则配置表"
      },
      {
        "depth": 3,
        "slug": "3-表关系和索引",
        "text": "3. 表关系和索引"
      },
      {
        "depth": 2,
        "slug": "五redis异步任务队列与短期状态",
        "text": "五、Redis：异步任务队列与短期状态"
      },
      {
        "depth": 3,
        "slug": "1-为什么需要-redis",
        "text": "1. 为什么需要 Redis"
      },
      {
        "depth": 3,
        "slug": "2-redis-中应该保存什么",
        "text": "2. Redis 中应该保存什么"
      },
      {
        "depth": 2,
        "slug": "六docker把服务网络和数据组织起来",
        "text": "六、Docker：把服务、网络和数据组织起来"
      },
      {
        "depth": 3,
        "slug": "1-服务组成",
        "text": "1. 服务组成"
      },
      {
        "depth": 3,
        "slug": "2-compose-配置解析",
        "text": "2. Compose 配置解析"
      },
      {
        "depth": 3,
        "slug": "3-启动与验证",
        "text": "3. 启动与验证"
      },
      {
        "depth": 2,
        "slug": "七异常处理与运维重点",
        "text": "七、异常处理与运维重点"
      },
      {
        "depth": 3,
        "slug": "1-常见故障定位",
        "text": "1. 常见故障定位"
      },
      {
        "depth": 3,
        "slug": "2-安全和生产注意事项",
        "text": "2. 安全和生产注意事项"
      },
      {
        "depth": 2,
        "slug": "八总结",
        "text": "八、总结"
      },
      {
        "depth": 2,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-24",
    "title": "8/17–8/23 技术学习总结：Nginx、MySQL、Redis 与 Docker",
    "description": "总结 8 月 17 日至 23 日对 Nginx、MySQL、Redis 和 Docker 的学习，补充 Markdown 写作规范，",
    "publishedAt": "2026-08-24",
    "draft": false,
    "html": "<h3 id=\"本周学习主题\">本周学习主题</h3>\n<p>本周围绕 Nginx、MySQL、Redis 和 Docker 四项技术展开学习，同时补充 Markdown 文档写作与代码展示规范。学习重点不只是记住命令和概念，而是理解每项技术解决什么问题、应该保存什么数据、如何与其他服务配合，以及出现故障时应该从哪里排查。</p>\n<h2 id=\"一本周的学习收获\">一、本周的学习收获</h2>\n<p>这周学习下来，我对四项技术的分工有了更具体的认识。Nginx 主要解决请求入口、静态资源和反向代理的问题；MySQL 负责保存需要长期查询的数据，并通过索引、事务和日志保证数据能够被正确使用；Redis 更适合缓存、队列和变化较快的临时状态；Docker 则负责把不同服务的运行环境、网络和启动方式统一起来。</p>\n<p>以前看这些内容时，我容易把注意力放在命令和配置上。这次边学边整理后，开始能把命令和实际问题联系起来：为什么请求需要反向代理，为什么耗时任务要放到队列里，为什么缓存不能代替数据库，以及为什么容器重启后还需要单独考虑数据保存。</p>\n<p>另外，本周也补充了 Markdown 的写作方式。技术内容如果只是连续堆在一起，后面很难复习和查找；使用合适的标题、代码块、表格和流程图，可以让学习过程和代码示例更容易被别人理解，同时主要是方便自己回看。</p>\n<h2 id=\"二技术一nginx\">二、技术一：Nginx</h2>\n<h3 id=\"1-从-http-服务理解-nginx\">1. 从 HTTP 服务理解 Nginx</h3>\n<p>浏览器输入 URL 后，需要有一个对外提供 HTTP 服务的进程接收请求，再返回 HTML、JavaScript、图片或 JSON。Nginx 可以作为 HTTP 服务器直接返回静态文件，也可以作为反向代理把动态请求转给后端应用。</p>\n<pre><code class=\"language-text\">浏览器\n    │  GET /index.html\n    ▼\nNginx\n    ├── 静态文件：直接读取 /var/www/index.html\n    └── 动态请求：转发给 Rust API\n</code></pre>\n<p>如果所有请求都由业务应用处理，后端就需要同时负责业务逻辑、数据库访问和大量静态资源。将静态文件和动态请求分开，可以让业务服务更专注于登录、任务和数据处理。</p>\n<h3 id=\"2-反向代理示例解析\">2. 反向代理示例解析</h3>\n<pre><code class=\"language-nginx\">server {\n    listen 80;\n    server_name example.com;\n\n    location / {\n        root /usr/share/nginx/html;\n        try_files $uri $uri/ /index.html;\n    }\n\n    location /api/ {\n        proxy_pass http://api:8080;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n    }\n}\n</code></pre>\n<p>这段配置可以这样理解：</p>\n<ol>\n<li><code>listen 80</code> 让 Nginx 监听 HTTP 端口。</li>\n<li><code>location /</code> 处理普通页面请求，从静态目录读取前端资源；<code>try_files</code> 适合单页应用的前端路由回退。</li>\n<li><code>location /api/</code> 匹配 API 请求，并将请求转发到 Docker 网络中的 <code>api:8080</code>。</li>\n<li><code>proxy_set_header</code> 把原始主机和客户端地址传给后端，便于日志、审计和问题排查。</li>\n</ol>\n<p>前端只需要请求 <code>/api/v1/videos/upload</code>，不需要知道 Rust API 的内部端口。以后 API 换机器、换端口或增加副本时，浏览器端的请求路径仍然可以保持稳定。</p>\n<h3 id=\"3-负载均衡与并发模型\">3. 负载均衡与并发模型</h3>\n<p>Nginx 还可以将请求分配给多个后端实例：</p>\n<pre><code class=\"language-nginx\">upstream api_servers {\n    server api-1:8080;\n    server api-2:8080;\n}\n\nserver {\n    listen 80;\n\n    location /api/ {\n        proxy_pass http://api_servers;\n    }\n}\n</code></pre>\n<p>这里的 <code>upstream</code> 定义了后端服务组，Nginx 默认可以使用轮询方式分发请求。</p>\n<p>本周还了解了 Nginx 的 Master/Worker 模型：Master 负责读取配置、管理 Worker 和处理重载；Worker 负责实际接收和处理连接。这样可以在不中断已有连接的情况下完成配置更新，也能通过多个 Worker 处理并发请求。</p>\n<h2 id=\"三技术二mysql\">三、技术二：MySQL</h2>\n<h3 id=\"1-从数据页到索引\">1. 从数据页到索引</h3>\n<p>本周对 MySQL 的理解从“保存几张表”进一步深入到存储引擎如何组织数据。InnoDB 会以数据页为基本单位读写数据，Buffer Pool 用来缓存热点页，索引则帮助数据库减少无效扫描。</p>\n<p>MySQL 常用 B+ 树索引，主要原因是它的非叶子节点只保存键和指针，能够在一个节点中放入更多索引项；叶子节点按顺序连接，既适合等值查询，也适合范围查询和排序。</p>\n<pre><code class=\"language-sql\">CREATE TABLE video_jobs (\n    id BIGINT PRIMARY KEY AUTO_INCREMENT,\n    filename VARCHAR(255) NOT NULL,\n    status VARCHAR(32) NOT NULL,\n    created_at DATETIME NOT NULL,\n    INDEX idx_status_created (status, created_at)\n);\n\nSELECT id, filename, status\nFROM video_jobs\nWHERE status = &#39;processing&#39;\nORDER BY created_at DESC\nLIMIT 20;\n</code></pre>\n<p><code>idx_status_created</code> 是联合索引。查询先按 <code>status</code> 筛选，再按 <code>created_at</code> 排序，能够减少扫描范围。索引不是越多越好，因为写入时需要同步维护索引，并且索引也会占用内存和磁盘空间。</p>\n<h3 id=\"2-事务与日志的分工\">2. 事务与日志的分工</h3>\n<p>本周重点理解了 Undo Log、Redo Log 和 Binlog 的区别：</p>\n<ul>\n<li><strong>Undo Log</strong>：保存修改前的信息，用于回滚事务和支持 MVCC。</li>\n<li><strong>Redo Log</strong>：记录数据页的物理修改，保证数据库崩溃恢复时能够重做已经提交的操作。</li>\n<li><strong>Binlog</strong>：记录逻辑层面的变更事件，常用于主从复制和增量恢复。</li>\n</ul>\n<p>例如创建任务时，任务记录和任务状态不应出现明显不一致，可以使用事务：</p>\n<pre><code class=\"language-sql\">START TRANSACTION;\n\nINSERT INTO video_jobs (filename, status, created_at)\nVALUES (&#39;demo.mp4&#39;, &#39;pending&#39;, NOW());\n\nINSERT INTO job_events (job_id, event_type, status)\nVALUES (LAST_INSERT_ID(), &#39;job_created&#39;, &#39;recorded&#39;);\n\nCOMMIT;\n</code></pre>\n<p>如果中间步骤失败，应执行 <code>ROLLBACK</code>，而不是让前端自行猜测数据库到底完成了哪一步。实际项目中还需要根据表结构、隔离级别和并发场景继续验证事务边界。</p>\n<h3 id=\"3-事务如何避免库存超卖一个通用例子\">3. 事务如何避免库存超卖：一个通用例子</h3>\n<pre><code class=\"language-sql\">START TRANSACTION;\n\nSELECT stock\nFROM products\nWHERE id = 1\nFOR UPDATE;\n\nUPDATE products\nSET stock = stock - 1\nWHERE id = 1 AND stock &gt; 0;\n\nCOMMIT;\n</code></pre>\n<p><code>FOR UPDATE</code> 会锁定需要修改的行，让并发事务按顺序处理；<code>stock &gt; 0</code> 又是最后一道业务条件检查。视频任务虽然不是库存场景，但相同的思路适用于“同一个任务只能被一个 Worker 抢到”或“同一个事件不能重复确认”等问题。</p>\n<h2 id=\"四技术三redis\">四、技术三：Redis</h2>\n<h3 id=\"1-数据结构与使用场景\">1. 数据结构与使用场景</h3>\n<p>本周学习了 Redis 常见数据结构，并将它们与项目场景对应起来：</p>\n<table>\n<thead>\n<tr>\n<th>数据结构</th>\n<th>常用场景</th>\n<th>视频平台中的可能用途</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>String</td>\n<td>简单值、计数器、过期缓存</td>\n<td>任务进度、限流计数、去重标记</td>\n</tr>\n<tr>\n<td>Hash</td>\n<td>一组字段</td>\n<td>保存任务的临时状态</td>\n</tr>\n<tr>\n<td>List</td>\n<td>简单队列</td>\n<td>轻量级任务等待队列</td>\n</tr>\n<tr>\n<td>Set</td>\n<td>去重和集合运算</td>\n<td>用户权限、已处理任务集合</td>\n</tr>\n<tr>\n<td>Sorted Set</td>\n<td>带分值排序</td>\n<td>延迟任务、按优先级处理</td>\n</tr>\n<tr>\n<td>Stream</td>\n<td>可追踪的消息流</td>\n<td>视频处理任务和消费记录</td>\n</tr>\n</tbody></table>\n<p>简单的缓存示例：</p>\n<pre><code class=\"language-text\">SET video:job:job_001:progress &quot;45&quot; EX 3600\nGET video:job:job_001:progress\n</code></pre>\n<p><code>EX 3600</code> 表示让进度在一小时后自动过期，避免临时数据无限占用内存。但过期只是清理机制，不代表任务最终结果已经保存；最终状态仍应写回 MySQL。</p>\n<h3 id=\"2-为什么视频处理需要异步队列\">2. 为什么视频处理需要异步队列</h3>\n<p>视频上传后通常还要抽帧、检测、跟踪、规则判断，甚至调用大模型。如果 API 一直等待完整处理结束，用户会长时间占用一个 HTTP 请求，超时后也很难判断任务是否已经执行了一部分。</p>\n<p>更合理的流程是：</p>\n<pre><code class=\"language-text\">API 创建任务\n    ↓\nRedis Stream / 队列\n    ↓\nWorker 消费任务\n    ↓\n更新处理进度\n    ↓\nMySQL 保存最终状态和事件\n</code></pre>\n<p>任务消息需要包含足够的信息，而不是只有一句“处理这个视频”：</p>\n<pre><code class=\"language-json\">{\n  &quot;job_id&quot;: &quot;job_001&quot;,\n  &quot;source_uri&quot;: &quot;/media/videos/demo.mp4&quot;,\n  &quot;detector_version&quot;: &quot;mock-v1&quot;,\n  &quot;rule_version&quot;: &quot;rule-v1&quot;,\n  &quot;created_at&quot;: &quot;2026-08-23T10:20:00Z&quot;\n}\n</code></pre>\n<p>Worker 取到消息后，先根据 <code>job_id</code> 查询 MySQL，确认任务没有完成或取消，再将状态改为 <code>processing</code>。成功后写入事件并改为 <code>completed</code>；失败后记录错误原因，再根据重试次数决定是否重新投递。</p>\n<h3 id=\"3-redis-可靠性边界\">3. Redis 可靠性边界</h3>\n<p>本周也认识到，“使用队列”不等于“任务一定可靠”。后续需要继续考虑：</p>\n<ul>\n<li>消费者组和消息确认，避免消息被取走后直接丢失。</li>\n<li>Worker 中途退出时，任务能否重新被领取。</li>\n<li>失败重试是否有上限，反复失败的任务是否进入死信队列。</li>\n<li>同一个任务重复消费时，是否具备幂等性。</li>\n<li>用户取消任务后，Worker 是否会检查取消状态。</li>\n<li>Redis 暂时不可用时，API 应该返回什么错误，是否允许稍后重试。</li>\n</ul>\n<p>例如，任务开始处理前可以用数据库条件更新控制抢占：</p>\n<pre><code class=\"language-sql\">UPDATE video_jobs\nSET status = &#39;processing&#39;, started_at = NOW()\nWHERE id = ? AND status = &#39;queued&#39;;\n</code></pre>\n<p>只有更新成功的 Worker 才能继续处理任务。这样即使消息重复投递，也不会让两个 Worker 同时执行同一个处于 <code>queued</code> 状态的任务。</p>\n<h3 id=\"4-缓存问题的基本认识\">4. 缓存问题的基本认识</h3>\n<p>缓存击穿、缓存雪崩和缓存穿透都可能影响系统稳定性：</p>\n<ul>\n<li><strong>缓存击穿</strong>：某个热点键突然失效，大量请求同时访问数据库。</li>\n<li><strong>缓存雪崩</strong>：大量键在相近时间过期，数据库瞬间承受高压力。</li>\n<li><strong>缓存穿透</strong>：查询大量不存在的数据，缓存和数据库都被反复访问。</li>\n</ul>\n<p>对应的常见处理方式包括热点数据加互斥锁、过期时间增加随机值、对空结果短暂缓存，以及对输入参数做校验。当前视频平台主要使用 Redis 做异步任务和临时进度，缓存策略需要等真实查询场景稳定后再细化。</p>\n<h2 id=\"五技术四docker\">五、技术四：Docker</h2>\n<h3 id=\"1-镜像容器和-dockerfile\">1. 镜像、容器和 Dockerfile</h3>\n<p>Docker 的价值不只是“把程序打包后部署”，更重要的是将代码、运行时、依赖和启动方式描述清楚，使开发、测试和部署环境更接近。</p>\n<p>可以把镜像理解为模板，把容器理解为镜像启动后的运行实例：同一个镜像可以创建多个容器，每个容器有自己的进程、网络和文件系统视图。</p>\n<pre><code class=\"language-dockerfile\">FROM rust:1.85 AS builder\nWORKDIR /app\nCOPY backend/ .\nRUN cargo build --release\n\nFROM debian:bookworm-slim\nWORKDIR /app\nCOPY --from=builder /app/target/release/backend /app/backend\nEXPOSE 8080\nCMD [&quot;/app/backend&quot;]\n</code></pre>\n<p>这是一个多阶段构建示例。第一阶段负责编译，第二阶段只复制最终可执行文件，不把 Rust 编译器和完整源码带入运行镜像，可以减小镜像体积并降低运行环境复杂度。</p>\n<h3 id=\"2-compose-如何组织多服务\">2. Compose 如何组织多服务</h3>\n<pre><code class=\"language-yaml\">services:\n  api:\n    build: ./apps/api\n    environment:\n      DATABASE_URL: mysql://app:password@mysql:3306/video\n      REDIS_URL: redis://redis:6379\n    depends_on:\n      mysql:\n        condition: service_healthy\n      redis:\n        condition: service_started\n\n  mysql:\n    image: mysql:8.4\n    environment:\n      MYSQL_DATABASE: video\n      MYSQL_USER: app\n      MYSQL_PASSWORD: password\n      MYSQL_ROOT_PASSWORD: root-password\n    volumes:\n      - mysql_data:/var/lib/mysql\n\n  redis:\n    image: redis:7\n\nvolumes:\n  mysql_data:\n</code></pre>\n<p>这里的关键点是：容器之间使用服务名连接，例如 API 连接 <code>mysql:3306</code> 和 <code>redis:6379</code>，而不是依赖宿主机的 <code>localhost</code>。<code>mysql_data</code> 是数据卷，容器重新创建后，数据库数据仍可以保留。</p>\n<p>实际项目中还应把密码放到 <code>.env</code> 或密钥管理系统，并将 <code>.env</code> 加入 <code>.gitignore</code>；<code>.dockerignore</code> 则应排除 <code>.git</code>、<code>node_modules</code>、构建产物和敏感配置，避免把不必要或不应公开的内容打进镜像。</p>\n<h3 id=\"3-docker-网络与数据持久化\">3. Docker 网络与数据持久化</h3>\n<p>视频平台的服务关系可以简化为：</p>\n<pre><code class=\"language-text\">nginx\n  ├── frontend\n  └── api ─── mysql\n          └── redis\n\nworker ─── mysql\n        └── redis\n</code></pre>\n<p>浏览器只访问 Nginx 对外暴露的端口；Nginx 通过 Compose 网络访问 API；API 和 Worker 通过内部网络访问 MySQL、Redis。MySQL 和 Redis 不必全部暴露到公网，这样可以减少外部攻击面。</p>\n<p>还需要区分两种数据：</p>\n<ul>\n<li>MySQL 任务和事件数据需要数据卷或独立数据库保证持久化。</li>\n<li>视频、截图和短片段不适合直接放入数据库，应该使用文件目录、对象存储或独立媒体存储，并在 MySQL 中保存地址和元数据。</li>\n</ul>\n<h3 id=\"4-compose-的验证顺序\">4. Compose 的验证顺序</h3>\n<p>容器显示 <code>Up</code> 只能说明进程暂时启动，不能证明业务完整可用。本周形成的排查顺序是：</p>\n<ol>\n<li>检查容器是否启动。</li>\n<li>检查 MySQL、Redis 健康状态。</li>\n<li>检查 Nginx 是否能访问 API。</li>\n<li>检查 API 是否能写入 MySQL 和 Redis。</li>\n<li>检查 Worker 是否真的消费到任务。</li>\n<li>检查任务状态、事件结果和前端页面是否一致。</li>\n</ol>\n<p>这个顺序可以把“端口问题、网络问题、数据库问题、队列问题和业务问题”分开，减少只盯着前端页面排查的情况。</p>\n<h2 id=\"六markdown-文档写作与代码表达\">六、Markdown 文档写作与代码表达</h2>\n<h3 id=\"1-markdown-在技术周报中的作用\">1. Markdown 在技术周报中的作用</h3>\n<p>本周除了学习四项基础设施技术，也整理了 Markdown 的常用语法。Markdown 的价值不只是写出带格式的文字，更重要的是可以用统一、清晰、容易维护的方式记录技术过程。标题表达层级，列表表达步骤，表格表达对比，代码块表达可执行示例，引用块则适合突出结论或风险。</p>\n<p>整理技术总结时，不能只是把每天的学习内容原样拼接起来，还需要让我会看的时候快速找到三个问题的答案：本周学习了什么、形成了什么理解、下一步准备如何验证。因此本周总结按照“学习收获 → 技术分项 → 代码解析 → 综合案例 → 风险与计划”的顺序整理。</p>\n<h3 id=\"2-常用语法示例\">2. 常用语法示例</h3>\n<pre><code class=\"language-markdown\"># 一级标题\n\n## 二级标题\n\n本段说明背景，使用 **加粗文字** 突出重点，使用 `inline code` 标记命令或变量。\n\n&gt; 这是需要单独提醒读者的结论、风险或注意事项。\n\n- 第一项\n- 第二项\n\n| 技术 | 主要职责 |\n| --- | --- |\n| Redis | 队列和短期状态 |\n\n```bash\ndocker compose up -d\n</code></pre>\n<pre><code>\n这段示例中，标题用于建立文章目录和阅读路径；行内代码适合写命令、字段名和状态值；引用块适合突出结论或注意事项；表格适合展示多个技术之间的分工；代码围栏则可以保留语言标识，让阅读者更容易识别 Bash、SQL、Nginx 或 YAML 配置。\n\n### 3. 代码示例的写法\n\n技术文档中的代码不能只贴出来，还需要说明它解决什么问题、关键字段是什么、运行后会产生什么结果。例如 Docker Compose 代码中，应该解释服务名、环境变量、依赖关系和数据卷，而不是只给出一段 YAML。\n\n```sql\nSELECT id, status\nFROM video_jobs\nWHERE status = &#39;processing&#39;\nORDER BY created_at DESC\nLIMIT 20;\n</code></pre>\n<p>这条 SQL 的说明应该至少包含：查询目标是任务表，筛选条件是处理中任务，结果按创建时间倒序排列，最多返回 20 条。这样读者能够理解代码和业务含义之间的关系，也方便后续检查索引是否匹配查询条件。</p>\n<h3>4. 技术文档的阅读友好性</h3>\n<p>后续写日报和周报时需要注意以下几点：</p>\n<ul>\n<li>一个标题只表达一个主题，避免在同一小节中混合多个无关概念。</li>\n<li>先写结论和作用，再写原理和细节，降低阅读门槛。</li>\n<li>代码块前说明使用场景，代码块后解释关键行。</li>\n<li>流程图只保留关键节点，避免把所有实现细节堆在一张图里。</li>\n<li>表格用于对比和归纳，不用于替代大段解释。</li>\n<li>链接和参考资料集中放在文章末尾，正文只保留必要的引用。</li>\n<li>技术名词、状态值、命令和文件名使用反引号，避免和普通文字混淆。</li>\n</ul>\n<h2>参考资料</h2>\n<ul>\n<li><a href=\"https://docs.docker.com/manuals/\">Docker Documentation</a></li>\n<li><a href=\"https://nginx.org/en/docs/\">Nginx Documentation</a></li>\n<li><a href=\"https://redis.io/docs/latest/\">Redis Documentation</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/\">MySQL 8.4 Reference Manual</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "本周学习主题",
        "text": "本周学习主题"
      },
      {
        "depth": 2,
        "slug": "一本周的学习收获",
        "text": "一、本周的学习收获"
      },
      {
        "depth": 2,
        "slug": "二技术一nginx",
        "text": "二、技术一：Nginx"
      },
      {
        "depth": 3,
        "slug": "1-从-http-服务理解-nginx",
        "text": "1. 从 HTTP 服务理解 Nginx"
      },
      {
        "depth": 3,
        "slug": "2-反向代理示例解析",
        "text": "2. 反向代理示例解析"
      },
      {
        "depth": 3,
        "slug": "3-负载均衡与并发模型",
        "text": "3. 负载均衡与并发模型"
      },
      {
        "depth": 2,
        "slug": "三技术二mysql",
        "text": "三、技术二：MySQL"
      },
      {
        "depth": 3,
        "slug": "1-从数据页到索引",
        "text": "1. 从数据页到索引"
      },
      {
        "depth": 3,
        "slug": "2-事务与日志的分工",
        "text": "2. 事务与日志的分工"
      },
      {
        "depth": 3,
        "slug": "3-事务如何避免库存超卖一个通用例子",
        "text": "3. 事务如何避免库存超卖：一个通用例子"
      },
      {
        "depth": 2,
        "slug": "四技术三redis",
        "text": "四、技术三：Redis"
      },
      {
        "depth": 3,
        "slug": "1-数据结构与使用场景",
        "text": "1. 数据结构与使用场景"
      },
      {
        "depth": 3,
        "slug": "2-为什么视频处理需要异步队列",
        "text": "2. 为什么视频处理需要异步队列"
      },
      {
        "depth": 3,
        "slug": "3-redis-可靠性边界",
        "text": "3. Redis 可靠性边界"
      },
      {
        "depth": 3,
        "slug": "4-缓存问题的基本认识",
        "text": "4. 缓存问题的基本认识"
      },
      {
        "depth": 2,
        "slug": "五技术四docker",
        "text": "五、技术四：Docker"
      },
      {
        "depth": 3,
        "slug": "1-镜像容器和-dockerfile",
        "text": "1. 镜像、容器和 Dockerfile"
      },
      {
        "depth": 3,
        "slug": "2-compose-如何组织多服务",
        "text": "2. Compose 如何组织多服务"
      },
      {
        "depth": 3,
        "slug": "3-docker-网络与数据持久化",
        "text": "3. Docker 网络与数据持久化"
      },
      {
        "depth": 3,
        "slug": "4-compose-的验证顺序",
        "text": "4. Compose 的验证顺序"
      },
      {
        "depth": 2,
        "slug": "六markdown-文档写作与代码表达",
        "text": "六、Markdown 文档写作与代码表达"
      },
      {
        "depth": 3,
        "slug": "1-markdown-在技术周报中的作用",
        "text": "1. Markdown 在技术周报中的作用"
      },
      {
        "depth": 3,
        "slug": "2-常用语法示例",
        "text": "2. 常用语法示例"
      },
      {
        "depth": 2,
        "slug": "二级标题",
        "text": "二级标题"
      },
      {
        "depth": 3,
        "slug": "3-代码示例的写法",
        "text": "3. 代码示例的写法"
      },
      {
        "depth": 3,
        "slug": "4-技术文档的阅读友好性",
        "text": "4. 技术文档的阅读友好性"
      },
      {
        "depth": 2,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-23",
    "title": "8/23 视频事件检索平台：基础骨架搭建",
    "description": "记录视频事件检索平台第一轮升级后的基础服务、任务流程和当前验证结果，并总结本周 Docker、Nginx、Redis 与 MySQL 的学习。",
    "publishedAt": "2026-08-23",
    "draft": false,
    "html": "<h3 id=\"今天的项目进展\">今天的项目进展</h3>\n<p>今天主要围绕视频事件检索平台的基础骨架进行联调。前面学习 Docker、Nginx、Redis 和 MySQL 时，更多是在理解每个工具本身；今天则是把这些东西真正放到了一个项目里。</p>\n<p>目前 Rust API、React 前端、Worker、MySQL、Redis 和 Nginx 已经可以通过 Docker Compose 一起启动。前端页面可以访问，API、数据库和缓存服务也都已经接通，视频事件检索的基本页面和任务流程已经有了。</p>\n<p>现在还不能说平台已经完成。当前完成的是工程骨架和服务之间的连接，检测器仍然是 MockDetector，大模型也只是预留接口，真实视频链路、YOLO 推理、事件证据和复杂检索还需要继续验证。</p>\n<h2 id=\"当前系统的基本流程\">当前系统的基本流程</h2>\n<p>目前先按照下面的流程组织：</p>\n<pre><code class=\"language-text\">用户上传视频\n    ↓\nReact 前端\n    ↓\nNginx 反向代理\n    ↓\nRust API\n    ├── MySQL 保存任务信息\n    └── Redis 投递异步任务\n            ↓\n        Worker 处理视频\n            ↓\n      检测器和规则引擎\n            ↓\n        MySQL 保存事件\n            ↓\n        前端查询和复核\n</code></pre>\n<p>用户在前端选择视频后，请求先经过 Nginx，再转发给 Rust API。API 保存视频文件，读取文件名和大小等基础信息，尝试读取视频时长和分辨率，然后在 MySQL 中创建一条视频任务记录，最后返回任务 ID。</p>\n<p>上传完成后，前端调用处理接口：</p>\n<pre><code class=\"language-http\">POST /api/v1/videos/{job_id}/process\n</code></pre>\n<p>API 将任务放入 Redis 队列，并把任务状态设置为 <code>queued</code>。Worker 监听 Redis，拿到任务后把状态改为 <code>processing</code>，再开始执行后续分析。现在页面上已经能够看到处理任务和进度，当前测试任务显示进度为 1%，说明任务已经进入处理流程。</p>\n<h2 id=\"今天搭好的服务骨架\">今天搭好的服务骨架</h2>\n<h3 id=\"nginx把前端和-api-放到一个入口\">Nginx：把前端和 API 放到一个入口</h3>\n<p>今天的 Nginx 不只是一个单独启动的 Web 服务器，而是作为整个系统的入口。前端页面、API 请求和后续的媒体文件访问，都可以从同一个地址进入：</p>\n<pre><code class=\"language-text\">/       → React 前端\n/api/   → Rust API\n/media/ → 视频、截图和事件片段\n</code></pre>\n<p>这样前端不需要直接知道 Rust API 的内部端口，API、MySQL 和 Redis 也不需要对外开放。前端请求 <code>/api/v1/videos/upload</code> 时，Nginx 会把请求转给 API；以后视频截图和短片段准备好之后，也可以继续通过 <code>/media/</code> 访问。</p>\n<p>Nginx 这一层对视频项目还比较重要，因为视频上传和回放都比普通接口更容易遇到大小、超时和权限问题。现在先把路由和反向代理跑通，后面还要继续补上传大小、API 超时、媒体访问控制和日志配置。</p>\n<p>今天联调时，前端实际上只需要面对 Nginx 暴露出来的地址，不需要自己区分前端开发端口和 API 端口。这样做以后，前端请求路径可以保持稳定，后面即使 API 换机器、换端口，或者增加 Worker，也不会影响浏览器端的访问方式。Nginx 现在主要做转发，后续还要继续确认上传请求能不能承受真实 MP4 文件，以及 <code>/media/</code> 访问视频片段时是否需要权限和过期时间。</p>\n<p>目前前端上传视频的请求路径是：</p>\n<pre><code class=\"language-http\">POST /api/v1/videos/upload\n</code></pre>\n<p>前端不需要把请求直接发到 Rust 服务的内部地址，而是统一发给 Nginx。Nginx 根据 <code>/api/</code> 这个前缀找到 API 服务，把请求体、请求头和必要的代理信息转发过去。以后前端请求事件列表，就是 <code>GET /api/v1/events</code>；如果请求某个任务，就是 <code>GET /api/v1/jobs/{id}</code>，路径仍然保持一致。</p>\n<p>这样做以后，外部访问方式和内部服务实现就分开了。前端只需要知道系统入口，后端服务可以在 Docker 网络里使用服务名通信。后面如果要增加 API 副本，也可以先在 Nginx 后面调整，而不用修改前端代码。</p>\n<h3 id=\"mysql保存任务和事件\">MySQL：保存任务和事件</h3>\n<p>今天 MySQL 主要用于保存视频任务和事件数据。视频文件本身不直接存入数据库，数据库保存的是任务和文件之间的关系，例如文件名、文件大小、时长、哈希、处理状态和错误信息。</p>\n<p>当前任务状态大致会经历：</p>\n<pre><code class=\"language-text\">pending → queued → processing → completed\n                              ↘ failed\n                              ↘ cancelled\n</code></pre>\n<p>事件表后面需要保存事件类型、严重程度、发生时间、检测目标、置信度、证据地址和版本信息。今天的骨架中已经为事件查询和事件详情预留了位置，后续真实检测结果接入后，再把检测结果和规则结果完整保存下来。</p>\n<p>我觉得 MySQL 在这里最重要的作用，不只是把数据保存下来，而是让任务和事件以后可以查、可以分页、可以复核。Redis 里的进度会变化，缓存也可能失效，但任务最后成功还是失败、某个事件什么时候发生，这些内容需要长期保留。</p>\n<p>从今天的任务流程来看，MySQL 至少要把“任务本身”和“任务产生的事件”区分开。任务记录需要知道视频来自哪里、文件叫什么、现在处于哪个状态、什么时候开始和结束；事件记录则需要知道发生了什么、发生在视频的哪个时间段、置信度是多少、有没有截图和片段。以后前端按事件类型、状态、时间范围查询时，也需要提前考虑这些字段怎么建索引。</p>\n<p>另外，任务状态不能只由前端显示决定。前端显示 <code>processing</code>，并不代表 Worker 一定还在正常运行，最终还是要以数据库中的任务状态和错误信息为准。后面还需要补任务失败原因、重试次数、开始时间、结束时间等字段，这样出现问题时才能知道任务停在哪里。\n<img src=\"image-8.png\" alt=\"alt text\"></p>\n<p>目前 <code>video_jobs</code> 可以围绕下面这些字段继续完善：</p>\n<pre><code class=\"language-text\">id              任务 ID\nsource_uri      视频文件位置\nfilename        原始文件名\nsha256          文件指纹\nduration_ms     视频时长\nstatus          当前状态\nprogress        处理进度\nerror_code      错误类型\nerror_message   错误信息\nstarted_at      开始时间\nfinished_at     结束时间\n</code></pre>\n<p><code>events</code> 则需要保存另一类信息：事件是什么、发生在视频什么时间、由什么目标触发、置信度是多少、有没有证据，以及当时使用了哪个规则和模型版本。例如 <code>person_enter_zone</code> 事件不能只保存一个标题，还要保存起始时间、结束时间、目标轨迹和区域信息，否则后面无法回放和复核。</p>\n<p>今天先把任务查询和事件查询接口接起来，下一步还需要使用真实视频确认：上传完成后数据库里是否真的有任务记录，处理开始后状态是否正确变化，失败时错误信息是否能够保存，事件写入后前端查询是否能拿到同一条数据。</p>\n<h3 id=\"redis把视频处理放到后台\">Redis：把视频处理放到后台</h3>\n<p>视频处理不适合放在一个普通 API 请求里同步完成。上传一个视频以后，还要抽帧、调用检测器、执行规则判断，后面还可能调用大模型。如果 API 一直等到全部完成，用户需要等待很长时间，失败后也不好重试。</p>\n<p>所以今天的骨架里，API 只负责创建任务，Redis 负责传递任务，Worker 负责处理任务：</p>\n<pre><code class=\"language-text\">API 创建任务\n    ↓\nRedis 队列\n    ↓\nWorker 消费任务\n    ↓\n更新任务进度\n    ↓\n保存最终结果\n</code></pre>\n<p>当前先使用 Redis 队列把流程跑通。后面还需要继续完善消费者组、消息确认、失败重试、死信队列和任务超时。特别是 Worker 处理到一半退出以后，任务不能一直卡在 <code>processing</code>，同一个任务重复执行时也不能无限生成重复事件。</p>\n<p>今天的 Redis 主要验证的是“API 能不能把任务交给 Worker”。真正接入视频以后，还要确认队列中的任务内容是否足够，不能只传一个模糊的任务 ID，至少要能找到视频文件、任务类型和相关配置。Worker 取到任务以后，需要先更新数据库状态，再开始处理；处理成功后写入完成状态，处理失败则记录错误信息，并决定是否重新投递。</p>\n<p>队列消息本身也需要定义清楚，不能只写一句“处理这个视频”：</p>\n<pre><code class=\"language-json\">{\n  &quot;job_id&quot;: &quot;job_001&quot;,\n  &quot;source_uri&quot;: &quot;/media/videos/demo.mp4&quot;,\n  &quot;detector_version&quot;: &quot;mock-v1&quot;,\n  &quot;rule_version&quot;: &quot;rule-v1&quot;,\n  &quot;created_at&quot;: &quot;2026-08-23T10:20:00Z&quot;\n}\n</code></pre>\n<p>Worker 取到任务以后，先根据 <code>job_id</code> 查数据库，确认任务是不是可以处理，再把状态改成 <code>processing</code>。如果任务已经是 <code>completed</code>，就不应该再次执行；如果任务已经取消，也应该直接结束。处理成功后，Worker 保存事件并把任务改成 <code>completed</code>；处理失败则保存错误原因，按照重试次数决定是否再次进入队列。</p>\n<p>进度也不能只在 Worker 内存里修改。前端刷新页面以后，仍然需要知道任务走到哪一步，所以可以把当前阶段和百分比写入 Redis，例如：</p>\n<pre><code class=\"language-text\">vision:job:job_001:progress\nstage=detecting\npercent=45\n</code></pre>\n<p>但 Redis 里的进度属于临时状态，任务完成后的最终结果还是要写回 MySQL。</p>\n<h3 id=\"docker让服务一起启动\">Docker：让服务一起启动</h3>\n<p>今天能把前端、API、Worker、MySQL、Redis 和 Nginx 放在一起运行，主要依靠 Docker Compose。当前的服务关系可以理解为：</p>\n<pre><code class=\"language-text\">nginx\n  ├── frontend\n  └── api\n          ├── mysql\n          └── redis\n\nworker\n  ├── mysql\n  └── redis\n</code></pre>\n<p>API 和 Worker 可以使用同一套后端代码，但启动命令不同。API 对外提供接口，Worker 在后台消费任务。MySQL 和 Redis 是内部依赖，前端通过 Nginx 访问，不需要自己处理每个服务的端口。</p>\n<p>MySQL 数据还需要使用数据卷保存，视频和截图也要使用独立的文件目录或对象存储。否则容器重新创建以后，服务虽然可以重新启动，但里面的数据和文件可能已经不在了。</p>\n<p>Docker Compose 这次解决的主要是开发和联调问题。以前启动项目时，需要分别启动前端、后端、数据库和缓存，还要自己记住端口和环境变量；现在可以把这些配置集中到 Compose 中，由服务名连接其他容器。比如 API 连接的不是写死的宿主机地址，而是 Compose 网络里的 MySQL 和 Redis 服务。</p>\n<p>不过容器能启动，并不代表整个服务真的可用。MySQL 还需要等待健康检查通过，API 才能正常连接；Redis 虽然显示 <code>Up</code>，也还要验证任务是否真的能写入和取出；Worker 显示运行中，也还要确认它是不是在监听正确的队列。因此今天的 <code>Up</code> 更像是第一层验证，后面还要继续做接口、任务和数据层面的验证。</p>\n<p>Compose 里的服务不能只看端口有没有映射，还要看容器之间的连接方式。浏览器访问的是 Nginx 暴露的端口，Nginx 通过服务名访问 API；API 通过 Docker 网络里的 <code>mysql</code> 和 <code>redis</code> 访问数据库和缓存；Worker 使用同样的网络连接 MySQL 和 Redis。MySQL、Redis 的端口可以只在内部网络使用，不必全部暴露给外部。</p>\n<p>今天通过 Compose 把几个服务一起启动以后，排查问题也有了比较清楚的顺序：先看容器是否启动，再看健康检查，再看 Nginx 是否能访问 API，然后看 API 是否能连接 MySQL 和 Redis，最后看 Worker 是否真的消费到了任务。后面遇到问题时，可以按照这条顺序排查，而不是只盯着前端页面。</p>\n<h2 id=\"当前已经完成的内容\">当前已经完成的内容</h2>\n<h3 id=\"后端\">后端</h3>\n<ul>\n<li>Rust Axum API 基础框架已经搭好。</li>\n<li>视频上传接口已经预留并可以创建任务。</li>\n<li>视频任务创建和状态查询接口已经完成。</li>\n<li>事件查询接口已经完成基础结构。</li>\n<li>MySQL 数据持久化已经接通。</li>\n<li>Redis 异步任务队列已经接通。</li>\n<li>Worker 服务已经可以启动并监听任务。</li>\n<li>检测器接口已经预留，当前使用 MockDetector。</li>\n<li>规则引擎已经有基础结构。</li>\n<li>大模型分析接口和分析字段已经预留。</li>\n</ul>\n<h3 id=\"前端\">前端</h3>\n<ul>\n<li>React 前端页面已经搭好。</li>\n<li>视频事件检索主界面已经可以访问。</li>\n<li>页面包含事件流和事件详情区域。</li>\n<li>页面可以展示任务处理进度。</li>\n<li>页面可以展示事件状态。</li>\n<li>事件确认和忽略按钮已经预留。</li>\n<li>前端已经放进 Nginx 容器中运行。</li>\n</ul>\n<h3 id=\"部署和联调\">部署和联调</h3>\n<ul>\n<li>Docker Compose 服务编排已经完成。</li>\n<li>API、Worker 和前端容器可以启动。</li>\n<li>MySQL 容器可以启动并通过健康检查。</li>\n<li>Redis 容器可以启动并通过健康检查。</li>\n<li>服务之间的端口和网络关系已经接通。</li>\n<li>前端页面可以正常访问。</li>\n</ul>\n<p>当前测试环境中的服务状态如下：</p>\n<pre><code class=\"language-text\">api       Up\nworker    Up\nfrontend  Up\nmysql     Up (healthy)\nredis     Up (healthy)\n</code></pre>\n<p><img src=\"image-7.png\" alt=\"alt text\"></p>\n<h2 id=\"现在还没有完成的部分\">现在还没有完成的部分</h2>\n<p>今天虽然把骨架搭起来了，但离完整功能还有一段距离：</p>\n<ol>\n<li>检测器目前仍然是 MockDetector，还没有接入真实 YOLO。</li>\n<li>大模型接口只是预留结构，还没有接入具体模型服务。</li>\n<li>Redis 队列还需要增加消费者组、ACK 和失败重试。</li>\n<li>视频事件的截图和短视频证据还需要完善。</li>\n</ol>\n<p>这些内容不能因为页面已经能打开、容器已经显示 <code>Up</code> 就当成已经完成。容器启动正常，只能说明服务进程暂时能运行；真实视频能不能处理、任务失败能不能恢复、事件结果准不准确，还需要继续测试。</p>\n<h2 id=\"下一步计划\">下一步计划</h2>\n<h3 id=\"接入真实-yolo\">接入真实 YOLO</h3>\n<p>后续可以把 YOLO 单独作为推理服务，避免把模型推理代码直接写进 Rust 业务后端：</p>\n<pre><code class=\"language-text\">Rust Worker\n    ↓\n调用 YOLO 推理服务\n    ↓\n返回类别、置信度和边界框\n    ↓\n规则引擎判断业务事件\n</code></pre>\n<p>计划识别的目标包括人员、车辆、安全帽、烟雾、火焰、包裹、叉车和反光背心等。Rust 负责任务调度、数据保存和业务流程，YOLO 服务负责模型推理，这样以后更换模型时影响会小一些。</p>\n<h3 id=\"完善事件规则\">完善事件规则</h3>\n<p>规则引擎先实现比较明确的场景：人员进入禁区、人员长时间停留、未佩戴安全帽、未穿反光背心、烟火检测、车辆逆行、人员跌倒、区域人数超限、物品遗留和人车混行。</p>\n<p>YOLO 负责判断“画面里有什么”，规则引擎负责判断“这是不是一个业务事件”。例如：</p>\n<pre><code class=\"language-text\">检测到 person\n    ↓\n判断是否进入指定区域\n    ↓\n判断是否持续超过 10 秒\n    ↓\n生成 person_enter_zone 事件\n</code></pre>\n<h3 id=\"最后再接入大模型\">最后再接入大模型</h3>\n<p>大模型暂时放在后面，主要用于事件摘要、严重程度解释、误报分析、处置建议和自然语言检索。它不应该直接决定是否发生高风险事件，也不应该直接执行删除、停机或其他生产操作。</p>\n<h2 id=\"本周总结\">本周总结</h2>\n<p>这周算是把一条比较完整的学习线走了一遍。看 Nginx 时，主要是在补 HTTP 服务和网关的基础知识；看 MySQL 时，开始从“数据库就是几张表”的理解，往数据页、索引、缓存和事务里面继续往下看；看 Redis 时，重点放在缓存、队列和消息流；看 Docker，把这些服务放到容器和 Compose 里面；今天则真正把它们放进视频事件检索平台的骨架中，看到它们之间应该怎么配合。</p>\n<p>学习 Nginx 的时候，我其实是在补一个以前容易忽略的部分：浏览器看到的页面和后端接口，并不是天然就能直接互相访问，中间还需要有一个服务来接收请求、返回静态资源、转发 API。看到反向代理和负载均衡以后，我才比较清楚为什么项目不应该让前端直接记住后端的 IP 和端口。今天 Nginx 能把页面和 API 组织到一起，算是把那天学到的内容第一次放进了实际项目里。</p>\n<p>MySQL 这两天的内容比较偏底层，但和今天的任务流程关系很大。以前我只知道创建一张任务表，保存视频名称和状态就可以了；现在会继续想到，事件以后要按什么字段查询，任务失败以后要不要保留错误信息，人工确认事件时要修改哪些数据，规则和模型版本要不要跟着事件一起保存。数据页和索引解决的是查询效率，事务解决的是多张表一起修改时的一致性，这些都不是页面能直接看出来的东西，但以后任务一多就会影响系统。</p>\n<p>Redis 让我开始关注“任务怎么走”。如果没有队列，上传接口可能要一直等着视频处理；有了队列以后，又会出现消息丢失、重复消费、任务卡住和失败重试等问题。今天只是把任务从 API 送到 Worker 的第一步跑起来，后面还需要把消费确认和失败处理补好。也就是说，Redis 不是加上以后任务就自动可靠了，队列后面的规则还需要由 Worker 和数据库一起保证。</p>\n<p>Docker 则把这些问题放到了同一个环境里。今天能看到 API、Worker、前端、MySQL、Redis 和 Nginx 一起启动，是因为它们的镜像、网络、端口、环境变量和数据卷已经有了基本安排。以前我可能只会把 Docker 当成“启动服务的工具”，现在会更注意服务之间的连接、数据库是否持久化、容器重建以后数据是否还在，以及开发环境和以后生产环境之间有什么差别。</p>\n<p>这一周我感觉比较明显的一点，是以前学技术时容易把注意力放在某一个功能上，比如 Redis 有哪些数据结构、Docker 有哪些命令、MySQL 的索引怎么工作。现在再回头看项目，会更容易想到这些技术是为了解决什么问题。Nginx 是为了把前端、API 和媒体访问组织起来，MySQL 是为了保存以后还要查询和复核的任务、事件，Redis 是为了把耗时的视频分析从接口请求里拿出来，Docker 则是为了让这些服务能够在一套环境里启动和联调。</p>\n<p>今天看到前端页面可以访问、服务状态显示正常时，说明之前学的内容确实开始落到项目里了。但这也只是第一步。页面能打开不代表视频真的处理成功，任务进入队列也不代表失败以后能够恢复，MySQL 能保存一条记录也不代表事件结构已经适合长期查询。后面还需要用真实视频继续验证，尤其是 Worker 消费、任务状态变化、事件写入和前端展示这一整条链路。</p>\n<p>今天的骨架搭好以后，我也能看到项目目前的边界。前端现在主要负责上传、查看任务和展示事件；API 负责接收请求、创建任务和查询数据；Worker 负责后面的耗时处理；MySQL 保存需要长期保留的结果；Redis 负责任务传递和临时状态；Nginx 负责外部访问。这个划分还会继续调整，但比所有代码都放在一个服务里更容易往后扩展。</p>\n<p>我现在比较想先把真实视频链路跑通，而不是马上加入很多复杂能力。先确认一个 MP4 上传以后，任务能不能正确创建，Redis 能不能收到任务，Worker 能不能消费，状态能不能从 <code>queued</code> 变成 <code>processing</code>，最后能不能保存事件。这个过程稳定以后，再接 YOLO 和规则；规则稳定以后，再考虑大模型和自然语言检索。</p>\n<p>这周我也发现，项目升级里最容易写得漂亮的部分，往往是架构图和功能列表，真正麻烦的是中间那些状态变化。例如任务创建成功了，但视频文件没有保存好怎么办；Redis 里有任务，但 MySQL 没有对应记录怎么办；Worker 处理失败以后，页面显示什么；同一个事件重复生成以后，前端要不要合并。这些问题暂时还没有全部解决，但今天搭完骨架以后，至少可以在真实流程里一个个验证，而不是只停留在计划书里。</p>\n<p>这周对我来说，Docker、Nginx、Redis 和 MySQL 不再是四篇分开的学习笔记了。它们现在分别对应项目中的入口、数据、任务和运行环境。以后项目继续升级时，可能还会遇到很多新的问题，比如文件存储、GPU 推理、失败重试、权限管理和并发任务，但至少现在已经有了一个可以继续往上搭的基础，而不是只停留在想法和计划上。</p>\n<p>如果把这周的学习和今天的结果放在一起看，我觉得现在还不能急着评价功能做得好不好，应该先看这条链路是不是能稳定走通。只要上传、创建任务、进入队列、Worker 消费、写入数据库和页面展示这几步可以重复验证，后面接入真实 YOLO 时，问题就会集中在模型和检测结果上；接入大模型时，问题就会集中在分析和 Prompt 上，不会每增加一个功能就重新排查整个项目。</p>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "今天的项目进展",
        "text": "今天的项目进展"
      },
      {
        "depth": 2,
        "slug": "当前系统的基本流程",
        "text": "当前系统的基本流程"
      },
      {
        "depth": 2,
        "slug": "今天搭好的服务骨架",
        "text": "今天搭好的服务骨架"
      },
      {
        "depth": 3,
        "slug": "nginx把前端和-api-放到一个入口",
        "text": "Nginx：把前端和 API 放到一个入口"
      },
      {
        "depth": 3,
        "slug": "mysql保存任务和事件",
        "text": "MySQL：保存任务和事件"
      },
      {
        "depth": 3,
        "slug": "redis把视频处理放到后台",
        "text": "Redis：把视频处理放到后台"
      },
      {
        "depth": 3,
        "slug": "docker让服务一起启动",
        "text": "Docker：让服务一起启动"
      },
      {
        "depth": 2,
        "slug": "当前已经完成的内容",
        "text": "当前已经完成的内容"
      },
      {
        "depth": 3,
        "slug": "后端",
        "text": "后端"
      },
      {
        "depth": 3,
        "slug": "前端",
        "text": "前端"
      },
      {
        "depth": 3,
        "slug": "部署和联调",
        "text": "部署和联调"
      },
      {
        "depth": 2,
        "slug": "现在还没有完成的部分",
        "text": "现在还没有完成的部分"
      },
      {
        "depth": 2,
        "slug": "下一步计划",
        "text": "下一步计划"
      },
      {
        "depth": 3,
        "slug": "接入真实-yolo",
        "text": "接入真实 YOLO"
      },
      {
        "depth": 3,
        "slug": "完善事件规则",
        "text": "完善事件规则"
      },
      {
        "depth": 3,
        "slug": "最后再接入大模型",
        "text": "最后再接入大模型"
      },
      {
        "depth": 2,
        "slug": "本周总结",
        "text": "本周总结"
      }
    ]
  },
  {
    "id": "2026-08-22-video-platform-upgrade",
    "title": "Docker、Nginx、Redis 与 MySQL：视频事件检索平台升级思路",
    "description": "把这周对 Docker、Nginx、Redis 和 MySQL 的学习，整理成视频事件检索平台的升级思路。",
    "publishedAt": "2026-08-22",
    "draft": false,
    "html": "<h3 id=\"本周总结\">本周总结</h3>\n<p>这周主要在补 Docker、Nginx、Redis 和 MySQL。17 号先看的 Nginx，刚开始其实还是从“浏览器访问一个网页，到底是谁把网页返回回来”这个问题开始的，后面才慢慢看到 HTTP 服务、反向代理、负载均衡和并发这些内容。18、19 号看 MySQL，内容比较多，从数据页、B+ 树和 Buffer Pool，一直看到事务、Undo Log、Redo Log、Binlog，期间也记了一些慢查询、锁和分页的问题。20 号看 Redis，主要是缓存、数据结构、Stream、发布订阅，还有缓存失效这些问题。21 号看 Docker，镜像、容器、Dockerfile、网络、数据卷和 Compose 基本都过了一遍。</p>\n<p>以前看这些东西，很多时候是看完一个算一个，知道一些命令和概念就过去了。这次我一直在拿自己的项目对照。Nginx 那篇里，我就在想前端和 API 为什么不应该直接互相暴露；MySQL 看到索引和事务时，我会想到以后事件列表怎么查、人工复核时怎么同时改几张表；Redis 看到 Stream 时，就会想到视频处理不能让一个接口一直等着；Docker 看到网络和数据卷时，又会想到这些服务启动以后怎么互相找到，MySQL 的数据怎么保留下来。</p>\n<p>我以前容易把学习变成记东西：Nginx 记 <code>location</code>，Redis 记 <code>SET</code>、<code>GET</code>，Docker 记 <code>pull</code>、<code>run</code> 和 <code>exec</code>。这些命令当然要会，但只记命令的话，过几天就容易忘，也不知道实际该用在什么地方。放到这个项目里以后，问题就具体了：视频处理要花很长时间，接口是不是不应该一直等？任务进度应该放在哪里？事件结果放在哪里？容器重新创建以后数据库还在不在？这些问题反而让我开始知道前面那些概念到底是为了解决什么。</p>\n<p>现在我大概能把这条流程想明白了。用户从前端上传视频，请求先经过 Nginx，再到 API。API 创建一个任务以后就返回，不负责一直处理完整个视频，后面的抽帧、检测和事件生成交给 Worker。Worker 处理的时候，把进度临时放到 Redis；任务最后成功还是失败、生成了哪些事件，再写到 MySQL；视频和截图则放到文件存储。虽然这些现在还只是升级计划，并不是项目已经完成的功能，但至少每一部分应该放在哪里，已经没有之前那么模糊了。</p>\n<p>把这些东西放回视频事件检索项目以后，我现在比较认可的做法就是先把任务和服务分开。用户上传视频以后，API 只创建任务，把任务放到 Redis 里，Worker 再去做抽帧、检测、跟踪和生成事件。处理过程中的进度可以放 Redis，任务最后是什么状态、产生了哪些事件、证据在哪里、使用了哪个模型版本，这些还是要放 MySQL。Nginx 负责让前端、API 和媒体文件从一个入口访问，Docker Compose 负责先把这些服务在本地跑起来。</p>\n<p>视频分析这里最明显的问题就是耗时。上传一个视频以后，抽帧、检测、跟踪和大模型分析都不是马上能结束的。如果 API 一直等到视频处理完才返回，用户要等很久，失败以后也不好重新处理。所以这部分应该拆开，API 只负责接收上传和创建任务，Worker 负责后面的处理。这个想法其实就是从 Redis 的队列和 Docker 里服务分工这些内容里慢慢联系出来的。</p>\n<p>Redis Stream 在这里就是把任务先放下来，交给后面的 Worker 处理。前端拿到 <code>job_id</code> 后可以去看进度，Worker 失败以后也有机会重试。不过任务放进队列并不代表问题就解决了，还要考虑同一个任务被处理两次怎么办，Worker 处理到一半退出怎么办，用户取消任务以后 Worker 要不要继续，以及 Redis 暂时连不上时 API 应该怎么返回。这些问题我现在还没有全部做完，但至少已经知道后面不能漏掉。</p>\n<p>这周看 MySQL 和 Redis 时，我对两者怎么分工也更清楚了。任务进度、缓存结果和去重标记变化很快，放 Redis 比较合适；但视频任务最后成功还是失败、事件什么时候发生、对应哪张截图、人工有没有确认，这些以后都要查，不能只放 Redis。前端查看任务时可以先读 Redis 的进度，但 Redis 重启以后，系统还是要能从 MySQL 找到任务结果。简单说，Redis 适合放“现在正在发生的事情”，MySQL 适合放“以后还要查的结果”。</p>\n<p>视频文件和业务数据也不能混在一起。数据库里保存文件地址、哈希、大小、时长和关联关系就够了，真正的视频、截图和短片段放到 volume、MinIO 或其他对象存储里。规则和 Prompt 以后肯定会改，所以历史事件还要记下当时用的版本。Worker 如果重试，同一个视频也不能一直生成重复事件，这部分需要用任务 ID 和事件指纹做去重。以前我可能会先把这些内容都塞进一个地方，学完 MySQL、Redis 和 Docker 以后，才发现这样做后面会很难维护。</p>\n<p>目前这套升级还没有真正全部做出来，YOLO、FFmpeg、Worker 和大模型接口都只是计划中的部分。这周先把它们之间的关系想清楚了：哪些事情由 API 做，哪些事情放到后台处理，哪些数据必须保存，哪些地方可以先用 Mock 顶上。比起一开始就做页面，我觉得先把这些问题想明白，后面接真实功能时少返工一些。</p>\n<p>升级计划里还有实时 RTSP、自然语言检索、向量检索和大模型报告，这些我都想做，但现在还不能一起上。如果任务状态、事件数据和文件访问都没跑顺，就直接接模型，出了问题也不知道是哪里的问题。所以我准备先用 Mock Detector 和 Mock Analyzer 把创建任务、投递任务、消费任务、保存事件和查询事件走通，再一点点换成 FFmpeg、真实检测模型和大模型分析。</p>\n<p>我现在的想法比较简单：先把服务跑起来，再看哪里真的需要扩展。</p>\n<h2 id=\"四项技术在项目中的分工\">四项技术在项目中的分工</h2>\n<h3 id=\"nginx统一访问入口\">Nginx：统一访问入口</h3>\n<p>Nginx 放在浏览器和内部服务之间，对外提供一个统一的访问地址：</p>\n<pre><code class=\"language-text\">浏览器\n  ↓\nNginx\n  ├── /       → React 前端\n  ├── /api/   → Rust API\n  └── /media/ → 图片、截图和视频片段\n</code></pre>\n<p>这样浏览器不需要知道 API、Worker、文件存储分别运行在哪个端口。Nginx 负责静态资源、API 反向代理和媒体访问，后续还可以继续增加上传大小、请求超时、访问日志和负载均衡等配置。</p>\n<p>对于视频项目，<code>/media/</code> 需要单独考虑权限和访问方式。开发阶段可以访问本地 volume，后续使用 MinIO 或兼容 S3 的对象存储，并通过受控路径或短期签名 URL 返回资源。</p>\n<h3 id=\"mysql保存核心业务数据\">MySQL：保存核心业务数据</h3>\n<p>MySQL 负责保存需要长期存在、能够查询和审计的数据。视频和图片本身不直接放进 MySQL，但视频任务、事件和处理结果需要持久化保存。</p>\n<p>升级计划中，MySQL 可以先设计这些表：</p>\n<table>\n<thead>\n<tr>\n<th>表</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>video_jobs</code></td>\n<td>视频来源、状态、进度和错误信息</td>\n</tr>\n<tr>\n<td><code>events</code></td>\n<td>事件类型、时间范围、严重程度和置信度</td>\n</tr>\n<tr>\n<td><code>event_rules</code></td>\n<td>区域、停留时长和冷却规则</td>\n</tr>\n<tr>\n<td><code>prompt_versions</code></td>\n<td>Prompt 版本和启用状态</td>\n</tr>\n<tr>\n<td><code>model_versions</code></td>\n<td>检测模型和分析模型版本</td>\n</tr>\n<tr>\n<td><code>event_reviews</code></td>\n<td>人工确认、忽略和修改记录</td>\n</tr>\n</tbody></table>\n<p>事件检索主要会使用事件类型、严重程度、状态和时间范围，因此这些字段需要根据真实查询建立索引。完整检测结果和大模型输出可以先放在 JSON 字段里，等查询需求稳定后，再决定是否拆成独立表。</p>\n<p>这周学习事务和日志后，我也意识到，任务状态、事件生成和人工复核之间需要保持一致。例如事件确认时，更新 <code>events</code> 和写入 <code>event_reviews</code> 应该放在同一个事务里，不能只完成一半。</p>\n<h3 id=\"redis处理异步任务和短期状态\">Redis：处理异步任务和短期状态</h3>\n<p>Redis 在这个项目中不是 MySQL 的替代品，而是负责变化快、访问频繁或适合异步处理的内容：</p>\n<pre><code class=\"language-text\">vision:queue:video-analysis       视频分析任务\nvision:job:{id}:progress           任务实时进度\nvision:event-search:{query_hash}  高频检索缓存\nvision:dedup:{job_id}:{fingerprint} 事件去重标记\n</code></pre>\n<p>视频分析可能需要 FFmpeg、目标检测、跟踪和大模型分析，如果让 API 一直等待，接口会被长任务占用。因此 API 只负责创建任务，Worker 从 Redis Stream 中消费任务：</p>\n<pre><code class=\"language-text\">用户上传视频\n  ↓\nRust API 创建 video_job\n  ↓\nRedis Stream 投递分析任务\n  ↓\nWorker 异步处理\n  ↓\nRedis 更新进度\n  ↓\nMySQL 保存最终任务和事件\n</code></pre>\n<p>这也是我这周学习 Redis Stream 后对项目最直接的应用。发布订阅适合实时通知，但不保存历史消息；Stream 更适合需要消费记录、失败重试和消费者组的任务队列。</p>\n<p>Redis 只保存进度、队列和缓存，最终业务数据仍然写入 MySQL。Worker 还需要具备幂等性：同一个任务重复执行时，不能无限生成重复事件。可以根据任务 ID、事件类型、时间范围和目标轨迹生成事件指纹，用于去重。</p>\n<h3 id=\"docker统一多服务环境\">Docker：统一多服务环境</h3>\n<p>Docker 负责把应用和运行环境封装起来，Compose 负责在本地或测试环境统一启动多个服务。项目升级后，第一版服务可以包括：</p>\n<pre><code class=\"language-text\">nginx     统一入口\nfrontend  React 前端\napi       Rust API\nworker    视频分析任务\nmysql     任务和事件数据库\nredis     队列、进度和缓存\nminio     视频和图片文件存储\n</code></pre>\n<p>服务之间通过 Docker 网络使用服务名通信，例如 API 连接 <code>mysql:3306</code> 和 <code>redis:6379</code>，内部服务不需要暴露到公网。MySQL 必须使用数据卷保存数据，视频文件也应该使用独立的文件存储或 volume。</p>\n<p>Compose 适合目前的开发和测试阶段。现阶段没有必要直接引入 Kubernetes；等真正遇到多机调度、自动扩缩容和高可用需求时，再单独评估 Swarm 或 Kubernetes。</p>\n<h2 id=\"四项技术如何串成一条任务链路\">四项技术如何串成一条任务链路</h2>\n<p>结合升级计划，系统的主要流程可以整理为：</p>\n<pre><code class=\"language-text\">浏览器\n  ↓\nNginx\n  ↓\nRust API\n  ├── MySQL：创建和查询任务\n  └── Redis Stream：投递异步任务\n          ↓\n      Rust Worker\n       ├── FFmpeg 抽帧与切片\n       ├── Detector 目标检测\n       ├── Tracker 目标跟踪\n       ├── Event Rule Engine 生成事件\n       └── Vision Analyzer 分析和摘要\n          ↓\n      MySQL + 文件存储\n</code></pre>\n<p>一次视频任务可以分成几个阶段：</p>\n<ol>\n<li>用户上传视频，API 检查类型和大小，保存文件并创建 <code>video_jobs</code>。</li>\n<li>API 把任务写入 Redis Stream，立即返回 <code>job_id</code>，不让 HTTP 请求一直等待。</li>\n<li>Worker 读取视频元数据，完成抽帧、检测、跟踪和规则判断。</li>\n<li>系统保存事件的时间范围、置信度、关键帧、短视频片段和版本信息。</li>\n<li>MySQL 保存最终任务和事件，Redis 保存短期进度，前端展示处理状态。</li>\n<li>用户按事件类型、时间范围、严重程度和关键词检索，并进行人工复核。</li>\n</ol>\n<p>第一阶段应该由规则引擎决定是否产生事件，大模型只负责解释、摘要和建议。这样规则可以被测试和复现，也不会让大模型直接决定高风险动作。大模型不可用时，基础事件仍然可以生成，并标记为 <code>analysis_pending</code> 或 <code>fallback</code>。</p>\n<h2 id=\"升级时需要注意的边界\">升级时需要注意的边界</h2>\n<p>这次学习也让我提前看到了一些不能忽略的问题：</p>\n<ul>\n<li>MySQL 是核心数据来源，Redis 故障不能导致已经保存的事件丢失。</li>\n<li>MySQL 数据必须使用 volume，容器删除和数据删除要明确区分。</li>\n<li>Worker 需要支持重试、取消和幂等，避免重复生成事件。</li>\n<li>视频上传需要限制类型、大小和处理目录，防止路径穿越和磁盘被占满。</li>\n<li>视频和图片二进制不放进 MySQL 或 Redis。</li>\n<li>检测模型、规则和 Prompt 都要保存版本，保证历史事件可以审计。</li>\n<li>低置信度事件进入人工复核，大模型不能直接控制生产设备。</li>\n<li>自然语言检索只能转换成结构化过滤条件，不能让模型直接拼接 SQL。</li>\n</ul>\n<h2 id=\"下一步的开发计划\">下一步的开发计划</h2>\n<p>我准备先完成最小闭环，再逐步增加真实能力。</p>\n<h3 id=\"第一阶段工程骨架\">第一阶段：工程骨架</h3>\n<ul>\n<li>初始化 Rust workspace、React 工程和 Compose。</li>\n<li>完成 Nginx、API、MySQL 和 Redis 的连接。</li>\n<li>建立 <code>VideoJob</code>、<code>Event</code>、<code>Detection</code> 和 <code>AnalysisResult</code> 类型。</li>\n<li>使用 Mock Detector 和 Mock Analyzer。</li>\n<li>跑通“创建任务 → 生成模拟事件 → 检索 → 查看详情”。</li>\n</ul>\n<h3 id=\"第二阶段视频处理链路\">第二阶段：视频处理链路</h3>\n<ul>\n<li>接入视频上传和文件存储。</li>\n<li>使用 FFmpeg 获取元数据、抽帧和切片。</li>\n<li>使用 Redis Stream 驱动 Worker。</li>\n<li>把任务状态、错误和进度保存下来。</li>\n</ul>\n<h3 id=\"第三阶段规则和证据\">第三阶段：规则和证据</h3>\n<ul>\n<li>实现区域进入、停留、越界和冷却规则。</li>\n<li>生成关键帧和事件短视频片段。</li>\n<li>完成失败重试、事件去重和 MySQL 检索。</li>\n</ul>\n<h3 id=\"第四阶段大模型和检索优化\">第四阶段：大模型和检索优化</h3>\n<ul>\n<li>接入统一的 <code>VisionAnalyzer</code> 接口。</li>\n<li>做 Prompt 版本化和结构化输出校验。</li>\n<li>增加人工复核、事件统计和自然语言检索。</li>\n<li>根据实际数据再评估向量检索和实时 RTSP。</li>\n</ul>\n<h2 id=\"今天的总结\">今天的总结</h2>\n<p>这周学习 Docker、Nginx、Redis 和 MySQL 后，我对项目升级的认识从“增加视频分析功能”，变成了“先建立稳定的任务和数据基础设施”。</p>\n<pre><code class=\"language-text\">Docker：让环境可以重复\nNginx：让访问入口稳定\nRedis：让任务处理异步化\nMySQL：让业务数据持久可靠\n</code></pre>\n<p>它们组合起来，才能支撑视频从上传、处理、生成事件，到保存证据、检索和人工复核的完整流程。下一步最重要的不是立即接入所有模型，而是先把 API、Worker、Redis、MySQL 和文件存储之间的最小闭环跑通。</p>\n<h3 id=\"参考资料\">参考资料</h3>\n<ul>\n<li><a href=\"https://docs.docker.com/manuals/\">Docker Documentation</a></li>\n<li><a href=\"https://nginx.org/en/docs/\">Nginx Documentation</a></li>\n<li><a href=\"https://redis.io/docs/latest/\">Redis Documentation</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/\">MySQL 8.4 Reference Manual</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "本周总结",
        "text": "本周总结"
      },
      {
        "depth": 2,
        "slug": "四项技术在项目中的分工",
        "text": "四项技术在项目中的分工"
      },
      {
        "depth": 3,
        "slug": "nginx统一访问入口",
        "text": "Nginx：统一访问入口"
      },
      {
        "depth": 3,
        "slug": "mysql保存核心业务数据",
        "text": "MySQL：保存核心业务数据"
      },
      {
        "depth": 3,
        "slug": "redis处理异步任务和短期状态",
        "text": "Redis：处理异步任务和短期状态"
      },
      {
        "depth": 3,
        "slug": "docker统一多服务环境",
        "text": "Docker：统一多服务环境"
      },
      {
        "depth": 2,
        "slug": "四项技术如何串成一条任务链路",
        "text": "四项技术如何串成一条任务链路"
      },
      {
        "depth": 2,
        "slug": "升级时需要注意的边界",
        "text": "升级时需要注意的边界"
      },
      {
        "depth": 2,
        "slug": "下一步的开发计划",
        "text": "下一步的开发计划"
      },
      {
        "depth": 3,
        "slug": "第一阶段工程骨架",
        "text": "第一阶段：工程骨架"
      },
      {
        "depth": 3,
        "slug": "第二阶段视频处理链路",
        "text": "第二阶段：视频处理链路"
      },
      {
        "depth": 3,
        "slug": "第三阶段规则和证据",
        "text": "第三阶段：规则和证据"
      },
      {
        "depth": 3,
        "slug": "第四阶段大模型和检索优化",
        "text": "第四阶段：大模型和检索优化"
      },
      {
        "depth": 2,
        "slug": "今天的总结",
        "text": "今天的总结"
      },
      {
        "depth": 3,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-21-docker",
    "title": "8/21 Docker：镜像、容器与多服务编排",
    "description": "记录 Docker 的镜像与容器、Dockerfile、网络和数据卷，以及 Compose、Swarm 与 Kubernetes 的学习过程",
    "publishedAt": "2026-08-21",
    "draft": false,
    "html": "<h3 id=\"docker镜像容器与多服务编排\">Docker：镜像、容器与多服务编排</h3>\n<h4 id=\"docker-是什么\">Docker 是什么</h4>\n<p>在传统开发过程中，一个项目能否正常运行，除了依赖源代码，还受到操作系统、编程语言版本、第三方库、环境变量和配置文件等因素影响。例如，一个 Go 项目可能要求特定版本的 Go；一个 Node.js 项目可能要求特定版本的 Node.js 和 npm。开发、测试和生产环境配置不一致时，项目就可能出现运行错误。</p>\n<p>Docker 的思路是将应用程序和它依赖的运行环境一起封装起来，形成一个可以重复运行的容器。</p>\n<p>Docker 中有几个核心概念：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">概念</th>\n<th align=\"left\">可以怎么理解</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">Image（镜像）</td>\n<td align=\"left\">用于创建容器的只读模板，包含程序运行所需的文件、依赖和配置</td>\n</tr>\n<tr>\n<td align=\"left\">Container（容器）</td>\n<td align=\"left\">镜像运行之后产生的实例，也就是实际启动后的进程</td>\n</tr>\n<tr>\n<td align=\"left\">Dockerfile</td>\n<td align=\"left\">镜像的构建说明文件，描述基础镜像、依赖安装、文件复制和启动命令</td>\n</tr>\n<tr>\n<td align=\"left\">Registry</td>\n<td align=\"left\">保存和分发镜像的仓库，例如 Docker Hub</td>\n</tr>\n</tbody></table>\n<p>它们之间的关系可以概括为：</p>\n<pre><code>Dockerfile ──构建──&gt; Image 镜像 ──运行──&gt; Container 容器\n</code></pre>\n<p>Docker 容器和虚拟机都可以提供隔离的运行环境，但实现方式不同：虚拟机通常需要虚拟出完整硬件环境，并运行一个完整的客户操作系统；容器则主要隔离应用程序和用户空间，共享宿主机操作系统内核，因此启动更快、资源占用更少。容器本质上仍然是宿主机上的一个特殊进程。</p>\n<p>需要注意的是，在 Windows 上使用 Docker Desktop 时，Docker Desktop 通常会通过 WSL2 或轻量级虚拟机运行 Linux 容器。所以虽然操作方式和 Linux 上一致，但底层并不是直接运行在 Windows 内核上。</p>\n<h5 id=\"基本工作流程\">基本工作流程</h5>\n<p>开发者编写 Dockerfile，使用 <code>docker build</code> 构建镜像；镜像可以保存在本地，也可以推送到 Docker Hub 等镜像仓库；其他机器通过 <code>docker pull</code> 下载镜像；最后使用 <code>docker run</code> 根据镜像启动容器：</p>\n<pre><code>编写 Dockerfile\n      ↓\ndocker build 构建镜像\n      ↓\ndocker push 推送镜像\n      ↓\ndocker pull 下载镜像\n      ↓\ndocker run 启动容器\n</code></pre>\n<p>第一次运行 Docker 官方提供的测试镜像时，如果本地没有 <code>hello-world</code> 镜像，Docker 会先从镜像仓库下载镜像，然后根据镜像创建并启动容器。容器运行完成后会输出欢迎信息并自动退出。这个过程说明了 Docker 的基本机制：</p>\n<pre><code>本地没有镜像\n      ↓\n从 Registry 拉取镜像\n      ↓\n根据镜像创建容器\n      ↓\n启动容器并执行默认命令\n      ↓\n程序执行完成，容器退出\n</code></pre>\n<h4 id=\"docker-镜像与镜像管理\">Docker 镜像与镜像管理</h4>\n<p>Docker 容器并不是凭空创建出来的，而是根据某个镜像启动的运行实例。镜像类似程序安装包或模板，容器则是这个模板真正运行起来之后的实例。</p>\n<p>Docker 镜像通常由多层只读文件系统组成，每一层对应镜像构建过程中的一部分内容。分层设计可以复用相同基础层：例如多个应用都基于 <code>ubuntu</code> 或 <code>node</code> 镜像构建时，它们可以共享相同的基础内容，不需要重复保存，从而减少磁盘空间和网络传输开销。</p>\n<p>镜像本身一般不会直接修改；容器运行过程中产生的新文件变化，会写入镜像之上的可写层，默认只属于当前容器。镜像标签如果只写 <code>latest</code>，可能会随着镜像仓库更新而发生变化。为了保证项目环境稳定，通常应该指定明确版本。</p>\n<p><code>docker pull</code> 用于从镜像仓库下载镜像。如果本地没有对应镜像，Docker 会从 Docker Hub 或配置好的私有仓库中获取。下载完成后，可以使用 <code>docker images</code> 查看本地镜像。</p>\n<p>还可以使用 <code>docker image inspect</code> 查看镜像的配置、环境变量、默认启动命令、工作目录、暴露端口、镜像层和架构等信息。在排查容器启动问题时，<code>inspect</code> 是一个非常常用的命令。\n<img src=\"image-3.png\" alt=\"alt text\"></p>\n<h4 id=\"docker-容器与容器生命周期\">Docker 容器与容器生命周期</h4>\n<p>镜像是用于创建容器的只读模板，而容器是镜像启动后的运行实例。一个镜像可以创建多个容器，同一个容器也可以被停止后再次启动。镜像主要负责提供程序和运行环境，容器则负责真正运行程序，因此镜像和容器是模板与实例的关系。\n可以使用下面的命令根据 MySQL 镜像创建并启动一个容器：</p>\n<pre><code class=\"language-bash\">docker run -d --name mysql-demo -p 3306:3306 -e MYSQL_ROOT_PASSWORD=123456 mysql:8.4\n</code></pre>\n<p>这条命令中：</p>\n<ul>\n<li><code>docker run</code>：创建并启动容器；</li>\n<li><code>-d</code>：让容器在后台运行；</li>\n<li><code>--name mysql-demo</code>：为容器指定名称；</li>\n<li><code>-p 3306:3306</code>：将宿主机 <code>3306</code> 端口映射到容器 <code>3306</code> 端口；</li>\n<li><code>-e MYSQL_ROOT_PASSWORD=123456</code>：设置 MySQL 的 root 用户密码；</li>\n<li><code>mysql:8.4</code>：使用 MySQL 8.4 镜像。</li>\n</ul>\n<p>如果本地没有这个镜像，Docker 会先自动下载镜像，再创建容器。此时可以使用本机 MySQL 客户端连接：</p>\n<pre><code class=\"language-bash\">mysql -h 127.0.0.1 -P 3306 -u root -p\n</code></pre>\n<p>接下来可以查看 MySQL 容器日志、停止容器、启动容器：</p>\n<pre><code class=\"language-bash\">docker logs &lt;MySQL容器名称&gt;\ndocker stop &lt;MySQL容器名称&gt;\ndocker start &lt;MySQL容器名称&gt;\n</code></pre>\n<p>此时 <code>docker ps</code> 中看不到该容器，但 <code>docker ps -a</code> 仍然可以看到它，只是状态变成了 <code>Exited</code>。停止容器并不等于删除容器，容器的配置、名称和关联的数据卷仍然保留。</p>\n<p><code>docker pause</code> 会暂停容器中的进程，<code>docker unpause</code> 会恢复容器中的进程。不过 MySQL 是数据库服务，实际使用时不建议随意暂停，因为暂停期间 MySQL 无法正常处理新的连接和请求。删除容器前也需要先停止容器。</p>\n<p>Docker 容器有一个很重要的特点：容器是否持续运行，取决于容器中的主进程是否持续运行。MySQL 容器的主进程就是 MySQL 服务；只要 MySQL 服务正常运行，容器就会保持运行；如果 MySQL 主进程结束，容器也会随之退出。</p>\n<h4 id=\"dockerfile-与镜像构建\">Dockerfile 与镜像构建</h4>\n<p>Dockerfile 是一个普通文本文件，用来描述如何构建 Docker 镜像。它把基础镜像、文件复制、环境配置和初始化操作记录下来，Docker 再根据这些指令生成新的镜像。可以把 Dockerfile 理解为镜像的构建说明书：</p>\n<pre><code>Dockerfile + 相关文件\n          ↓\ndocker build\n          ↓\n生成新的 Docker 镜像\n          ↓\ndocker run\n          ↓\n启动新的 Docker 容器\n</code></pre>\n<p>Dockerfile 中最常见的指令是：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">指令</th>\n<th align=\"left\">作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\"><code>FROM</code></td>\n<td align=\"left\">指定基础镜像</td>\n</tr>\n<tr>\n<td align=\"left\"><code>COPY</code></td>\n<td align=\"left\">将本地文件复制到镜像中</td>\n</tr>\n<tr>\n<td align=\"left\"><code>RUN</code></td>\n<td align=\"left\">在构建镜像时执行命令</td>\n</tr>\n<tr>\n<td align=\"left\"><code>ENV</code></td>\n<td align=\"left\">设置环境变量</td>\n</tr>\n<tr>\n<td align=\"left\"><code>EXPOSE</code></td>\n<td align=\"left\">说明容器需要使用的端口</td>\n</tr>\n<tr>\n<td align=\"left\"><code>VOLUME</code></td>\n<td align=\"left\">声明数据卷</td>\n</tr>\n<tr>\n<td align=\"left\"><code>CMD</code> / <code>ENTRYPOINT</code></td>\n<td align=\"left\">指定容器启动时执行的命令</td>\n</tr>\n<tr>\n<td align=\"left\">虽然已经有现成的 mysql:8.4 镜像，但仍然可以基于它构建一个自定义 MySQL 镜像。例如，在一个单独的文件夹中创建 Dockerfile 文件：</td>\n<td align=\"left\"></td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-dockerfile\">FROM mysql:8.4\n\nCOPY init.sql /docker-entrypoint-initdb.d/01-init.sql\n</code></pre>\n<p>同时在同一个文件夹中创建 init.sql 文件：</p>\n<pre><code class=\"language-sql\">CREATE DATABASE IF NOT EXISTS docker_demo;\n\nUSE docker_demo;\n\nCREATE TABLE IF NOT EXISTS user (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(50) NOT NULL\n);\n\nINSERT INTO user (name) VALUES (&#39;张三&#39;), (&#39;李四&#39;);\n</code></pre>\n<p>这里的 <code>FROM mysql:8.4</code> 表示当前镜像基于官方 MySQL 8.4 镜像构建。<code>COPY init.sql /docker-entrypoint-initdb.d/01-init.sql</code> 表示将本地初始化 SQL 文件复制到 MySQL 容器约定的初始化目录中。</p>\n<p>MySQL 官方镜像在第一次初始化一个全新的数据目录时，会执行 <code>/docker-entrypoint-initdb.d/</code> 目录下的 <code>.sql</code>、<code>.sh</code> 等文件，因此可以通过这种方式自动创建数据库、表和初始化数据。在 Dockerfile 和 init.sql 所在目录中执行：</p>\n<pre><code class=\"language-bash\">docker build -t mysql-demo:1.0 .\n</code></pre>\n<p>其中，<code>docker build</code> 表示构建镜像；<code>-t mysql-demo:1.0</code> 表示给生成镜像命名为 <code>mysql-demo</code>，标签为 <code>1.0</code>；最后的 <code>.</code> 表示使用当前目录作为构建上下文。构建上下文是 Docker 在构建过程中可以访问的文件范围，因此 Dockerfile 和 init.sql 都需要位于当前目录或其子目录中。\n构建完成后，可以查看新生成的镜像。\n<img src=\"image-4.png\" alt=\"alt text\"></p>\n<p>这时候镜像已经构建完成，可以根据它创建一个新的测试容器：</p>\n<pre><code class=\"language-docker\">docker run -d --name mysql-demo-container -p 3307:3306 -e MYSQL_ROOT_PASSWORD=123456 mysql-demo:1.0\n</code></pre>\n<p>如果 MySQL 启动成功，可以进入容器中的 MySQL：</p>\n<pre><code class=\"language-bash\">docker exec -it mysql-demo-container mysql -uroot -p\n</code></pre>\n<p>输入密码后进入 MySQL，查看一下存储的数据：\n<img src=\"image-5.png\" alt=\"alt text\"></p>\n<p>如果能够查询到 张三 和 李四，说明 Dockerfile 中复制的初始化 SQL 已经成功执行。\n需要注意，MySQL 官方镜像中的初始化脚本通常只会在数据目录第一次初始化时执行。如果容器已经使用了旧的数据卷，后续重新启动容器时，/docker-entrypoint-initdb.d/ 中的 SQL 文件一般不会再次执行。因此，如果修改了 init.sql，直接重启已有容器可能看不到新的数据库内容。学习时可以使用一个全新的测试容器和全新的数据卷验证初始化效果，但不要随意删除正在使用的 MySQL 容器或数据卷。\n还需要区分 <code>RUN</code>、<code>CMD</code> 和 <code>ENTRYPOINT</code>：<code>RUN</code> 在构建镜像时执行，例如安装依赖或复制文件；<code>CMD</code> 用于指定容器默认启动命令；<code>ENTRYPOINT</code> 用于指定容器的主要执行程序。</p>\n<p>对于 MySQL 官方镜像，MySQL 的启动脚本和默认启动命令已经在基础镜像中配置好了，所以自定义 Dockerfile 通常只需要通过 <code>COPY</code> 添加初始化文件，不需要重新编写 MySQL 的启动命令。</p>\n<h4 id=\"docker-网络与数据卷\">Docker 网络与数据卷</h4>\n<p>Docker 网络解决的是容器之间如何通信的问题，数据卷解决的是容器删除或重建后数据如何保留的问题。对于 MySQL 这类数据库服务来说，这两个部分都非常重要：应用容器需要通过网络访问 MySQL，MySQL 数据则需要通过数据卷进行持久化保存。</p>\n<h5 id=\"docker-网络\">Docker 网络</h5>\n<p>Docker 安装后会自动创建默认的 bridge 网络。查看当前 Docker 网络：</p>\n<pre><code class=\"language-bash\">docker network ls\n</code></pre>\n<p>其中，<code>bridge</code> 是默认的桥接网络，<code>host</code> 表示使用宿主机网络，<code>none</code> 表示不使用网络。平时使用 <code>docker run</code> 启动容器时，如果没有指定网络，容器会自动连接到默认的 <code>bridge</code> 网络。\n<img src=\"image-6.png\" alt=\"alt text\"></p>\n<p>在实际项目中，通常不建议让所有容器都使用默认的 bridge 网络，而是创建一个自定义网络：</p>\n<pre><code class=\"language-bash\">docker network create app-network\n</code></pre>\n<p>自定义网络的一个重要作用是让同一个网络中的容器可以通过容器名称互相访问。</p>\n<h5 id=\"数据卷\">数据卷</h5>\n<p>容器本身的文件系统属于容器生命周期的一部分。如果直接将数据库文件保存到容器内部，删除容器后数据可能会丢失。数据卷是 Docker 管理的一种持久化存储方式，它独立于容器存在；即使删除容器，数据卷仍然可以保留。因此，MySQL 的数据目录通常需要挂载数据卷。</p>\n<p>创建一个名为 <code>mysql-data</code> 的数据卷：</p>\n<pre><code class=\"language-bash\">docker volume create mysql-data\n</code></pre>\n<p>查看所有数据卷：</p>\n<pre><code class=\"language-bash\">docker volume ls\n</code></pre>\n<p>检查具体数据卷信息：</p>\n<pre><code class=\"language-bash\">docker volume inspect mysql-data\n</code></pre>\n<p>使用数据卷创建一个新的 MySQL 测试容器：</p>\n<pre><code class=\"language-powershell\">docker run -d `\n  --name mysql-volume-test `\n  -p 3307:3306 `\n  -e MYSQL_ROOT_PASSWORD=123456 `\n  --mount type=volume,src=mysql-data,dst=/var/lib/mysql `\n  mysql:8.4\n</code></pre>\n<p>也可以使用更短的 <code>-v</code> 写法：</p>\n<pre><code class=\"language-bash\">docker run -d --name mysql-volume-test -p 3307:3306 -e MYSQL_ROOT_PASSWORD=123456 -v mysql-data:/var/lib/mysql mysql:8.4\n</code></pre>\n<p>这条命令使用 <code>mysql:8.4</code> 镜像，创建并后台启动一个名为 <code>mysql-volume-test</code> 的 MySQL 容器，同时把 MySQL 数据保存到 Docker 数据卷中。然后进入 MySQL 容器创建测试数据。</p>\n<p>进入 MySQL 后执行：</p>\n<pre><code class=\"language-sql\">CREATE DATABASE docker_demo;\n\nUSE docker_demo;\n\nCREATE TABLE user (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(50)\n);\n\nINSERT INTO user (name) VALUES (&#39;张三&#39;);\n\nSELECT * FROM user;\n</code></pre>\n<p>停止并删除测试容器：</p>\n<pre><code class=\"language-bash\">docker stop mysql-volume-test\ndocker rm mysql-volume-test\n</code></pre>\n<p>然后使用同一个数据卷重新创建一个 MySQL 容器，再次进入 MySQL 查询。如果能查到之前插入的“张三”，说明数据已经保存在 <code>mysql-data</code> 数据卷中，而不是保存在原来的容器内部。</p>\n<p>这个过程说明：容器可以被删除和重新创建，但只要数据卷还存在，数据库数据就可以继续使用。</p>\n<h4 id=\"docker-compose\">Docker Compose</h4>\n<p>前面使用 <code>docker run</code> 启动 MySQL 时，需要在命令中手动设置容器名称、端口、环境变量和数据卷。当项目中有多个容器时，如果每个容器都单独执行一长串命令，管理起来会比较麻烦。</p>\n<p>Docker Compose 可以通过一个 <code>compose.yaml</code> 文件统一描述服务、网络、数据卷和环境变量，再使用一条命令创建和启动整套服务。它主要用于管理多容器应用，例如一个项目可能包含后端服务、MySQL 数据库和 Redis 缓存。</p>\n<p>如果使用 <code>docker run</code>，需要分别启动多个容器；使用 Docker Compose 后，可以将这些配置写入同一个 YAML 文件中，再通过 <code>docker compose up</code> 统一启动。</p>\n<p>首先创建一个新的文件夹，例如：</p>\n<pre><code class=\"language-text\">mysql-compose\n├── compose.yaml\n└── init.sql\n</code></pre>\n<p>在 <code>compose.yaml</code> 文件中写入：</p>\n<pre><code class=\"language-yaml\">services:\n  mysql:\n    image: mysql:8.4\n    container_name: compose-mysql\n    environment:\n      MYSQL_ROOT_PASSWORD: 123456\n      MYSQL_DATABASE: docker_demo\n    ports:\n      - &quot;3308:3306&quot;\n    volumes:\n      - mysql-compose-data:/var/lib/mysql\n      - ./init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro\n\nvolumes:\n  mysql-compose-data:\n</code></pre>\n<p>这里：</p>\n<ul>\n<li><code>services</code>：应用服务集合；</li>\n<li><code>mysql</code>：服务名称；</li>\n<li><code>image</code>：指定使用 <code>mysql:8.4</code> 镜像；</li>\n<li><code>container_name</code>：指定容器名称；</li>\n<li><code>environment</code>：设置 MySQL 的 root 密码和默认数据库；</li>\n<li><code>ports</code>：将宿主机 <code>3308</code> 端口映射到容器 <code>3306</code> 端口；</li>\n<li><code>volumes</code>：挂载 MySQL 数据卷和初始化 SQL 文件。</li>\n</ul>\n<p>其中，<code>mysql-compose-data:/var/lib/mysql</code> 表示将名为 <code>mysql-compose-data</code> 的 Docker 数据卷挂载到 MySQL 数据目录；<code>./init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro</code> 表示将当前目录的 <code>init.sql</code> 文件挂载到初始化目录，最后的 <code>ro</code> 表示只读。</p>\n<p>在 <code>compose.yaml</code> 所在目录打开 PowerShell，然后执行：</p>\n<pre><code class=\"language-bash\">docker compose up -d\n</code></pre>\n<p><code>docker compose up</code> 会根据 <code>compose.yaml</code> 创建需要的网络、数据卷和容器，<code>-d</code> 表示让服务在后台运行。</p>\n<h4 id=\"docker-swarm-与-kubernetes\">Docker Swarm 与 Kubernetes</h4>\n<p>前面学习的 <code>docker run</code>、Dockerfile、数据卷和 Docker Compose，主要用于在一台 Docker 主机上构建和管理容器应用。当应用部署到多台服务器，并且需要自动扩容、故障恢复、负载均衡和滚动更新时，就需要使用容器编排工具。</p>\n<h5 id=\"docker-swarm\">Docker Swarm</h5>\n<p>Docker Swarm 是 Docker Engine 内置的集群管理功能，可以将多台运行 Docker Engine 的机器组成一个集群。Swarm 集群中的节点主要分为 Manager 和 Worker 两种角色：</p>\n<ul>\n<li>Manager：维护集群状态、调度服务和管理节点；</li>\n<li>Worker：主要负责运行 Manager 分配的任务。</li>\n</ul>\n<p>Swarm 中的服务不是单独描述某一个容器，而是描述某个应用服务应该以什么镜像运行、运行几个副本、使用哪些端口和网络等。</p>\n<p>初始化一个单节点 Swarm 集群：</p>\n<pre><code class=\"language-bash\">docker swarm init\n</code></pre>\n<p>执行后，当前机器会成为 Manager 节点。如果要实现真正的高可用和多机部署，则需要多台可以互相通信的 Docker 主机。初始化 Swarm 后，Docker 会输出加入 Worker 节点所需的命令和 Token，其他机器可以使用这个命令加入集群。</p>\n<p>在 Swarm 中，使用 <code>docker service create</code> 创建服务。Swarm Manager 会自动创建新的任务；如果某个节点发生故障，Manager 会根据服务的期望状态重新调度任务。</p>\n<p>需要注意，MySQL 属于有状态服务，而 Swarm 的服务副本通常更适合无状态应用。Web 服务、API 服务和任务处理服务可以比较容易地运行多个副本；但 MySQL 这类数据库涉及数据卷、主从复制、数据一致性和故障恢复，不能简单地把副本数量设为多个就实现高可用。因此，实际项目中通常会单独设计数据库集群，而不是直接对普通 MySQL 容器执行多副本扩展。</p>\n<h5 id=\"kubernetes\">Kubernetes</h5>\n<p>此外，Kubernetes 也是一种容器编排平台，但它的功能范围和生态比 Docker Swarm 更完整。Kubernetes 可以管理容器化应用的部署、扩缩容、服务发现、负载均衡、配置、密钥、存储和故障恢复。</p>\n<p>Kubernetes 通常使用 YAML 文件描述应用的期望状态，再由控制器不断调整实际运行状态。可以先把几个工具的适用范围这样理解：</p>\n<pre><code class=\"language-text\">docker run\n    ↓\n管理单个容器\n\nDocker Compose\n    ↓\n管理单机上的多容器应用\n\nDocker Swarm\n    ↓\n管理多台 Docker 主机上的服务\n\nKubernetes\n    ↓\n管理大规模、复杂的容器化应用集群\n</code></pre>\n<p>Docker Compose 适合本地开发、测试环境和简单部署，例如通过一个 compose.yaml 文件启动后端、MySQL 和 Redis。Docker Swarm 直接集成在 Docker Engine 中，学习成本相对较低，适合 Docker 生态下的基础集群部署。Kubernetes 功能更丰富，适合复杂的生产环境，但学习和运维成本也更高。</p>\n<h3 id=\"参考资料\">参考资料</h3>\n<ul>\n<li><a href=\"https://www.runoob.com/docker/docker-tutorial.html\">Docker 教程（菜鸟教程）</a></li>\n<li><a href=\"https://docs.docker.com/manuals/\">Docker Documentation</a></li>\n<li><a href=\"https://docs.docker.com/get-started/docker-concepts/\">Docker 基础概念</a></li>\n<li><a href=\"https://docs.docker.com/reference/dockerfile/\">Dockerfile reference</a></li>\n<li><a href=\"https://docs.docker.com/compose/\">Docker Compose</a></li>\n<li><a href=\"https://docs.docker.com/engine/storage/volumes/\">Docker Volumes</a></li>\n<li><a href=\"https://docs.docker.com/engine/network/\">Docker Networking overview</a></li>\n<li><a href=\"https://docs.docker.com/engine/swarm/\">Docker Swarm mode</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "docker镜像容器与多服务编排",
        "text": "Docker：镜像、容器与多服务编排"
      },
      {
        "depth": 4,
        "slug": "docker-是什么",
        "text": "Docker 是什么"
      },
      {
        "depth": 5,
        "slug": "基本工作流程",
        "text": "基本工作流程"
      },
      {
        "depth": 4,
        "slug": "docker-镜像与镜像管理",
        "text": "Docker 镜像与镜像管理"
      },
      {
        "depth": 4,
        "slug": "docker-容器与容器生命周期",
        "text": "Docker 容器与容器生命周期"
      },
      {
        "depth": 4,
        "slug": "dockerfile-与镜像构建",
        "text": "Dockerfile 与镜像构建"
      },
      {
        "depth": 4,
        "slug": "docker-网络与数据卷",
        "text": "Docker 网络与数据卷"
      },
      {
        "depth": 5,
        "slug": "docker-网络",
        "text": "Docker 网络"
      },
      {
        "depth": 5,
        "slug": "数据卷",
        "text": "数据卷"
      },
      {
        "depth": 4,
        "slug": "docker-compose",
        "text": "Docker Compose"
      },
      {
        "depth": 4,
        "slug": "docker-swarm-与-kubernetes",
        "text": "Docker Swarm 与 Kubernetes"
      },
      {
        "depth": 5,
        "slug": "docker-swarm",
        "text": "Docker Swarm"
      },
      {
        "depth": 5,
        "slug": "kubernetes",
        "text": "Kubernetes"
      },
      {
        "depth": 3,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-21-markdown",
    "title": "8/21 Markdown：基本语法学习笔记",
    "description": "整理 Markdown 的标题、段落、代码、链接、图片和表格等常用语法",
    "publishedAt": "2026-08-21",
    "draft": false,
    "html": "<h3 id=\"markdown-基本语法学习笔记\">Markdown 基本语法学习笔记</h3>\n<h4 id=\"一markdown-是什么\">一、Markdown 是什么</h4>\n<p>Markdown 是一种轻量级标记语言。它通过少量易读的符号描述文档结构，让纯文本同时具备较好的可读性和排版能力。</p>\n<p>例如，下面这段文字即使不经过渲染，也能看出标题、列表和代码的层次：</p>\n<pre><code class=\"language-markdown\"># 项目说明\n\n这是项目的简介。\n\n## 安装步骤\n\n1. 安装依赖\n2. 修改配置\n3. 启动项目\n</code></pre>\n<p>Markdown 文件通常使用 <code>.md</code> 作为扩展名，可以被转换成 HTML，也可以用于生成 PDF、技术文档、博客文章、README 文件和学习笔记。它的重点不是让作者花大量时间调整字体和间距，而是让作者专注于内容本身。</p>\n<h4>二、标题语法</h4>\n<p>Markdown 使用 <code>#</code> 表示标题，<code>#</code> 的数量决定标题级别，最多可以使用六级标题：</p>\n<pre><code class=\"language-markdown\"># 一级标题\n## 二级标题\n### 三级标题\n#### 四级标题\n##### 五级标题\n###### 六级标题\n</code></pre>\n<p>渲染后，一级标题最大，六级标题最小。通常建议遵循下面的结构：</p>\n<pre><code class=\"language-markdown\"># 文章主题\n\n## 第一部分\n### 第一部分中的小节\n\n## 第二部分\n### 第二部分中的小节\n</code></pre>\n<h5>标题的使用建议</h5>\n<ol>\n<li>一篇文章通常只保留一个一级标题，用来表示文章主题。</li>\n<li>不要为了让文字变大而滥用标题，强调文字应使用粗体。</li>\n<li>不要跳过层级，例如从 <code>##</code> 直接跳到 <code>####</code>。</li>\n<li>标题符号和文字之间最好保留一个空格：<code>## 正确写法</code>。</li>\n<li>标题前后最好留一个空行，能够减少不同编辑器之间的解析差异。</li>\n</ol>\n<p>错误示例：</p>\n<pre><code class=\"language-markdown\">#正确但不推荐\n##错误示例\n</code></pre>\n<p>推荐写法：</p>\n<pre><code class=\"language-markdown\"># 正确写法\n## 推荐写法\n</code></pre>\n<h4>三、段落与换行</h4>\n<p>普通文字直接书写即可。Markdown 通常以空行区分段落：</p>\n<pre><code class=\"language-markdown\">这是第一段。它可以包含多句话，直到遇到空行。\n\n这是第二段。空行会让内容变成新的段落。\n</code></pre>\n<h5>段内换行</h5>\n<p>如果只是想在同一段中换一行，可以在上一行末尾添加两个空格，再按回车：</p>\n<pre><code class=\"language-markdown\">第一行文字。  \n第二行文字。\n</code></pre>\n<p>也可以使用 HTML 的 <code>&lt;br&gt;</code> 标签：</p>\n<pre><code class=\"language-markdown\">第一行文字。&lt;br&gt;\n第二行文字。\n</code></pre>\n<p>不过，不同平台对单个回车的处理方式并不完全一致。为了让文章在博客、GitHub 和其他编辑器中保持稳定，建议使用空行表示新段落，只有确实需要同一段内换行时才使用两个空格或 <code>&lt;br&gt;</code>。</p>\n<h4 id=\"四级标题\">四、强调文字</h4>\n<h5 id=\"五级标题\">斜体</h5>\n<p>使用一对星号或下划线包裹文字：</p>\n<pre><code class=\"language-markdown\">*斜体文字*\n_斜体文字_\n</code></pre>\n<p>效果：<em>斜体文字</em>。</p>\n<p>斜体适合表示补充说明、术语的特别强调或外文词语，但不建议整段使用斜体，否则阅读压力会比较大。</p>\n<h5>粗体</h5>\n<p>使用两对星号或下划线包裹文字：</p>\n<pre><code class=\"language-markdown\">**粗体文字**\n__粗体文字__\n</code></pre>\n<p>效果：<strong>粗体文字</strong>。</p>\n<p>粗体更适合突出结论、关键词和操作步骤。例如：<strong>提交代码前必须先运行测试</strong>。</p>\n<h5>粗斜体</h5>\n<p>同时使用三颗星号：</p>\n<pre><code class=\"language-markdown\">***粗斜体文字***\n</code></pre>\n<p>效果：<em><strong>粗斜体文字</strong></em>。</p>\n<h5>删除线</h5>\n<p>删除线不是最早的基础 Markdown 语法，但很多编辑器都支持使用两个波浪线：</p>\n<pre><code class=\"language-markdown\">~~已经删除的内容~~\n</code></pre>\n<p>效果：<del>已经删除的内容</del>。</p>\n<p>它适合表示修改前的内容、已经废弃的方案或自我纠正，不建议把删除线当作普通强调手段。</p>\n<h4>五、列表语法</h4>\n<h5 id=\"标题的使用建议\">无序列表</h5>\n<p>无序列表可以使用 <code>-</code>、<code>*</code> 或 <code>+</code>：</p>\n<pre><code class=\"language-markdown\">- Redis\n- MySQL\n- Rust\n</code></pre>\n<p>效果：</p>\n<ul>\n<li>Redis</li>\n<li>MySQL</li>\n<li>Rust</li>\n</ul>\n<p>同一组列表最好统一使用同一种符号，不要在同一层级混用 <code>-</code>、<code>*</code> 和 <code>+</code>。</p>\n<h5>有序列表</h5>\n<p>有序列表使用数字加英文句点：</p>\n<pre><code class=\"language-markdown\">1. 安装依赖\n2. 修改配置\n3. 启动服务\n</code></pre>\n<p>效果：</p>\n<ol>\n<li>安装依赖</li>\n<li>修改配置</li>\n<li>启动服务</li>\n</ol>\n<p>有些 Markdown 编辑器会自动修正编号，因此即使全部写成 <code>1.</code>，渲染时也可能显示为连续的数字。不过为了提高源文件的可读性，手动写出正确编号仍然是更好的习惯。</p>\n<h5>嵌套列表</h5>\n<p>在列表项前增加两个或四个空格，可以表示子列表：</p>\n<pre><code class=\"language-markdown\">- 前端\n  - HTML\n  - CSS\n  - JavaScript\n- 后端\n  - Java\n  - Go\n  - Rust\n</code></pre>\n<p>嵌套列表的缩进必须保持一致。如果某一项缩进太少，编辑器可能会把它解析成新的一级列表。</p>\n<h5 id=\"段内换行\">任务列表</h5>\n<p>任务列表常用于记录待办事项，写法是方括号加空格或 <code>x</code>：</p>\n<pre><code class=\"language-markdown\">- [ ] 学习标题和段落语法\n- [x] 整理代码块语法\n- [ ] 完成文章发布\n</code></pre>\n<p>效果：</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> 学习标题和段落语法</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\"> 整理代码块语法</li>\n<li><input disabled=\"\" type=\"checkbox\"> 完成文章发布</li>\n</ul>\n<p>任务列表属于许多平台支持的扩展语法。如果某个平台没有显示复选框，通常仍会把它当作普通列表展示。</p>\n<h4 id=\"四强调文字\">六、引用块</h4>\n<p>使用 <code>&gt;</code> 表示引用：</p>\n<pre><code class=\"language-markdown\">&gt; Markdown 的重点是表达内容结构，而不是复杂的视觉排版。\n</code></pre>\n<p>效果：</p>\n<blockquote>\n<p>Markdown 的重点是表达内容结构，而不是复杂的视觉排版。</p>\n</blockquote>\n<p>引用块也可以包含多段内容：</p>\n<pre><code class=\"language-markdown\">&gt; 这是引用的第一段。\n&gt;\n&gt; 这是引用的第二段。\n</code></pre>\n<p>通过增加 <code>&gt;</code> 可以实现嵌套引用：</p>\n<pre><code class=\"language-markdown\">&gt; 外层引用\n&gt;&gt; 内层引用\n</code></pre>\n<p>引用适合用于记录资料原文、提示重要信息、补充背景说明或展示命令执行结果。引用内容最好注明来源，不要把整篇外部文章直接复制进来。</p>\n<h4>七、代码语法</h4>\n<h5 id=\"粗体\">行内代码</h5>\n<p>使用一对反引号包裹短代码：</p>\n<pre><code class=\"language-markdown\">使用 `git status` 可以查看当前工作区状态。\n</code></pre>\n<p>效果：使用 <code>git status</code> 可以查看当前工作区状态。</p>\n<p>行内代码适合表示命令、变量名、函数名、文件名、配置项和键盘按键，例如 <code>package.json</code>、<code>npm install</code> 和 <code>PORT</code>。</p>\n<h5 id=\"粗斜体\">多行代码块</h5>\n<p>使用三个反引号包裹多行代码：</p>\n<pre><code class=\"language-markdown\">```\nconst message = &#39;Hello Markdown&#39;;\nconsole.log(message);\n```\n</code></pre>\n<p>代码块的开头可以写语言名称，以便编辑器进行语法高亮：</p>\n<pre><code class=\"language-markdown\">```javascript\nconst message = &#39;Hello Markdown&#39;;\nconsole.log(message);\n```\n</code></pre>\n<p>常见语言标识包括：</p>\n<pre><code class=\"language-text\">javascript、typescript、python、java、go、rust、css、html、json、yaml、bash、sql\n</code></pre>\n<p>展示命令时，可以使用 <code>bash</code>：</p>\n<pre><code class=\"language-bash\">pnpm install\npnpm dev\n</code></pre>\n<p>展示 JSON 时，可以使用 <code>json</code>：</p>\n<pre><code class=\"language-json\">{\n  &quot;name&quot;: &quot;markdown-note&quot;,\n  &quot;version&quot;: &quot;1.0.0&quot;\n}\n</code></pre>\n<p>代码块中的内容会按照原样显示，因此不要在代码块内部额外添加不必要的缩进。代码块前后也建议各留一个空行。</p>\n<h4>八、链接语法</h4>\n<p>标准链接的写法是：</p>\n<pre><code class=\"language-markdown\">[Markdown 基本语法](https://markdown.com.cn/basic-syntax/)\n</code></pre>\n<p>效果：<a href=\"https://markdown.com.cn/basic-syntax/\">Markdown 基本语法</a>。</p>\n<p>链接文字应该尽量说明目标内容，不建议把一长串 URL 直接作为链接文字。比如“查看 Markdown 基本语法”比“点击这里”更容易理解，也更有利于无障碍阅读。</p>\n<h5>给链接增加标题</h5>\n<p>可以在 URL 后面增加可选标题：</p>\n<pre><code class=\"language-markdown\">[Markdown 教程](https://markdown.com.cn/basic-syntax/ &quot;查看基本语法&quot;)\n</code></pre>\n<h5 id=\"无序列表\">自动链接</h5>\n<p>部分 Markdown 编辑器会自动识别完整 URL：</p>\n<pre><code class=\"language-markdown\">https://markdown.com.cn/basic-syntax/\n</code></pre>\n<p>也可以使用尖括号明确表示自动链接：</p>\n<pre><code class=\"language-markdown\">&lt;https://markdown.com.cn/basic-syntax/&gt;\n&lt;example@example.com&gt;\n</code></pre>\n<h4>九、图片语法</h4>\n<p>图片语法和链接非常相似，只是在前面增加一个感叹号：</p>\n<pre><code class=\"language-markdown\">![图片说明](https://example.com/image.png)\n</code></pre>\n<p>其中，方括号里的文字叫作替代文本。当图片无法加载，或读者使用屏幕阅读器时，替代文本可以帮助读者理解图片内容。</p>\n<p>也可以为图片添加标题：</p>\n<pre><code class=\"language-markdown\">![Markdown 标志](https://example.com/markdown.png &quot;Markdown Logo&quot;)\n</code></pre>\n<p>图片路径可以是网络地址，也可以是相对于当前 Markdown 文件的本地路径：</p>\n<pre><code class=\"language-markdown\">![项目目录结构](./images/project-tree.png)\n</code></pre>\n<p>使用本地图片时要注意文件名大小写、相对路径层级和部署后的目录结构。文章在本地能显示，并不代表发布到网站后一定能显示。</p>\n<h4>十、表格语法</h4>\n<p>表格通常用于展示字段、对比结果或配置说明：</p>\n<pre><code class=\"language-markdown\">| 语法 | 作用 |\n| --- | --- |\n| `#` | 创建标题 |\n| `-` | 创建列表 |\n| `&gt;` | 创建引用 |\n</code></pre>\n<p>效果：</p>\n<table>\n<thead>\n<tr>\n<th>语法</th>\n<th>作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>#</code></td>\n<td>创建标题</td>\n</tr>\n<tr>\n<td><code>-</code></td>\n<td>创建列表</td>\n</tr>\n<tr>\n<td><code>&gt;</code></td>\n<td>创建引用</td>\n</tr>\n</tbody></table>\n<p>通过冒号可以控制对齐方式：</p>\n<pre><code class=\"language-markdown\">| 左对齐 | 居中 | 右对齐 |\n| :--- | :---: | ---: |\n| 内容 | 内容 | 内容 |\n</code></pre>\n<p>表格中的竖线如果属于正文内容，需要使用反斜杠转义：</p>\n<pre><code class=\"language-markdown\">| 命令 | 说明 |\n| --- | --- |\n| `a \\| b` | 使用管道符连接两个值 |\n</code></pre>\n<p>表格适合简短内容。如果单元格里需要放很长的段落、代码块或复杂列表，建议改用多个小节，避免表格在手机上难以阅读。</p>\n<h4>十一、分隔线</h4>\n<p>使用三个或更多的连字符、星号或下划线可以创建分隔线：</p>\n<pre><code class=\"language-markdown\">---\n</code></pre>\n<p>也可以写成：</p>\n<pre><code class=\"language-markdown\">***\n___\n</code></pre>\n<p>分隔线适合区分文章中两个相对独立的部分，例如正文和附录。不要频繁使用分隔线，否则文章会被切割得过于零散。</p>\n<h4 id=\"六引用块\">十二、转义字符</h4>\n<p>Markdown 中有一些特殊字符具有语法含义。如果希望它们按照普通文字显示，可以在前面添加反斜杠：</p>\n<pre><code class=\"language-markdown\">\\*这不是斜体\\*\n\\# 这不是标题\n\\[这不是链接\\]\n</code></pre>\n<p>常见需要转义的字符包括：</p>\n<pre><code class=\"language-text\">\\\\ 反斜杠\n\\` 反引号\n\\* 星号\n\\_ 下划线\n\\{ \\} 花括号\n\\[ \\] 方括号\n\\( \\) 小括号\n\\# 井号\n\\+ 加号\n\\- 连字符\n\\. 句点\n\\! 感叹号\n\\| 管道符\n</code></pre>\n<p>例如，如果想在文章中展示标题写法，而不是让它真的变成标题，可以写成：</p>\n<pre><code class=\"language-markdown\">\\# 这段文字会显示井号\n</code></pre>\n<h4 id=\"七代码语法\">十三、内嵌 HTML</h4>\n<p>Markdown 支持在文档中混合使用部分 HTML：</p>\n<pre><code class=\"language-html\">&lt;br&gt;\n&lt;span&gt;这是一段 HTML 文本。&lt;/span&gt;\n</code></pre>\n<p>也可以使用 HTML 控制图片宽度或增加特定结构，但不同平台对 HTML 的支持不完全一致。因此，日常写作时应优先使用 Markdown 自身的语法，只有在 Markdown 无法满足需求时再使用 HTML。</p>\n<p>需要特别注意的是，某些网站会过滤危险或不安全的 HTML 标签和属性。不要在不清楚平台安全策略的情况下嵌入脚本、事件属性或外部组件。</p>\n<h4>十四、推荐的文章写作流程</h4>\n<p>写一篇 Markdown 文章时，可以按照下面的顺序组织：</p>\n<ol>\n<li>先确定一个清晰的一级标题。</li>\n<li>使用二级标题拆分主要内容。</li>\n<li>每个小节先用普通段落解释概念。</li>\n<li>使用列表整理多个并列要点。</li>\n<li>使用代码块展示可以复制的代码或命令。</li>\n<li>使用引用块突出注意事项或外部资料。</li>\n<li>使用链接补充官方文档和参考资料。</li>\n<li>发布前检查标题层级、空行、代码闭合、图片路径和表格格式。</li>\n</ol>\n<p>不要一开始就纠结字体、颜色和复杂布局。先把内容结构写清楚，再根据发布平台支持的扩展语法进行优化，通常能够获得更稳定的结果。</p>\n<h3>参考资料</h3>\n<p><a href=\"https://markdown.com.cn/basic-syntax/\">Markdown 基本语法</a></p>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "markdown-基本语法学习笔记",
        "text": "Markdown 基本语法学习笔记"
      },
      {
        "depth": 4,
        "slug": "一markdown-是什么",
        "text": "一、Markdown 是什么"
      },
      {
        "depth": 2,
        "slug": "安装步骤",
        "text": "安装步骤"
      },
      {
        "depth": 4,
        "slug": "二标题语法",
        "text": "二、标题语法"
      },
      {
        "depth": 2,
        "slug": "二级标题",
        "text": "二级标题"
      },
      {
        "depth": 3,
        "slug": "三级标题",
        "text": "三级标题"
      },
      {
        "depth": 4,
        "slug": "四级标题",
        "text": "四级标题"
      },
      {
        "depth": 5,
        "slug": "五级标题",
        "text": "五级标题"
      },
      {
        "depth": 2,
        "slug": "第一部分",
        "text": "第一部分"
      },
      {
        "depth": 3,
        "slug": "第一部分中的小节",
        "text": "第一部分中的小节"
      },
      {
        "depth": 2,
        "slug": "第二部分",
        "text": "第二部分"
      },
      {
        "depth": 3,
        "slug": "第二部分中的小节",
        "text": "第二部分中的小节"
      },
      {
        "depth": 5,
        "slug": "标题的使用建议",
        "text": "标题的使用建议"
      },
      {
        "depth": 2,
        "slug": "推荐写法",
        "text": "推荐写法"
      },
      {
        "depth": 4,
        "slug": "三段落与换行",
        "text": "三、段落与换行"
      },
      {
        "depth": 5,
        "slug": "段内换行",
        "text": "段内换行"
      },
      {
        "depth": 4,
        "slug": "四强调文字",
        "text": "四、强调文字"
      },
      {
        "depth": 5,
        "slug": "斜体",
        "text": "斜体"
      },
      {
        "depth": 5,
        "slug": "粗体",
        "text": "粗体"
      },
      {
        "depth": 5,
        "slug": "粗斜体",
        "text": "粗斜体"
      },
      {
        "depth": 5,
        "slug": "删除线",
        "text": "删除线"
      },
      {
        "depth": 4,
        "slug": "五列表语法",
        "text": "五、列表语法"
      },
      {
        "depth": 5,
        "slug": "无序列表",
        "text": "无序列表"
      },
      {
        "depth": 5,
        "slug": "有序列表",
        "text": "有序列表"
      },
      {
        "depth": 5,
        "slug": "嵌套列表",
        "text": "嵌套列表"
      },
      {
        "depth": 5,
        "slug": "任务列表",
        "text": "任务列表"
      },
      {
        "depth": 4,
        "slug": "六引用块",
        "text": "六、引用块"
      },
      {
        "depth": 4,
        "slug": "七代码语法",
        "text": "七、代码语法"
      },
      {
        "depth": 5,
        "slug": "行内代码",
        "text": "行内代码"
      },
      {
        "depth": 5,
        "slug": "多行代码块",
        "text": "多行代码块"
      },
      {
        "depth": 4,
        "slug": "八链接语法",
        "text": "八、链接语法"
      },
      {
        "depth": 5,
        "slug": "给链接增加标题",
        "text": "给链接增加标题"
      },
      {
        "depth": 5,
        "slug": "自动链接",
        "text": "自动链接"
      },
      {
        "depth": 4,
        "slug": "九图片语法",
        "text": "九、图片语法"
      },
      {
        "depth": 4,
        "slug": "十表格语法",
        "text": "十、表格语法"
      },
      {
        "depth": 4,
        "slug": "十一分隔线",
        "text": "十一、分隔线"
      },
      {
        "depth": 4,
        "slug": "十二转义字符",
        "text": "十二、转义字符"
      },
      {
        "depth": 4,
        "slug": "十三内嵌-html",
        "text": "十三、内嵌 HTML"
      },
      {
        "depth": 4,
        "slug": "十四推荐的文章写作流程",
        "text": "十四、推荐的文章写作流程"
      },
      {
        "depth": 3,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-20",
    "title": "8/20 Redis：缓存、数据结构与可靠性",
    "description": "记录 Redis 的缓存机制、基础数据结构、消息与原子操作，以及持久化和缓存问题等学习过程",
    "publishedAt": "2026-08-20",
    "draft": false,
    "html": "<h3 id=\"redis缓存数据结构与可靠性\">Redis：缓存、数据结构与可靠性</h3>\n<h4 id=\"redis-是什么\">Redis 是什么</h4>\n<p>Redis 是一个开源、基于内存的键值型数据库，也可以称为内存数据结构服务。它以 Key-Value 的形式保存数据。</p>\n<p>可以先把它理解成：<strong>用 Key 找 Value 的内存数据库</strong>。因为数据主要放在内存中，读写速度很快，所以经常被用作缓存，减少应用程序对 MySQL 等数据库的访问压力。</p>\n<p>Redis 不只支持简单字符串，还内置了多种数据结构：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">数据结构</th>\n<th align=\"left\">特点</th>\n<th align=\"left\">常见场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">String</td>\n<td align=\"left\">字符串</td>\n<td align=\"left\">Token、验证码、计数器、JSON</td>\n</tr>\n<tr>\n<td align=\"left\">Hash</td>\n<td align=\"left\">哈希</td>\n<td align=\"left\">保存对象的多个字段</td>\n</tr>\n<tr>\n<td align=\"left\">List</td>\n<td align=\"left\">有序且允许重复</td>\n<td align=\"left\">队列、栈、消息列表</td>\n</tr>\n<tr>\n<td align=\"left\">Set</td>\n<td align=\"left\">自动去重</td>\n<td align=\"left\">标签、交并差集</td>\n</tr>\n<tr>\n<td align=\"left\">Sorted Set</td>\n<td align=\"left\">带分数的有序集合</td>\n<td align=\"left\">排行榜、排序</td>\n</tr>\n<tr>\n<td align=\"left\">Stream</td>\n<td align=\"left\">消息流</td>\n<td align=\"left\">消息队列、事件记录</td>\n</tr>\n</tbody></table>\n<h5 id=\"redis-作为缓存\">Redis 作为缓存</h5>\n<p>假设维护了一个商品服务，它背后直连 MySQL：商品服务每秒需要对外提供 <code>1w</code> 次查询，但 MySQL 只能承受每秒约 <code>5k</code> 次查询。这类大流量查询场景很常见，这时候加一层 Redis 就能分担数据库压力。</p>\n<p>查询内存比查询磁盘快。MySQL 的数据主要在磁盘中，如果将热点数据放进内存，很多查询就不需要再走磁盘。当用户查询商品时，程序可以先查询 Redis：</p>\n<pre><code>用户请求\n   ↓\n查询 Redis\n   ↓\n缓存命中：直接返回\n   ↓\n缓存未命中：查询 MySQL\n   ↓\n将查询结果写入 Redis\n</code></pre>\n<p>除此之外，Redis 还可以保存登录状态和 Token，并设置过期时间；也能快速实现访问量、点赞数、库存数量等计数功能。使用 List 还能实现简单任务队列。</p>\n<h5 id=\"为什么速度快\">为什么速度快</h5>\n<p>主要有几个原因：</p>\n<ol>\n<li>数据主要保存在内存中，减少磁盘 I/O。</li>\n<li>Redis 内置了多种高效数据结构。</li>\n<li>命令执行模型较简单，核心命令通常通过事件循环处理。</li>\n<li>使用轻量级通信协议。</li>\n<li>很多常用操作的时间复杂度较低，例如 <code>GET</code>、<code>SET</code> 通常是 <code>O(1)</code>。</li>\n</ol>\n<p>一些常用命令：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">命令</th>\n<th align=\"center\">作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\"><code>PING</code></td>\n<td align=\"center\">检查 Redis 是否正常连接</td>\n</tr>\n<tr>\n<td align=\"left\"><code>SET</code></td>\n<td align=\"center\">保存字符串数据</td>\n</tr>\n<tr>\n<td align=\"left\"><code>GET</code></td>\n<td align=\"center\">获取字符串数据</td>\n</tr>\n<tr>\n<td align=\"left\"><code>EXISTS</code></td>\n<td align=\"center\">判断 Key 是否存在</td>\n</tr>\n<tr>\n<td align=\"left\"><code>TYPE</code></td>\n<td align=\"center\">查看 Value 的数据类型</td>\n</tr>\n<tr>\n<td align=\"left\"><code>DEL</code></td>\n<td align=\"center\">删除 Key</td>\n</tr>\n</tbody></table>\n<h4 id=\"基础数据stringhash-与缓存使用\">基础数据：String、Hash 与缓存使用</h4>\n<p>Redis 的数据通过 Key-Value 形式保存：Key 用于查找数据，Value 用于保存具体内容。例如 <code>user:1001:name</code> 是 Key，<code>张三</code> 是 Value。</p>\n<p>Key 通常使用冒号命名，例如 <code>user:1001:name</code>、<code>article:1001:views</code>、<code>login:code:13800138000</code>。这样能体现数据的业务含义，也方便后续管理。</p>\n<h5 id=\"string保存简单值与计数\">String：保存简单值与计数</h5>\n<p>String 是最基本的数据类型，可以保存普通文本、数字、Token、验证码和 JSON 字符串等内容。通过 <code>SET</code> 保存数据，<code>GET</code> 读取数据：</p>\n<pre><code class=\"language-redis\">SET user:1001:name &quot;张三&quot;\nGET user:1001:name\n</code></pre>\n<p>执行后可以读取到“张三”。如果对同一个 Key 再次执行 SET，新的值会覆盖旧的值：</p>\n<pre><code class=\"language-redis\">SET user:1001:name &quot;李四&quot;\nGET user:1001:name\n</code></pre>\n<p>最终结果为“李四”。如果需要同时读取多个 Key，可以使用 MGET：</p>\n<pre><code class=\"language-redis\">SET user:1001:age 20\nSET user:1001:city &quot;深圳&quot;\nMGET user:1001:name user:1001:age user:1001:city\n</code></pre>\n<p>MGET 可以一次获取多个 Key，减少客户端与 Redis 之间的网络交互次数。\nRedis 还可以对保存为数字的 String 进行自增操作。例如实现文章阅读量统计：</p>\n<pre><code class=\"language-redis\">SET article:1001:views 0\nINCR article:1001:views\nINCRBY article:1001:views 10\nGET article:1001:views\n</code></pre>\n<p>其中 INCR 表示自增 1，INCRBY 表示按照指定数值增加。执行完成后，阅读量会从 0 增加到 11。Redis 的这种计数操作可以应用于文章阅读量、点赞数、接口访问次数等场景。如果 Key 中保存的不是数字，例如保存的是“张三”，则不能使用 INCR 进行增加。</p>\n<h5 id=\"过期时间\">过期时间</h5>\n<p>缓存数据通常不应该永久保存，因此 Redis 支持设置过期时间。例如保存一个 60 秒后失效的验证码：</p>\n<pre><code class=\"language-redis\">SET login:code:13800138000 &quot;9527&quot; EX 60\nTTL login:code:13800138000\n</code></pre>\n<p><code>EX 60</code> 表示过期时间为 60 秒，<code>TTL</code> 用于查看剩余秒数：</p>\n<ul>\n<li>正数：Key 还存在，并且有对应的剩余时间；</li>\n<li><code>-1</code>：Key 存在，但没有设置过期时间；</li>\n<li><code>-2</code>：Key 不存在或已经过期。</li>\n</ul>\n<p>也可以分两步设置过期时间：</p>\n<pre><code class=\"language-redis\">SET product:1001 &quot;手机&quot;\nEXPIRE product:1001 30\nTTL product:1001\n</code></pre>\n<p>这表示商品缓存保存 30 秒。过期时间结束后，Redis 会自动删除这个 Key。</p>\n<h5 id=\"hash保存对象字段\">Hash：保存对象字段</h5>\n<p>String 适合保存一个简单值，而 Hash 适合保存一个对象的多个字段。使用 Hash 保存用户信息时，可以执行：</p>\n<pre><code class=\"language-redis\">HSET user:1001 name &quot;张三&quot;\nHSET user:1001 age 20\nHSET user:1001 city &quot;北京&quot;\n</code></pre>\n<p>这里的 user:1001 是 Redis 的 Key，name、age 和 city 是 Hash 内部的字段。可以使用 HGET 获取某一个字段，可以使用 HMGET 一次获取多个字段，如果想查看整个 Hash，可以使用 HGETALL。\n需要注意，HGETALL 会返回这个 Hash 中的全部字段。如果一个 Hash 中保存了很多字段，一次性获取全部内容可能会产生较大的网络开销，因此实际开发中应该根据需要使用 HGET 或 HMGET。删除 Hash 中的字段可以使用 HDEL。Hash 也支持对数字字段进行增加。例如统计用户积分：</p>\n<pre><code class=\"language-redis\">HSET user:1001 score 100\nHINCRBY user:1001 score 10\nHGET user:1001 score\n</code></pre>\n<p>HINCRBY 适合用于积分、余额、数量等数字字段的增加。\nString 和 Hash 都可以保存对象，但使用方式不同。使用多个 String 保存用户信息时，需要创建多个 Key；而 Hash 一条指令就可以解决：</p>\n<pre><code class=\"language-redis\">HSET user:1001 name &quot;张三&quot; age 20 city &quot;北京&quot;\n</code></pre>\n<p>因此，当一个对象包含多个相关字段时，Hash 通常更直观，也更方便统一管理。不过 Hash 的过期时间是设置在整个 Key 上的，不能直接给某一个普通字段单独设置过期时间。</p>\n<h4 id=\"队列集合与排序\">队列、集合与排序</h4>\n<p>这一部分主要包括 List、Set、Sorted Set 和 HyperLogLog：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">类型</th>\n<th align=\"left\">特点</th>\n<th align=\"left\">适合场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">List</td>\n<td align=\"left\">有序、允许重复</td>\n<td align=\"left\">队列、栈、消息列表、最近访问记录</td>\n</tr>\n<tr>\n<td align=\"left\">Set</td>\n<td align=\"left\">无序、自动去重</td>\n<td align=\"left\">标签、交集、并集、差集</td>\n</tr>\n<tr>\n<td align=\"left\">Sorted Set</td>\n<td align=\"left\">成员不重复，并按分数排序</td>\n<td align=\"left\">排行榜、排序场景</td>\n</tr>\n<tr>\n<td align=\"left\">HyperLogLog</td>\n<td align=\"left\">低内存的基数估算</td>\n<td align=\"left\">UV、关键词数、设备数</td>\n</tr>\n</tbody></table>\n<h5 id=\"list队列与栈\">List：队列与栈</h5>\n<p>List 是一种有序且允许重复元素的数据结构。使用 <code>RPUSH</code> 从右侧添加元素，使用 <code>LPOP</code> 从左侧取出元素：</p>\n<pre><code class=\"language-redis\">DEL task:queue\nRPUSH task:queue &quot;task-1&quot;\nRPUSH task:queue &quot;task-2&quot;\nRPUSH task:queue &quot;task-3&quot;\nLRANGE task:queue 0 -1\nLPOP task:queue\n</code></pre>\n<p>执行 LRANGE task:queue 0 -1 可以查看整个列表，结果为 task-1、task-2、task-3。执行 LPOP 后会取出并删除最左侧的 task-1。使用 RPUSH 添加、LPOP 取出，可以实现先进先出的队列；使用 LPUSH 添加、LPOP 取出，则可以实现后进先出的栈。LLEN 可以查看列表长度，BLPOP 可以在列表为空时等待新任务，适合实现简单的任务消费功能。</p>\n<h5 id=\"set自动去重与集合运算\">Set：自动去重与集合运算</h5>\n<p>Set 是一种无序且不允许重复元素的数据结构。使用 <code>SADD</code> 添加元素：</p>\n<pre><code class=\"language-redis\">DEL user:1001:tags\nSADD user:1001:tags &quot;redis&quot;\nSADD user:1001:tags &quot;mysql&quot;\nSADD user:1001:tags &quot;redis&quot;\nSMEMBERS user:1001:tags\n</code></pre>\n<p>虽然 redis 被添加了两次，但 Set 中只会保存一份，因为 Set 会自动去重。使用 SCARD 可以查看集合元素数量，使用 SREM 可以删除指定元素。Set 的一个重要特点是支持集合运算。例如有两个用户的兴趣标签：</p>\n<pre><code class=\"language-redis\">SADD user:1001:tags &quot;redis&quot; &quot;mysql&quot; &quot;rust&quot;\nSADD user:1002:tags &quot;redis&quot; &quot;java&quot; &quot;python&quot;\n</code></pre>\n<p>使用 SINTER 查询两个集合的交集，使用 SUNION 查询并集。使用 SDIFF 查询差集。</p>\n<h5 id=\"sorted-set带分数的排序集合\">Sorted Set：带分数的排序集合</h5>\n<p>Sorted Set，也叫 ZSet，是一种有序集合。它和 Set 一样不允许成员重复，但每个成员都会关联一个分数，Redis 会按照分数进行排序。可以使用 <code>ZADD</code> 添加排行榜数据：</p>\n<pre><code class=\"language-redis\">DEL game:ranking\nZADD game:ranking 98 &quot;张三&quot;\nZADD game:ranking 95 &quot;李四&quot;\nZADD game:ranking 100 &quot;王五&quot;\n</code></pre>\n<p>ZREVRANGE 按照分数从高到低返回结果，WITHSCORES 表示同时返回分数。结果类似：</p>\n<pre><code class=\"language-redis\">ZREVRANGE game:ranking 0 -1 WITHSCORES\n王五\n100\n张三\n98\n李四\n95\n</code></pre>\n<p><code>ZRANGE</code> 可以按照从低到高排序，可以使用 <code>ZSCORE</code> 查询某个成员的分数，也可以使用 <code>ZINCRBY</code> 增加成员分数。</p>\n<h5 id=\"hyperloglog只关心数量时\">HyperLogLog：只关心数量时</h5>\n<p>HyperLogLog 是 Redis 提供的一种基数统计结构，主要用于统计大量数据中不同元素的数量。例如统计网站一天的独立访客数量：</p>\n<pre><code class=\"language-redis\">PFADD website:uv &quot;user-1001&quot;\nPFADD website:uv &quot;user-1002&quot;\nPFADD website:uv &quot;user-1001&quot;\nPFCOUNT website:uv\n</code></pre>\n<p>由于 user-1001 被重复添加，PFCOUNT 统计出的独立用户数量是 2，而不是 3。HyperLogLog 不会保存具体的用户列表，只会对不同元素的数量进行估算，因此占用内存很小，适合统计网站 UV、搜索关键词数量和不同设备数量等场景。\nHyperLogLog 的优点是节省内存，缺点是统计结果是估算值，并且不能查看具体有哪些元素。如果业务需要准确保存成员信息，就应该使用 Set；如果只关心不同元素的数量，可以使用 HyperLogLog。\n简单总结一下：List 适合队列和栈；Set 适合交集、并集、差集和共同数据计算；Sorted Set 适合排行榜和排序场景；HyperLogLog 适合对大量数据进行低内存的基数统计。</p>\n<h4 id=\"redis-消息与原子操作\">Redis 消息与原子操作</h4>\n<p>这一部分主要包括发布订阅、Stream、事务、Pipeline 和 Lua 脚本，分别用于消息传递、任务处理、批量操作，以及保证多个命令按照整体逻辑执行。</p>\n<h5 id=\"发布订阅与-stream\">发布订阅与 Stream</h5>\n<p>Redis 发布订阅功能由发布者、频道和订阅者组成。订阅者先订阅一个频道：</p>\n<pre><code class=\"language-redis\">SUBSCRIBE news\n</code></pre>\n<p>执行后，当前客户端会进入等待消息状态。此时需要打开另一个 PowerShell 窗口，进入 Redis 后执行：</p>\n<pre><code class=\"language-redis\">PUBLISH news &quot;test&quot;\n</code></pre>\n<p>订阅者窗口会收到消息。PUBLISH 的返回值表示当前有多少个订阅者收到了消息。发布订阅适合实现实时通知、聊天室消息、配置更新通知等功能。但是它不会保存历史消息，如果订阅者发布消息时没有在线，就无法收到之前的消息，因此不适合要求消息可靠保存的任务。\nStream 是 Redis 提供的消息流数据结构，适合保存消息并支持后续读取。可以使用 XADD 添加消息：</p>\n<pre><code class=\"language-redis\">XADD order:stream * user_id 1001 amount 2999\nXADD order:stream * user_id 1002 amount 1999\n</code></pre>\n<p>其中 <code>*</code> 表示让 Redis 自动生成消息 ID。查看 Stream 中的消息：</p>\n<pre><code class=\"language-redis\">XRANGE order:stream - +\n</code></pre>\n<p><code>-</code> 表示从最早的消息开始，<code>+</code> 表示读取到最后一条消息。Stream 会保存消息内容，因此与发布订阅相比更加可靠，适合订单事件、日志记录和消息队列等场景。Stream 还支持消费者组，可以让多个消费者共同处理消息，适合实际项目中的异步任务处理。</p>\n<h5 id=\"事务pipeline-与-lua\">事务、Pipeline 与 Lua</h5>\n<p>Redis 事务可以将多个命令放入一个事务中执行。使用 <code>MULTI</code> 开始事务，后续命令会先进入队列，使用 <code>EXEC</code> 后才会真正执行：</p>\n<pre><code class=\"language-redis\">MULTI\nSET account:1001:balance 1000\nINCR account:1001:balance\nGET account:1001:balance\nEXEC\n</code></pre>\n<p>在 <code>MULTI</code> 和 <code>EXEC</code> 之间执行的命令不会立即返回最终结果，而是先返回 <code>QUEUED</code>。执行 <code>EXEC</code> 后，Redis 会按照命令加入的顺序依次执行；使用 <code>DISCARD</code> 可以取消还没有执行的事务。</p>\n<p>需要注意，Redis 事务不等同于 MySQL 事务：命令一旦开始执行，通常不会因为其中一个命令失败而自动回滚。因此，使用事务时需要提前保证命令和参数正确。</p>\n<p>Redis 还支持使用 <code>WATCH</code> 实现乐观锁。例如先监视一个库存 Key：</p>\n<pre><code class=\"language-redis\">WATCH product:1001:stock\nGET product:1001:stock\n</code></pre>\n<p>如果其他客户端在当前事务执行前修改了这个 Key，事务可能执行失败。这样可以避免多个客户端同时修改同一个数据时产生覆盖问题。\nPipeline 的作用是一次向 Redis 发送多个命令，减少客户端和 Redis 之间的网络往返次数。它主要解决网络效率问题，而不是事务问题。</p>\n<p>Pipeline 可以减少网络通信次数，但其中的命令不一定具备原子性；如果需要保证多个命令作为一个整体执行，应使用事务或 Lua 脚本。Lua 脚本可以将多个 Redis 命令放在一个脚本中执行，并且脚本执行期间不会被其他 Redis 命令插入。</p>\n<pre><code class=\"language-redis\">SET product:1001:stock 10\nEVAL &quot;local stock = tonumber(redis.call(&#39;GET&#39;, KEYS[1])); if stock and stock &gt; 0 then return redis.call(&#39;DECR&#39;, KEYS[1]); else return -1 end&quot; 1 product:1001:stock\n</code></pre>\n<p>这个脚本表示：只有库存大于 0 时才扣减库存；库存不足时返回 <code>-1</code>。\nLua 脚本适合处理判断并修改这类必须连续完成的操作，例如库存扣减、限流、优惠券领取和分布式锁续期。相比先执行 GET 再执行 DECR，Lua 可以避免多个客户端之间的并发问题。</p>\n<h4 id=\"redis-数据可靠性与缓存\">Redis 数据可靠性与缓存</h4>\n<p>Redis 的数据主要保存在内存中，读写速度快；但服务重启或服务器故障可能导致数据丢失。因此 Redis 提供了持久化机制，将内存中的数据保存到磁盘。</p>\n<h5 id=\"rdb-与-aof\">RDB 与 AOF</h5>\n<p><strong>RDB</strong> 是 Redis 默认支持的一种持久化方式：在某个时间点生成当前数据集的快照，保存为 RDB 文件。</p>\n<ul>\n<li><code>SAVE</code>：直接在主进程中生成快照，执行期间可能阻塞其他请求，因此一般不建议在生产环境频繁使用。</li>\n<li><code>BGSAVE</code>：创建后台任务生成 RDB 文件，Redis 主线程仍可继续处理客户端请求，更适合实际运行中的服务。</li>\n</ul>\n<p>RDB 文件较紧凑、恢复速度较快，适合备份和快速重启；但它保存的是某个时间点的快照。如果两次快照之间 Redis 发生故障，最近一段时间的数据可能丢失。</p>\n<p><strong>AOF</strong> 则会记录 Redis 执行过的写命令。Redis 重启时，可以重新执行 AOF 文件中的命令来恢复数据。它通常比 RDB 更能减少数据丢失，但文件可能更大，恢复时也需要重新执行命令。</p>\n<p>AOF 支持不同的刷盘策略，常见策略是每秒刷盘一次，在性能和数据安全之间取得平衡。实际使用中，RDB 更适合快照备份，AOF 更适合减少数据丢失，两者可以同时启用。</p>\n<h5 id=\"过期淘汰与缓存穿透\">过期、淘汰与缓存穿透</h5>\n<p>除了持久化，Redis 还会通过过期时间管理缓存数据。例如：</p>\n<pre><code class=\"language-redis\">SET product:1001 &quot;A&quot; EX 60\nTTL product:1001\n</code></pre>\n<p>当 TTL 变为 0 后，Key 会被 Redis 删除。Redis 的过期删除并不一定在时间刚到时立即删除，而是结合被动删除和定期检查机制完成。当客户端访问一个已经过期的 Key 时，Redis 会发现它过期并将其删除；同时 Redis 也会定期检查部分设置了过期时间的 Key。\n如果 Redis 内存达到上限，还需要通过淘汰策略删除部分 Key。可以查看当前最大内存和淘汰策略：</p>\n<pre><code class=\"language-redis\">CONFIG GET maxmemory\nCONFIG GET maxmemory-policy\n</code></pre>\n<p>常见淘汰策略包括按照最近最少使用淘汰、按照访问频率淘汰、随机淘汰，以及只淘汰设置了过期时间的 Key。实际项目中，如果 Redis 主要用于缓存，通常会设置最大内存和合理的淘汰策略，避免 Redis 因内存不足无法继续写入。\n缓存穿透是指请求查询一个 Redis 和数据库中都不存在的数据。由于缓存中没有数据，每次请求都会访问数据库，大量无效请求可能造成数据库压力。常见解决办法是缓存空值：</p>\n<pre><code class=\"language-redis\">SET product:1 &quot;__NULL__&quot; EX 60\n</code></pre>\n<p>下次再查询 product:1 时，如果读取到 <strong>NULL</strong>，就可以直接判断数据不存在，而不必再次查询数据库。</p>\n<h4 id=\"缓存击穿缓存雪崩主从复制与哨兵模式\">缓存击穿、缓存雪崩、主从复制与哨兵模式</h4>\n<p>缓存击穿和缓存雪崩，都是缓存失效后大量请求直接访问数据库、从而造成压力的问题。</p>\n<h5 id=\"缓存击穿一个热点-key-同时失效\">缓存击穿：一个热点 Key 同时失效</h5>\n<p>缓存击穿是指某一个访问量非常高的热点 Key 在过期瞬间，大量请求同时发现缓存不存在，然后同时查询数据库并重新写入缓存。例如某个热门商品的缓存即将过期，很多用户同时访问这个商品，所有请求都会执行以下流程：</p>\n<pre><code>查询 Redis，发现缓存不存在\n        ↓\n大量请求同时查询 MySQL\n        ↓\n大量请求同时重新写入 Redis\n</code></pre>\n<p>这种问题的特点是只有一个或少数几个热点 Key 失效，但访问量非常大。解决缓存击穿的常见方式是使用互斥锁，只允许一个请求负责查询数据库和重建缓存，其他请求等待或暂时返回。可以使用 Redis 的 SET NX EX 实现简单的互斥锁。\n除了互斥锁，还可以使用逻辑过期解决缓存击穿。逻辑过期是指缓存数据本身不立即删除，而是在数据中保存一个过期时间。请求发现数据逻辑上已经过期后，可以先返回旧数据，再由后台线程异步更新缓存，这样可以减少用户请求等待数据库的时间。</p>\n<h5 id=\"缓存雪崩大量-key-集中失效\">缓存雪崩：大量 Key 集中失效</h5>\n<p>缓存雪崩是指大量缓存 Key 在同一时间集中失效，导致大量请求同时访问数据库。例如系统在同一时间缓存了大量商品，并且都设置了 300 秒过期：</p>\n<pre><code class=\"language-redis\">SET product:1001 &quot;data-1&quot; EX 300\nSET product:1002 &quot;data-2&quot; EX 300\nSET product:1003 &quot;data-3&quot; EX 300\n</code></pre>\n<p>如果这些 Key 的创建时间接近，那么它们也可能在接近的时间一起失效，造成数据库瞬间出现大量请求。缓存雪崩通常比缓存击穿影响范围更大，因为它可能涉及大量 Key。解决缓存雪崩的常见方式是给过期时间增加随机值，例如基础过期时间为 300 秒，再随机增加 0 到 60 秒。</p>\n<h5 id=\"主从复制与-sentinel\">主从复制与 Sentinel</h5>\n<p>Redis 主从复制是指将一个 Redis 实例配置为主节点，将其他 Redis 实例配置为副本节点。主节点负责处理写请求，副本节点从主节点同步数据，也可以承担部分读请求。</p>\n<p>副本节点可以通过 <code>REPLICAOF</code> 连接到主节点。主从复制能提供数据副本、分担读取压力，并在主节点故障时为故障转移提供候选节点。</p>\n<p>需要注意，Redis 主从复制默认是异步复制：主节点成功执行写入后，副本节点可能还没有完全同步，因此主节点故障时仍可能丢失少量数据。主从复制本身不会自动选举新的主节点，也不会自动修改客户端连接地址；高可用和自动故障转移需要额外使用 Sentinel（哨兵）。</p>\n<p>哨兵模式，也就是 Redis Sentinel，是建立在主从复制之上的高可用方案，主要负责监控、通知、自动故障转移和服务发现。一个典型的哨兵架构如下：</p>\n<pre><code>        Sentinel 1\n       /          \\\nSentinel 2      Sentinel 3\n       \\          /\n        Redis 主节点\n        /        \\\n   副本节点1   副本节点2\n</code></pre>\n<p>哨兵会持续检查主节点和副本节点是否正常。如果主节点发生故障，多个哨兵会共同判断主节点是否真的不可用，然后选出一个副本节点提升为新的主节点，并让其他副本节点重新复制新的主节点。应用程序通过哨兵查询当前主节点地址，而不是把主节点地址永久写死。</p>\n<h3 id=\"资料来源\">资料来源</h3>\n<ul>\n<li><a href=\"https://redis.io/docs/latest/commands/redis-8-10-commands/\">Redis Commands（官方文档）</a></li>\n<li><a href=\"https://www.runoob.com/redis/redis-geo.html\">Redis GEO 命令说明</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "redis缓存数据结构与可靠性",
        "text": "Redis：缓存、数据结构与可靠性"
      },
      {
        "depth": 4,
        "slug": "redis-是什么",
        "text": "Redis 是什么"
      },
      {
        "depth": 5,
        "slug": "redis-作为缓存",
        "text": "Redis 作为缓存"
      },
      {
        "depth": 5,
        "slug": "为什么速度快",
        "text": "为什么速度快"
      },
      {
        "depth": 4,
        "slug": "基础数据stringhash-与缓存使用",
        "text": "基础数据：String、Hash 与缓存使用"
      },
      {
        "depth": 5,
        "slug": "string保存简单值与计数",
        "text": "String：保存简单值与计数"
      },
      {
        "depth": 5,
        "slug": "过期时间",
        "text": "过期时间"
      },
      {
        "depth": 5,
        "slug": "hash保存对象字段",
        "text": "Hash：保存对象字段"
      },
      {
        "depth": 4,
        "slug": "队列集合与排序",
        "text": "队列、集合与排序"
      },
      {
        "depth": 5,
        "slug": "list队列与栈",
        "text": "List：队列与栈"
      },
      {
        "depth": 5,
        "slug": "set自动去重与集合运算",
        "text": "Set：自动去重与集合运算"
      },
      {
        "depth": 5,
        "slug": "sorted-set带分数的排序集合",
        "text": "Sorted Set：带分数的排序集合"
      },
      {
        "depth": 5,
        "slug": "hyperloglog只关心数量时",
        "text": "HyperLogLog：只关心数量时"
      },
      {
        "depth": 4,
        "slug": "redis-消息与原子操作",
        "text": "Redis 消息与原子操作"
      },
      {
        "depth": 5,
        "slug": "发布订阅与-stream",
        "text": "发布订阅与 Stream"
      },
      {
        "depth": 5,
        "slug": "事务pipeline-与-lua",
        "text": "事务、Pipeline 与 Lua"
      },
      {
        "depth": 4,
        "slug": "redis-数据可靠性与缓存",
        "text": "Redis 数据可靠性与缓存"
      },
      {
        "depth": 5,
        "slug": "rdb-与-aof",
        "text": "RDB 与 AOF"
      },
      {
        "depth": 5,
        "slug": "过期淘汰与缓存穿透",
        "text": "过期、淘汰与缓存穿透"
      },
      {
        "depth": 4,
        "slug": "缓存击穿缓存雪崩主从复制与哨兵模式",
        "text": "缓存击穿、缓存雪崩、主从复制与哨兵模式"
      },
      {
        "depth": 5,
        "slug": "缓存击穿一个热点-key-同时失效",
        "text": "缓存击穿：一个热点 Key 同时失效"
      },
      {
        "depth": 5,
        "slug": "缓存雪崩大量-key-集中失效",
        "text": "缓存雪崩：大量 Key 集中失效"
      },
      {
        "depth": 5,
        "slug": "主从复制与-sentinel",
        "text": "主从复制与 Sentinel"
      },
      {
        "depth": 3,
        "slug": "资料来源",
        "text": "资料来源"
      }
    ]
  },
  {
    "id": "2026-08-19",
    "title": "8/19 MySQL：实操记录",
    "description": "记录学习 MySQL 的实操和常用命令。",
    "publishedAt": "2026-08-19",
    "draft": false,
    "html": "<h3 id=\"mysql实操\">MySQL实操</h3>\n<h4 id=\"创建数据库\">创建数据库</h4>\n<p>建立一个简单的商城数据库进行练习。首先在装好环境的情况下登录到MySQL：</p>\n<pre><code class=\"language-pwsh\">mysql -u root -p\n</code></pre>\n<p>创建一个名为 shop_demo 的数据库：</p>\n<pre><code class=\"language-SQL\">CREATE DATABASE IF NOT EXISTS shop_demo\nCHARACTER SET utf8mb4\nCOLLATE utf8mb4_unicode_ci;\n</code></pre>\n<p>这里使用 utf8mb4，可以较好地支持中文和特殊字符。\n可以使用以下和三个指令选择数据库和查看当前使用的数据库和删除数据库。</p>\n<pre><code class=\"language-SQL\">USE shop_demo;\nSELECT DATABASE();\nDROP DATABASE shop_demo;（删除数据库会删除其中的所有表和数据）\n</code></pre>\n<p>接下来进行数据表的创建：</p>\n<pre><code class=\"language-SQL\">CREATE TABLE users (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    username VARCHAR(50) NOT NULL UNIQUE,\n    email VARCHAR(100) UNIQUE,\n    created_at DATETIME DEFAULT CURRENT_TIMESTAMP\n);\n</code></pre>\n<p>解析：\nid: 用户 id，整数类型，作为主键，自增长。\nusername: 用户名，变长字符串，不允许为空，不能重复。\nemail: 用户邮箱，变长字符串，不能重复。\ncreated_at: 用户注册时间字段，日期和时间类型，如果插入数据时不指定时间，就自动使用当前时间。</p>\n<h4 id=\"创建数据表\">创建数据表</h4>\n<p>接下来创建另外几个商品表、订单表、订单明显表：</p>\n<pre><code class=\"language-SQL\">CREATE TABLE products (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    product_name VARCHAR(100) NOT NULL,\n    price DECIMAL(10,2) NOT NULL,\n    stock INT NOT NULL DEFAULT 0,\n    created_at DATETIME DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE TABLE orders (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    user_id INT NOT NULL,\n    total_amount DECIMAL(10,2) NOT NULL DEFAULT 0,\n    status VARCHAR(20) NOT NULL DEFAULT &#39;待支付&#39;,\n    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,\n\n    FOREIGN KEY (user_id) REFERENCES users(id)\n);\nCREATE TABLE order_items (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    order_id INT NOT NULL,\n    product_id INT NOT NULL,\n    quantity INT NOT NULL,\n    price DECIMAL(10,2) NOT NULL,\n\n    FOREIGN KEY (order_id) REFERENCES orders(id),\n    FOREIGN KEY (product_id) REFERENCES products(id)\n);\n</code></pre>\n<p>其中需要特别注意的是order_items 表中的 order_id，必须对应 orders 表中已经存在的 id。\n创建完之后可以通过命令来查看数据库的表和表的结构：</p>\n<pre><code class=\"language-SQL\">SHOW TABLES;\nDESC users;\n</code></pre>\n<p><figure><img src=\"image-1.png\" alt=\"alt text\" title=\"表和表结构\"><figcaption>表和表结构</figcaption></figure></p>\n<pre><code class=\"language-SQL\">DROP TABLE users;\nDROP TABLE [IF EXISTS] users;\n</code></pre>\n<p>删除数据表指令，IF EXISTS 是一个可选的子句，表示如果表存在才执行删除操作，避免因为表不存在而引发错误。</p>\n<h4 id=\"插入测试数据\">插入测试数据</h4>\n<pre><code class=\"language-SQL\">INSERT INTO users (username, email)\nVALUES\n(&#39;张三&#39;, &#39;zhangsan@example.com&#39;),\n(&#39;李四&#39;, &#39;lisi@example.com&#39;),\n(&#39;王五&#39;, &#39;wangwu@example.com&#39;);\n\nINSERT INTO products (product_name, price, stock)\nVALUES\n(&#39;机械键盘&#39;, 299.00, 20),\n(&#39;无线鼠标&#39;, 99.00, 50),\n(&#39;显示器&#39;, 1299.00, 10);\n</code></pre>\n<p>分别是插入用户和插入商品，插入数据后，可以通过以下语句查看：</p>\n<pre><code class=\"language-SQL\">SELECT * FROM users;\nSELECT * FROM products;\n</code></pre>\n<h4 id=\"基本查询\">基本查询</h4>\n<pre><code class=\"language-SQL\">SELECT *\nFROM products\nWHERE price &gt; 100;\n\nSELECT *\nFROM products\nWHERE price BETWEEN 100 AND 1000;\n\nSELECT *\nFROM products\nWHERE product_name LIKE &#39;%鼠标%&#39;;\n\nSELECT *\nFROM products\nORDER BY price DESC;\n\nSELECT COUNT(*) AS product_count\nFROM products;\n</code></pre>\n<p>这几个查询分别表示：查询价格大于 100 元的商品、查询价格在 100 到 1000 元之间的商品、查询商品名称中包含“鼠标”的商品、按照价格从高到低排序、统计商品数量。\n其中各子句的含义：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">关键字</th>\n<th align=\"center\">作用</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\"><code>FROM</code></td>\n<td align=\"center\">指定数据来源表</td>\n</tr>\n<tr>\n<td align=\"left\"><code>WHERE</code></td>\n<td align=\"center\">筛选符合条件的数据</td>\n</tr>\n<tr>\n<td align=\"left\"><code>BETWEEN</code></td>\n<td align=\"center\">查询某个范围内的数据</td>\n</tr>\n<tr>\n<td align=\"left\"><code>LIKE</code></td>\n<td align=\"center\">进行模糊查询</td>\n</tr>\n<tr>\n<td align=\"left\"><code>ORDER BY</code></td>\n<td align=\"center\">对查询结果进行排序</td>\n</tr>\n<tr>\n<td align=\"left\"><code>COUNT(*)</code></td>\n<td align=\"center\">统计数据行数</td>\n</tr>\n<tr>\n<td align=\"left\"><code>AS</code></td>\n<td align=\"center\">给字段或查询结果设置别名</td>\n</tr>\n</tbody></table>\n<h4 id=\"修改和删除数据\">修改和删除数据</h4>\n<pre><code class=\"language-SQL\">UPDATE products\nSET price = 259.00\nWHERE product_name = &#39;机械键盘&#39;;\n\nUPDATE products\nSET stock = stock + 10\nWHERE id = 1;\n\nDELETE FROM users\nWHERE username = &#39;王五&#39;;\n</code></pre>\n<p>上面分别表示了修改机械键盘的价格、增加商品库存、删除某个用户。需要特别注意的是 UPDATE 和 DELETE 是一个可选的子句，用于指定删除的行。如果省略 WHERE 子句，将更新/删除表中的所有行。</p>\n<h4 id=\"多表查询\">多表查询</h4>\n<p>要想将这几个表链接在一起首先插入一条订单和订单明细：</p>\n<pre><code class=\"language-SQL\">INSERT INTO orders (user_id, total_amount, status)\nVALUES (1, 299.00, &#39;待支付&#39;);\n\nINSERT INTO order_items\n(order_id, product_id, quantity, price)\nVALUES\n(1, 1, 1, 259.00);\n</code></pre>\n<p>然后执行:</p>\n<pre><code class=\"language-SQL\">SELECT\n    u.username AS `用户名`,\n    p.product_name AS `商品名称`,\n    oi.quantity AS `购买数量`\nFROM orders AS o\nJOIN users AS u\n    ON o.user_id = u.id\nJOIN order_items AS oi\n    ON o.id = oi.order_id\nJOIN products AS p\n    ON oi.product_id = p.id;\n</code></pre>\n<p>意思是查询用户名、商品名称和购买数量。分别将四张表链接在了一起。此外如果将 JOIN改成LEFT JOIN（左连接）将会变成获取左表所有记录，即使右表没有对应匹配的记录。同理RIGHT JOIN（右连接）与 LEFT JOIN 相反，用于获取右表所有记录，即使左表没有对应匹配的记录。\n<img src=\"image-2.png\" alt=\"alt text\"></p>\n<h4 id=\"事务\">事务</h4>\n<p>事务可以理解为一组必须“全部成功或全部失败”的 SQL 操作。事务是必须满足4个条件（ACID）：：原子性（Atomicity，或称不可分割性）、一致性（Consistency）、隔离性（Isolation，又称独立性）、持久性（Durability）。\n原子性：一个事务（transaction）中的所有操作，要么全部完成，要么全部不完成，不会结束在中间某个环节。事务在执行过程中发生错误，会被回滚（Rollback）到事务开始前的状态，就像这个事务从来没有执行过一样。\n一致性：在事务开始之前和事务结束以后，数据库的完整性没有被破坏。这表示写入的资料必须完全符合所有的预设规则，这包含资料的精确度、串联性以及后续数据库可以自发性地完成预定的工作。\n隔离性：数据库允许多个并发事务同时对其数据进行读写和修改的能力，隔离性可以防止多个事务并发执行时由于交叉执行而导致数据的不一致。事务隔离分为不同级别，包括读未提交（Read uncommitted）、读提交（read committed）、可重复读（repeatable read）和串行化（Serializable）。\n持久性：事务处理结束后，对数据的修改就是永久的，即便系统故障也不会丢失。</p>\n<p>例如用户下单时，通常需要完成以下操作：</p>\n<ol>\n<li>创建订单</li>\n<li>添加订单明细</li>\n<li>扣减商品库存</li>\n<li>修改订单状态\n如果库存扣减失败，前面创建的订单也应该撤销，这时就需要使用事务：</li>\n</ol>\n<pre><code class=\"language-SQL\">START TRANSACTION;\nINSERT INTO orders\n(user_id, total_amount, status)\nVALUES\n(1, 497.00, &#39;已支付&#39;);\n\nSET @order_id = LAST_INSERT_ID();\n\nINSERT INTO order_items\n(order_id, product_id, quantity, price)\nVALUES\n(@order_id, 1, 1, 259.00),\n(@order_id, 2, 2, 99.00);\n\nUPDATE products\nSET stock = stock - 1\nWHERE id = 1\n  AND stock &gt;= 1;\n\nUPDATE products\nSET stock = stock - 2\nWHERE id = 2\n  AND stock &gt;= 2;\n\nCOMMIT;\n</code></pre>\n<p>其中最后的COMMIT表示保存所有的操作，如果发现库存不足，或者某一步执行失败，不要执行 COMMIT，而是执行：ROLLBACK;这样本次事务中的操作都会撤销。\n事务中常用的命令：</p>\n<pre><code class=\"language-SQL\">BEGIN;                 -- 开始事务\nCOMMIT;                -- 提交事务\nROLLBACK;              -- 回滚事务\nSAVEPOINT point1;      -- 设置保存点\nROLLBACK TO point1;    -- 回滚到保存点\n</code></pre>\n<h4 id=\"修改表结构\">修改表结构</h4>\n<p>当我们需要修改数据表名或者修改数据表字段时，就需要使用到 MySQL ALTER 命令。MySQL 的 ALTER 命令用于修改数据库、表和索引等对象的结构。ALTER 命令允许你添加、修改或删除数据库对象，并且可以用于更改表的列定义、添加约束、创建和删除索引等操作。</p>\n<pre><code class=\"language-SQL\">ALTER TABLE users\nADD phone VARCHAR(20);\n\nALTER TABLE users\nMODIFY phone VARCHAR(30);\n\nALTER TABLE users\nDROP COLUMN phone;\n\nALTER TABLE users\nRENAME TO customers;\n</code></pre>\n<p>给用户表添加手机号，修改字段类型，删除字段，修改表明等操作，还可以修改主键。</p>\n<h4 id=\"索引\">索引</h4>\n<p>MySQL 索引是一种数据结构，用于加快数据库查询的速度和性能。MySQL 索引的建立对于 MySQL 的高效运行是很重要的，索引可以大大提高 MySQL 的检索速度。创建索引时，你需要确保该索引是应用在 SQL 查询语句的条件(一般作为 WHERE 子句的条件)。实际上，索引也是一张表，该表保存了主键与索引字段，并指向实体表的记录。索引虽然能够提高查询性能，但也需要注意以下几点：索引需要占用额外的存储空间，过多或不合理的索引可能会导致性能下降，因此需要谨慎选择和规划索引，对表进行插入、更新和删除操作时，索引需要维护，可能会影响性能。\n使用 CREATE INDEX 语句可以创建普通索引。普通索引是最常见的索引类型，用于加速对表中数据的查询。\nCREATE INDEX 的语法：</p>\n<pre><code class=\"language-SQL\">CREATE INDEX idx_username\nON users(username);\n\nSHOW INDEX FROM users; \n\nDROP INDEX idx_username\nON users;\n</code></pre>\n<p>分别是给 username 创建索引，查看索引，和删除索引。\n还可以再创建表的时候使用 INDEX 指令直接指定索引，也可以使用ALTER 命令添加和删除索引。</p>\n<h4 id=\"临时数据复制表\">临时数据、复制表</h4>\n<p>MySQL 临时表在我们需要保存一些临时数据时是非常有用的。临时表只在当前连接可见，当关闭连接时，MySQL 会自动删除表并释放所有空间。 </p>\n<pre><code class=\"language-SQL\">CREATE TEMPORARY TABLE temp_products (\n    id INT,\n    product_name VARCHAR(100),\n    price DECIMAL(10,2)\n);\n\nINSERT INTO temp_products\nVALUES\n(1, &#39;机械键盘&#39;, 259.00),\n(2, &#39;无线鼠标&#39;, 99.00);\n\nSELECT *\nFROM temp_products;\n</code></pre>\n<p>如果我们需要完全的复制 MySQL 的数据表，包括表的结构，索引，默认值等。使用 SHOW CREATE TABLE 命令获取创建数据表(CREATE TABLE) 语句，该语句包含了原数据表的结构，索引等。</p>\n<pre><code class=\"language-SQL\">CREATE TABLE products_backup AS\nSELECT *\nFROM products;\n\nINSERT INTO products_backup\nSELECT *\nFROM products;\n</code></pre>\n<p>复制表结构和数据，然后再复制数据。</p>\n<h4 id=\"自动编号和重复数据处理\">自动编号和重复数据处理</h4>\n<p>MySQL 中常用 AUTO_INCREMENT 实现自动编号：</p>\n<pre><code class=\"language-SQL\">CREATE TABLE test_users (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    username VARCHAR(50)\n);\n</code></pre>\n<p>插入数据时不需要手动设置 id：</p>\n<pre><code class=\"language-SQL\">INSERT INTO test_users (username)\nVALUES\n(&#39;张三&#39;),\n(&#39;李四&#39;);\n</code></pre>\n<p>有些 MySQL 数据表中可能存在重复的记录，有些情况我们允许重复数据的存在，但有时候我们也需要删除这些重复的数据。\n查询不重复的用户名：</p>\n<pre><code class=\"language-SQL\">SELECT DISTINCT username\nFROM users;\n</code></pre>\n<p>DISTINCT 可以去除查询结果中的重复数据。\n如果想查询重复邮箱：</p>\n<pre><code class=\"language-SQL\">SELECT\n    email,\n    COUNT(*) AS email_count\nFROM users\nGROUP BY email\nHAVING email_count &gt; 1;\n</code></pre>\n<p>也可以在在表结构层面，也可以使用 UNIQUE 防止重复。像是之前创建用户表时候，email VARCHAR(100) UNIQUE,表示不能重复。</p>\n<h4 id=\"数据库备份和安全\">数据库备份和安全</h4>\n<p>数据库导出在操作系统终端中执行：</p>\n<pre><code>mysqldump -u root -p shop_demo &gt; shop_demo.sql\n</code></pre>\n<p>输入秘密后就会把 shop_demo 数据库导出成一个 SQL 文件。\n这样就可以把备份文件中的数据恢复到新数据库中。数据库备份很重要，可以用于： 防止数据丢失、更换电脑时迁移数据、搭建测试环境、恢复误删除的数据。\nSQL 注入是指用户输入的内容影响了原本的 SQL 结构。实际开发中，不能直接拼接用户输入，例如：</p>\n<pre><code>SELECT * FROM users WHERE username = &#39;用户输入&#39;;\n</code></pre>\n<p>应该使用参数化查询或预处理语句：</p>\n<pre><code class=\"language-SQL\">SELECT *\nFROM users\nWHERE username = ?;\n</code></pre>\n<p>同时还应该：不让应用程序使用 root 、用户只授予应用程序必要的权限、验证用户输入、不直接向用户显示数据库错误信息</p>\n<h4 id=\"函数和运算符\">函数和运算符</h4>\n<p>常用函数\n统计商品数量：</p>\n<pre><code class=\"language-SQL\">SELECT COUNT(*) AS 商品数量\nFROM products;\n</code></pre>\n<p>查询平均价格：</p>\n<pre><code class=\"language-SQL\">SELECT AVG(price) AS 平均价格\nFROM products;\n</code></pre>\n<p>查询最高价和最低价：</p>\n<pre><code class=\"language-SQL\">SELECT\n    MAX(price) AS 最高价格,\n    MIN(price) AS 最低价格\nFROM products;\n</code></pre>\n<p>处理字符串：</p>\n<pre><code class=\"language-SQL\">SELECT CONCAT(username, &#39;-&#39;, email)\nFROM users;\n</code></pre>\n<p>获取当前时间：</p>\n<pre><code class=\"language-SQL\">SELECT NOW();\n</code></pre>\n<p>常用运算符\n查询价格大于 100 元并且有库存的商品：</p>\n<pre><code class=\"language-SQL\">SELECT *\nFROM products\nWHERE price &gt; 100\n  AND stock &gt; 0;\n</code></pre>\n<p>查询价格范围：</p>\n<pre><code class=\"language-SQL\">SELECT *\nFROM products\nWHERE price BETWEEN 100 AND 1000;\n</code></pre>\n<p>查询指定商品：</p>\n<pre><code class=\"language-SQL\">SELECT *\nFROM products\nWHERE product_name IN (&#39;机械键盘&#39;, &#39;无线鼠标&#39;);\n</code></pre>\n<p>这些运算法可以组合起来使用。</p>\n<h3 id=\"参考资料\">参考资料</h3>\n<ul>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/what-is-mysql.html\">MySQL 教程 | 菜鸟教程</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "mysql实操",
        "text": "MySQL实操"
      },
      {
        "depth": 4,
        "slug": "创建数据库",
        "text": "创建数据库"
      },
      {
        "depth": 4,
        "slug": "创建数据表",
        "text": "创建数据表"
      },
      {
        "depth": 4,
        "slug": "插入测试数据",
        "text": "插入测试数据"
      },
      {
        "depth": 4,
        "slug": "基本查询",
        "text": "基本查询"
      },
      {
        "depth": 4,
        "slug": "修改和删除数据",
        "text": "修改和删除数据"
      },
      {
        "depth": 4,
        "slug": "多表查询",
        "text": "多表查询"
      },
      {
        "depth": 4,
        "slug": "事务",
        "text": "事务"
      },
      {
        "depth": 4,
        "slug": "修改表结构",
        "text": "修改表结构"
      },
      {
        "depth": 4,
        "slug": "索引",
        "text": "索引"
      },
      {
        "depth": 4,
        "slug": "临时数据复制表",
        "text": "临时数据、复制表"
      },
      {
        "depth": 4,
        "slug": "自动编号和重复数据处理",
        "text": "自动编号和重复数据处理"
      },
      {
        "depth": 4,
        "slug": "数据库备份和安全",
        "text": "数据库备份和安全"
      },
      {
        "depth": 4,
        "slug": "函数和运算符",
        "text": "函数和运算符"
      },
      {
        "depth": 3,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-18",
    "title": "8/18–19 MySQL：学习记录与常见问题",
    "description": "记录学习 MySQL 的数据页、索引、缓存、事务日志，以及慢查询、并发和分页。",
    "publishedAt": "2026-08-18",
    "draft": false,
    "html": "<h3 id=\"mysql存储引擎事务日志与常见问题\">MySQL：存储引擎、事务日志与常见问题</h3>\n<h4 id=\"什么是-mysql\">什么是 MySQL</h4>\n<p>MySQL 是一个关系型数据库管理系统（RDBMS）。是一款存放和管理数据的软件, 它介于应用和数据之间，通过一些设计，将大量数据，变成一张张像 excel 的数据表。为应用提供创建(Create), 读取(Read), 更新(Update), 删除(Delete)等核心操作。它以一个长期运行的服务进程存在，接收客户端发来的 SQL，把数据按表、行、列和关系管理起来，并处理并发、权限、事务、恢复与查询优化。不只是数据像 Excel 表格，而是表之间可以通过键建立可验证的业务关系。\nMySQL 在一个真实后端中的位置以用户创建订单为例：</p>\n<pre><code class=\"language-text\">浏览器 / App\n   ↓ HTTP\n后端服务（Rust / Go / Node.js）\n   ↓ SQL + 数据库连接\nMySQL Server\n   ↓\n磁盘中的数据与日志\n</code></pre>\n<p>后端不会直接操作 MySQL 的数据文件。它通过驱动或 ORM 建立连接、发送 SQL、接收结果。\n在数据量小、没有并发时，JSON、CSV、Excel 都能存数据。但一旦成为在线业务系统，就会遇到几个硬问题:\n并发问题例如,两个用户同时抢最后一件商品，文件系统不会自动替你正确处理这个业务并发。MySQL 的事务与锁机制就是为这种问题服务的；后面讲 Undo Log、Redo Log 和锁时，会把这个例子完整跑通。查询问题，如果订单有一千万条，想找用户a最近的20条已支付订单,文件方式往往意味着读很多内容、需要自己过滤，MySQL 可以通过索引缩小范围，仅访问必要数据，一致性问题，下单通常至少涉及两次修改，创建订单-扣减库存，如果订单创建成功、扣库存失败，系统数据就不一致。MySQL 的事务让这两步成为一个整体，要么都成功，要么都失败。</p>\n<h4 id=\"数据页\">数据页</h4>\n<p>MySQL 将数据组织成 excel 表的样子。excel 文件在磁盘上是个xls 文件，MySQL 的数据表也类似，在磁盘上则是个ibd 后缀的文件。数据表越大，磁盘上的 ibd 文件也就越大。直接读写一个大文件里的全部数据会很慢，所以 MySQL 将数据拆成一个个数据页，每页大小 16KB。这样我们读写部分表数据的时候，就只需要读取磁盘里的几个数据页就好。假如一张表有一千万行，磁盘上不可能把每一行当作一个单独的小文件管理，那样随机 I/O 和查找成本都会极高。InnoDB 的做法是：将数据和索引划分为固定大小的页，再以页作为大多数 I/O、缓存和索引树管理的基本单位。索引页默认大小是 16KB。这样我们读写部分表数据的时候，就只需要读取磁盘里的几个数据页就好。</p>\n<h4 id=\"索引\">索引</h4>\n<p>但数据页那么多，查某条数据时，怎么知道要读哪些数据页？可以为每个数据页加入页号，再为每行数据加个序号，这个序号其实就是所谓的主键。按主键大小排序，将每个数据页里最小的主键序号和所在页的页号提出来，放入到一个新生成的数据页中，并且给数据页加入层级的概念。样我们就可以通过上层的数据页快速缩小查找范围，加速查找数据页的过程。现在页跟页之间看起来就像是一棵倒过来的树，这棵可以加速查找数据页的树，就是常说的B+树（B-tree）索引。以上是针对主键的索引，也就是主键索引。按同样的思路，也可以为其他数据表的列去建立索引，比如用户表的名称字段，这样我们就能快速查找到名字为A的用户有哪些，这就是所谓的辅助索引。</p>\n<p>在 InnoDB 中，表数据并不是独立放一份、主键索引再放一份。主键索引的叶子节点保存完整行数据，因此它也被称为聚簇索引：</p>\n<pre><code class=\"language-sql\">CREATE TABLE orders (\n  id BIGINT NOT NULL AUTO_INCREMENT,\n  user_id BIGINT NOT NULL,\n  order_no VARCHAR(32) NOT NULL,\n  total_amount DECIMAL(12, 2) NOT NULL,\n  PRIMARY KEY (id)\n);\n</code></pre>\n<p>这里的 PRIMARY KEY (id) 就是聚簇索引。把id设为主键,此外需要注意的是，如果将id设为了主键需要注意id不能为空，并且id必须唯一。\n除聚簇索引外的索引，统称二级索引，辅助索引，用一个例子说明白主键索引和辅助索引的关系，假设有一张表，假设表中有数据：</p>\n<table>\n<thead>\n<tr>\n<th align=\"right\">id</th>\n<th align=\"left\">user_id</th>\n<th align=\"left\">order_no</th>\n<th align=\"right\">total_amount</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"right\">1</td>\n<td align=\"left\">a</td>\n<td align=\"left\">O001</td>\n<td align=\"right\">99.00</td>\n</tr>\n<tr>\n<td align=\"right\">2</td>\n<td align=\"left\">b</td>\n<td align=\"left\">O002</td>\n<td align=\"right\">199.00</td>\n</tr>\n<tr>\n<td align=\"right\">3</td>\n<td align=\"left\">a</td>\n<td align=\"left\">O003</td>\n<td align=\"right\">59.00</td>\n</tr>\n<tr>\n<td align=\"right\">4</td>\n<td align=\"left\">b</td>\n<td align=\"left\">O004</td>\n<td align=\"right\">88.00</td>\n</tr>\n</tbody></table>\n<p>如果现在执行 <code>SELECT * FROM orders WHERE user_id = b</code>。\n此时只有 id 是主键索引，user_id 没有索引,MySQL 不知道值为 b 的记录在哪，只能一行行判断。</p>\n<pre><code class=\"language-text\">id=1，user_id=7，跳过\nid=2，user_id=42，保留\nid=3，user_id=7，跳过\nid=4，user_id=42，保留\n</code></pre>\n<p>当时数据量少的时候没问题，但是如果数据有千万行，每次执行请求的时候都这样扫，接口自然就慢。解决的方案就是给user_id建立辅助索引。</p>\n<pre><code class=\"language-sql\">CREATE INDEX idx_orders_user_id ON orders(user_id);\n</code></pre>\n<p>MySQL 会额外维护一个按 user_id 排序的目录：</p>\n<pre><code class=\"language-text\">user_id 索引：\n\n7  → id=1\n7  → id=3\n42 → id=2\n42 → id=4\n</code></pre>\n<p>再次执行查询 <code>user_id = b</code> 的指令时，MySQL 会先查目录，找到 <code>user_id = 42</code>，得到 <code>id = 2</code>、<code>id = 4</code>，再找到两条完整订单。因此，索引的作用不是让数据库更聪明，而是避免无意义地检查大量不相关数据。但是索引提升读性能，也会增加存储与写入成本，所以要按高频查询建立。</p>\n<h4 id=\"buffer-pool\">Buffer Pool</h4>\n<p>但就算有了索引，数据也还是在磁盘上。每次都读磁盘太慢了。MySQL在磁盘数据和应用之间，加一层进程内缓存，缓存里装的就是前面提到的 16KB 数据和索引页, 它就是所谓的 Buffer Pool。读数据的时候优先读 Buffer Pool，有数据就返回，没数据才去磁盘里读取，减少了读磁盘的次数，大大提升了性能。一次典型的查询：</p>\n<pre><code class=\"language-text\">SQL：查询 id = 1001 的订单\n  ↓\n主键索引定位到目标数据页\n  ↓\nBuffer Pool 中有这页？\n  ├─ 有：内存读取，快\n  └─ 没有：磁盘读取该页，再放进 Buffer Pool\n</code></pre>\n<p>Buffer Pool 里缓存两种东西1.表数据页2.索引页，如果它们都在 Buffer Pool 中，查询几乎不用等待磁盘 I/O。因此，索引与 Buffer Pool 的关系是：\n索引：告诉 MySQL 该访问哪一页\nBuffer Pool：尽量让这一页已经在内存中\n两者是配合关系，不是替代关系。\n但是内存有限，Buffer Pool 放不下整个数据库时，InnoDB 必须淘汰不常使用的页，为新页腾位置。最近常用的数据页尽量留下，长期不用的数据页优先淘汰，官方文档说明，InnoDB 使用一种改进的 LRU（最近最少使用）策略管理页：经常使用的页保留在较“年轻”的区域，较少使用的页会逐步进入更容易被淘汰的区域。\n官方文档也提到，专用服务器常将最高约 80% 的物理内存分配给它，但前提是仍给操作系统与其他进程留下空间。但是具体的实际操作还是要做一些平衡Buffer Pool 太小会导致热点页频繁被淘汰，延迟升高。Buffer Pool 太大会导致操作系统或其他进程内存不足，性能反而更差。\n<figure><img src=\"./image.png\" alt=\"Buffer Pool\" title=\"Buffer Pool List\"><figcaption>Buffer Pool List</figcaption></figure></p>\n<h4 id=\"自适应哈希索引adaptive-hash-indexahi\">自适应哈希索引（Adaptive Hash Index，AHI）</h4>\n<p>就算有了 buffer pool，要查到某个数据页，也依然要查找 B+树，查询复杂度 O(lgn)。自适应哈希表就是再次基础上进行优化，使用查询复杂度为 O(1)。记录每个数据页的查询频率，对于热点数据页，我们以查询的值为 key，数据页地址为 value，构建 hash 表。如果B+ 树索引像按字母或拼音排好的纸质目录的话，自适应哈希索引像手机通讯录的快速映射，key → value。对于合适的等值查找，哈希表平均可以非常快地定位目标；但它不适合范围查询。因为哈希表没有顺序概念。你问它 1，它能直接找；你问它“1 到 100 所有值”，它无法像 B+ 树那样沿着有序叶子页持续向后扫描。\n此外自适应哈希表它有三个自动特征：自动观察查询模式、自动决定是否建立、自动决定只为哪些热点索引页建立。\n总结一下三者之间的关系：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">组件</th>\n<th align=\"left\">本质</th>\n<th align=\"left\">谁创建</th>\n<th align=\"center\">是否落盘</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">普通 B+ 树索引</td>\n<td align=\"left\">业务查询的正式目录</td>\n<td align=\"left\">开发者通过 DDL 创建</td>\n<td align=\"center\">是</td>\n</tr>\n<tr>\n<td align=\"left\">Buffer Pool</td>\n<td align=\"left\">数据页和索引页的内存缓存</td>\n<td align=\"left\">InnoDB 自动管理</td>\n<td align=\"center\">否</td>\n</tr>\n<tr>\n<td align=\"left\">自适应哈希索引</td>\n<td align=\"left\">热点 B+ 树访问的内存加速路径</td>\n<td align=\"left\">InnoDB 自动管理</td>\n<td align=\"center\">否</td>\n</tr>\n</tbody></table>\n<h4 id=\"change-buffer\">Change Buffer</h4>\n<p>有了自适应hash和索引的加持，读性能提高了。接下来就是要想到如何提高写入的性能了，大部分数据表，除了主键索引外，我们还会加一些辅助索引。比如对用户名加个辅助索引。那对于这类数据表的写操作，更新完主键索引的数据页之后，还需要更新辅助索引页。这样读取辅助索引页的磁盘 IO 必然少不了。我们可以先将要写入的数据收集到一块内存里，等哪天磁盘里的索引页正好被读入 Buffer pool 的时候，再将写入数据应用到索引页中。通过这个方式减少大量的磁盘 IO，提升性能。而这个将写操作收集起来的地方，就是所谓的 Change Buffer，它其实是 Buffer pool 的一部分。</p>\n<pre><code class=\"language-text\">写入一条订单\n  ↓\n发现目标二级索引页不在 Buffer Pool\n  ↓\n先把“对该二级索引的变更”暂存到 Change Buffer\n  ↓\n以后该二级索引页因查询等原因进入 Buffer Pool\n  ↓\n把之前积累的变更合并进去\n</code></pre>\n<p>相当于是先记在待办清单，下次的时候再一起改，Change Buffer 是 InnoDB 内部持久化与合并机制的一部分，不是把业务数据延迟生效。对事务、查询和一致性而言，InnoDB 会正确处理这些待合并变更。此外它不适用于主键索引。</p>\n<h4 id=\"undo-log\">Undo Log</h4>\n<p>在数据库中，有一个叫事务的概念。简单来说，就是可以让多行数据，要么同时更新成功，要么同时更新失败。也就是所谓的原子性。为了实现这一点，我们就需要知道写数据时每行数据原来长啥样，方便对更新后的数据行，进行回滚，因此就有了 Undo Log。假如有一笔支付订单，支付成功时，要完成两步，第一步：订单改为已支付，第二步：库存减一，如果第一步成功，第二步因某种原因失败：这就出现了业务不一致：系统显示订单已支付，却没有扣库存。所以我们把多步操作放入事务，如果中途出错MySQL 必须知道订单修改前是什么样，库存修改前是多少，才能恢复原状。Undo Log 就承担这项工作。更新 buffer pool 数据页的时候，会用旧数据生成 undo log 记录，存储在 Buffer Pool 中的特殊 undo log 内存页中。并随着 buffer pool 的刷盘机制，不定时写入到磁盘的 undo log 文件中。</p>\n<h4 id=\"redo-log\">Redo Log</h4>\n<p>Undo 负责撤回，Redo 负责已经提交的修改在宕机后如何恢复；两者方向相反。上面提到的Undo Log都是 buffer pool 相关的内容，它们本质上都是内存。如果内存数据只写了一半到磁盘中，数据库进程就崩了，那一个事务里的多行数据就没能做到同时更新成功。我们将事务中更新数据行的操作都写入到 redo log buffer 内存中，然后在事务提交的时候进行 redo log 刷磁盘，将数据固化到 redo log 文件中。数据库进程崩溃重启后，就能通过 redo log file 找到历史操作记录，重做数据。保证了事务里的多行数据变更，要么都成功，要么都失败。redo log file 是顺序写入的，buffer pool 的内存数据是随机分散在磁盘各处的，顺序写磁盘性能是随机写的几十倍，所以很多存储系统在写数据时都会搞个日志来记录操作，方便服务重启后进行数据对账，确保数据的一致性和完整性，这类操作就是所谓的 Write-Ahead Logging (WAL) 。\n此外redo log buffer 也是内存，buffer pool 也是内存，如果 redo log buffer 里的数据还没来得及写入到 redo log，数据库进程就崩了，那redo log buffer里的数据也会丢失，所以 redo log 的作用并不是保证所有数据不丢失，而是确保已提交事务的变更不会丢失。但因为 redo log 刷盘频率很高，所以丢失数据的概率很低。redo log 本质上是写入性能和数据完整性折中的产物。</p>\n<h4 id=\"innodb-和-server-层\">InnoDB 和 Server 层</h4>\n<p>我们将上面提到的内容，分为内存和磁盘两部分，一部分是内存里的自适应哈希，buffer pool，以及 redo log buffer。另一部分是磁盘里存放行数据和索引的.ibd 文件, 以及 undo log, redo log 等文件。它们共同构成了 innodb 存储引擎。并对外提供一系列函数接口。</p>\n<pre><code>InnoDB\n├─ 内存部分\n│  ├─ Buffer Pool\n│  ├─ Adaptive Hash Index\n│  ├─ Change Buffer（配置允许时）\n│  └─ Log Buffer\n│\n└─ 磁盘与持久化部分\n   ├─ 表数据与索引页\n   ├─ Undo Log\n   └─ Redo Log\n</code></pre>\n<p>我们平时写的 SQL 语句，最终都会转换成 InnoDB 提供的这些接口函数调用。比如：INSERT 语句会调用 write_row() 接口来插入数据行。UPDATE 语句会调用 update_row() 接口来更新数据行。CREATE TABLE 语句会调用 create() 接口来创建新表。DROP TABLE 语句会调用 drop() 接口来删除表。\nServer 层，本质上是 SQL 语句 和 InnoDB 存储引擎之间的中间层。在 Server 层内提供一个连接管理模块，用于管理来自应用的网络连接。并提供一个分析器，用于判断 SQL 语句有没有语法错误，再提供一个优化器，用于根据一定的规则选择该用什么索引，生成执行计划。之后，提供一个执行器，根据执行计划去调用Innodb 存储引擎的接口函数。server 层和存储引擎层共同构成了一个完整的数据库，它就是我们常说的 Mysql 数据库。</p>\n<h4 id=\"binlog\">Binlog</h4>\n<p>Server 层会将历史上所有变更操作记录到磁盘上的日志文件中，这个日志文件就是所谓的Binlog。一旦误删表，就可以利用 Binlog 来恢复数据。看上去功能和Redo Log很相似，但是还是有一点区别的，简单来说Redo Log是让当前这台 MySQL 宕机后恢复，而Binlog是让其他 MySQL 或恢复工具知道发生过什么：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">对比</th>\n<th align=\"left\">Redo Log</th>\n<th align=\"left\">Binlog</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">所在层</td>\n<td align=\"left\">InnoDB 层</td>\n<td align=\"left\">MySQL Server 层</td>\n</tr>\n<tr>\n<td align=\"left\">主要用途</td>\n<td align=\"left\">崩溃恢复</td>\n<td align=\"left\">复制、增量恢复、审计变更</td>\n</tr>\n<tr>\n<td align=\"left\">服务范围</td>\n<td align=\"left\">主要围绕 InnoDB</td>\n<td align=\"left\">Server 层统一记录</td>\n</tr>\n<tr>\n<td align=\"left\">内容形态</td>\n<td align=\"left\">InnoDB 恢复所需的物理/低层变更信息</td>\n<td align=\"left\">数据库变更事件</td>\n</tr>\n<tr>\n<td align=\"left\">是否循环使用</td>\n<td align=\"left\">有检查点和容量管理</td>\n<td align=\"left\">按文件持续生成并按策略清理</td>\n</tr>\n<tr>\n<td align=\"left\">能否直接代替备份</td>\n<td align=\"left\">不能</td>\n<td align=\"left\">也不能单独代替完整备份</td>\n</tr>\n</tbody></table>\n<p>Binlog主要的两大功能，主从复制和时间点恢复（Point-in-Time Recovery）。</p>\n<h4 id=\"mysql-主从架构\">MySQL 主从架构</h4>\n<p>由于 Binlog 记录了一个 MySQL 的所有变更操作，因此我们还可以利用 binlog 数据，&quot;复制&quot;一个新的 MySQL 出来。原来的 master 叫主数据库，复制出来的则是从数据库，主数据库负责承接写流量，从数据库负责读流量，这样就可以让 MySQL 承接更高的读写流量。它就是经典的 MySQL 主从同步架构。</p>\n<pre><code>主库：\n- 接收写请求\n- 执行事务\n- 写入 Binlog\n- 可承接强一致读取\n\n副本库：\n- 获取主库 Binlog\n- 保存 Relay Log\n- 重放事务\n- 承接部分读请求\n- 可用于备份或故障切换准备\n</code></pre>\n<p>副本库通常应限制写入，避免出现主库有一份数据，副本库被业务直接改了另一份数据，复制重放时冲突。</p>\n<h3 id=\"mysql-的常见问题和自己的理解\">MySQL 的常见问题和自己的理解</h3>\n<p>前面主要学习了 MySQL 的基本架构，包括数据页、索引、Buffer Pool、Undo Log、Redo Log、Server 层和 Binlog。接下来继续学习几个实际开发中经常遇到的问题。</p>\n<h4 id=\"mysql-的索引为什么使用-b-树而不使用跳表\">MySQL 的索引为什么使用 B+ 树，而不使用跳表</h4>\n<p>MySQL 中最常见的索引结构是 B+ 树。B+ 树可以看成由多个数据页组成的多层结构，每个数据页默认大小约为 16KB。非叶子节点保存索引值和页号，叶子节点保存真正的数据，或者保存对应的主键值。查询数据时，MySQL 会先从根节点开始，根据索引值判断目标数据在哪个范围，然后逐层进入对应的数据页，最终定位到数据记录。相比于直接遍历整张表，B+ 树可以将查询范围快速缩小。\nB+ 树的叶子节点之间还是有序连接的，因此比较适合范围查询：</p>\n<pre><code>SELECT *\nFROM orders\nWHERE id BETWEEN 100 AND 200;\n</code></pre>\n<p>执行这类查询时，MySQL 先定位到 id=100 的位置，然后沿着叶子节点向后读取，直到 id=200，而不需要重新查找每一条数据。跳表也是一种可以将查询复杂度降低到 O(logN) 的数据结构。Redis 的 ZSet 就使用了跳表。但是 MySQL 和 Redis 面临的问题不同：</p>\n<table>\n<thead>\n<tr>\n<th align=\"left\">对比</th>\n<th align=\"left\">MySQL B+ 树</th>\n<th align=\"left\">Redis 跳表</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"left\">数据主要存放位置</td>\n<td align=\"left\">磁盘</td>\n<td align=\"left\">内存</td>\n</tr>\n<tr>\n<td align=\"left\">主要关注点</td>\n<td align=\"left\">减少磁盘 I/O</td>\n<td align=\"left\">减少内存操作复杂度</td>\n</tr>\n<tr>\n<td align=\"left\">是否适合范围查询</td>\n<td align=\"left\">适合</td>\n<td align=\"left\">也适合</td>\n</tr>\n<tr>\n<td align=\"left\">插入维护成本</td>\n<td align=\"left\">需要维护页分裂和树结构</td>\n<td align=\"left\">通过随机层数插入</td>\n</tr>\n<tr>\n<td align=\"left\">典型应用</td>\n<td align=\"left\">InnoDB 索引</td>\n<td align=\"left\">Redis ZSet</td>\n</tr>\n</tbody></table>\n<p>MySQL 选择 B+ 树，主要是因为 B+ 树的一个节点可以存放很多索引项，树的高度较低，查询一条数据时需要访问的数据页较少。数据库查询的瓶颈很多时候不是 CPU，而是磁盘 I/O，因此降低树的高度非常重要。不过，单表两千万行或者三层 B+ 树可以存储两千万数据只能作为经验参考，并不是 MySQL 的硬性限制。实际性能还受到数据行大小、索引长度、Buffer Pool 大小、磁盘类型和缓存命中率等因素影响。在 InnoDB 中，主键索引的叶子节点保存完整的数据行，所以主键索引又叫聚簇索引。普通索引的叶子节点一般保存索引字段和主键值，查询完整数据时还需要根据主键再次查找聚簇索引，这个过程叫回表。</p>\n<h4 id=\"mysql-数据库查询很慢除了索引还可能因为什么\">MySQL 数据库查询很慢，除了索引还可能因为什么</h4>\n<p>遇到查询变慢的问题，不能第一时间就认为是没有索引。应该先通过执行计划确认 SQL 实际是怎么执行的。常见的排查方向包括以下几种。\n第一种情况是索引没有被使用。如果对索引字段使用函数，MySQL 可能无法直接使用原来的索引。可以改写成范围查询。\n第二种情况是索引的区分度太低。例如 status、gender、is_deleted 等字段，字段值种类很少，单独建立索引后，优化器可能认为使用索引的收益并不高。\n第三种情况是回表次数太多。如果通过二级索引找到大量主键，然后再逐条回表查询完整数据，整体性能也可能比较差。如果查询所需要的字段都在索引中，就可能形成覆盖索引，减少回表次数。\n第四种情况是 Buffer Pool 太小。索引只能帮助 MySQL 定位数据页，但如果对应的数据页不在 Buffer Pool 中，仍然需要从磁盘读取。\n第五种情况是锁等待。比如两个事务同时修改同一件商品的库存，SQL 本身可能执行很快，但如果前一个事务没有提交，后一个事务就必须等待锁释放。\n除此之外，查询慢还可能是因为返回数据太多、连接池配置不合理、网络传输耗时、排序产生临时表，或者应用层出现了 N+1 查询问题。\n因此，排查慢查询时应该按照下面的顺序进行：</p>\n<pre><code>确认 SQL 是否真的慢\n  ↓\n使用 EXPLAIN 查看执行计划\n  ↓\n检查是否使用了正确索引\n  ↓\n查看预计扫描行数\n  ↓\n检查是否存在回表\n  ↓\n检查锁等待、Buffer Pool 和连接池\n  ↓\n检查应用层是否重复查询\n</code></pre>\n<h4 id=\"分库分表为什么会带来读扩散问题\">分库分表为什么会带来读扩散问题</h4>\n<p>当一张表的数据量越来越大时，可以考虑水平分表，假设按照用户 id 进行分片，那么查询主键时可以快速定位分表，但是，如果查询条件不是分片键，就无法直接知道数据在哪张表，此时程序可能需要同时查询。如果有 100 张分表，就可能需要并发执行 100 次查询，再由中间层合并结果。这种随着分表数量增加，查询次数也不断增加的问题，就叫读扩散。读扩散的本质是，主键是分片键普通查询字段不是分片键，查询无法定位具体分表只能广播到多个分表。\n解决读扩散的一种方式是建立额外的索引路由表。查询时先通过昵称找到用户 id，再根据用户 id 定位真实的用户分表。这种方式可以减少查询的分表数量，但需要额外维护一张表。当昵称发生修改时，用户表和索引表需要同时更新，因此会增加开发和一致性处理成本。如果业务需要支持复杂的模糊搜索、多条件组合搜索、标签搜索和排序，可以考虑使用 Elasticsearch 等搜索系统。MySQL 更适合保存订单、库存、金额等需要事务保证的数据，搜索引擎则更适合处理复杂的检索需求。</p>\n<h4 id=\"两个事务同时扣库存为什么不会超卖\">两个事务同时扣库存，为什么不会超卖</h4>\n<p>库存扣减是数据库系统中非常典型的并发场景。假设商品库存只有 1 件，同时来了两个购买请求。如果代码只是先查询库存，再在程序中判断，就可能产生超卖问题。两个事务可能同时读取到库存为 1：</p>\n<pre><code>事务 A：读取 stock = 1\n事务 B：读取 stock = 1\n\n事务 A：判断库存充足\n事务 B：判断库存充足\n\n事务 A：库存减 1\n事务 B：库存减 1\n</code></pre>\n<p>两个事务都认为库存充足，就可能创建两笔订单，但实际只有一件商品。\n一种方式是使用悲观锁：</p>\n<pre><code>START TRANSACTION;\nSELECT stock\nFROM products\nWHERE id = 1\nFOR UPDATE;\n</code></pre>\n<p>FOR UPDATE 会对查询到的记录加排他锁。事务 A 获取锁之后，事务 B 再查询同一行时就需要等待。事务 A 检查库存并扣减，事务 B 只有等事务 A 提交之后，才能继续执行。此时它读取到的库存已经发生变化。\n另一种更简洁的方式是使用带条件的原子更新：</p>\n<pre><code>START TRANSACTION;\n\nUPDATE products\nSET stock = stock - 1\nWHERE id = 1\n  AND stock &gt; 0;\n\n-- 如果影响行数为 0，则回滚\n-- 如果影响行数为 1，则继续创建订单\n\nINSERT INTO orders (\n  user_id,\n  order_no,\n  status,\n  total_amount\n)\nVALUES (\n  1,\n  &#39;ORD202608190001&#39;,\n  &#39;PENDING&#39;,\n  299.00\n);\n\nCOMMIT;\n</code></pre>\n<p>这里事务的作用是保证扣库存和创建订单两个操作要么都成功，要么都失败。这样库存也会恢复，避免出现库存已经扣减、订单却没有创建成功的情况。但是，事务也不能无限扩大。如果事务中包含大量网络请求、远程调用或复杂计算，就会长时间持有数据库锁，导致其他请求等待。还需要注意死锁问题，假设事务 A先锁订单再锁库存，事务 B先锁库存再锁订单，这样就会造成死锁问题，MySQL 通常会检测到死锁，并主动回滚其中一个事务。应用程序应该捕获死锁异常，并根据业务情况进行重试。</p>\n<h4 id=\"mysql-深分页为什么越来越慢\">MySQL 深分页为什么越来越慢</h4>\n<p>在后台管理系统、订单列表和日志查询中，经常需要分页，查询第一页时速度通常很快，因为 MySQL 只需要读取前几条数据。\n当页码变大时：</p>\n<pre><code>SELECT *\nFROM orders\nORDER BY id\nLIMIT 1000000, 20;\n</code></pre>\n<p>虽然最终只需要返回 20 条数据，但 MySQL 通常需要先找到并扫描前面的 1000000 条记录，然后跳过这些记录，最后返回第 1000001 到 1000020 条数据，所以 LIMIT 1000000, 20 并不是只读取 20 条数据，而是先读取一百万多条，再丢弃前面的数据。这就是深分页越来越慢的原因。\n可以使用 EXPLAIN 观察扫描行数。随着偏移量增加，rows 通常也会变大。一种更适合大数据量的方式是使用游标分页，也叫基于主键的分页。</p>\n<pre><code>SELECT *\nFROM orders\nWHERE id &gt; 1000000\nORDER BY id\nLIMIT 20;\n</code></pre>\n<p>第一次查询时不需要传入上一页的 id，查询结果中最后一条数据的 id 假设是 20。下一页就使用，这样 MySQL 可以通过主键索引直接定位到 id 大于 20 的位置，不需要从第一条数据开始扫描。</p>\n<pre><code>普通分页：\n从开头扫描 → 跳过大量数据 → 返回结果\n\n游标分页：\n直接定位到上一次结束位置 → 返回下一批数据\n</code></pre>\n<p>但是它也有一定限制。它更适合下一页和上一页这种连续翻页，不适合直接跳到第 100 页。分页的本质不是简单地使用 LIMIT，而是要考虑数据量增长之后，数据库是否还需要扫描大量无用数据。小数据量时普通分页没有问题，但当数据达到百万级、千万级时，游标分页通常更合适。</p>\n<h4 id=\"参考资料\">参考资料</h4>\n<ul>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/what-is-mysql.html\">What Is MySQL?</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-physical-structure.html\">InnoDB Physical Structure</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-index-types.html\">InnoDB Index Types</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-buffer-pool.html\">InnoDB Buffer Pool</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-adaptive-hash.html\">Adaptive Hash Index</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-change-buffer.html\">Change Buffer</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/innodb-on-disk-structures.html\">InnoDB On-Disk Structures</a></li>\n<li><a href=\"https://dev.mysql.com/doc/refman/8.4/en/explain.html\">EXPLAIN Statement</a></li>\n<li><a href=\"https://blog.csdn.net/crazymakercircle/article/details/136760409\">MySQL 查询慢的常见原因</a></li>\n<li><a href=\"https://developer.cloud.tencent.com/information/mysql%E6%9F%A5%E8%AF%A2%E6%95%B0%E6%8D%AE%E5%BA%93%E6%85%A2%E7%9A%84%E5%8E%9F%E5%9B%A0-ask\">MySQL 查询数据库慢的原因</a></li>\n<li><a href=\"https://javaguide.cn/high-performance/read-and-write-separation-and-library-subtable.html\">读写分离与分库分表</a></li>\n<li><a href=\"https://www.zhihu.com/question/430649694\">深分页问题讨论</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "mysql存储引擎事务日志与常见问题",
        "text": "MySQL：存储引擎、事务日志与常见问题"
      },
      {
        "depth": 4,
        "slug": "什么是-mysql",
        "text": "什么是 MySQL"
      },
      {
        "depth": 4,
        "slug": "数据页",
        "text": "数据页"
      },
      {
        "depth": 4,
        "slug": "索引",
        "text": "索引"
      },
      {
        "depth": 4,
        "slug": "buffer-pool",
        "text": "Buffer Pool"
      },
      {
        "depth": 4,
        "slug": "自适应哈希索引adaptive-hash-indexahi",
        "text": "自适应哈希索引（Adaptive Hash Index，AHI）"
      },
      {
        "depth": 4,
        "slug": "change-buffer",
        "text": "Change Buffer"
      },
      {
        "depth": 4,
        "slug": "undo-log",
        "text": "Undo Log"
      },
      {
        "depth": 4,
        "slug": "redo-log",
        "text": "Redo Log"
      },
      {
        "depth": 4,
        "slug": "innodb-和-server-层",
        "text": "InnoDB 和 Server 层"
      },
      {
        "depth": 4,
        "slug": "binlog",
        "text": "Binlog"
      },
      {
        "depth": 4,
        "slug": "mysql-主从架构",
        "text": "MySQL 主从架构"
      },
      {
        "depth": 3,
        "slug": "mysql-的常见问题和自己的理解",
        "text": "MySQL 的常见问题和自己的理解"
      },
      {
        "depth": 4,
        "slug": "mysql-的索引为什么使用-b-树而不使用跳表",
        "text": "MySQL 的索引为什么使用 B+ 树，而不使用跳表"
      },
      {
        "depth": 4,
        "slug": "mysql-数据库查询很慢除了索引还可能因为什么",
        "text": "MySQL 数据库查询很慢，除了索引还可能因为什么"
      },
      {
        "depth": 4,
        "slug": "分库分表为什么会带来读扩散问题",
        "text": "分库分表为什么会带来读扩散问题"
      },
      {
        "depth": 4,
        "slug": "两个事务同时扣库存为什么不会超卖",
        "text": "两个事务同时扣库存，为什么不会超卖"
      },
      {
        "depth": 4,
        "slug": "mysql-深分页为什么越来越慢",
        "text": "MySQL 深分页为什么越来越慢"
      },
      {
        "depth": 4,
        "slug": "参考资料",
        "text": "参考资料"
      }
    ]
  },
  {
    "id": "2026-08-17",
    "title": "8/17 Nginx、服务器、网关的学习",
    "description": "记录 HTTP 服务、反向代理、负载均衡、并发模型及 Nginx 在 Chatbot 项目中的应用思路。",
    "publishedAt": "2026-08-17",
    "draft": false,
    "html": "<h3 id=\"nginx服务器网关的学习\">Nginx、服务器、网关的学习</h3>\n<h3 id=\"nginx\">Nginx</h3>\n<p>今日学习 Nginx，并且补齐一下服务器、网关等知识。\n在进入Nginx的学习之前我觉得应该先了解一下为何需要HTTP服务，想要让本地的浏览器，获取到放在远端服务器上的 html 可以在远端服务器启动一个进程，这个进程对外提供 http 服务，简单来说就是提供了个 url。用户在浏览器中输入这个 url， 回车，浏览器就会向 这个进程发起 http 请求，进程收到浏览器的请求后，就将 html 文件发给浏览器，浏览器完成解析和展示，而像这种根据浏览器请求，返回 html 文件的服务进程，其实就叫 http 服务器。有了它，html 文件就能部署到远端服务器上，对外提供网页服务了。Nginx 就可以充当 HTTP 服务器。</p>\n<pre><code class=\"language-text\">浏览器\n  ↓  HTTP 请求：请给我 /index.html\n服务器上的 HTTP 服务器进程\n  ↓  读取文件\n/var/www/index.html\n  ↓  HTTP 响应\n浏览器\n</code></pre>\n<p>像是如果让后端程序直接发这些文件，像是Node、Rust都能返回 HTML、JS 和图片。但如果一个网站有大量静态资源，让业务应用既处理订单、登录、数据库，又处理所有静态文件，更自然的分工应该是：</p>\n<pre><code>浏览器\n  ↓\nNginx\n  ├─ 静态文件：Nginx 直接返回\n  └─ 动态请求：交给业务后端\n</code></pre>\n<p>除此之外正常的网站并不会只有静态的文件，真实网站还需要动态数据。例如用户打开商品页面等，这个请求不能只读一个固定文件，因为商品价格、库存、评价随时会变。此时的业务逻辑：</p>\n<pre><code>浏览器\n  ↓ 请求商品信息\n后端程序\n  ↓ 查询数据库\n数据库\n  ↓ 返回商品数据\n后端程序\n  ↓ 返回 JSON\n浏览器\n</code></pre>\n<p>此时如果直接让浏览器访问后端，会出现几个问题，一是后端的端口暴露了，用户直接知道了内部机器的IP和端口，此外如果以后你从 8080 改成 3000，或换服务器，前端代码也要改。\n还有就是将来如果上线用户量增大，后端的服务器从一台扩容为多台，浏览器该访问哪一台？让浏览器自己随机选后端是糟糕的设计，后端节点变动时，每个客户端都需要更新；健康状态也无法统一判断。\n前端与后端端口不同，它们协议、域名、端口都不同，浏览器会受到同源策略限制。</p>\n<h3 id=\"反向代理\">反向代理</h3>\n<p>在外部流量和后端服务之间加一个统一入口，有了反向代理，我们对外就可以只提供一个url域名，背后根据需要, 随时扩缩容服务。\n这个反向代理的功能，正好可以加到前面放 html 文件的进程上。那现在这个进程既可以为 前端 html 文件提供 http 服务器的功能，当 html 文件被加载到浏览器，并向后端发起请求的时候，这个进程还能为后端服务器提供反向代理的功能。</p>\n<pre><code>浏览器\n  ↓\nhttps://www.example.com\n  ↓\nNginx\n  ├─ 请求 /        → 返回前端静态文件\n  └─ 请求 /api/*   → 转给后端服务\n                      ↓\n                 10.0.0.11:8080\n</code></pre>\n<p>用户始终只访问一个地址。\n接下来就是需要通过配置告知Nginx应该如何转给后端，Nginx 根据请求路径做分流：</p>\n<pre><code>/             → 前端页面\n/assets/app.js → 前端静态文件\n/api/users    → 后端接口\n/api/orders   → 后端接口\n</code></pre>\n<pre><code class=\"language-nginx\">server {\n    listen 80;\n\n    location / {\n        root /var/www/site;\n    }\n\n    location /api/ {\n        proxy_pass http://127.0.0.1:3000;\n    }\n}\n</code></pre>\n<p>像是这段配置简单来说就是监听80端口，如果用户请求以 / 开头：到 /var/www/site 目录找静态文件。但如果用户请求以 /api/ 开头：不要找文件，转发到本机 3000 端口的后端程序，路径决定请求去哪里。\n此外负载均衡的能力，也是通过反向代理实现的，像这种，屏蔽掉背后具体有哪些服务器的代理方式，就是我们常说的反向代理。</p>\n<h3 id=\"负载均衡\">负载均衡</h3>\n<p>假设在业务场景下，当前的后端服务器只有一台，一开始没有问题，用户变多后，服务 A 的 CPU、内存、数据库连接可能逐渐不够用。你新增两台一模一样的商品，它们处理同一种业务、访问同一套数据库或共享数据。但是就会引出一个问题，用户的请求来了，应该交给A还是B还是C？Nginx负载均衡就是解决这个问题的。\n如果没有负载均衡用户直接访问后端A，即使 B、C 很空闲，A 也可能被压垮，更糟糕的是，A 挂掉后，用户会直接失败，浏览器不应该负责发现、选择和切换后端节点。\nNginx 充当统一入口：</p>\n<pre><code>                        ┌→ 服务 A\n浏览器 → Nginx ─────────┼→ 服务 B\n                        └→ 服务 C\n</code></pre>\n<p>Nginx 决定把本次请求交给谁，这就是负载均衡。\n配置例子如下：</p>\n<pre><code class=\"language-nginx\">upstream product_service {\n    server 10.0.0.11:8080;\n    server 10.0.0.12:8080;\n    server 10.0.0.13:8080;\n}\n# 定义一个后端服务组\nserver {\n    listen 80;\n    server_name www.example.com;\n\n    location /api/products/ {\n        proxy_pass http://product_service;\n    }\n    # 当请求命中 /api/products/ 时，转给 product_service 中的一台机器。\n}\n</code></pre>\n<p>Nginx 在没有额外配置的情况下默认按轮询分发。</p>\n<pre><code>第 1 个请求 → A\n第 2 个请求 → B\n第 3 个请求 → C\n第 4 个请求 → A\n第 5 个请求 → B\n</code></pre>\n<p>此外还有一种情况，当各请求耗时差异很大，应当采用最少连接的配置方案。</p>\n<pre><code class=\"language-nginx\">upstream product_service {\n    least_conn;\n\n    server 10.0.0.11:8080;\n    server 10.0.0.12:8080;\n    server 10.0.0.13:8080;\n}\n</code></pre>\n<p>新请求优先交给当前活跃连接数更少的后端。对请求处理时间差异较大的场景，常比单纯轮询合理。\n还可以给每台后端设置失败判定：</p>\n<pre><code class=\"language-nginx\">upstream product_service {\n    server 10.0.0.11:8080 max_fails=3 fail_timeout=10s;\n    server 10.0.0.12:8080 max_fails=3 fail_timeout=10s;\n    server 10.0.0.13:8080 max_fails=3 fail_timeout=10s;\n}\n</code></pre>\n<p>一台后端挂了的话，在 10 秒内，某服务连续失败 3 次，Nginx 就会暂时认为它不可用，一段时间内不再优先把请求发给它。</p>\n<h3 id=\"并发\">并发</h3>\n<p>假设有1万人打开了你的网站，不代表Nginx正在同时做1万件CPU计算，很多用户只是在进行打开网站后的阅读，请求发到后端在等待数据查询的结果等，这些都叫连接还存在，但绝大部分时间没有CPU工作要做，所以一万个并发的链接并不等于CPU同时计算一万件事情，Nginx 的强项，就是高效地管理这些大多数时间在等待的连接。\n在传统服务器处理流程，处理流程大概是如下：</p>\n<pre><code>接收请求\n→ 转给后端\n→ 后端查数据库\n→ 等待查询结果\n→ 返回结果\n</code></pre>\n<p>假设数据库查询耗时 500ms。在 2ms ~ 500ms 之间，它几乎什么都没做，只是在等网络和后端。</p>\n<pre><code>处理者 1：\n0ms    收到 A\n1ms    转发给后端\n2ms    开始等待后端\n500ms  后端返回\n501ms  返回给 A\n</code></pre>\n<p>这就是一请求占一个线程/进程直到完成的问题。\nNginx解决这个问题的做法就是等待的时候把控制权让出来，Nginx的worker处理A时，不会一直坐着等，worker 没有忘掉 A；它只是把 A 的下一步挂起来，等事件发生再继续。这也叫做异步非阻塞 I/O。</p>\n<pre><code>0ms     处理 A，发给后端\n2ms     A 等待后端；worker 改去处理 B\n\n3ms     处理 B，发给后端\n5ms     B 等待后端；worker 改去处理 C\n\n6ms     处理 C，发给后端\n8ms     C 等待后端；worker 继续处理新请求\n\n500ms   A 的后端返回\n        操作系统通知 worker\n        worker 处理 A 的响应并发回浏览器\n</code></pre>\n<p>所以对用户而言，很多请求像是同时进行的，这叫并发。但如果机器只有一个 CPU 核，某个瞬间 CPU 仍只能执行一段指令，真正同时使用多个 CPU 核，叫并行。Nginx 同时利用两者。因此常见配置为：</p>\n<pre><code class=\"language-nginx\">worker_processes auto;\n</code></pre>\n<p>让 Nginx 大致按 CPU 核数启动 worker。</p>\n<h4 id=\"master-和-worker\">Master 和 Worker</h4>\n<p>Nginx 不是让多个 Worker 分别绑定同一个端口，实际是 Master 启动时先创建并绑定 <code>IP:80</code> 的监听 socket，再创建多个 Worker；Worker 继承并共同使用这个监听 socket。客户端的新连接先进入操作系统维护的等待队列，某个 Worker 调用 <code>accept()</code> 时，内核会原子地把这一个连接交给它，其他 Worker 不会再处理同一连接，因此既不会端口冲突，也不会重复接收。Nginx 和操作系统还会协调多个 Worker 接收连接，避免所有 Worker 因一个新连接同时被唤醒。</p>\n<p>此外，配置 reload 时，新 Worker 接收新连接，旧 Worker 完成已有连接后退出，保证任意时间一直有 Worker 在工作。也就是所谓的滚动升级。因此还需要一个新的进程协调各个 Worker 谁先谁后，这个协调进程，就是所谓的 Master 进程。</p>\n<pre><code>worker_processes auto;\n\nserver {\n    listen 80;\n}\n</code></pre>\n<p>实际Nginx的工作流程如下：</p>\n<pre><code>1. master 启动\n2. master 创建并绑定监听 socket：0.0.0.0:80\n3. master 开始监听这个 socket\n4. master 创建 Worker # 数量根据 CPU 核数，也可以自定义\n5. worker 继承/共享这个监听 socket\n</code></pre>\n<h4 id=\"共享内存与模块化网关能力\">共享内存与模块化网关能力</h4>\n<p>多 Worker 进程的情况下，同一个客户端的多个请求会随机打到某个 Worker。对于限流这种需要计数的场景，计数会被分散到多个 Worker 上单独处理，所以还需要给这些 Worker 进程分配一个共享内存区域，方便多个进程之间共用同一份数据做逻辑，确保系统数据一致性。\n此外在它上面也可以加入一些通用网关能力，比如加个日志，记录每次调用的结果，方便后续排查问题，又比如加个对输入输出的内容进行压缩的功能，减小网络带宽消耗，又或者是对某个 IP 进行限流或封禁，甚至还可以修改输入输出的内容。这部分功能设计为开放接口，让用户通过自定义模块来实现特定功能。像是基础网关只支持http，可以通过自定义模块让它支持tcp，udp，http2和websocket，Nginx的生态非常丰富，资料和模块也是非常的多，那么多种能力，可以通过配置文件，也就是nginx.conf，想用什么能力，就在配置文件上声明清楚就行。</p>\n<h4 id=\"proxy-cache\">Proxy Cache</h4>\n<p>作为网关，它在收到前端网页请求后，会转发给后端，并将后端处理结果中转给前端。如果它能将响应结果缓存起来，下次收到同样的请求时，就可以直接将缓存里的数据返回给前端，从而减少响应时间和网络负载。如果将数据放进内存里，内存较贵，不太合适；可以维护一些磁盘文件，用于在前端请求后端的过程中暂存后端响应的结果。后面再有相同请求，就可以将磁盘里的数据返回。这又是经典的空间换时间，用廉价的磁盘空间换取网络传输和 CPU 计算耗时。对于后端响应较慢或重复请求较多的场景，读写磁盘通常比直接将请求打到后端来得快。这些用于缓存响应数据的磁盘文件，就是所谓的 Proxy Cache。</p>\n<h3 id=\"chatbot-项目\">chatbot 项目</h3>\n<p>在这项目中的 Caddy 只承担 HTTP 反向代理和基于 Host/路径的路由，Nginx 能完整覆盖，进行项目的优化，Nginx的生态更加成熟稳定，方便后续的迭代更新。</p>\n<pre><code class=\"language-nginx\">events {}\n\nhttp {\n    map $http_upgrade $connection_upgrade {\n        default upgrade;\n        &quot;&quot;      close;\n    }\n\n    server {\n        listen 80;\n        server_name example.localhost;\n\n        location ^~ /api/ {\n            proxy_pass http://127.0.0.1:4173;\n            proxy_http_version 1.1;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n        }\n\n        location = /healthz {\n            proxy_pass http://127.0.0.1:4173;\n            proxy_http_version 1.1;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n        }\n\n        location / {\n            proxy_pass http://127.0.0.1:3000;\n            proxy_http_version 1.1;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n        }\n    }\n\n    server {\n        listen 80;\n        server_name ~^(?&lt;project&gt;.+)\\.example\\.localhost$;\n\n        location / {\n            proxy_pass http://127.0.0.1:4173;\n            proxy_http_version 1.1;\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $scheme;\n            proxy_set_header Upgrade $http_upgrade;\n            proxy_set_header Connection $connection_upgrade;\n        }\n    }\n}\n</code></pre>\n<p>让 Nginx 作为项目唯一的访问入口。浏览器访问 example.localhost 时，Nginx 会先判断请求内容：如果是 /api/ 开头的接口请求，或者 /healthz 健康检查，就转发给 Rust 后端的 4173 端口；如果是打开工作台页面、加载前端资源等其他请求，就转发给 Next.js 的 3000 端口。访问 coffee-demo.example.localhost 这类项目子域名时，Nginx 会统一交给 Rust 后端处理，因为后端需要根据访问的域名判断应该返回哪个已发布项目。配置里的 proxy_set_header 是把用户原本访问的域名、客户端 IP 和访问协议传给后端，其中最重要的是 Host，没有它，后端就无法区分不同项目子域名。</p>\n<h3 id=\"资料来源\">资料来源</h3>\n<ul>\n<li><a href=\"https://docs.nginx.com/nginx/admin-guide/web-server/reverse-proxy\">Nginx 反向代理</a></li>\n<li><a href=\"https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/\">Nginx HTTP 负载均衡</a></li>\n<li><a href=\"https://nginx.org/en/docs/dev/development_guide.html\">Nginx 开发指南</a></li>\n<li><a href=\"https://nginx.org/en/docs/beginners_guide.html\">Nginx 初学者指南</a></li>\n<li><a href=\"https://docs.nginx.com/nginx/admin-guide/basic-functionality/managing-configuration-files/\">Nginx 配置文件管理</a></li>\n<li><a href=\"https://nginx.org/en/docs/control.html\">Nginx 信号控制</a></li>\n</ul>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "nginx服务器网关的学习",
        "text": "Nginx、服务器、网关的学习"
      },
      {
        "depth": 3,
        "slug": "nginx",
        "text": "Nginx"
      },
      {
        "depth": 3,
        "slug": "反向代理",
        "text": "反向代理"
      },
      {
        "depth": 3,
        "slug": "负载均衡",
        "text": "负载均衡"
      },
      {
        "depth": 3,
        "slug": "并发",
        "text": "并发"
      },
      {
        "depth": 4,
        "slug": "master-和-worker",
        "text": "Master 和 Worker"
      },
      {
        "depth": 4,
        "slug": "共享内存与模块化网关能力",
        "text": "共享内存与模块化网关能力"
      },
      {
        "depth": 4,
        "slug": "proxy-cache",
        "text": "Proxy Cache"
      },
      {
        "depth": 3,
        "slug": "chatbot-项目",
        "text": "chatbot 项目"
      },
      {
        "depth": 3,
        "slug": "资料来源",
        "text": "资料来源"
      }
    ]
  },
  {
    "id": "2026-08-14",
    "title": "8/14项目结构、Docker 与 Rust 的理解",
    "description": "记录博客与 Chatbot 项目的结构优化，以及对 Docker 和 Rust 的学习理解",
    "publishedAt": "2026-08-14",
    "draft": false,
    "html": "<h3 id=\"项目结构docker-与-rust-的理解\">项目结构、Docker 与 Rust 的理解</h3>\n<h3 id=\"1-blog-项目的重新优化结构\">1. Blog 项目的重新优化结构</h3>\n<p>主要在完善个人博客项目，目标不是单纯把页面做得好看一点，而是希望把它整理成一个后续能持续记录、维护和迭代的个人技术博客。</p>\n<p>刚开始上手优化的时候，感觉目录结构和页面风格都比较杂。文件虽然不算特别多，但页面、组件、样式、接口和文档混在一起后，后续想改一个功能时不太容易判断应该放在哪里。于是我先从项目结构入手，对目录职责、文件命名和文档位置做了整理。比如把不同应用按职责区分，博客前端、留言板接口、文档和测试分别归类；同时统一了组件、变量和文件的命名方式，这一点也是需要重点培养的习惯，之前写文件代码都是想到取啥名，后面还是要严格遵守命名习惯，让后面自己回来看代码时也能更容易理解。\n后续命名需要遵循核心目标是：一致、易读、便于工具处理。常见约定方法像是。</p>\n<p>将原先的 Astro 前端改造为 Vite + React + TypeScript，同时保留 GitHub Pages 静态部署方式：</p>\n<pre><code class=\"language-text\">astro-blog/\n├─ apps/\n│  ├─ web/                              # React + Vite 博客前端\n│  │  ├─ public/                        # 图标、GitHub Pages 404 回退页\n│  │  ├─ scripts/\n│  │  │  └─ generate-diary-content.ts   # Markdown 转文章数据\n│  │  ├─ src/\n│  │  │  ├─ app/                        # 路由、页面、基础布局\n│  │  │  ├─ content/diary/              # Markdown 日记原文\n│  │  │  ├─ features/                   # 日记、留言板等业务模块\n│  │  │  ├─ generated/                  # 构建生成的文章数据\n│  │  │  ├─ site/                       # 网站配置、导航、页脚、主题\n│  │  │  ├─ styles/                     # 全局与文章样式\n│  │  │  └─ main.tsx                    # React 入口\n│  │  ├─ tests/                         # 前端测试\n│  │  ├─ index.html\n│  │  ├─ package.json\n│  │  ├─ tsconfig.json\n│  │  └─ vite.config.ts\n│  │\n│  └─ guestbook-api/                    # 留言板后端（后续继续完善）\n│\n├─ .github/\n│  └─ workflows/\n│     └─ deploy.yml                      # GitHub Pages 自动部署\n│\n├─ docs/                                # 架构、设计与迁移文档\n├─ README.md\n└─ AGENTS.md\n</code></pre>\n<p>React 主要负责页面的内容和交互，比如文章列表、文章详情、导航这些页面；Vite 主要负责在本地启动项目和打包发布，所以平时改完代码能比较快看到效果。项目里的文章是用 Markdown 写的，再通过脚本生成网站要用的数据，最后打包成静态网页部署到 GitHub Pages。Vite 的原理可以理解成：把开发和打包这两件事分开，并尽量减少等待。开发时，Vite 会在本地启动一个服务器，把浏览器需要的代码按模块提供出来。修改某个组件后，它会找到这个组件以及相关部分，只更新必要内容，浏览器不需要每次都重新加载整个网站。</p>\n<h3 id=\"2-chatbot-项目的优化结构\">2. Chatbot 项目的优化结构</h3>\n<h4 id=\"21-packages-的职责\">2.1 packages 的职责</h4>\n<p>周二说明之后意识到其实还是有很多的问题存在，像是 <code>packages/</code> 下面具体应该是放啥，其实本质上我也不是很清楚。后续再了解之后知道了，一般来说 <code>packages/</code> 下应该放的是：多个应用共享的 UI 组件、多个应用共享的 API Client。</p>\n<p>它们之间都有一些共同的特征，像是是否被至少两个应用使用，或明确会成为独立能力；有没有稳定的对外 API 接口；是不是能独立版本化和维护。</p>\n<p>而我目前项目下的 <code>packages/</code> 只有一个模块，它不是共享组件库，而是一个完整的 React/Vite 网站起始模板，Rust API 会读取它的文件内容，用来生成新项目。从职责和依赖关系来看，应该不需要单独保留 <code>packages/</code>，因为目前来说只有 Rust API 使用这个模板，它也不需要独立发布。这个模板也不能单独脱离 API 运行，更合理的结构应该是：</p>\n<pre><code class=\"language-text\">apps/api/resources/templates/react-vite/\n</code></pre>\n<p>这样更符合“谁拥有、谁维护、谁使用”的原则。</p>\n<h4 id=\"22-docker-的位置和理解\">2.2 Docker 的位置和理解</h4>\n<p>除此之外就是 Docker 的问题，刚开始我对 Docker 的理解比较模糊，只知道它似乎和部署有关，能把程序做成镜像，方便在服务器上运行。最初我将 Docker 放到 <code>platform</code> 下面，认为 Docker 只是部署平台的一部分。</p>\n<p>但实际上，<code>Dockerfile</code>、<code>compose.yaml</code> 和 <code>.dockerignore</code> 更接近服务本身：它们定义了服务如何构建、依赖什么环境、暴露什么端口、怎样启动。当服务代码发生变化时，Docker 配置往往也需要同步修改。把它放在服务或项目根目录附近，降低遗漏 Docker 构建更新的风险。</p>\n<p>后续也是多学习了一下 Docker，Docker 不只是一个方便部署的镜像工具，更重要的是它能把应用的运行环境标准化、可复现化。它把代码、运行时、依赖和启动方式描述清楚，让开发、测试和部署环境尽可能一致。</p>\n<p>简单来说，像是我之前启动一个项目需要分别执行：</p>\n<pre><code class=\"language-bash\">cargo run\nnpm run dev\n</code></pre>\n<p>之类的。如果后续还需要加入更多的服务，类似于 PostgreSQL 等等，都是要分别手动启动。使用 Docker Compose 后，可以将这些服务及其网络、端口、环境变量、数据卷统一定义，一条命令启动完整环境。</p>\n<p>这也让我理解了为什么 Docker 配置文件应该放在项目根目录，而不是单独放在 <code>platform</code> 目录下。</p>\n<p>优化后的项目结构：</p>\n<pre><code class=\"language-text\">chatbot-demo/\n├─ apps/\n│  ├─ api/                         # Rust 后端 API\n│  │  ├─ src/\n│  │  │  ├─ app/                   # 应用启动、路由、状态\n│  │  │  ├─ domain/                # 领域模型\n│  │  │  ├─ features/\n│  │  │  │  ├─ deployment/         # 部署相关功能\n│  │  │  │  └─ sites/              # 站点生成与管理\n│  │  │  ├─ integrations/          # 外部服务集成\n│  │  │  ├─ templates/             # 模板生成逻辑\n│  │  │  ├─ main.rs\n│  │  │  ├─ metadata.rs\n│  │  │  └─ quality.rs\n│  │  ├─ resources/\n│  │  │  └─ templates/\n│  │  │     └─ react-vite/         # 生成站点使用的 React 模板\n│  │  ├─ Cargo.toml\n│  │  ├─ Cargo.lock\n│  │  ├─ Dockerfile\n│  │  └─ .dockerignore\n│  │\n│  └─ web/                         # Next.js 前端\n│     ├─ app/                       # 页面与路由\n│     ├─ components/                # 通用组件\n│     ├─ features/forge/            # Forge 业务模块\n│     ├─ hooks/                     # React Hooks\n│     ├─ lib/                       # 工具函数与客户端\n│     ├─ scripts/                   # 前端脚本\n│     ├─ tests/                     # 前端测试\n│     ├─ package.json\n│     ├─ package-lock.json\n│     ├─ Dockerfile\n│     └─ next.config.mjs\n│\n├─ platform/\n│  ├─ gateway/\n│  │  └─ config/                    # Caddy 网关配置\n│  └─ scripts/                      # 启动、代理、质量检查脚本\n│\n├─ quality/                         # 质量检查场景与报告目录\n├─ docs/                            # 项目文档\n├─ skills/                          # 项目使用的设计技能包\n├─ runtime/                         # 本地运行时数据，不提交\n│\n├─ docker-compose.yml               # 整个项目的一键启动入口\n├─ README.md\n└─ .gitignore\n</code></pre>\n<h4 id=\"23-docker-应该如何使用\">2.3 Docker 应该如何使用</h4>\n<p>要想使用需要包含以下四个配置分别是：Dockerfile 服务如何构建、如何启动，compose.yaml多个服务如何一键协作启动、.dockerignore哪些文件不应打入镜像、.env / .env.example 运行配置和敏感信息如何管理。\n2.3.1首先是Dockerfile就像是构建说明书，告诉Docker从什么基础环境开始、复制哪些代码、执行哪些构建命令，以及容器启动后运行什么。\n以Rust 示例：</p>\n<pre><code class=\"language-text\"># 使用带 Rust 编译器的基础镜像\nFROM rust:1.85 AS builder\n\n# 容器内的工作目录\nWORKDIR /app\n\n# 复制后端代码\nCOPY backend/ .\n\n# 编译 Release 可执行文件\nRUN cargo build --release\n\n# 第二阶段：使用更小的运行镜像\nFROM debian:bookworm-slim\n\nWORKDIR /app\n\n# 只从编译阶段拿最终程序，不带编译器和源码\nCOPY --from=builder /app/target/release/backend /app/backend\n\n# 程序监听的端口，仅作说明\nEXPOSE 8080\n\n# 容器启动时执行的命令\nCMD [&quot;/app/backend&quot;]\n</code></pre>\n<p>2.3.2compose.yaml就像是系统说明书，把多个容器组织到一起例如：</p>\n<pre><code class=\"language-yaml\">services:\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}\n      POSTGRES_DB: ${POSTGRES_DB}\n    ports:\n      - &quot;5432:5432&quot;\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n\n  backend:\n    build:\n      context: .\n      dockerfile: backend/Dockerfile\n    environment:\n      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}\n    ports:\n      - &quot;8080:8080&quot;\n    depends_on:\n      - db\n\nvolumes:\n  postgres_data:\n</code></pre>\n<p>像是这份这份 compose.yaml 定义了一套由 PostgreSQL 和后端服务组成的开发环境：db 服务使用 PostgreSQL 16，并从 .env 读取数据库账号、密码和库名，将电脑的 5432 映射到容器端口，同时通过 postgres_data 卷持久保存数据；backend 服务则根据项目根目录下的 backend/Dockerfile 构建镜像，将电脑的 8080 映射到后端容器的 8080，并使用 DATABASE_URL 通过 Docker 内部服务名 db:5432 连接数据库，depends_on 表示 Compose 会先启动数据库容器再启动后端容器。\n2.3.3\n.env 与 .env.example不同电脑、不同环境的配置不同；密码更不能写进代码或 Dockerfile。因此使用环境变量。</p>\n<pre><code class=\"language-text\">POSTGRES_USER=appuser\nPOSTGRES_PASSWORD=your-local-password\nPOSTGRES_DB=appdb\n</code></pre>\n<p>Compose 中的 ${POSTGRES_USER} 会读取这里的值。还有就是.env是自己实际使用的本地配置，记得加入 .gitignore，不能提交\n2.3.4.dockerignore\n.dockerignore 主要就是告诉Docker哪些文件不要进入镜像，用于排除不该打进镜像的内容：</p>\n<pre><code class=\"language-text\"># Git 与编辑器文件\n.git\n.gitignore\n.vscode\n.idea\n\n# 密钥与本地配置\n.env\n.env.*\n#等等...\n</code></pre>\n<p>不把 .env、密钥等敏感信息打进镜像，还有就是避免将开发垃圾文件或旧构建产物混入正式镜像。</p>\n<h4 id=\"24-docker-和-postgresql\">2.4 Docker 和 PostgreSQL</h4>\n<p>在了解 Docker 的过程中，我发现它不仅用于打包和部署服务，也很适合快速启动 PostgreSQL 这类开发依赖。以前如果要使用数据库，可能需要在本机单独安装、配置版本和账号密码；而使用 Docker 后，可以通过统一的配置快速启动一个隔离的 PostgreSQL 环境，并通过数据卷保留数据库数据。</p>\n<p>Docker 和 PostgreSQL 是相互配合的，Docker 解决 PostgreSQL 的启动、运行环境和版本统一问题，PostgreSQL 则负责业务数据的存储、查询和管理。这也是后续着重学习的方向。</p>\n<h4 id=\"25-caddy\">2.5 caddy</h4>\n<p>对 Caddy 进行了一些了解：\nCaddy是一个Web 服务器和反向代理工具。它通常部署在业务服务之前，对外负责域名访问、HTTPS、安全连接和请求转发；后端应用只需要专注业务逻辑，例如运行在 8080、3000 之类的内部端口。\n我认为 Caddy 最突出的作用是用较少配置得到正确的默认结果。传统方案里，配置 HTTPS 往往还要额外申请证书、编写续期脚本、处理 HTTP 跳转和重载服务；Caddy 把这些整合为默认能力。只要域名解析正确、80 和 443 端口开放，它通常可以自动申请和续期证书。\n与 Nginx 相比，Caddy 的优势在于配置更简洁、自动 HTTPS 体验更好、适合快速上线和日常维护。Nginx 的优势则是生态成熟、资料丰富、并发等，如果是小中型项目、独立服务、快速部署的话我认为可以优先考虑 Caddy。\n还需要了解一下Cadyy一些基础配置，像是本项目也有用写到的配置反向代理:</p>\n<pre><code>http://example.localhost {\n\thandle /api/* {\n\t\treverse_proxy 127.0.0.1:4173\n\t}\n\n\thandle /healthz {\n\t\treverse_proxy 127.0.0.1:4173\n\t}\n\n\thandle {\n\t\treverse_proxy 127.0.0.1:3000\n\t}\n}\n\nhttp://*.example.localhost {\n\treverse_proxy 127.0.0.1:4173\n}\n</code></pre>\n<p>简单来说，Caddy 在这里相当于一个本地流量分发器：主域名下的 API 和健康检查请求去 4173，普通页面请求去 3000，而所有子域名请求统一去 4173。其中 reverse_proxy 就是反向代理，浏览器以为自己访问的是 example.localhost，实际上 Caddy 在后台替它访问了 127.0.0.1:3000 或 127.0.0.1:4173。\n除此之外Caddy还有一些常用功能的配置例如：\n多服务负载均衡</p>\n<pre><code>api.example.com {\n    reverse_proxy 127.0.0.1:8081 127.0.0.1:8082 127.0.0.1:8083 {\n        lb_policy round_robin\n        health_uri /actuator/health\n    }\n}\n</code></pre>\n<p>Caddy 会以轮询方式向多个后端实例分发请求，并检查健康状态。\n使用已有 HTTPS 证书,本地或内网 HTTPS等。</p>\n<h3 id=\"3-rust-的理解\">3. Rust 的理解</h3>\n<p>Rust 虽然说是过了一遍，但是也是一知半解，这几天也是一直在思考 Rust 的特质是啥，和其他语言的区别是啥。也是一遍学习一遍思考，在继续学习的过程中，我开始把 Rust 和自己比较熟悉的 C++ 放在一起思考。两者都强调性能，也都可以用于系统编程，但它们在内存管理和并发安全方面采取了不同的思路。</p>\n<p>我注意到以下几个问题的区别。</p>\n<h4 id=\"内存泄漏\">内存泄漏</h4>\n<p>像是内存泄漏问题，程序申请了一块内存，但后来找不到它了，也没有释放它。在 C++ 中：</p>\n<pre><code class=\"language-cpp\">void work() {\n    int* p = new int(42);\n}\n</code></pre>\n<p><code>p</code> 是一个指针变量，函数结束后指针没了，但堆上的 <code>42</code> 还占着内存。反复调用 <code>work()</code>，内存会越来越少。正确的写法应该是要加上 <code>delete</code>，但人工 <code>delete</code> 很容易漏掉、重复释放，或者在异常路径下没有执行。</p>\n<p>在 Rust 中：</p>\n<pre><code class=\"language-rust\">fn work() {\n    let value = Box::new(42);\n}\n</code></pre>\n<p>当 <code>value</code> 离开作用域时，Rust 自动释放 <code>Box</code> 里的内存。这就涉及到 Rust 自身的特殊默认规则了，在生命周期结束的时候释放数据。我觉得这也算是 Rust 严谨的一个特性，Rust 能避免大多数由忘记释放造成的内存泄漏。</p>\n<h4 id=\"使用已释放内存\">使用已释放内存</h4>\n<p>除此之外还有类似于使用已释放内存，像是 C++：</p>\n<pre><code class=\"language-cpp\">int* p = new int(42);\ndelete p;\n\nstd::cout &lt;&lt; *p;\n</code></pre>\n<p>使用已释放内存，它可能打印垃圾值，也可能直接崩溃。</p>\n<p>但是在 Rust：</p>\n<pre><code class=\"language-rust\">let value = Box::new(42);\ndrop(value);\n\nprintln!(&quot;{value}&quot;);\n</code></pre>\n<p>直接会编译错误，Rust 知道 <code>value</code> 已经被销毁，因此禁止再次使用。</p>\n<h4 id=\"数据竞争\">数据竞争</h4>\n<p>还有就是数据竞争的问题，多个线程同时访问同一份数据时，其中至少有一个线程会修改数据，而访问过程又没有经过正确同步。</p>\n<pre><code class=\"language-cpp\">int counter = 0;\n\nvoid add() {\n    counter = counter + 1;\n}\n\nthread1(add);\nthread2(add);\n</code></pre>\n<p>表面上看，两个线程各自加一次，结果应该是 <code>2</code>。但实际上，最终结果可能只有 <code>1</code>，多个线程执行这些步骤时，顺序可能交错。由于线程调度是不确定的，这种问题有时出现，有时不出现，因此非常难以测试和定位。</p>\n<p>在 C++ 中，这种问题一般的解法是加锁，需要自己使用互斥锁来解决，但是是否安全主要依赖程序自己判断。如果忘记加锁，或者某个代码路径没有正确加锁，编译器通常不会主动阻止。</p>\n<p>Rust 则会限制这种行为，像是：</p>\n<pre><code class=\"language-rust\">use std::thread;\n\nfn main() {\n    let mut counter = 0;\n\n    thread::scope(|scope| {\n        scope.spawn(|| {\n            counter += 1;\n        });\n\n        scope.spawn(|| {\n            counter += 1;\n        });\n    });\n}\n</code></pre>\n<p>Rust 会拒绝这段代码，因为两个线程都想获得 <code>counter</code> 的可变访问权。编译器无法确认它们之间是否安全，因此直接报错。如果 Rust 程序确实需要让多个线程共享并修改数据，就必须明确使用同步工具：<code>Arc</code> 和 <code>Mutex</code>。当初看到并发章节的时候也是不太理解这些为啥要这么写，现在也是有点理解了。</p>\n<p>因此，Rust 并不是禁止多线程共享数据，而是要求明确表达共享和修改的方式。</p>\n<p>所以 Rust 语言来说不是绝对不会出错，而是把很多 C++ 中要靠经验、规范和代码审查保证的问题，提前变成了编译器可以检查的问题。从而规避掉一些在真实生产环境下才会暴露的问题。</p>\n<h3 id=\"资料来源\">资料来源</h3>\n<p><a href=\"https://zh.wikipedia.org/wiki/%E9%A7%9D%E5%B3%B0%E5%BC%8F%E5%A4%A7%E5%B0%8F%E5%AF%AB\">驼峰命名法</a>\n<a href=\"https://docs.docker.com/get-started/docker-overview/\">Docker 官方概览</a>\n<a href=\"https://zh.wikipedia.org/wiki/Caddy\">Caddy 介绍</a>、<a href=\"https://caddyserver.com/docs/\">官方文档</a>\n<a href=\"https://rustwiki.org/zh-CN/book/ch16-03-shared-state.html?highlight=Arc#mutex%E7%9A%84-api\">Rust 并发共享状态</a></p>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "项目结构docker-与-rust-的理解",
        "text": "项目结构、Docker 与 Rust 的理解"
      },
      {
        "depth": 3,
        "slug": "1-blog-项目的重新优化结构",
        "text": "1. Blog 项目的重新优化结构"
      },
      {
        "depth": 3,
        "slug": "2-chatbot-项目的优化结构",
        "text": "2. Chatbot 项目的优化结构"
      },
      {
        "depth": 4,
        "slug": "21-packages-的职责",
        "text": "2.1 packages 的职责"
      },
      {
        "depth": 4,
        "slug": "22-docker-的位置和理解",
        "text": "2.2 Docker 的位置和理解"
      },
      {
        "depth": 4,
        "slug": "23-docker-应该如何使用",
        "text": "2.3 Docker 应该如何使用"
      },
      {
        "depth": 4,
        "slug": "24-docker-和-postgresql",
        "text": "2.4 Docker 和 PostgreSQL"
      },
      {
        "depth": 4,
        "slug": "25-caddy",
        "text": "2.5 caddy"
      },
      {
        "depth": 3,
        "slug": "3-rust-的理解",
        "text": "3. Rust 的理解"
      },
      {
        "depth": 4,
        "slug": "内存泄漏",
        "text": "内存泄漏"
      },
      {
        "depth": 4,
        "slug": "使用已释放内存",
        "text": "使用已释放内存"
      },
      {
        "depth": 4,
        "slug": "数据竞争",
        "text": "数据竞争"
      },
      {
        "depth": 3,
        "slug": "资料来源",
        "text": "资料来源"
      }
    ]
  },
  {
    "id": "2026-08-11",
    "title": "8/11项目结构的思路与学习",
    "description": "项目结构的思路与学习",
    "publishedAt": "2026-08-11",
    "draft": false,
    "html": "<h3 id=\"项目结构的思路与学习\">项目结构的思路与学习</h3>\n<p>今天我重新梳理了官网项目的目录结构。此前我对“项目结构”的理解更多停留在文件分类：页面放页面目录、组件放组件目录，能实现功能就继续往下写。这种做法没有真正回答“这段代码为什么在这里”“它未来由谁维护”“它会影响哪些模块”。</p>\n<h3 id=\"1-代码应按业务归属和变化范围组织\">1. 代码应按业务归属和变化范围组织</h3>\n<p>首先，首页、新闻、产品、解决方案等是不同业务模块。一个只服务新闻的卡片、数据或页面，应该和新闻业务放在一起；不能因为它“看起来像一个通用组件”，就过早放进全局 <code>components</code>。</p>\n<p>只有当代码已经被多个业务真实使用，而且对外接口稳定时，才值得抽取为共享组件、通用 hook 或工具。这样可以避免共享目录逐渐变成什么都往里塞的“杂物间”。</p>\n<h3 id=\"2-需要关注依赖方向而不仅是文件位置\">2. 需要关注依赖方向，而不仅是文件位置</h3>\n<p>应用层负责组装路由、布局和业务模块；业务模块可以使用共享组件、工具和配置；但共享层不应该反过来依赖某一个具体业务。</p>\n<p>如果共享组件需要 import 新闻、产品等具体业务的数据或组件，就说明边界可能没有划清楚。不能只看“能不能跑”，还要看依赖方向是否合理。</p>\n<h3 id=\"3-工程质量需要靠校验机制\">3. 工程质量需要靠校验机制</h3>\n<p>项目已有 lint、类型检查、环境配置校验、路由校验、目录结构校验和生产构建等机制。它们的意义不是增加流程，而是把容易遗漏的约定变成可以重复执行的检查。</p>\n<p>例如新增路由时，不能只修改页面；还需要同步维护路径定义、路由注册和对应校验。新增图片时，也需要遵守资源目录和环境路径规则。</p>\n<h3 id=\"4-目录模板不能机械套用\">4. 目录模板不能机械套用</h3>\n<p>我也认识到 <code>features</code>、<code>components</code>、<code>hooks</code> 这套结构更适合多业务前端项目；后端、移动端、CLI、数据或 AI 项目应先遵守各自语言和框架的约定。</p>\n<p>我需要学习的不是背一套目录，而是根据项目类型判断：代码的职责是什么、谁使用它、它会和谁一起变化、怎样验证这个设计是合理的。</p>\n<h2 id=\"我需要养成的工作习惯\">我需要养成的工作习惯</h2>\n<p>后续每次开发前，我会先完成以下判断，再开始写代码：</p>\n<ol>\n<li>这次需求属于哪个业务模块？</li>\n<li>本次会修改哪些文件，为什么是这些文件？</li>\n<li>是否新增路由、静态资源、配置或对外接口？</li>\n<li>这段代码是业务专属，还是已经存在真实的跨业务复用？</li>\n<li>改动完成后，应该用什么命令或流程验证？</li>\n</ol>\n<h2 id=\"后续行动\">后续行动</h2>\n<p>将之前的项目chat bot 按照思路进行一下结构的优化。</p>\n<h3 id=\"开始时的状态\">开始时的状态</h3>\n<p>今天把之前的 chatbot-demo 作为一次实际练习进行了整理。项目最初能运行，但仓库根目录同时放着 Rust 后端源码、Next.js 前端、React/Vite 站点模板、Caddy 配置、Docker 文件、本地脚本、运行数据和质量文件。功能本身没有问题，但第一次打开仓库时很难判断每个目录是“可运行的应用”“平台配置”还是“本地生成的数据”。</p>\n<p>当时前端在 <code>web/</code>，Rust 源码直接在根目录 <code>src/</code>，站点模板在 <code>templates/</code>，部署文件在 <code>deploy/</code>，脚本在 <code>scripts/</code>。这会带来两个问题：一是目录名称只能说明技术或历史来源，不能说明职责；二是后续移动文件时，Docker、脚本、编译期资源引用和视觉检查路径容易漏改。</p>\n<p>优化前的目录大致如下：</p>\n<pre><code class=\"language-text\">chatbot-demo/\n├─ src/                         # Rust API 源码直接放在根目录\n├─ web/                         # Next.js 工作台前端\n├─ templates/react-vite/        # 用于生成网站的 React/Vite 模板\n├─ deploy/                      # Caddy 配置\n├─ scripts/                     # 启动、验证、质量检查脚本\n├─ tools/caddy/                 # 本地 Caddy 二进制文件\n├─ data/                        # SQLite 等本地数据\n├─ uploads/                     # 项目上传与生成文件\n├─ Dockerfile\n├─ docker-compose.yml\n├─ Cargo.toml\n├─ Cargo.lock\n└─ ROADMAP.md\n</code></pre>\n<p>可以看到，应用代码、部署配置、工具、运行数据和仓库级文件都处于同一层级。虽然每个目录单独看都能理解，但整体上缺少一条明确的组织主线。</p>\n<h3 id=\"优化思路\">优化思路</h3>\n<p>这次没有单纯按“文件类型”重新归类，而是先区分每部分在整个产品里的角色：</p>\n<ol>\n<li>可独立启动的程序放进 <code>apps/</code>；</li>\n<li>被应用复用、用于生成网站的工程模板放进 <code>packages/</code>；</li>\n<li>Docker、Caddy、启动脚本等运行平台能力统一放进 <code>platform/</code>；</li>\n<li>SQLite、上传内容、构建缓存等本地运行数据统一放进 <code>runtime/</code>，并通过 <code>.gitignore</code> 排除；</li>\n<li>路线图放进 <code>docs/</code>，质量场景规则保留在 <code>quality/</code>，生成报告不提交。</li>\n</ol>\n<p>整理时的关键不是移动目录本身，而是同步检查所有路径依赖：Rust 的 <code>include_str!</code>、模板的相对路径、Docker Compose 的构建上下文、PowerShell 脚本、Caddy 配置，以及视觉检查脚本的位置。实际调试中也发现，视觉检查仍指向旧的 <code>web/scripts/visual-check.mjs</code>，导致构建成功后被错误判定为检查失败。修正为新路径后，真实生成项目的构建和视觉检查都通过了。</p>\n<h3 id=\"优化后的项目结构\">优化后的项目结构</h3>\n<pre><code class=\"language-text\">chatbot-demo/\n├─ apps/\n│  ├─ forge-api/                 # Rust + Axum API、生成编排、构建与视觉检查\n│  └─ forge-workbench/           # Next.js 工作台前端\n├─ packages/ **\n│  └─ site-template/react-vite/  # 受控生成网站的 React/Vite 模板\n├─ platform/\n│  ├─ docker/                    # Dockerfile 与 Compose\n│  ├─ gateway/config/            # Caddy 路由配置\n│  └─ scripts/                   # 启动、验证和质量检查脚本\n├─ runtime/                      # 本地数据库、上传文件和运行数据（不提交）\n├─ quality/\n│  ├─ scenarios.json             # 可提交的质量规则\n│  └─ reports/                   # 本地测试报告（不提交）\n├─ docs/                         # 项目文档与路线图\n├─ skills/                       # 本地 UI/UX 辅助能力\n└─ README.md                     # 架构、目录与启动说明\n</code></pre>\n<p>整理后，看到目录名称就能知道它服务于什么：要启动产品看 <code>apps/</code>，要改基础设施看 <code>platform/</code>，要调整生成模板看 <code>packages/</code>，本地运行留下的数据则只在 <code>runtime/</code>。根目录也只保留仓库级说明和忽略规则，减少了“所有东西都堆在顶层”的感觉。</p>\n<h3 id=\"后续维护原则\">后续维护原则</h3>\n<p>项目结构优化不是一次性搬家，而是建立边界。后续新增功能时，要先判断它属于应用、可复用模板、平台能力还是本地运行数据；新增路径后，要把相关脚本、容器配置和测试一起更新。每次目录调整完成后，也需要像功能开发一样执行构建、测试和真实运行验证，避免“结构看起来更整齐，但运行链路已经断掉”。</p>\n",
    "headings": [
      {
        "depth": 3,
        "slug": "项目结构的思路与学习",
        "text": "项目结构的思路与学习"
      },
      {
        "depth": 3,
        "slug": "1-代码应按业务归属和变化范围组织",
        "text": "1. 代码应按业务归属和变化范围组织"
      },
      {
        "depth": 3,
        "slug": "2-需要关注依赖方向而不仅是文件位置",
        "text": "2. 需要关注依赖方向，而不仅是文件位置"
      },
      {
        "depth": 3,
        "slug": "3-工程质量需要靠校验机制",
        "text": "3. 工程质量需要靠校验机制"
      },
      {
        "depth": 3,
        "slug": "4-目录模板不能机械套用",
        "text": "4. 目录模板不能机械套用"
      },
      {
        "depth": 2,
        "slug": "我需要养成的工作习惯",
        "text": "我需要养成的工作习惯"
      },
      {
        "depth": 2,
        "slug": "后续行动",
        "text": "后续行动"
      },
      {
        "depth": 3,
        "slug": "开始时的状态",
        "text": "开始时的状态"
      },
      {
        "depth": 3,
        "slug": "优化思路",
        "text": "优化思路"
      },
      {
        "depth": 3,
        "slug": "优化后的项目结构",
        "text": "优化后的项目结构"
      },
      {
        "depth": 3,
        "slug": "后续维护原则",
        "text": "后续维护原则"
      }
    ]
  },
  {
    "id": "2026-08-09-compute-research-and-go",
    "title": "8/3-8/9 工作与学习记录",
    "description": "记录算力中心调研平台创建、Supabase 学习、工程化重构与 Go 学习进度",
    "publishedAt": "2026-08-09",
    "draft": false,
    "html": "<h2 id=\"一项目工作\">一、项目工作</h2>\n<h3 id=\"1-算力中心调研平台创建\">1. 算力中心调研平台创建</h3>\n<p>本周开始创建算力中心调研平台。平台的目标是对算力中心相关信息进行集中整理和展示，后续可用于沉淀调研资料、对比不同算力中心的基础信息，并为进一步分析提供统一的数据入口。</p>\n<p>在开始阶段，先围绕页面结构和信息展示方式进行了梳理，例如调研对象的基本信息、产品或服务能力、所在区域、资源情况和相关说明等。通过先把信息结构理清，再逐步补充页面内容，可以避免一开始只关注视觉效果而忽略后续数据整理和扩展的需求。</p>\n<h3 id=\"2-supabase-数据库学习与使用\">2. Supabase 数据库学习与使用</h3>\n<p>本周学习了 Supabase 的数据库使用方式，并开始思考如何把平台中的调研信息从静态内容转为可维护的数据。</p>\n<p>Supabase 以 PostgreSQL 为基础，除了提供数据库外，也把表管理、SQL 操作、权限控制、认证、存储和 API 能力整合在一个平台中。对于前端项目来说，开发者不需要从零搭建一套数据库服务和基础接口，就可以先创建数据表、配置字段，并通过客户端或 API 读取和写入数据。</p>\n<p>在学习过程中，重点理解了以下几个概念：</p>\n<ul>\n<li><strong>表（table）</strong>：用于保存同一类数据，例如算力中心、调研记录或标签；</li>\n<li><strong>字段（column）</strong>：用于描述每条数据的属性，例如名称、区域、介绍、创建时间；</li>\n<li><strong>行（row）</strong>：一条具体的数据记录；</li>\n<li><strong>SQL</strong>：用于创建表、查询、插入、更新和删除数据；</li>\n<li><strong>PostgreSQL</strong>：Supabase 底层使用的关系型数据库；</li>\n<li><strong>Row Level Security，RLS</strong>：按数据行控制访问权限，决定不同用户可以读取或修改哪些数据。</li>\n</ul>\n<p>例如，算力中心信息可以抽象成一张表：</p>\n<pre><code class=\"language-sql\">create table compute_centers (\n  id bigint generated always as identity primary key,\n  name text not null,\n  region text,\n  description text,\n  created_at timestamptz default now()\n);\n</code></pre>\n<p>前端后续就不必把每一条调研资料硬编码在页面中，而是可以从数据库查询数据：</p>\n<pre><code class=\"language-typescript\">const { data, error } = await supabase\n  .from(&#39;compute_centers&#39;)\n  .select(&#39;*&#39;);\n</code></pre>\n<p>这样做的意义在于，后续新增或更新调研对象时，可以优先更新数据，而不是每次都修改页面代码。页面负责展示，数据库负责保存和管理数据，职责会更清晰。</p>\n<h3 id=\"3-从静态-html-转向-nextjs-16-react-19-typescript\">3. 从静态 HTML 转向 Next.js 16 + React 19 + TypeScript</h3>\n<p>算力中心调研平台最开始是以静态 HTML 页面为基础进行搭建的。当时认为需求比较简单，先快速完成页面展示即可。但随着需要考虑数据维护、组件复用、页面扩展、数据库连接和后续功能迭代，单纯使用静态 HTML 的方式开始不够合适。</p>\n<p>因此，后续将项目调整为工程化的 Next.js 16 + React 19 + TypeScript 技术栈：</p>\n<pre><code class=\"language-text\">Next.js 16\n  └── 路由、页面组织、服务端能力和项目结构\n\nReact 19\n  └── 组件化组织页面和交互状态\n\nTypeScript\n  └── 为数据结构和组件参数提供类型约束\n\nSupabase\n  └── 保存和读取调研平台数据\n</code></pre>\n<p>改成工程化项目后，页面可以拆分成更清晰的模块，例如列表、筛选、详情、表单和导航等；同时也更方便后续连接 Supabase、增加数据管理功能，或扩展不同类型的调研内容。</p>\n<p>这一过程也带来了一个比较重要的反思：一开始把需求理解得过于简单，只想用最轻量的方式快速完成。但即使是看起来不复杂的平台，也应该先考虑它后续是否要维护、是否要接数据、是否会增加页面和交互，以及多人或多次迭代时如何保持代码清晰。</p>\n<pre><code class=\"language-text\">先完成一个页面\n不等于\n完成一个可维护的项目\n\n页面能显示\n不等于\n数据、结构和后续扩展已经准备好\n</code></pre>\n<p>以后面对新的需求时，需要更早建立工程化思维：先判断需求的生命周期和后续变化，再选择合适的技术方案。简单需求可以快速起步，但项目结构、数据边界和可维护性仍然需要提前考虑，而不是等问题变复杂后再被动重构。</p>\n<h2 id=\"二本周感想\">二、本周感想</h2>\n<p>本周最大的收获，是开始更加重视“从页面到项目”的区别。</p>\n<p>静态 HTML 很适合快速验证页面结构和视觉方向，但当项目需要持续维护、接入数据库、增加交互或扩展多个页面时，工程化结构会更有优势。Next.js、React 和 TypeScript 并不是为了让项目显得复杂，而是为了让后续修改有更清楚的边界：组件放在哪里、数据从哪里来、类型如何约束、页面如何组织。</p>\n<p>Supabase 的学习也让我认识到，数据库并不是一个单独放在后端的概念。对于调研平台这类项目，数据模型会直接影响页面如何展示、内容如何维护，以及后续能否方便地进行筛选、查询和更新。先设计好数据表和字段，再连接前端页面，会比把所有内容直接写死在页面中更适合长期维护。</p>\n<h2 id=\"三go-学习进度\">三、Go 学习进度</h2>\n<p>本周开始进行go语言的学习</p>\n<h3 id=\"1-安装基础语法与第一个程序\">1 ：安装、基础语法与第一个程序</h3>\n<p>完成 Go 环境和基础命令的学习，了解了 <code>go run</code>、<code>go fmt</code> 等常用命令，并学习了包、<code>main</code> 函数、变量、基本类型和函数。</p>\n<pre><code class=\"language-go\">package main\n\nimport &quot;fmt&quot;\n\nfunc add(a int, b int) int {\n    return a + b\n}\n\nfunc main() {\n    result := add(3, 4)\n    fmt.Println(result)\n}\n</code></pre>\n<p>Go 的语法比较简洁，例如函数使用 <code>func</code> 定义，变量可以使用 <code>:=</code> 进行类型推断。第一天的练习方向是完成一个支持加减乘除的命令行计算器，并养成运行 <code>go fmt</code> 格式化代码的习惯。</p>\n<h3 id=\"2-流程控制与集合\">2 ：流程控制与集合</h3>\n<p>学习了 <code>for</code>、<code>if</code>、<code>switch</code> 和 <code>defer</code>，以及数组、slice 和 map 的基本使用。</p>\n<p>Go 没有传统语言中完全独立的 <code>while</code> 关键字，循环主要通过 <code>for</code> 完成：</p>\n<pre><code class=\"language-go\">for i := 1; i &lt;= 3; i++ {\n    fmt.Println(i)\n}\n</code></pre>\n<p><code>slice</code> 可以理解为动态长度的数据序列，<code>map</code> 则用于保存键值对。通过统计一句话中每个单词出现次数的练习，开始学习如何使用 map 管理和更新数据：</p>\n<pre><code class=\"language-go\">counts := make(map[string]int)\n\nfor _, word := range words {\n    counts[word]++\n}\n</code></pre>\n<p>这里的 <code>range</code> 用于遍历集合，<code>_</code> 表示当前不需要使用索引位置，只关心每一个单词。</p>\n<h3 id=\"3-结构体指针和方法\">3 ：结构体、指针和方法</h3>\n<p>学习了 <code>struct</code>、指针和方法。结构体用于将相关数据组织在一起，例如可以定义一本书或一条待办事项：</p>\n<pre><code class=\"language-go\">type Book struct {\n    Title  string\n    Author string\n    Pages  int\n}\n</code></pre>\n<p>Go 中的指针用于让函数或方法操作原始数据。使用指针接收者时，方法可以修改结构体本身：</p>\n<pre><code class=\"language-go\">func (b *Book) AddPages(pages int) {\n    b.Pages += pages\n}\n</code></pre>\n<p>这一部分让我开始理解“传值”和“传指针”的区别：传值会得到一份副本，适合不需要修改原数据的场景；传指针则可以修改原对象，但也需要更清楚地知道数据会在哪里被改变。</p>\n<h3 id=\"4-接口与错误处理\">4 ：接口与错误处理</h3>\n<p>学习了 interface、类型断言和 Go 中的 <code>error</code> 处理方式。</p>\n<p>Go 的接口不需要提前显式声明“某个类型要实现哪个接口”，只要一个类型实现了接口要求的方法，就可以被当作该接口使用：</p>\n<pre><code class=\"language-go\">type Describer interface {\n    Describe() string\n}\n\ntype Center struct {\n    Name string\n}\n\nfunc (c Center) Describe() string {\n    return &quot;Compute center: &quot; + c.Name\n}\n</code></pre>\n<p>错误处理方面，Go 通常把错误作为函数的额外返回值显式返回：</p>\n<pre><code class=\"language-go\">func readFile(path string) (string, error) {\n    data, err := os.ReadFile(path)\n    if err != nil {\n        return &quot;&quot;, fmt.Errorf(&quot;read file %q: %w&quot;, path, err)\n    }\n\n    return string(data), nil\n}\n</code></pre>\n<p>调用方需要主动检查 <code>err</code>，而不是依赖异常机制自动跳转。这样的写法一开始会感觉代码多一些，但能让错误处理路径直接出现在函数调用附近，也更容易看出哪些步骤可能失败。</p>\n<p>我对 Go 的整体特点有了初步感受：语言语法比较直接，强调可读性、明确的数据处理和显式错误处理。接下来会继续学习模块、测试、并发和 HTTP API，将基础语法逐渐连接到实际后端项目中。</p>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "一项目工作",
        "text": "一、项目工作"
      },
      {
        "depth": 3,
        "slug": "1-算力中心调研平台创建",
        "text": "1. 算力中心调研平台创建"
      },
      {
        "depth": 3,
        "slug": "2-supabase-数据库学习与使用",
        "text": "2. Supabase 数据库学习与使用"
      },
      {
        "depth": 3,
        "slug": "3-从静态-html-转向-nextjs-16-react-19-typescript",
        "text": "3. 从静态 HTML 转向 Next.js 16 + React 19 + TypeScript"
      },
      {
        "depth": 2,
        "slug": "二本周感想",
        "text": "二、本周感想"
      },
      {
        "depth": 2,
        "slug": "三go-学习进度",
        "text": "三、Go 学习进度"
      },
      {
        "depth": 3,
        "slug": "1-安装基础语法与第一个程序",
        "text": "1 ：安装、基础语法与第一个程序"
      },
      {
        "depth": 3,
        "slug": "2-流程控制与集合",
        "text": "2 ：流程控制与集合"
      },
      {
        "depth": 3,
        "slug": "3-结构体指针和方法",
        "text": "3 ：结构体、指针和方法"
      },
      {
        "depth": 3,
        "slug": "4-接口与错误处理",
        "text": "4 ：接口与错误处理"
      }
    ]
  },
  {
    "id": "2026-08-03-portal-forge-and-rust",
    "title": "7/27-8/3 工作与学习记录",
    "description": "记录坤元智算门户网站运维、Forge 项目展示页面设计与 Rust学习进度",
    "publishedAt": "2026-08-03",
    "draft": false,
    "html": "<h2 id=\"727-83-工作与学习记录\">7/27-8/3 工作与学习记录</h2>\n<h2 id=\"一项目工作\">一、项目工作</h2>\n<h3 id=\"1-坤元智算门户网站运维\">1. 坤元智算门户网站运维</h3>\n<p>本周继续进行坤元智算门户网站的日常运维和主页面维护，主要完成了标题更新、页面跳转修复，以及产品软件页面的新增。</p>\n<h4 id=\"页面跳转修复\">页面跳转修复</h4>\n<p>本周还检查并修复了部分页面跳转问题。网站中的按钮、导航和内容链接看起来是比较基础的功能，但如果目标地址、路由路径或跳转逻辑出现问题，会直接影响用户继续浏览网站。</p>\n<p>此次修复重点是确认：</p>\n<ul>\n<li>点击入口后是否能进入正确页面；</li>\n<li>页面内部链接是否保持一致；</li>\n<li>新增页面是否能从主页面或相关导航正常访问；</li>\n<li>修改后是否影响原有页面的跳转。</li>\n</ul>\n<h4 id=\"新增产品软件页面\">新增产品软件页面</h4>\n<p>本周新增了产品软件页面，用于集中展示相关产品和软件信息。</p>\n<h3 id=\"2-forge-项目介绍与页面设计\">2. Forge 项目介绍与页面设计</h3>\n<p>本周还对 Forge 项目进行了页面设计，并将 Forge 项目介绍内容整理到门户网站中。</p>\n<p>Forge 项目可以理解为一个由 AI 驱动的网站生成与发布平台：用户输入需求，系统分析需求、生成网站代码、完成构建检查和页面截图检查，最后将通过验证的网站部署出去。</p>\n<pre><code class=\"language-text\">用户描述需求\n        ↓\nForge 分析生成目标\n        ↓\n生成或修改网站代码\n        ↓\n执行构建检查\n        ↓\n进行页面截图检查\n        ↓\n部署通过验证的网站\n</code></pre>\n<p>这次主要完成的是 Forge 项目介绍页面的内容整理与视觉设计，让用户可以从门户网站中了解 Forge 的项目定位、核心流程和应用方向。</p>\n<p>页面设计时重点考虑了以下内容：</p>\n<ul>\n<li>Forge 是什么，主要解决什么问题；</li>\n<li>AI 如何参与网站生成流程；</li>\n<li>从需求输入到网站部署的完整链路；</li>\n<li>Forge 适合应用在哪些网站生成或内容展示场景中。</li>\n</ul>\n<p>目前 Forge 页面已经完成设计和内容整理，但暂时还没有接入到服务器环境。因此现阶段主要是完成前端页面和展示内容，后续还需要根据服务器部署方式、访问入口和资源配置进行接入与验证。</p>\n<h2 id=\"二rust-学习进度\">二、Rust 学习进度</h2>\n<p>本周继续学习《The Rust Programming Language》第 17–20 章，内容涵盖异步编程、面向对象相关特性、模式匹配以及高级特性。其中第 20 章内容相对集中和抽象，因此本周重点放在第 20 章。</p>\n<h3 id=\"第-17-章异步编程与-asyncawait\">第 17 章：异步编程与 Async/Await</h3>\n<p>第 17 章主要学习 Rust 中的异步编程。<code>async</code> 用于声明一个异步函数，<code>.await</code> 用于等待异步操作完成：</p>\n<pre><code class=\"language-rust\">async fn fetch_message() -&gt; String {\n    String::from(&quot;hello from async task&quot;)\n}\n\nasync fn main_task() {\n    let message = fetch_message().await;\n    println!(&quot;{message}&quot;);\n}\n</code></pre>\n<p>异步编程的核心不是同时创建很多线程，而是在等待网络、文件或其他耗时操作时，让程序可以先处理其他任务。Rust 使用 <code>Future</code> 表示“未来会完成的计算”，而异步运行时负责实际调度这些任务。通过这一章，我对后端接口、流式消息和 Agent 任务为什么经常使用异步有了更直观的认识。</p>\n<h3 id=\"第-18-章面向对象相关特性\">第 18 章：面向对象相关特性</h3>\n<p>第 18 章介绍了 Rust 如何通过 <code>struct</code>、<code>enum</code>、trait 和封装来组织对象和行为。Rust 没有完全照搬传统面向对象语言中的继承机制，而是更倾向于通过 trait 表达共享行为。</p>\n<pre><code class=\"language-rust\">trait Draw {\n    fn draw(&amp;self);\n}\n\nstruct Button;\n\nimpl Draw for Button {\n    fn draw(&amp;self) {\n        println!(&quot;draw a button&quot;);\n    }\n}\n</code></pre>\n<h3 id=\"第-19-章模式匹配与模式语法\">第 19 章：模式匹配与模式语法</h3>\n<p>第 19 章进一步学习了模式匹配。前面接触过的 <code>match</code>、<code>if let</code> 和 <code>while let</code> 都属于模式匹配的使用方式。模式可以把数据拆开，并根据不同结构执行对应逻辑：</p>\n<pre><code class=\"language-rust\">enum Message {\n    Move { x: i32, y: i32 },\n    Write(String),\n}\n\nfn main() {\n    let message = Message::Move { x: 3, y: 5 };\n\n    match message {\n        Message::Move { x, y } =&gt; println!(&quot;move to ({x}, {y})&quot;),\n        Message::Write(text) =&gt; println!(&quot;message: {text}&quot;),\n    }\n}\n</code></pre>\n<p>模式匹配的作用不只是判断某个值是否相等，还可以同时取出枚举、结构体、元组或引用内部的数据。这使得 Rust 在处理 <code>Option</code>、<code>Result</code> 和复杂数据结构时，能够把不同情况写得比较清楚。</p>\n<p>第 20 章把前面学习的所有权、trait、泛型、模块、闭包和错误处理进一步组合起来，内容包括不安全 Rust、高级 trait、关联类型、完全限定语法、函数指针、闭包返回、宏等。第一遍学习时不需要把每一个语法都完全掌握，重点是建立“这些高级能力分别解决什么问题”的概念。</p>\n<h3 id=\"1-unsafe-rust\">1. Unsafe Rust</h3>\n<p>Rust 默认通过所有权和借用规则来保证内存安全，但某些底层场景需要绕过编译器的一部分检查，这时可以使用 <code>unsafe</code>。</p>\n<pre><code class=\"language-rust\">unsafe {\n    // 在这里可以执行需要人工保证安全的操作\n}\n</code></pre>\n<p><code>unsafe</code> 并不代表整段代码一定有问题，而是表示编译器无法替开发者证明这段操作是否安全，责任需要由开发者承担。</p>\n<p>常见的 <code>unsafe</code> 能力包括：</p>\n<ul>\n<li>解引用原始指针；</li>\n<li>调用不安全函数或方法；</li>\n<li>访问或修改可变静态变量；</li>\n<li>实现不安全 trait；</li>\n<li>调用外部函数接口。</li>\n</ul>\n<p>例如原始指针可以指向一个变量，但 Rust 不会像普通引用一样自动保证它的有效性：</p>\n<pre><code class=\"language-rust\">fn main() {\n    let mut number = 5;\n\n    let raw_pointer = &amp;mut number as *mut i32;\n\n    unsafe {\n        *raw_pointer += 1;\n    }\n\n    println!(&quot;{number}&quot;);\n}\n</code></pre>\n<p>这类代码的重点是：尽量把 <code>unsafe</code> 范围控制得很小，把需要人工确认安全的部分隔离出来。大多数普通业务代码仍然应该优先使用 Rust 的安全抽象。</p>\n<h3 id=\"2-高级-trait-与关联类型\">2. 高级 Trait 与关联类型</h3>\n<p>前面学习 trait 时，trait 可以理解成一组行为约定。第 20 章进一步介绍了关联类型（associated type），它可以让 trait 在定义时声明一种“与实现类型相关的类型”。</p>\n<p>例如标准库中的 <code>Iterator</code> trait：</p>\n<pre><code class=\"language-rust\">pub trait Iterator {\n    type Item;\n\n    fn next(&amp;mut self) -&gt; Option&lt;Self::Item&gt;;\n}\n</code></pre>\n<p>这里的 <code>Item</code> 表示迭代器每次产生的元素类型。不同迭代器可以有不同的 <code>Item</code>，但实现者只需要确定一次，不必像泛型一样在每次使用时重复指定类型。</p>\n<p>第 20 章还介绍了运算符重载。Rust 中的 <code>+</code> 实际上对应 <code>Add</code> trait：</p>\n<pre><code class=\"language-rust\">use std::ops::Add;\n\n#[derive(Debug, Copy, Clone)]\nstruct Point {\n    x: i32,\n    y: i32,\n}\n\nimpl Add for Point {\n    type Output = Point;\n\n    fn add(self, other: Point) -&gt; Point {\n        Point {\n            x: self.x + other.x,\n            y: self.y + other.y,\n        }\n    }\n}\n</code></pre>\n<p>实现后就可以让两个 <code>Point</code> 使用 <code>+</code>：</p>\n<pre><code class=\"language-rust\">let result = Point { x: 1, y: 0 } + Point { x: 2, y: 3 };\n</code></pre>\n<h3 id=\"3-完全限定语法\">3. 完全限定语法</h3>\n<p>当多个 trait 或类型中存在同名方法时，Rust 需要一种明确指定“到底调用哪个方法”的写法，这就是完全限定语法。</p>\n<pre><code class=\"language-rust\">&lt;Type as Trait&gt;::function(...)\n</code></pre>\n<p>例如：</p>\n<pre><code class=\"language-rust\">trait Animal {\n    fn baby_name() -&gt; String;\n}\n\nstruct Dog;\n\nimpl Dog {\n    fn baby_name() -&gt; String {\n        String::from(&quot;Spot&quot;)\n    }\n}\n\nimpl Animal for Dog {\n    fn baby_name() -&gt; String {\n        String::from(&quot;puppy&quot;)\n    }\n}\n\nfn main() {\n    println!(&quot;{}&quot;, Dog::baby_name());\n    println!(&quot;{}&quot;, &lt;Dog as Animal&gt;::baby_name());\n}\n</code></pre>\n<p>第一句调用 <code>Dog</code> 自己实现的方法，第二句则明确调用 <code>Animal</code> trait 中为 <code>Dog</code> 实现的方法。这个语法虽然不常在简单项目中出现，但在阅读复杂库代码时很有用。</p>\n<h3 id=\"4-函数指针与闭包\">4. 函数指针与闭包</h3>\n<p>函数不仅可以被直接调用，也可以作为参数传给其他函数。函数指针的类型写作 <code>fn</code>：</p>\n<pre><code class=\"language-rust\">fn add_one(x: i32) -&gt; i32 {\n    x + 1\n}\n\nfn do_twice(f: fn(i32) -&gt; i32, value: i32) -&gt; i32 {\n    f(value) + f(value)\n}\n\nfn main() {\n    let answer = do_twice(add_one, 5);\n    println!(&quot;{answer}&quot;);\n}\n</code></pre>\n<p>这里 <code>do_twice</code> 不关心传进来的具体函数叫什么，只要求它接收一个 <code>i32</code> 并返回一个 <code>i32</code>。</p>\n<p>函数和闭包有相似之处，但闭包还能捕获外部环境变量。第 20 章还涉及如何返回闭包，这通常需要使用 trait object：</p>\n<pre><code class=\"language-rust\">fn returns_closure() -&gt; Box&lt;dyn Fn(i32) -&gt; i32&gt; {\n    Box::new(|x| x + 1)\n}\n</code></pre>\n<p>因为不同闭包的具体类型不同，编译器无法直接确定返回值大小；使用 <code>Box&lt;dyn Fn(...)&gt;</code> 可以把闭包放到堆上，并通过 trait object 进行统一处理。</p>\n<h3 id=\"5-宏\">5. 宏</h3>\n<p>宏和普通函数不同，宏可以在编译阶段根据输入生成代码。之前经常使用的 <code>println!</code>、<code>vec!</code> 和 <code>assert_eq!</code> 都是宏。</p>\n<pre><code class=\"language-rust\">let values = vec![1, 2, 3];\nprintln!(&quot;{values:?}&quot;);\n</code></pre>\n<p>宏适合处理可变数量参数、重复代码生成或需要在编译期展开的场景。但宏的定义和调试通常比普通函数复杂，所以实际项目中应该优先使用函数和 trait；只有当普通 Rust 代码无法方便解决问题时，再考虑宏。</p>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "727-83-工作与学习记录",
        "text": "7/27-8/3 工作与学习记录"
      },
      {
        "depth": 2,
        "slug": "一项目工作",
        "text": "一、项目工作"
      },
      {
        "depth": 3,
        "slug": "1-坤元智算门户网站运维",
        "text": "1. 坤元智算门户网站运维"
      },
      {
        "depth": 4,
        "slug": "页面跳转修复",
        "text": "页面跳转修复"
      },
      {
        "depth": 4,
        "slug": "新增产品软件页面",
        "text": "新增产品软件页面"
      },
      {
        "depth": 3,
        "slug": "2-forge-项目介绍与页面设计",
        "text": "2. Forge 项目介绍与页面设计"
      },
      {
        "depth": 2,
        "slug": "二rust-学习进度",
        "text": "二、Rust 学习进度"
      },
      {
        "depth": 3,
        "slug": "第-17-章异步编程与-asyncawait",
        "text": "第 17 章：异步编程与 Async/Await"
      },
      {
        "depth": 3,
        "slug": "第-18-章面向对象相关特性",
        "text": "第 18 章：面向对象相关特性"
      },
      {
        "depth": 3,
        "slug": "第-19-章模式匹配与模式语法",
        "text": "第 19 章：模式匹配与模式语法"
      },
      {
        "depth": 3,
        "slug": "1-unsafe-rust",
        "text": "1. Unsafe Rust"
      },
      {
        "depth": 3,
        "slug": "2-高级-trait-与关联类型",
        "text": "2. 高级 Trait 与关联类型"
      },
      {
        "depth": 3,
        "slug": "3-完全限定语法",
        "text": "3. 完全限定语法"
      },
      {
        "depth": 3,
        "slug": "4-函数指针与闭包",
        "text": "4. 函数指针与闭包"
      },
      {
        "depth": 3,
        "slug": "5-宏",
        "text": "5. 宏"
      }
    ]
  },
  {
    "id": "2026-07-26-chatbot-quality-and-rust",
    "title": "7/20-7/26 工作与学习记录",
    "description": "记录 Chatbot 稳定性修复、Agent 工作流优化、多媒体能力调研以及 Rust 学习进度",
    "publishedAt": "2026-07-26",
    "draft": false,
    "html": "<h2 id=\"720-726-工作与学习记录\">7/20-7/26 工作与学习记录</h2>\n<h2 id=\"一项目工作\">一、项目工作</h2>\n<h3 id=\"1-修复-chatbot-下载按钮导致的-hydration-error\">1. 修复 Chatbot 下载按钮导致的 Hydration Error</h3>\n<p>本周首先修复了 Chatbot 中下载按钮的前后端首帧状态不一致问题。</p>\n<p>问题的根源是：服务端渲染页面时，下载按钮的 <code>disabled</code> 状态与客户端 JavaScript 接管页面后的初始状态不同。Next.js 在浏览器中进行 hydration 时，会要求服务端生成的 HTML 与客户端第一次渲染的结果保持一致；一旦两边的属性不同，就会出现 hydration error。</p>\n<pre><code class=\"language-text\">服务端首帧 HTML\n下载按钮：disabled = true\n        ↓\n客户端首次渲染\n下载按钮：disabled = false\n        ↓\nNext.js 发现结构或属性不一致\n        ↓\nHydration error\n        ↓\n后续聊天交互无法正常继续\n</code></pre>\n<p>这个问题表面上出现在下载按钮上，但影响的是整段页面交互。因为 hydration 失败后，React 无法可靠地接管这部分页面，后续聊天消息、按钮点击等交互都会受到影响。</p>\n<p>修复过程中重点是让服务端和客户端在第一次渲染时使用同一份确定的初始状态，等客户端完成挂载并拿到真实状态后，再更新按钮是否可用。通过这次问题，我更清楚地理解了 Next.js 服务端渲染和客户端 hydration 的关系：服务端负责先输出页面，客户端负责接管交互，但两边第一眼看到的页面必须一致。</p>\n<h3 id=\"2-在生成前增加网站行业识别\">2. 在生成前增加网站行业识别</h3>\n<p>本周还优化了网站生成前的需求处理流程。系统不再直接把用户的一句话交给生成模型，而是会先结合用户描述判断目标网站大致属于什么行业或场景，例如餐饮、咖啡店、电商、个人作品集、企业官网等。</p>\n<pre><code class=\"language-text\">用户需求\n        ↓\n识别网站类型 / 行业场景\n        ↓\n补充对应的页面结构、内容重点和视觉方向\n        ↓\n生成网站代码\n</code></pre>\n<p>增加这一步的意义是让后续生成更有针对性。不同类型的网站关注点不同：</p>\n<ul>\n<li>餐饮或咖啡店网站更需要菜单、门店信息、预约或联系入口；</li>\n<li>电商网站更关注商品展示、分类、价格和购买路径；</li>\n<li>企业官网更关注品牌介绍、服务内容、案例和联系方式；</li>\n<li>个人作品集更关注个人介绍、项目作品和技能展示。</li>\n</ul>\n<p>先完成行业识别后，Agent 可以带着更明确的上下文生成页面，而不是只依据通用模板拼出一个结构相似的网站。这也让我理解到，AI 生成网站并不只是“让模型写代码”，前面的需求理解和上下文整理同样决定了最终结果是否贴合用户真实需求。</p>\n<h3 id=\"3-agent-工作流增加截图检查\">3. Agent 工作流增加截图检查</h3>\n<p>之前 Agent 的主要闭环是：生成代码、构建项目、构建成功后部署。本周在构建验证之后增加了截图检查，使 Agent 不只判断“能不能编译”，还会继续检查“页面实际显示出来是否合理”。</p>\n<pre><code class=\"language-text\">用户提出需求\n        ↓\n分析需求并识别网站场景\n        ↓\n生成或修改项目代码\n        ↓\n执行构建\n        ↓\n构建成功\n        ↓\n启动页面并生成截图\n        ↓\n检查页面视觉结果\n        ↓\n通过后部署\n</code></pre>\n<p>单纯的构建成功只能说明代码没有明显的语法、类型或打包错误，但不能说明页面视觉效果一定正确。例如页面可能出现布局错位、文字被遮挡、图片没有显示、按钮位置异常，或者生成内容和用户需求不匹配。这些问题在 <code>npm run build</code> 成功时仍然可能存在。</p>\n<p>截图检查把验证从“代码层面”推进到了“页面结果层面”。如果发现页面存在明显问题，Agent 可以把截图和检查结果作为下一轮修复依据，再回到生成、构建和验证流程中。</p>\n<pre><code class=\"language-text\">代码正确\n不等于\n页面正确\n\n构建检查\n  + 截图检查\n  = 更完整的发布前验证\n</code></pre>\n<p>这次优化让我更能理解 Agent 的完整含义：它不是一次性输出答案，而是能够观察结果、发现问题、继续调整的执行闭环。</p>\n<h3 id=\"4-新增图片生成-api-入口\">4. 新增图片生成 API 入口</h3>\n<p>本周新增了图片生成 API 入口，使网站生成流程可以调用图像生成能力，为页面提供更贴合主题的视觉素材。</p>\n<p>在没有图片生成能力时，网站通常只能使用占位图、外部图片地址或简单的纯色布局；新增统一 API 入口后，系统可以把页面主题、行业场景和视觉需求传递给图片生成服务，再将生成结果用于网站页面。</p>\n<pre><code class=\"language-text\">用户需求\n        ↓\n识别行业和页面主题\n        ↓\n构造图片生成提示词\n        ↓\n调用图片生成 API\n        ↓\n获得图片资源\n        ↓\n写入或引用到生成的网站中\n</code></pre>\n<p>把图片生成能力设计成 API 入口，而不是直接耦合到页面代码中，有利于后续替换不同的图片模型或服务，也能让 Agent 在需要视觉素材时通过统一方式调用。对于 AI 生成网站来说，文字、布局和图片共同决定最终体验；图片能力接入后，生成的网站会更接近完整的可展示页面。</p>\n<h3 id=\"5-视频模型调研\">5. 视频模型调研</h3>\n<p>本周对 Kling、Seedance 以及国内数字人产品进行了初步调研，重点不只是比较“哪家画面更好”，而是区分它们属于视频模型还是完整数字人平台，并关注后续 API 接入时的成本和产品边界。</p>\n<p>首先，Kling Avatar 2.0、Seedance 2.0、腾讯云智能数智人、阿里云灵眸和火山引擎数字人并不是同一类产品：</p>\n<table>\n<thead>\n<tr>\n<th>产品</th>\n<th>产品定位</th>\n<th>更接近的使用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Kling Avatar 2.0</td>\n<td>异步数字人视频生成</td>\n<td>人物图片、音频和表演提示生成口播或营销视频</td>\n</tr>\n<tr>\n<td>Seedance 2.0</td>\n<td>通用多模态视频生成模型</td>\n<td>人物、商品、场景和音频素材共同生成创意视频</td>\n</tr>\n<tr>\n<td>腾讯云智能数智人</td>\n<td>企业级数字人平台</td>\n<td>形象、播报、实时互动、云/端渲染和并发管理</td>\n</tr>\n<tr>\n<td>阿里云灵眸</td>\n<td>数字人 PaaS/SaaS 平台</td>\n<td>视频合成、实时会话、数字人资产和渲染接入</td>\n</tr>\n<tr>\n<td>火山引擎数字人</td>\n<td>多组数字人与视觉生成能力</td>\n<td>克隆形象、单图音频驱动、改口型和视频驱动</td>\n</tr>\n</tbody></table>\n<p>其中，Seedance 并不是专门的数字人平台。它可以生成人物说话、动作和口型效果，但更偏向通用视频创作；如果后续要做固定形象管理、长时口播或实时互动，还需要配合 TTS、人物资产、任务管理和内容审核等能力。Kling Avatar 2.0 的输入和产品示例则更集中于“固定人物说话和表演”，但网页端能力是否以同等参数开放为开发者 API，仍需要在接入前确认。</p>\n<p>价格方面，各家的计费单位并不统一，不能简单按每秒价格直接排序：</p>\n<table>\n<thead>\n<tr>\n<th>产品/能力</th>\n<th>当前调研到的计费口径</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Kling Avatar 2.0</td>\n<td>Studio 侧 Standard 为 4 Credits/秒，Professional 为 8 Credits/秒；与开发者 API 的对应关系需确认</td>\n</tr>\n<tr>\n<td>Seedance 2.0 Mini</td>\n<td>0.023 元/千 Tokens</td>\n</tr>\n<tr>\n<td>Seedance 2.0 Fast</td>\n<td>0.037 元/千 Tokens</td>\n</tr>\n<tr>\n<td>Seedance 2.0 标准版</td>\n<td>0.046 元/千 Tokens</td>\n</tr>\n<tr>\n<td>腾讯云 2D 播报视频</td>\n<td>1800 美元/10 小时，形象、并发等项目单独计费</td>\n</tr>\n<tr>\n<td>阿里云数字人视频合成</td>\n<td>9.9 元/分钟，实时服务与形象定制另行计费</td>\n</tr>\n</tbody></table>\n<p>Seedance 按 Token 计费，实际消耗会受时长、分辨率、输入图片/视频/音频数量影响；腾讯云和阿里云则把形象、视频生成、实时互动和渲染资源拆分计费。因此后续实际选型不应只看单次调用价格，而要统一测试同一条任务的可用率：</p>\n<pre><code class=\"language-text\">同一人物素材\n        + 同一段口播文案或音频\n        + 同样的视频时长与分辨率\n        ↓\n比较生成质量、等待时间、失败率和重试次数\n        ↓\n单条可用视频成本\n= 总消耗 ÷ 最终可用视频数量\n</code></pre>\n<p>从能力边界来看，预录口播视频可以重点测试 Kling Avatar、阿里云视频合成、火山单图音频驱动，以及 Seedance 的人物视频效果；实时数字人场景则需要重点关注腾讯云和阿里云的会话、并发与渲染能力，不能直接用 Kling 或 Seedance 的异步视频生成替代。</p>\n<h2 id=\"二rust-学习进度\">二、Rust 学习进度</h2>\n<p>本周继续学习《The Rust Programming Language》的第 15 章和第 16 章。</p>\n<h3 id=\"第-15-章智能指针\">第 15 章：智能指针</h3>\n<p>学习了 <code>Box&lt;T&gt;</code>、<code>Rc&lt;T&gt;</code>、<code>RefCell&lt;T&gt;</code> 等智能指针的基本概念，以及它们与普通引用的区别。</p>\n<pre><code class=\"language-rust\">fn main() {\n    let number = Box::new(5);\n\n    println!(&quot;{number}&quot;);\n}\n</code></pre>\n<p><code>Box&lt;T&gt;</code> 可以把数据放到堆上，同时由 Box 拥有并负责释放这份数据。它常用于递归类型、较大的数据，或者需要在编译期无法确定具体大小的场景。</p>\n<p>例如递归链表可以使用 <code>Box&lt;List&gt;</code>：</p>\n<pre><code class=\"language-rust\">enum List {\n    Cons(i32, Box&lt;List&gt;),\n    Nil,\n}\n</code></pre>\n<p>通过这一章，我开始理解智能指针不仅是“指向数据的地址”，还会附带所有权、引用计数或运行时借用检查等额外能力。</p>\n<h3 id=\"第-16-章无畏并发\">第 16 章：无畏并发</h3>\n<p>学习了线程、消息传递、共享状态并发，以及 <code>Mutex&lt;T&gt;</code> 和 <code>Arc&lt;T&gt;</code> 的基本使用。</p>\n<p>线程可以让多个任务并行执行：</p>\n<pre><code class=\"language-rust\">use std::thread;\n\nfn main() {\n    let handle = thread::spawn(|| {\n        println!(&quot;running in a new thread&quot;);\n    });\n\n    handle.join().unwrap();\n}\n</code></pre>\n<p>多个线程之间也可以通过 channel 传递消息：</p>\n<pre><code class=\"language-rust\">use std::sync::mpsc;\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel();\n\n    thread::spawn(move || {\n        tx.send(&quot;hello from thread&quot;).unwrap();\n    });\n\n    println!(&quot;{}&quot;, rx.recv().unwrap());\n}\n</code></pre>\n<p>这一章让我认识到，Rust 的并发设计和所有权规则是连在一起的。编译器会帮助检查多个线程是否安全地访问同一份数据，尽量把数据竞争等问题提前暴露在编译阶段。</p>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "720-726-工作与学习记录",
        "text": "7/20-7/26 工作与学习记录"
      },
      {
        "depth": 2,
        "slug": "一项目工作",
        "text": "一、项目工作"
      },
      {
        "depth": 3,
        "slug": "1-修复-chatbot-下载按钮导致的-hydration-error",
        "text": "1. 修复 Chatbot 下载按钮导致的 Hydration Error"
      },
      {
        "depth": 3,
        "slug": "2-在生成前增加网站行业识别",
        "text": "2. 在生成前增加网站行业识别"
      },
      {
        "depth": 3,
        "slug": "3-agent-工作流增加截图检查",
        "text": "3. Agent 工作流增加截图检查"
      },
      {
        "depth": 3,
        "slug": "4-新增图片生成-api-入口",
        "text": "4. 新增图片生成 API 入口"
      },
      {
        "depth": 3,
        "slug": "5-视频模型调研",
        "text": "5. 视频模型调研"
      },
      {
        "depth": 2,
        "slug": "二rust-学习进度",
        "text": "二、Rust 学习进度"
      },
      {
        "depth": 3,
        "slug": "第-15-章智能指针",
        "text": "第 15 章：智能指针"
      },
      {
        "depth": 3,
        "slug": "第-16-章无畏并发",
        "text": "第 16 章：无畏并发"
      }
    ]
  },
  {
    "id": "2026-07-19-chatbot-and-rust",
    "title": "7/13-7/19 工作与学习记录",
    "description": "记录本周门户网站运维、Chatbot 项目开发以及 Rust 学习进度",
    "publishedAt": "2026-07-19",
    "draft": false,
    "html": "<h2 id=\"713-719-工作与学习记录\">7/13-7/19 工作与学习记录</h2>\n<h2 id=\"一项目工作\">一、项目工作</h2>\n<h3 id=\"1-坤元智算官网门户网站运维\">1. 坤元智算官网门户网站运维</h3>\n<p>本周继续对坤元智算官网门户网站进行日常运维和内容维护，重点处理了网站运行过程中的基础支持工作，并新增了算力通链接。</p>\n<p>新增链接之后，用户可以从门户网站更方便地进入算力通相关服务，整体访问路径更加连贯。这个工作虽然看起来只是增加一个入口，但仍然需要考虑链接位置、页面展示、跳转是否正常，以及上线后是否会影响原有页面结构和用户使用习惯。</p>\n<h3 id=\"2-chatbot-项目开发\">2. Chatbot 项目开发</h3>\n<p>本周主要投入在 Chatbot 项目的开发和整体架构理解上。这个项目并不是一个简单的聊天页面，而是一个可以根据用户需求生成、修改、构建并发布网站的 AI 应用平台。</p>\n<p>项目的大致流程是：</p>\n<pre><code class=\"language-text\">用户输入自然语言需求\n        ↓\n前端工作台发送 API 请求\n        ↓\nRust 后端接收并整理项目上下文\n        ↓\n调用 AI 模型生成代码修改\n        ↓\n后端校验并直接应用修改\n        ↓\n构建生成的网站\n        ↓\n发布到对应的子域名\n</code></pre>\n<h4 id=\"前端nextjs-react\">前端：Next.js + React</h4>\n<p>项目的管理工作台使用 Next.js 和 React 实现，主要负责提供用户操作界面，例如：</p>\n<ul>\n<li>展示项目列表和项目详情；</li>\n<li>输入自然语言需求；</li>\n<li>查看 AI 返回的消息；</li>\n<li>显示文件变更和待审阅内容；</li>\n<li>接受或拒绝 AI 生成的修改；</li>\n<li>查看构建状态和部署结果。</li>\n</ul>\n<p>选择 Next.js 和 React，主要是因为 React 适合把页面拆分成多个可复用组件，而 Next.js 在 React 基础上提供了更完整的项目结构和工程能力。对于这种包含项目列表、聊天区域、文件变更区域和部署状态的工作台来说，组件化可以让不同区域相对独立，后续修改也更容易控制。</p>\n<p>这个项目里还有一个需要区分的地方：Next.js 工作台和 AI 最终生成的网站不是同一个前端。</p>\n<pre><code class=\"language-text\">Next.js + React\n└── 管理后台，用来创建和管理网站\n\nReact/Vite 项目\n└── AI 实际生成并发布的网站\n</code></pre>\n<p>这样拆分的好处是，管理平台和生成出来的产品互不干扰。工作台负责“生产”，生成的网站才是最终交付给用户访问的“产品”。</p>\n<h4 id=\"后端rust-axum\">后端：Rust + Axum</h4>\n<p>项目后端使用 Rust，并通过 Axum 提供 HTTP API。Rust 后端不是用来直接绘制页面的，而是负责整个系统的业务编排：</p>\n<ul>\n<li>接收前端请求；</li>\n<li>管理项目和会话；</li>\n<li>保存用户消息；</li>\n<li>调用 AI 模型；</li>\n<li>解析 AI 返回的文件修改；</li>\n<li>执行项目构建；</li>\n<li>检查构建结果；</li>\n<li>发布网站并返回访问地址。</li>\n</ul>\n<p>选择 Rust，主要是因为这个项目后端承担了比较多的控制职责，需要处理文件、构建命令、项目版本和发布流程。Rust 的类型系统和错误处理机制可以帮助提前发现一些问题，也能让文件路径、请求数据和业务状态更加明确。对于一个需要执行构建、处理多个服务并且不能轻易破坏旧版本的后端来说，Rust 更适合用来编写这种偏“系统编排器”的部分。</p>\n<p>Axum 则负责把 HTTP 路由和 Rust 函数连接起来。例如前端发送消息时，请求类似：</p>\n<pre><code class=\"language-rust\">// 路由示意\n.route(\n    &quot;/conversations/{conversation_id}/messages&quot;,\n    post(send_message_real),\n)\n</code></pre>\n<p>它表达的意思是：当收到某个会话的 POST 消息请求时，就交给 <code>send_message_real</code> 函数处理。</p>\n<p>后端函数可以从请求中同时拿到共享状态、路径参数和 JSON 数据：</p>\n<pre><code class=\"language-rust\">async fn send_message_real(\n    State(state): State&lt;AppState&gt;,\n    Path(conversation_id): Path&lt;String&gt;,\n    Json(request): Json&lt;MessageRequest&gt;,\n) -&gt; Response {\n    // 查找会话、保存消息、调用模型并返回结果\n}\n</code></pre>\n<p>这让我更清楚地理解了 API 的作用：API 不是一个抽象名词，而是把前端的操作转换成后端可以执行的函数调用。</p>\n<h4 id=\"前后端通信http-api-与-json\">前后端通信：HTTP API 与 JSON</h4>\n<p>前端通过 <code>fetch</code> 调用后端 API。例如部署项目时，前端会发送：</p>\n<pre><code class=\"language-javascript\">const response = await fetch(\n  `/api/v1/projects/${encodeURIComponent(projectId)}/deploy`,\n  { method: &#39;POST&#39; }\n);\n\nconst payload = await response.json();\n</code></pre>\n<p>这段代码的流程是：</p>\n<pre><code class=\"language-text\">用户点击部署按钮\n        ↓\nJavaScript 发起 POST 请求\n        ↓\nRust API 接收请求\n        ↓\n后端执行构建和部署\n        ↓\n返回 JSON 结果\n        ↓\n前端更新页面或打开网站地址\n</code></pre>\n<p>发送聊天消息时，请求体也是 JSON：</p>\n<pre><code class=\"language-javascript\">await fetch(`/api/v1/conversations/${conversationId}/messages`, {\n  method: &#39;POST&#39;,\n  headers: {\n    &#39;Content-Type&#39;: &#39;application/json&#39;\n  },\n  body: JSON.stringify({\n    content: text,\n    attachments: []\n  })\n});\n</code></pre>\n<p>选择 JSON，是因为它结构清晰，前端 JavaScript 和 Rust 后端都容易处理，也适合表达消息、项目、文件变更等结构化数据。</p>\n<h4 id=\"sse-流式消息\">SSE 流式消息</h4>\n<p>Chatbot 没有等模型全部生成完之后才一次性返回，而是使用流式方式把结果逐步发送给前端。这样用户可以看到类似“正在打字”的效果，等待感会更低。</p>\n<p>后端产生的事件可以简单理解为：</p>\n<pre><code class=\"language-text\">token\n    AI 新生成了一小段文字\n\nfile_change\n    AI 生成了文件修改建议\n\nmessage_end\n    当前回复结束\n</code></pre>\n<p>前端会不断读取后端传来的数据：</p>\n<pre><code class=\"language-javascript\">while (true) {\n  const { value, done } = await reader.read();\n\n  if (done) break;\n\n  buffer += decoder.decode(value, { stream: true });\n\n  // 拆分事件并逐步更新页面\n}\n</code></pre>\n<p>选择 SSE，是因为这个场景主要是服务器持续向浏览器推送生成进度，通信方向比较明确，不需要像 WebSocket 那样保持复杂的双向实时通信。对于 AI 回复、构建进度和部署状态这类服务端事件，SSE 结构更直观，也更容易在浏览器端使用。</p>\n<h4 id=\"ai-修改与直接部署机制\">AI 修改与直接部署机制</h4>\n<p>项目没有让 AI 直接执行 Shell 命令，也没有让 AI 无限制地操作服务器。AI 返回文件修改内容后，由后端负责解析、校验并直接应用到项目，然后进入构建和部署流程。</p>\n<pre><code class=\"language-text\">AI 生成文件修改\n        ↓\n平台检查路径和格式\n        ↓\n后端直接应用修改\n        ↓\n执行项目构建\n        ↓\n构建成功后部署\n</code></pre>\n<p>这样设计是因为 AI 的输出不能直接当作可信操作。AI 负责生成代码，平台负责限制文件范围、校验格式并执行构建；只有构建成功后才会部署，从而避免错误代码直接进入线上环境。</p>\n<h4 id=\"agent-工作流\">Agent 工作流</h4>\n<p>这个 Chatbot 项目更像一个带执行能力的 Agent，而不是单纯的问答机器人。它的核心是一个“观察、决策、执行、验证”的循环：</p>\n<pre><code class=\"language-text\">用户提出需求\n        ↓\nAgent 分析当前项目和用户意图\n        ↓\n调用 AI 生成文件修改\n        ↓\n后端解析并应用修改\n        ↓\n执行 npm ci / npm run build\n        ↓\n构建成功？\n   ┌────┴────┐\n   是       否\n   ↓        ↓\n部署上线   读取构建错误\n            ↓\n       反馈给 AI 修复\n            ↓\n        再次构建\n</code></pre>\n<p>Agent 的每一步职责并不完全相同：</p>\n<ul>\n<li><strong>观察</strong>：读取用户需求、当前项目文件、构建日志和运行状态；</li>\n<li><strong>决策</strong>：根据上下文判断需要创建或修改哪些文件；</li>\n<li><strong>执行</strong>：写入文件、运行依赖安装和构建命令；</li>\n<li><strong>验证</strong>：检查构建是否成功、产物是否完整，以及根目录是否存在 <code>index.html</code>；</li>\n<li><strong>修复</strong>：如果构建失败，就提取有限的错误信息交给 AI，让它生成下一轮修复内容。</li>\n</ul>\n<p>自动修复不是无限循环，而是有次数限制。这样可以避免某个错误让 Agent 一直重复构建，也能在多次修复仍然失败时保留问题现场，方便继续排查。</p>\n<p>这条工作流和普通 Chatbot 的区别在于，普通 Chatbot 通常只返回文字：</p>\n<pre><code class=\"language-text\">用户提问 → AI 返回答案\n</code></pre>\n<p>而这个项目会继续执行后续动作：</p>\n<pre><code class=\"language-text\">用户提出需求\n  → AI 生成修改\n  → 系统执行修改\n  → 构建验证\n  → 失败后自动修复\n  → 成功后直接部署\n</code></pre>\n<p>因此，AI 只是 Agent 中负责理解和生成方案的一部分，真正让它成为 Agent 的，是后端把模型输出接入了文件操作、构建、错误反馈和部署流程。</p>\n<h4 id=\"sqlite-与文件系统\">SQLite 与文件系统</h4>\n<p>项目数据分成两类保存：</p>\n<pre><code class=\"language-text\">SQLite\n└── 项目、会话、消息、变更等结构化信息\n\n文件系统\n└── 项目文件、生成文件、构建产物和发布版本\n</code></pre>\n<p>SQLite 适合保存有明确字段、需要查询和关联的数据；项目源文件和构建产物则更适合直接以文件形式保存。这样可以避免把大量代码内容全部塞进数据库，也让构建流程更自然，因为构建工具本来就是读取文件目录。</p>\n<h4 id=\"vite-构建与发布前验证\">Vite 构建与发布前验证</h4>\n<p>AI 生成或修改文件后，后端会在项目目录中执行构建，例如：</p>\n<pre><code class=\"language-powershell\">npm ci\nnpm run build\n</code></pre>\n<p>只有构建成功，并且产物中存在根目录 <code>index.html</code>，才允许进入发布流程：</p>\n<pre><code class=\"language-text\">构建新版本\n        ↓\n检查 dist/index.html\n        ↓\n检查通过后替换当前版本\n</code></pre>\n<p>这样可以避免构建失败后把线上网站替换成半成品。Vite 适合这里的原因是它构建速度快、配置相对简单，并且适合生成独立的前端静态产物。</p>\n<h4 id=\"caddy-反向代理\">Caddy 反向代理</h4>\n<p>项目当前使用 Caddy 作为统一入口，第一版曾经考虑过 Nginx，后来改成 Caddy 主要是为了让本地开发入口和线上部署结构更接近，同时减少反向代理配置。</p>\n<pre><code class=\"language-text\">浏览器\n  ↓\nCaddy :80\n  ├── /api/*       → Rust API :4173\n  ├── 其他路径     → Next.js :3000\n  └── 子域名       → 已发布的网站\n</code></pre>\n<p>Caddy 不负责业务逻辑，它更像一个门卫：根据路径和域名把请求转发给正确的服务。这样浏览器只需要访问一个入口，不必直接区分前端 <code>3000</code> 端口和后端 <code>4173</code> 端口，也能减少跨域和本地/线上访问方式不一致的问题。</p>\n<p>选择 Caddy 而不是继续使用 Nginx，主要是因为当前项目属于中小型 MVP，Caddy 配置更简洁，并且后续配置 HTTPS 更方便。Nginx 仍然是成熟稳定的方案，只是当前项目的路由场景用 Caddy 就已经足够。</p>\n<h4 id=\"docker-compose\">Docker Compose</h4>\n<p>Docker Compose 用来描述整套服务如何启动和连接：</p>\n<pre><code class=\"language-text\">api\n  └── Rust 后端\n\nweb\n  └── Next.js 工作台\n\ncaddy\n  └── 统一入口\n</code></pre>\n<p>选择 Compose，是因为前端和后端运行环境不同，分别管理更清晰；同时又可以用一个配置文件启动整套系统，让本地环境和部署环境尽可能一致。</p>\n<h2 id=\"二本周感想\">二、本周感想</h2>\n<p>这周最大的收获，是开始真正理解前端和后端是如何连通的。以前看到前端页面、API、后端服务和反向代理时，容易把它们当成一堆分散的技术名词；这周通过 Chatbot 项目的真实代码，把它们串成了一条完整链路：</p>\n<pre><code class=\"language-text\">用户点击按钮\n        ↓\n前端 JavaScript 触发事件\n        ↓\nfetch 调用 API\n        ↓\nCaddy 根据路径进行反向路由\n        ↓\nRust/Axum 路由接收请求\n        ↓\n后端执行业务逻辑并直接部署\n        ↓\nJSON 或 SSE 返回结果\n        ↓\n前端更新页面\n</code></pre>\n<p>我也开始理解反向代理存在的意义。它不是“又多加了一个服务”，而是把公网入口和内部服务隔离开：外部只需要访问一个地址，Caddy 再根据 <code>/api</code>、域名或其他路径，把请求交给对应的服务。</p>\n<p>API 也不只是“前后端之间的接口”这么抽象。它实际上定义了双方约定好的通信方式：请求使用什么方法、访问什么路径、发送什么 JSON、后端返回什么数据，以及失败时如何表示错误。只要这个约定一致，前端和后端就可以分别开发。</p>\n<p>这周还让我意识到，一个完整项目的重点不只是把功能写出来，还要考虑：</p>\n<ul>\n<li>AI 生成错误时怎么处理；</li>\n<li>用户是否需要审阅修改；</li>\n<li>构建失败时是否保留旧版本；</li>\n<li>前后端服务如何统一访问；</li>\n<li>数据应该存到数据库还是文件；</li>\n<li>每个模块应该承担什么职责。</li>\n</ul>\n<p>虽然目前还不能独立写完整个项目，但我已经开始能从“请求如何流动、模块如何分工、失败如何处理”的角度去理解项目，而不是只盯着某一行代码。</p>\n<h2 id=\"三rust-学习进度\">三、Rust 学习进度</h2>\n<p>本周 Rust 学习进度完成了《The Rust Programming Language》的第 11 章到第 14 章：</p>\n<h3 id=\"第-11-章自动化测试\">第 11 章：自动化测试</h3>\n<p>学习了如何使用 <code>#[test]</code>、<code>assert!</code>、<code>assert_eq!</code> 和 <code>assert_ne!</code> 编写测试，也了解了 <code>#[should_panic]</code> 和返回 <code>Result</code> 的测试写法。</p>\n<pre><code class=\"language-rust\">fn multiply(a: i32, b: i32) -&gt; i32 {\n    a * b\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn test_multiply() {\n        assert_eq!(multiply(4, 5), 20);\n    }\n}\n</code></pre>\n<p>通过这一章，我开始理解测试不是额外的负担，而是让程序自动检查行为是否符合预期。</p>\n<h3 id=\"第-12-章构建命令行程序\">第 12 章：构建命令行程序</h3>\n<p>完成了 <code>minigrep</code> 项目的学习，串联了命令行参数、文件读取、字符串搜索、<code>Result</code> 错误处理、生命周期和测试。</p>\n<pre><code class=\"language-rust\">fn search&lt;&#39;a&gt;(query: &amp;str, contents: &amp;&#39;a str) -&gt; Vec&lt;&amp;&#39;a str&gt; {\n    let mut results = Vec::new();\n\n    for line in contents.lines() {\n        if line.contains(query) {\n            results.push(line);\n        }\n    }\n\n    results\n}\n</code></pre>\n<h3 id=\"第-13-章闭包与迭代器\">第 13 章：闭包与迭代器</h3>\n<p>学习了闭包、<code>iter</code>、<code>into_iter</code>、<code>map</code>、<code>filter</code>、<code>collect</code>、<code>sum</code>、<code>any</code> 和 <code>all</code>。这些方法可以把集合处理流程写得更加清晰。</p>\n<pre><code class=\"language-rust\">let result: Vec&lt;i32&gt; = numbers\n    .into_iter()\n    .filter(|number| number % 2 == 0)\n    .map(|number| number * 10)\n    .collect();\n</code></pre>\n<p>这段代码可以理解为：取出数字，筛选偶数，把偶数乘以 10，最后收集成新的 <code>Vec</code>。</p>\n<h3 id=\"第-14-章cargo-与-cratesio\">第 14 章：Cargo 与 Crates.io</h3>\n<p>了解了 <code>Cargo.toml</code>、<code>Cargo.lock</code>、依赖管理以及常用命令：</p>\n<pre><code class=\"language-powershell\">cargo check\ncargo build\ncargo run\ncargo test\ncargo fmt\ncargo clippy\n</code></pre>\n<p>这一章让我对 Rust 项目的工程化结构有了更完整的认识。Cargo 不只是运行代码的工具，也负责项目创建、依赖管理、编译、测试和格式化。</p>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "713-719-工作与学习记录",
        "text": "7/13-7/19 工作与学习记录"
      },
      {
        "depth": 2,
        "slug": "一项目工作",
        "text": "一、项目工作"
      },
      {
        "depth": 3,
        "slug": "1-坤元智算官网门户网站运维",
        "text": "1. 坤元智算官网门户网站运维"
      },
      {
        "depth": 3,
        "slug": "2-chatbot-项目开发",
        "text": "2. Chatbot 项目开发"
      },
      {
        "depth": 4,
        "slug": "前端nextjs-react",
        "text": "前端：Next.js + React"
      },
      {
        "depth": 4,
        "slug": "后端rust-axum",
        "text": "后端：Rust + Axum"
      },
      {
        "depth": 4,
        "slug": "前后端通信http-api-与-json",
        "text": "前后端通信：HTTP API 与 JSON"
      },
      {
        "depth": 4,
        "slug": "sse-流式消息",
        "text": "SSE 流式消息"
      },
      {
        "depth": 4,
        "slug": "ai-修改与直接部署机制",
        "text": "AI 修改与直接部署机制"
      },
      {
        "depth": 4,
        "slug": "agent-工作流",
        "text": "Agent 工作流"
      },
      {
        "depth": 4,
        "slug": "sqlite-与文件系统",
        "text": "SQLite 与文件系统"
      },
      {
        "depth": 4,
        "slug": "vite-构建与发布前验证",
        "text": "Vite 构建与发布前验证"
      },
      {
        "depth": 4,
        "slug": "caddy-反向代理",
        "text": "Caddy 反向代理"
      },
      {
        "depth": 4,
        "slug": "docker-compose",
        "text": "Docker Compose"
      },
      {
        "depth": 2,
        "slug": "二本周感想",
        "text": "二、本周感想"
      },
      {
        "depth": 2,
        "slug": "三rust-学习进度",
        "text": "三、Rust 学习进度"
      },
      {
        "depth": 3,
        "slug": "第-11-章自动化测试",
        "text": "第 11 章：自动化测试"
      },
      {
        "depth": 3,
        "slug": "第-12-章构建命令行程序",
        "text": "第 12 章：构建命令行程序"
      },
      {
        "depth": 3,
        "slug": "第-13-章闭包与迭代器",
        "text": "第 13 章：闭包与迭代器"
      },
      {
        "depth": 3,
        "slug": "第-14-章cargo-与-cratesio",
        "text": "第 14 章：Cargo 与 Crates.io"
      }
    ]
  },
  {
    "id": "2026-07-10-learning-rust",
    "title": "7/6-7/12 工作与学习记录",
    "description": "记录本周的项目维护、新功能开发以及 Rust 学习实践",
    "publishedAt": "2026-07-12",
    "draft": false,
    "html": "<h2 id=\"76-712-工作与学习记录\">7/6-7/12 工作与学习记录</h2>\n<h2 id=\"一项目工作\">一、项目工作</h2>\n<h3 id=\"1-坤元智算官网门户站维护\">1. 坤元智算官网门户站维护</h3>\n<p>上周的主要工作集中在坤元智算官网门户站，期间主要进行了网站内容维护、页面优化以及部署脚本编写等工作。</p>\n<h4 id=\"图片资源优化\">图片资源优化</h4>\n<p>在检查门户网站页面时，发现部分页面使用的图片文件比较大，会对页面首次加载速度产生一定影响。因此对网站中的部分大图进行了压缩处理。</p>\n<p>本次优化主要是在不影响正常观看效果的前提下，适当降低图片文件大小，减少页面加载时需要传输的数据量。处理完成后，也对图片在页面中的展示效果进行了检查，确保没有出现明显的模糊、变形等问题。</p>\n<p>通过这次优化，进一步体会到页面加载速度不仅和代码有关，图片等静态资源的大小同样会影响用户的访问体验。</p>\n<h4 id=\"添加新的新闻内容\">添加新的新闻内容</h4>\n<p>根据网站内容更新需求，在门户网站中添加了一条新的新闻内容。</p>\n<p>本次工作主要包括：</p>\n<ul>\n<li>整理新闻标题和正文内容；</li>\n<li>准备并上传新闻相关图片；</li>\n<li>调整新闻的发布时间和展示信息；</li>\n<li>检查新闻列表和详情页面的显示效果；</li>\n</ul>\n<p>添加完成后，对新闻页面进行了整体检查，确保新内容能够正常展示，并且与网站原有的页面风格保持一致。</p>\n<h4 id=\"编写-windows-环境部署脚本\">编写 Windows 环境部署脚本</h4>\n<p>为了方便项目在 Windows 环境下进行部署，上周还编写了一个项目部署脚本，将原本需要手动执行的部分操作整理到脚本中。</p>\n<p>脚本目前主要用于检查构建目录、复制项目文件以及提示部署状态，示例代码如下：</p>\n<pre><code class=\"language-powershell\">Write-Host &quot;开始部署门户网站项目...&quot;\n\nif (!(Test-Path &quot;dist&quot;)) {\n    Write-Host &quot;未找到构建目录，请先执行项目构建&quot;\n    exit 1\n}\n\nif (!(Test-Path &quot;deploy&quot;)) {\n    New-Item -ItemType Directory &quot;deploy&quot; | Out-Null\n}\n\nWrite-Host &quot;正在复制项目文件...&quot;\nCopy-Item -Path &quot;dist\\\\*&quot; -Destination &quot;deploy\\\\&quot; -Recurse -Force\n\nWrite-Host &quot;门户网站部署完成&quot;\n</code></pre>\n<p>之前如果完全依靠手动操作，容易因为路径、文件复制等问题出现遗漏。将这些操作整理成脚本后，部署过程更加清晰，也方便后续重复使用。</p>\n<h3 id=\"2-算力运营平台项目\">2. 算力运营平台项目</h3>\n<p>上周也开始接触算力运营平台项目，目前主要处于项目熟悉阶段。</p>\n<p>刚开始接触新项目时，首先需要花一些时间了解项目是做什么的、各个功能模块之间有什么关系，以及项目代码是如何组织的。目前主要对项目背景、业务方向和代码结构进行了初步了解，并记录了一些暂时还不熟悉的业务内容。</p>\n<p>通过这次接触，对算力运营相关的平台有了一个比较初步的认识。后续还需要继续熟悉具体的业务流程和功能实现，逐步了解平台在算力资源管理、任务调度和运营管理等方面的功能。</p>\n<h2 id=\"二rust-学习\">二、Rust 学习</h2>\n<h3 id=\"1-学习进度\">1. 学习进度</h3>\n<p>上周开始学习 Rust，目前已经阅读《The Rust Programming Language》至第十章。</p>\n<p>前十章的内容覆盖得比较完整，从最基础的变量、函数和控制流开始，逐步学习了所有权、结构体、枚举、模块、集合、错误处理，以及第十章的泛型、Trait 和生命周期。</p>\n<p>Rust 和之前接触过的一些语言有比较明显的区别，尤其是所有权、借用和生命周期这些概念，需要花时间去理解。不过写了一些小例子之后，能够感受到 Rust 的编译器确实是在帮助开发者提前发现问题。</p>\n<h3 id=\"2-基础语法和猜数字练习\">2. 基础语法和猜数字练习</h3>\n<p>前几章主要学习了 Rust 的基本语法，包括变量声明、可变性、数据类型、函数、条件判断和循环。Rust 默认变量是不可变的，如果需要修改变量，需要显式加上 <code>mut</code>：</p>\n<pre><code class=\"language-rust\">fn main() {\n    let number = 10;\n    let mut count = 0;\n\n    while count &lt; number {\n        println!(&quot;count = {}&quot;, count);\n        count += 1;\n    }\n}\n</code></pre>\n<p>另外还按照书中的例子了解了猜数字游戏的基本实现。这个练习把输入、随机数生成、字符串解析、循环和 <code>match</code> 判断结合到了一起：</p>\n<pre><code class=\"language-rust\">use std::cmp::Ordering;\nuse std::io;\n\nfn main() {\n    let secret_number = 7;\n\n    loop {\n        println!(&quot;请输入一个数字：&quot;);\n\n        let mut guess = String::new();\n        io::stdin().read_line(&amp;mut guess).unwrap();\n\n        let guess: u32 = match guess.trim().parse() {\n            Ok(num) =&gt; num,\n            Err(_) =&gt; {\n                println!(&quot;请输入有效数字&quot;);\n                continue;\n            }\n        };\n\n        match guess.cmp(&amp;secret_number) {\n            Ordering::Less =&gt; println!(&quot;小了&quot;),\n            Ordering::Greater =&gt; println!(&quot;大了&quot;),\n            Ordering::Equal =&gt; {\n                println!(&quot;猜对了&quot;);\n                break;\n            }\n        }\n    }\n}\n</code></pre>\n<p>这个例子虽然简单，但把 Rust 的基础内容串了起来，也让我对 Rust 的输入处理和错误分支有了初步认识。</p>\n<h3 id=\"3-所有权实践\">3. 所有权实践</h3>\n<pre><code class=\"language-rust\">fn main() {\n    let first = String::from(&quot;hello&quot;);\n    let second = first;\n\n    println!(&quot;{}&quot;, second);\n    // first 的所有权已经转移给 second，不能继续使用 first\n}\n</code></pre>\n<p>如果需要复制一份新的数据，可以使用 <code>clone</code>：</p>\n<pre><code class=\"language-rust\">fn main() {\n    let first = String::from(&quot;hello&quot;);\n    let second = first.clone();\n\n    println!(&quot;first = {}&quot;, first);\n    println!(&quot;second = {}&quot;, second);\n}\n</code></pre>\n<p>通过这个练习，理解了 Rust 中“移动”和“复制”的区别。</p>\n<h3 id=\"4-引用和借用实践\">4. 引用和借用实践</h3>\n<pre><code class=\"language-rust\">fn calculate_length(text: &amp;String) -&gt; usize {\n    text.len()\n}\n\nfn main() {\n    let message = String::from(&quot;hello rust&quot;);\n    let length = calculate_length(&amp;message);\n\n    println!(&quot;{} 的长度是 {}&quot;, message, length);\n}\n</code></pre>\n<p>如果需要修改数据，则需要使用可变引用：</p>\n<pre><code class=\"language-rust\">fn add_suffix(text: &amp;mut String) {\n    text.push_str(&quot; language&quot;);\n}\n\nfn main() {\n    let mut message = String::from(&quot;Rust&quot;);\n    add_suffix(&amp;mut message);\n\n    println!(&quot;{}&quot;, message);\n}\n</code></pre>\n<h3 id=\"5-结构体枚举和模式匹配\">5. 结构体、枚举和模式匹配</h3>\n<p>在结构体之外，还学习了使用枚举表示一个值可能存在的多种状态。Rust 中的 <code>Option</code> 也是基于枚举实现的，可以用来表示“有值”或“没有值”：</p>\n<pre><code class=\"language-rust\">enum Message {\n    Text(String),\n    Number(i32),\n    Quit,\n}\n\nfn print_message(message: Message) {\n    match message {\n        Message::Text(text) =&gt; println!(&quot;文本：{}&quot;, text),\n        Message::Number(number) =&gt; println!(&quot;数字：{}&quot;, number),\n        Message::Quit =&gt; println!(&quot;退出&quot;),\n    }\n}\n</code></pre>\n<p>通过 <code>match</code> 可以对不同情况分别处理。相比使用很多空值判断，枚举能够让状态表达得更明确，编译器也会提醒是否遗漏了某种情况。</p>\n<h3 id=\"6-模块包和项目组织\">6. 模块、包和项目组织</h3>\n<p>第七章主要学习了 Rust 的包、crate、模块以及 <code>use</code> 和 <code>pub</code>。模块可以把相关功能组织在一起，<code>pub</code> 用来控制哪些内容可以被外部访问。</p>\n<pre><code class=\"language-rust\">mod hosting {\n    pub fn add_to_waitlist() {\n        println!(&quot;加入等待列表&quot;);\n    }\n}\n\nfn main() {\n    hosting::add_to_waitlist();\n}\n</code></pre>\n<p>这部分内容和实际项目开发联系比较紧密。随着代码量增加，如果所有代码都放在同一个文件中，会越来越难维护，因此需要通过模块划分职责。</p>\n<h3 id=\"7-集合类型实践\">7. 集合类型实践</h3>\n<p>第八章学习了 <code>Vec&lt;T&gt;</code>、字符串和哈希映射。<code>Vec</code> 可以保存一组相同类型的数据：</p>\n<pre><code class=\"language-rust\">fn main() {\n    let numbers = vec![1, 2, 3, 4, 5];\n\n    for number in &amp;numbers {\n        println!(&quot;{}&quot;, number);\n    }\n}\n</code></pre>\n<p>哈希映射则适合保存键值对，例如统计单词出现次数：</p>\n<pre><code class=\"language-rust\">use std::collections::HashMap;\n\nfn main() {\n    let words = vec![&quot;rust&quot;, &quot;web&quot;, &quot;rust&quot;];\n    let mut counts = HashMap::new();\n\n    for word in words {\n        let count = counts.entry(word).or_insert(0);\n        *count += 1;\n    }\n\n    println!(&quot;{:?}&quot;, counts);\n}\n</code></pre>\n<h3 id=\"8-错误处理\">8. 错误处理</h3>\n<p>第九章主要学习了 Rust 的错误处理方式。Rust 通常使用 <code>Result&lt;T, E&gt;</code> 表示可能成功也可能失败的操作，而不是直接忽略错误：</p>\n<pre><code class=\"language-rust\">use std::fs;\n\nfn read_config() -&gt; Result&lt;String, std::io::Error&gt; {\n    let content = fs::read_to_string(&quot;config.toml&quot;)?;\n    Ok(content)\n}\n\nfn main() {\n    match read_config() {\n        Ok(content) =&gt; println!(&quot;配置内容：{}&quot;, content),\n        Err(error) =&gt; println!(&quot;读取配置失败：{}&quot;, error),\n    }\n}\n</code></pre>\n<p>其中 <code>?</code> 可以在当前函数中快速传递错误，让代码比层层嵌套判断更加简洁。结合之前门户网站部署脚本的工作来看，部署过程中的目录不存在、文件复制失败等问题，也都适合加入明确的错误提示和处理逻辑。</p>\n<h3 id=\"9-泛型和-trait-实践\">9. 泛型和 Trait 实践</h3>\n<pre><code class=\"language-rust\">struct Counter {\n    value: u32,\n}\n\nimpl Counter {\n    fn increase(&amp;mut self) {\n        self.value += 1;\n    }\n\n    fn current(&amp;self) -&gt; u32 {\n        self.value\n    }\n}\n\nfn main() {\n    let mut counter = Counter { value: 0 };\n    counter.increase();\n    counter.increase();\n\n    println!(&quot;当前计数：{}&quot;, counter.current());\n}\n</code></pre>\n<p>第十章主要学习了泛型、Trait 和生命周期。泛型可以让同一个函数支持不同类型的数据，Trait 则可以为不同类型定义统一的行为。</p>\n<pre><code class=\"language-rust\">trait Summary {\n    fn summarize(&amp;self) -&gt; String;\n}\n\nstruct News {\n    title: String,\n}\n\nimpl Summary for News {\n    fn summarize(&amp;self) -&gt; String {\n        format!(&quot;新闻：{}&quot;, self.title)\n    }\n}\n</code></pre>\n<p>这一周主要还是以阅读和小例子练习为主。Rust 目前还处于入门阶段，生命周期等内容还需要继续结合具体代码理解。</p>\n",
    "headings": [
      {
        "depth": 2,
        "slug": "76-712-工作与学习记录",
        "text": "7/6-7/12 工作与学习记录"
      },
      {
        "depth": 2,
        "slug": "一项目工作",
        "text": "一、项目工作"
      },
      {
        "depth": 3,
        "slug": "1-坤元智算官网门户站维护",
        "text": "1. 坤元智算官网门户站维护"
      },
      {
        "depth": 4,
        "slug": "图片资源优化",
        "text": "图片资源优化"
      },
      {
        "depth": 4,
        "slug": "添加新的新闻内容",
        "text": "添加新的新闻内容"
      },
      {
        "depth": 4,
        "slug": "编写-windows-环境部署脚本",
        "text": "编写 Windows 环境部署脚本"
      },
      {
        "depth": 3,
        "slug": "2-算力运营平台项目",
        "text": "2. 算力运营平台项目"
      },
      {
        "depth": 2,
        "slug": "二rust-学习",
        "text": "二、Rust 学习"
      },
      {
        "depth": 3,
        "slug": "1-学习进度",
        "text": "1. 学习进度"
      },
      {
        "depth": 3,
        "slug": "2-基础语法和猜数字练习",
        "text": "2. 基础语法和猜数字练习"
      },
      {
        "depth": 3,
        "slug": "3-所有权实践",
        "text": "3. 所有权实践"
      },
      {
        "depth": 3,
        "slug": "4-引用和借用实践",
        "text": "4. 引用和借用实践"
      },
      {
        "depth": 3,
        "slug": "5-结构体枚举和模式匹配",
        "text": "5. 结构体、枚举和模式匹配"
      },
      {
        "depth": 3,
        "slug": "6-模块包和项目组织",
        "text": "6. 模块、包和项目组织"
      },
      {
        "depth": 3,
        "slug": "7-集合类型实践",
        "text": "7. 集合类型实践"
      },
      {
        "depth": 3,
        "slug": "8-错误处理",
        "text": "8. 错误处理"
      },
      {
        "depth": 3,
        "slug": "9-泛型和-trait-实践",
        "text": "9. 泛型和 Trait 实践"
      }
    ]
  }
];
